[
  {
    "id": "unsplit_nodes_implicit_index_retention",
    "explanation": "The remark for claim_3_6 (SplitTopologicalOrder) assigns new indices only to the split copies in $W$: \"for a node $v_j \\in W$ with index $j$ the node $v_j^0$ the index $j-\\frac{1}{3}$ and $v_j^1$ the index $j+\\frac{1}{3}$, and then ordering all nodes according to their index value.\" But the node set of $G_{\\spl(W)}$ (per def_3_11) is $J \\cup (V \\setminus W) \\cup W^0 \\cup W^1$, and the remark says nothing about the indices of the unsplit nodes $v_j \\in J \\cup (V \\setminus W)$. The only interpretation that makes the procedure well-defined is that each unsplit $v_j$ keeps its original index $j$ from the enumeration $v_1 < v_2 < \\cdots < v_n$ on $J \\cup V$.\n\nA pedantic reader applying the literal procedure would find unsplit nodes have no assigned index and would be unable to perform the final \"ordering all nodes according to their index value\" step. The natural fix is an explicit clause such as \"and to each $v_j \\in (J \\cup V) \\setminus W$ the index $j$.\" Downstream formalisation must supply this retention rule (in the Lean encoding the issue does not arise because we define the strict-total-order directly on $\\mathrm{SplitNode}\\;\\mathrm{Node}$ via a lexicographic-style comparison on (underlying-node, tag-in-\\{-1,0,+1\\}) and never refer to integer indices; but tex-faithful rewordings must spell it out).\n\nCorner case making the gap concrete: with $n = 2$ and $W = \\{v_2\\}$, the remark explicitly assigns $v_2^0 \\mapsto 5/3$ and $v_2^1 \\mapsto 7/3$. The literal text gives no index for $v_1$, so the relative order between $v_1$ and the split copies is unspecified by what is written; only the implicit \"$v_1$ keeps index $1$\" recovers the correct order $v_1 < v_2^0 < v_2^1$.",
    "observed_by_ref": "claim_3_6"
  }
]
