{
  "ref": "def_3_15",
  "title": "CollidersAndNon",
  "type": "definition",
  "def_or_claim": "def",
  "section": "3.3",
  "lean_file_path": "leanification/Chapter3_GraphTheory/Section3_3/CollidersAndNon.lean",
  "lean_code_with_comments": "def IsCollider {u v : Node} (p : Walk G u v) (k : ℕ) : Prop :=\n  1 ≤ k ∧ ∃ (vk : Node) (a₁ a₂ : Node × Node),\n    p.vertices[k]? = some vk ∧\n    p.edges[k - 1]? = some a₁ ∧\n    p.edges[k]? = some a₂ ∧\n    G.into vk a₁ ∧\n    G.into vk a₂\n\ndef IsNonCollider {u v : Node} (p : Walk G u v) (k : ℕ) : Prop :=\n  k ≤ p.length ∧ ¬ p.IsCollider k",
  "lean_code_without_comments": "def IsCollider {u v : Node} (p : Walk G u v) (k : ℕ) : Prop :=\n  1 ≤ k ∧ ∃ (vk : Node) (a₁ a₂ : Node × Node),\n    p.vertices[k]? = some vk ∧\n    p.edges[k - 1]? = some a₁ ∧\n    p.edges[k]? = some a₂ ∧\n    G.into vk a₁ ∧\n    G.into vk a₂\n\ndef IsNonCollider {u v : Node} (p : Walk G u v) (k : ℕ) : Prop :=\n  k ≤ p.length ∧ ¬ p.IsCollider k",
  "lean_explanation": "The definition introduces two `Prop`-valued predicates on walks in a CDMG, indexed by a natural-number position `k`. The implicit binder `Node : Type*` with `[DecidableEq Node]` is the ambient vertex carrier — the LN's silent assumption that $J \\cup V$ is drawn from some type is encoded as a single polymorphic type with decidable equality. The namespace-level `variable {G : CDMG Node}` carries the LN's once-at-the-top quantifier ``let $G = (J, V, E, L)$ be a CDMG'' through every signature, while `{u v : Node}` and `p : Walk G u v` package the walk $\\pi$ from $v_0 = u$ to $v_n = v$ via Mathlib-style endpoint binders. The position `k : ℕ` matches the LN's $k \\in \\{0, 1, \\dots, n\\}$ enumeration.\n\nThe Lean-specific idioms used here are minimal. `p.vertices[k]?` and `p.edges[k - 1]?` are Option-valued list lookups: they evaluate to `some` exactly when `k` lies in the lookup's range (`k ≤ p.length` for vertices, `k < p.length` for edges) and to `none` otherwise. We read `vk : Node` (the LN's $v_k$) and the candidate walk-edges `a₁ a₂ : Node × Node` (the LN's $a_{k-1}$ and $a_k$) directly out of these lookups in the same step that asserts in-range membership. The predicate `G.into vk a` is reused from `def_3_3` item~ii: it spells out exactly the LN's ``$a$ is an edge into $v_k$'' clause, so the Lean text is a direct transcription of the LN's set-builder.\n\nSubtraction on `ℕ` is truncated (`0 - 1 = 0`), so the explicit guard `1 ≤ k` is what excludes the spurious ``left'' slot at the start-position $k = 0$ — without it, the predicate would wrongly inspect `p.edges[0]?` where the LN's index set has no admissible $a_{-1}$.\n\nIn shape, `IsCollider` is a conjunction of one positional guard, three Option-membership conjuncts (which simultaneously witness in-range and bind `vk`, `a₁`, `a₂`), and two `G.into vk` clauses — matching exactly the LN's $\\mathrm{ah}_\\pi(k) = 2$ reading. `IsNonCollider` is then `k ≤ p.length ∧ ¬ p.IsCollider k` — an explicit in-range bound conjoined with the negation, mirroring the LN's $\\mathrm{ah}_\\pi(k) \\le 1$ classification.",
  "design_choices": "**Direct existential characterisation, no `ahCount` helper.** The LN defines $\\mathrm{ah}_\\pi(k) \\in \\{0, 1, 2\\}$ as a named cardinality and then classifies positions by whether the count equals $2$ or is $\\le 1$. A literal port would introduce a `ℕ`-valued counting helper computed via `if G.into v e then 1 else 0`, which would require a `Decidable (G.into v e)` instance — extra infrastructure with no semantic payoff. Since the count's range is fixed at three values, we instead classify directly: ``both walk-incident edges into $v_k$'' captures the $= 2$ case, and ``not both'' captures the $\\le 1$ case.\n\n**`IsCollider` is primary; `IsNonCollider` is the negation on the in-range fragment.** Encoding `IsNonCollider` as `k ≤ p.length ∧ ¬ p.IsCollider k` makes the LN's mutual-exclusivity statement (``every position is exactly one of collider / non-collider'') definitional rather than a separate theorem to prove. The asymmetry — `IsCollider` carries its in-range condition implicitly through the Option-membership conjuncts, while `IsNonCollider` carries `k ≤ p.length` explicitly because the negation is vacuously true off-range — is the price paid for this definitional shape, and it is small. The alternative, defining both predicates positively via a shared count, would require proving `count ≤ 1 ↔ count ≠ 2` as a separate lemma.\n\n**Self-loops contribute one, not two.** A directed self-loop $a = (v, v) \\in E$ traversed at a position with $v_k = v$ is a single walk-edge and contributes $1$ to the arrowhead count via the $E$-clause of `G.into`. The literal LN visual writings ($\\tuh, \\hut, \\huh, \\suh, \\hus$) would simultaneously classify such a position as a left chain, right chain, fork, *and* collider; the canonical tex resolves this ambiguity by taking the count-based reading as authoritative, and the Lean encoding inherits that resolution automatically through `G.into`.\n\n**`Prop`, not `Bool`.** Walks live at `Type _`; their classification at `Prop`. A decidable `Bool`-valued form would require deciding `G.into v e` at every elaboration site for no downstream benefit — later definitions in Section 3.3 (blockability, $\\sigma$-blocking) consume these predicates in hypothesis position, where `Prop` is the natural fit.\n\n**Distinct from `Walk.IsColliderWalk` (def_3_4 item~iv).** The chapter already has a *whole-walk* shape predicate `IsColliderWalk`; the present `IsCollider` is *position-level*. They are not interchangeable: `IsColliderWalk` additionally constrains the walk's endpoints and handles the $n = 1$ case via a bidirected-edge constraint. The names diverge intentionally so that pointwise consumers downstream do not silently inherit walk-level constraints.",
  "lean_source_urls": [
    {
      "title": "CollidersAndNon.lean",
      "url": "https://github.com/schipeltaper/causality-leanified/blob/server_setting_up_scaffold/leanification/Chapter3_GraphTheory/Section3_3/CollidersAndNon.lean#L201-L279"
    }
  ]
}
