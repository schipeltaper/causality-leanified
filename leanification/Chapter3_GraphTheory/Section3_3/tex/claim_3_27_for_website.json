{
  "ref": "claim_3_27",
  "title": "LabelRoman",
  "type": "lemma",
  "def_or_claim": "claim",
  "section": "3.3",
  "lean_file_path": "leanification/Chapter3_GraphTheory/Section3_3/LabelRomanDisproof.lean",
  "lean_code_with_comments": "def Walk.replaceWalkCaseI {G : CDMG Node} :\n    ∀ {u v : Node}, Walk G u v → ℕ → Prop\n  | _, _, .nil _ _, 0 => True\n  | _, _, .nil _ _, _ + 1 => False\n  | _, _, .cons _ (.forwardE _) _, 0 => True\n  | _, _, .cons _ (.backwardE _) _, 0 => False\n  | _, _, .cons _ (.bidir _) _, 0 => False\n  | _, _, .cons _ _ p, j + 1 => p.replaceWalkCaseI j\n\nabbrev G : CDMG Bool where\n  J := ∅\n  V := Finset.univ\n  hJV_disj := by simp\n  E := {(false, true), (true, false), (true, true)}\n  hE_subset := by\n    intro e he\n    fin_cases he <;> exact ⟨by decide, by decide⟩\n  L := ∅\n  hL_subset := by intro _ hs; cases hs\n  hL_irrefl := by intro _ hs; cases hs\n\ndef π : Walk G false true :=\n  Walk.cons true (WalkStep.backwardE h_ba)\n    (Walk.cons true (WalkStep.forwardE h_bb)\n      (Walk.nil true h_b_mem))\n\ndef σ_ij : Walk G false true :=\n  Walk.cons true (WalkStep.forwardE h_ab) (Walk.nil true h_b_mem)\n\ndef π' : Walk G false true :=\n  Walk.cons true (WalkStep.forwardE h_ab)\n    (Walk.cons true (WalkStep.forwardE h_bb)\n      (Walk.nil true h_b_mem))\n\ntheorem not_replace_walk :\n    ∃ (Node : Type) (_inst : DecidableEq Node) (G : CDMG Node)\n      (C : Set Node) (hC : C ⊆ ↑G.J ∪ ↑G.V) (u w : Node)\n      (π : Walk G u w) (hπ : π.IsSigmaOpenGiven C hC)\n      (i j : ℕ) (hij : i < j) (hjn : j ≤ π.length)\n      (v_i v_j : Node)\n      (h_get_i : π.vertices[i]? = some v_i)\n      (h_get_j : π.vertices[j]? = some v_j)\n      (h_Sc : v_i ∈ G.Sc v_j)\n      (σ_ij : Walk G v_i v_j) (π' : Walk G u w),\n      (π.replaceWalkCaseI j →\n         σ_ij.IsDirectedWalk ∧\n         (∀ τ : Walk G v_i v_j, τ.IsDirectedWalk → σ_ij.length ≤ τ.length)) ∧\n      (¬ π.replaceWalkCaseI j →\n         σ_ij.reverse.IsDirectedWalk ∧\n         (∀ τ : Walk G v_j v_i, τ.IsDirectedWalk → σ_ij.reverse.length ≤ τ.length)) ∧\n      (∀ x ∈ σ_ij.vertices, x ∈ G.Sc v_j) ∧\n      π'.vertices = (π.vertices.take (i + 1)).dropLast ++ σ_ij.vertices ++\n          π.vertices.drop (j + 1) ∧\n      ¬ π'.IsSigmaOpenGiven C hC\n-- claim_3_27 -- end statement\n  := by\n    refine ⟨Bool, inferInstance, G, ∅, Set.empty_subset _, false, true, π,\n            π_isSigmaOpen (Set.empty_subset _), 0, 1, Nat.zero_lt_one,\n            (by decide : 1 ≤ π.length), false, true, rfl, rfl, Sc_a_b,\n            σ_ij, π',\n            ?hCaseI,\n            (fun hno => absurd π_replaceWalkCaseI_one hno),\n            ?hP3, rfl, ?hP5⟩\n    case hCaseI =>\n      -- Case (i) fires (`π.replaceWalkCaseI 1` holds via the\n      -- `.forwardE h_bb` self-loop step at position 1 on `π`).  The\n      -- conjunction has two parts:\n      --   (a) `σ_ij.IsDirectedWalk`              — `σ_ij_isDirected`;\n      --   (b) `∀ τ, τ.IsDirectedWalk → σ_ij.length ≤ τ.length`\n      --                                          — discharged below.\n      -- For (b): `σ_ij.length = 1` (a single `.forwardE` cons cell),\n      -- so we need `1 ≤ τ.length` for any directed walk\n      -- `τ : Walk G false true`.  A length-0 walk would be `Walk.nil\n      -- v hv` of type `Walk G v v`, which would force `v = false` and\n      -- `v = true` — impossible by `Bool` constructor injectivity.\n      -- Lean's index unifier eliminates the `.nil` branch of the\n      -- `match` automatically; only the `.cons _ _ p` branch survives,\n      -- and its length is `p.length + 1 ≥ 1`.\n      intro _\n      refine ⟨σ_ij_isDirected, ?_⟩\n      intro τ _hτ\n      match τ with\n      | .cons _ _ p =>\n          change σ_ij.length ≤ p.length + 1\n          change (1 : ℕ) ≤ p.length + 1\n          exact Nat.succ_le_succ (Nat.zero_le _)\n    case hP3 =>\n      intro x hx\n      -- σ_ij.vertices = [false, true]; split on x = false vs x = true.\n      rcases List.mem_cons.mp hx with rfl | hx2\n      · exact Sc_a_b\n      · rcases List.mem_cons.mp hx2 with rfl | hx3\n        · exact Sc_b_b\n        · cases hx3\n    case hP5 =>\n      intro hopen\n      obtain ⟨hcol, _⟩ := hopen\n      have hin : true ∈ G.AncSet (∅ : Set Bool) :=\n        hcol 1 true π'_vertices_one π'_isCollider_one\n      -- `G.AncSet ∅ = ⋃ v ∈ ∅, G.Anc v = ∅`, so `true ∈ ∅` is False.\n      simp [CDMG.AncSet] at hin",
  "lean_code_without_comments": "def Walk.replaceWalkCaseI {G : CDMG Node} :\n    ∀ {u v : Node}, Walk G u v → ℕ → Prop\n  | _, _, .nil _ _, 0 => True\n  | _, _, .nil _ _, _ + 1 => False\n  | _, _, .cons _ (.forwardE _) _, 0 => True\n  | _, _, .cons _ (.backwardE _) _, 0 => False\n  | _, _, .cons _ (.bidir _) _, 0 => False\n  | _, _, .cons _ _ p, j + 1 => p.replaceWalkCaseI j\n\nabbrev G : CDMG Bool where\n  J := ∅\n  V := Finset.univ\n  hJV_disj := by simp\n  E := {(false, true), (true, false), (true, true)}\n  hE_subset := by\n    intro e he\n    fin_cases he <;> exact ⟨by decide, by decide⟩\n  L := ∅\n  hL_subset := by intro _ hs; cases hs\n  hL_irrefl := by intro _ hs; cases hs\n\ndef π : Walk G false true :=\n  Walk.cons true (WalkStep.backwardE h_ba)\n    (Walk.cons true (WalkStep.forwardE h_bb)\n      (Walk.nil true h_b_mem))\n\ndef σ_ij : Walk G false true :=\n  Walk.cons true (WalkStep.forwardE h_ab) (Walk.nil true h_b_mem)\n\ndef π' : Walk G false true :=\n  Walk.cons true (WalkStep.forwardE h_ab)\n    (Walk.cons true (WalkStep.forwardE h_bb)\n      (Walk.nil true h_b_mem))\n\ntheorem not_replace_walk :\n    ∃ (Node : Type) (_inst : DecidableEq Node) (G : CDMG Node)\n      (C : Set Node) (hC : C ⊆ ↑G.J ∪ ↑G.V) (u w : Node)\n      (π : Walk G u w) (hπ : π.IsSigmaOpenGiven C hC)\n      (i j : ℕ) (hij : i < j) (hjn : j ≤ π.length)\n      (v_i v_j : Node)\n      (h_get_i : π.vertices[i]? = some v_i)\n      (h_get_j : π.vertices[j]? = some v_j)\n      (h_Sc : v_i ∈ G.Sc v_j)\n      (σ_ij : Walk G v_i v_j) (π' : Walk G u w),\n      (π.replaceWalkCaseI j →\n         σ_ij.IsDirectedWalk ∧\n         (∀ τ : Walk G v_i v_j, τ.IsDirectedWalk → σ_ij.length ≤ τ.length)) ∧\n      (¬ π.replaceWalkCaseI j →\n         σ_ij.reverse.IsDirectedWalk ∧\n         (∀ τ : Walk G v_j v_i, τ.IsDirectedWalk → σ_ij.reverse.length ≤ τ.length)) ∧\n      (∀ x ∈ σ_ij.vertices, x ∈ G.Sc v_j) ∧\n      π'.vertices = (π.vertices.take (i + 1)).dropLast ++ σ_ij.vertices ++\n          π.vertices.drop (j + 1) ∧\n      ¬ π'.IsSigmaOpenGiven C hC\n\n  := by\n    refine ⟨Bool, inferInstance, G, ∅, Set.empty_subset _, false, true, π,\n            π_isSigmaOpen (Set.empty_subset _), 0, 1, Nat.zero_lt_one,\n            (by decide : 1 ≤ π.length), false, true, rfl, rfl, Sc_a_b,\n            σ_ij, π',\n            ?hCaseI,\n            (fun hno => absurd π_replaceWalkCaseI_one hno),\n            ?hP3, rfl, ?hP5⟩\n    case hCaseI =>\n\n      intro _\n      refine ⟨σ_ij_isDirected, ?_⟩\n      intro τ _hτ\n      match τ with\n      | .cons _ _ p =>\n          change σ_ij.length ≤ p.length + 1\n          change (1 : ℕ) ≤ p.length + 1\n          exact Nat.succ_le_succ (Nat.zero_le _)\n    case hP3 =>\n      intro x hx\n\n      rcases List.mem_cons.mp hx with rfl | hx2\n      · exact Sc_a_b\n      · rcases List.mem_cons.mp hx2 with rfl | hx3\n        · exact Sc_b_b\n        · cases hx3\n    case hP5 =>\n      intro hopen\n      obtain ⟨hcol, _⟩ := hopen\n      have hin : true ∈ G.AncSet (∅ : Set Bool) :=\n        hcol 1 true π'_vertices_one π'_isCollider_one\n\n      simp [CDMG.AncSet] at hin",
  "lean_explanation": "This row is a **disproof** of LN's `lem:replace_walk`. The theorem `not_replace_walk` is an explicit-witness existential: it packs every binder of the prove-side claim together with concrete witnesses on which the LN's conclusion (2) fails. The witness positions line up one-for-one with the prove-side's parameter list.\n\nThe existential binders correspond to the LN's data as follows. `Node : Type` is the vertex carrier (instantiated to `Bool` in the proof); `DecidableEq Node` is needed so that `Finset`-valued projections of the graph compute. `G : CDMG Node` is the graph; `C : Set Node` is the conditioning set with `hC : C ⊆ ↑G.J ∪ ↑G.V` typing it inside the input ∪ vertex set (the `↑` coercions land the `Finset`-valued `G.J`, `G.V` into `Set Node`). `u w : Node` are the walk endpoints and `π : Walk G u w` the walk itself, with `hπ : π.IsSigmaOpenGiven C hC` witnessing $C$-σ-openness. Positions `i j : ℕ` with `hij : i < j` and `hjn : j ≤ π.length` pick the replacement window; `v_i v_j : Node` are the vertices at those positions, certified by `h_get_i / h_get_j` against `π.vertices[i]? = some v_i` etc.; `h_Sc : v_i ∈ G.Sc v_j` is the LN's SCC hypothesis. Finally `σ_ij : Walk G v_i v_j` is the LN's prescribed replacement subwalk and `π' : Walk G u w` the modified walk.\n\nThe conjunction body asserts five facts: a case-(i)/case-(ii) implication pair giving direction (and **minimality**) of `σ_ij` over directed walks $v_i \\to v_j$ (or $v_j \\to v_i$); $\\sigma_{ij}.\\text{vertices} \\subseteq G.\\text{Sc}\\,v_j$; the LN's vertex-list factorisation $\\pi'.\\text{vertices} = (\\pi.\\text{vertices}.\\text{take}(i+1)).\\text{dropLast} \\;\\verb|++|\\; \\sigma_{ij}.\\text{vertices} \\;\\verb|++|\\; \\pi.\\text{vertices}.\\text{drop}(j+1)$; and the punchline `¬ π'.IsSigmaOpenGiven C hC` — the LN's claim that $\\pi'$ remains $\\sigma$-open is refuted on this instance.\n\nThe helper predicate `Walk.replaceWalkCaseI π j` is a structural-recursion `Prop` on `(π, j)` capturing the LN's case-(i)/case-(ii) discriminant: it returns `True` precisely when `j = π.length` or the walk-step at position $j$ is a `.forwardE` (a directed E-edge $v_j \\to v_{j+1}$), and `False` for `.backwardE` and `.bidir`. Mathlib idioms worth flagging: `Walk G u v` and `WalkStep G u v` are inductive families indexed by start/end vertices (cons-cells store a `WalkStep G u mid` and a `Walk G mid v`); `WalkStep` has three constructors `.forwardE / .backwardE / .bidir` for the three edge types; `Walk.IsCollider π k` reduces by structural recursion to a conjunction `s₀.IsInto v_k ∧ s₁.IsInto v_k` of arrowhead-into-vertex predicates on the two walk-steps incident to position $k$; `G.Sc v` denotes the strongly-connected-component set defined as `G.Anc v ∩ G.Desc v`.",
  "design_choices": "**Existential-witness counter-example shape.** Two negation shapes were considered. The flat `¬ (∀ … ∃ …)` shape instantiates as nested `intro`s over each of the 16 prove-side binders, with tactic bookkeeping scaling linearly with the binder count. The explicit-witness shape, used here, packs every witness into a single `∃` and discharges each conjunct of the negated body inline. We chose the explicit-witness shape because the prove-side `replace_walk` already uses an explicit parameter list (not `∀ ...` in the conclusion), so witness positions in `not_replace_walk` mirror the prove-side's parameter positions one-for-one — a reader can trace each witness back to a prove-side binder by counting.\n\n**Minimality clauses on `σ_ij` alongside direction.** The LN prescribes $\\sigma_{ij}$ as a *shortest* directed walk from $v_i$ to $v_j$ (case (i)) or as the reverse of a shortest directed walk from $v_j$ to $v_i$ (case (ii)). Without an explicit minimality clause the Lean negation would be strictly weaker than $\\neg$(LN claim): a non-shortest $\\sigma_{ij}$ in $\\text{Sc}(v_j)$ might fail $\\sigma$-openness on its $\\pi'$ splice even when the LN's shortest does not. Adding `∀ τ : Walk G v_i v_j, τ.IsDirectedWalk → σ_ij.length ≤ τ.length` (and its reverse on case (ii)) pins length and restores one-for-one correspondence with the LN's prose.\n\n**Dedicated helper predicate `Walk.replaceWalkCaseI`, not an inline disjunction.** Encoding the case discriminant inline would force two near-duplicate disjunction-of-WalkStep-constructor patterns into the binder block. Pulling out a named helper consolidates the discriminant into a single `Prop` on `(π, j)`, makes the signature readable, and gives the proof phase a clean `(h : π.replaceWalkCaseI j)` / `(h : ¬ π.replaceWalkCaseI j)` hypothesis to case-split on. `.bidir` is excluded from case (i) because the LN's `\\tuh` macro encodes a directed E-edge with tail at $v_j$ and head at $v_{j+1}$, whereas a `.bidir` step encodes a bidirected $L$-edge placing arrowheads at both endpoints (the LN's `\\huh`).\n\n**Counter-example data over `Bool`.** Two nodes is the minimum cardinality of $V$: the hypothesis $i < j$ plus $v_i \\in \\text{Sc}^G(v_j)$ forces a non-trivial SCC component. The directed 2-cycle $\\{(a,b), (b,a)\\}$ establishes $\\text{Sc}^G(b) = \\{a, b\\}$; the directed self-loop $(b, b)$ is the load-bearing third edge — the syntactic source of the new collider on $\\pi'$ that breaks σ-openness, without affecting the SCC computation. $L := \\emptyset$ keeps `WalkStep.bidir` out of scope, isolating the failure to the directed-edge encoding. Using `Bool` (vs an abstract `Fin 2`) makes every `CDMG` field reduce decidably, so `decide` / `rfl` / `trivial` discharge the structural fields. `abbrev` rather than `def` for `G` makes structure projections reduce definitionally.\n\n**Case (i) is the active failure trigger.** A single concrete failing instance refutes the universally-quantified claim, so building both case-(i) and case-(ii) counter-examples is unnecessary. Case (i) is the cleaner direction: the LN's \"shortest directed path $v_i \\to \\dots \\to v_j$\" lines up with `WalkStep.forwardE`'s default orientation, and the case-(ii) implication discharges vacuously since `π.replaceWalkCaseI 1` holds. The collider mechanism on $\\pi'$ is directly readable in the Lean term as `(.forwardE h_ab).IsInto true ∧ (.forwardE h_bb).IsInto true`, both conjuncts firing by node-equality on `WalkStep`'s type indices. A sibling file `Section3_3/LabelRoman.lean` retains the positive theorem signature with a `:= by sorry` placeholder, intentionally left intact in case a future remediation tightens case (i)'s trigger to exclude $v_{j+1} = v_j$ and routes self-loops into case (ii) — in which event this disproof file remains as historical documentation.",
  "lean_source_urls": [
    {
      "title": "LabelRomanDisproof.lean",
      "url": "https://github.com/schipeltaper/causality-leanified/blob/server_setting_up_scaffold/leanification/Chapter3_GraphTheory/Section3_3/LabelRomanDisproof.lean#L134-L593"
    }
  ]
}
