{
  "ref": "def_3_6",
  "title": "Acyclicity",
  "type": "definition",
  "def_or_claim": "def",
  "section": "3.1",
  "lean_file_path": "leanification/Chapter3_GraphTheory/Section3_1/Acyclicity.lean",
  "lean_statement": [
    {
      "name": "IsAcyclic",
      "kind": "def",
      "code": "/-- The CDMG `G` is *acyclic*: no node admits a non-trivial directed\nwalk to itself. Mirrors `lecture-notes/lecture_notes/graphs.tex` def\n3.6 (`\\label{def-acylic}`) verbatim, with \"non-trivial\" read as\n`1 ≤ π.length` per `Walks.lean`'s `Walk.length` doc. The trivial walk\n`Walk.nil v` is excluded by the `1 ≤ π.length` conjunct (it has\nlength zero), so a witness against acyclicity is genuinely a\ndirected cycle through `v` of one or more steps. Used as the\nprecondition for claim_3_2 (`AcyclicIffTopologicalOrder`), def_3_7\n(graph type names), and pervasively in chapters 4 -- 16. -/\ndef IsAcyclic (G : CDMG α) : Prop :=\n  ∀ v ∈ G, ¬ ∃ π : Walk G v v, π.IsDirected ∧ 1 ≤ π.length"
    }
  ],
  "lean_explanation": "The predicate fixes an ambient context: `α : Type*` is the vertex type and `G : CDMG α` is the conditional directed mixed graph under discussion, matching the LN's preamble \"$G = (J, V, E, L)$\". The universally bound `v` ranges over vertices of `G`, and inside the negated existential `π : Walk G v v` is a walk that starts and ends at the same node `v` — exactly the LN's \"walk from $v$ to itself\".\n\nTwo Mathlib idioms appear. First, `∀ v ∈ G, _` is sugar for `∀ v, v ∈ G → _`, where `v ∈ G` uses the `Membership α (CDMG α)` instance from def_3_2 that unfolds (definitionally) to `v ∈ G.J ∪ G.V`; this matches the LN's literal \"for any node $v \\in G$\". Second, the predicate lives in `Prop` and is registered under the `CDMG` namespace, so callers write `G.IsAcyclic` via dot-projection — reading just like the LN's prose \"the CDMG $G$ is acyclic\".\n\nThe shape is a universally quantified negated existential. The outer quantifier picks an arbitrary vertex `v` of `G`. The inner `¬ ∃ π : Walk G v v, π.IsDirected ∧ 1 ≤ π.length` denies that any walk from $v$ back to $v$ is both directed (every step `forward`, matching the LN's $\\tuh$) and non-trivial (length at least one). The trivial walk `Walk.nil v : Walk G v v` always exists but has `length = 0`, so the `1 ≤ π.length` conjunct rules it out — what's forbidden is a genuine directed cycle through `v` of one or more steps. The two ingredients `Walk.IsDirected` and `Walk.length` are imported verbatim from def_3_4, so the whole predicate is a one-line composition on top of the walk data layer.",
  "design_choices": "**Dot-projection `G.IsAcyclic`, namespaced under `Causality.CDMG`.** The LN's prose treats acyclicity as a property of the graph — \"the CDMG $G$ is acyclic\", \"if $G$ is acyclic then ...\", \"$G$ is acyclic iff ...\". Placing the predicate inside the `CDMG` namespace lets downstream callers write `G.IsAcyclic`, which reads exactly like the LN. The same convention is used by every other CDMG-level operator in this folder (`CDMG.Adjacent`, `CDMG.EdgeInto`, the arrow notations `⟶[G]` / `⟷[G]`, and the family operators of def_3_5). The alternative — a top-level `Causality.IsAcyclic G` — would be slightly more compact but would disconnect acyclicity from the CDMG family of operators and read less like the LN's \"$G$ is ...\" prose.\n\n**The outer quantifier writes `v ∈ G`, not `v ∈ G.J ∪ G.V`.** Both unfold to the same proposition via the `Membership α (CDMG α)` instance from def_3_2 (`CDMG.mem_iff` is `Iff.rfl`). We pick the `v ∈ G` form because the LN writes \"for any node $v \\in G$\" literally, and the project's convention — used uniformly across the family operators in `FamilyDirect.lean` — is to mirror that prose. Callers who need to peel `v ∈ G` back to a set-theoretic union reach for `simp [CDMG.mem_iff]` or `Set.mem_union`.\n\n**\"Non-trivial\" is encoded as `1 ≤ π.length`, not `π ≠ Walk.nil v`.** All three of `1 ≤ π.length`, `π.length ≠ 0`, and `π ≠ Walk.nil v` are propositionally equivalent once the endpoints `v = v` are pinned down, but the `Nat`-level form is the LN's own framing — def_3_4 parameterises walks by length $n \\geq 0$, and `Walks.lean`'s docstring already commits to \"non-trivial directed walk means length $\\geq 1$\". Choosing `1 ≤ π.length` also keeps the predicate compositional with later length-based arguments: claim_3_2's `→` direction inducts on walk length, and several later chapters compare `π.length` against `π.support.length`. The disequality form `π ≠ Walk.nil v` would force an awkward `Walk.nil` case-split at every use site, and for general walks `π : Walk G v w` with `v ≠ w` it is not even structurally equivalent to length-zero — the `Nat`-level form sidesteps that bookkeeping.\n\n**`Prop`, not `Type _` — no witness data carried by `IsAcyclic`.** The LN uses acyclicity only as a precondition — \"if $G$ is acyclic, then ...\" — never as data carrying constructive information. A `Type _`-valued formulation would force every later \"$G$ is acyclic\" hypothesis to drag an explicit witness around, cluttering signatures with no proof-theoretic gain (the predicate is decidable only in restricted finite settings, so constructive content would not be useful in general). `Prop` also matches Mathlib's `SimpleGraph.IsAcyclic` precedent.\n\n**Inline conjunction, no helper `IsNonTrivialDirectedCycle`.** The inner `π.IsDirected ∧ 1 ≤ π.length` is used in only this one place and reads cleanly inline; naming an alias would add an extra layer of unfolding for downstream `simp`/`rewrite` steps without compensating readability gains. If a later row repeatedly references the same conjunction (claim_3_2 might want to name \"directed cycle through $v$\"), that row can introduce the helper locally where it pays for itself.\n\n**No bundled simp characterisation lemma.** The defining body is already the shape downstream callers want to reason against; an `isAcyclic_iff` lemma would just be `Iff.rfl` and offer no rewriting power. The negation form $\\neg G.\\text{IsAcyclic} \\iff \\exists v \\in G,\\ \\exists \\pi : \\text{Walk}\\ G\\ v\\ v,\\ \\pi.\\text{IsDirected} \\wedge 1 \\le \\pi.\\text{length}$ (via `push_neg`) is a one-line derivation any caller can do, so we leave it for the row that first needs it — most likely claim_3_2's `←` direction — rather than pre-committing this file to a choice between bundled-`Exists` and unbundled-`bex` shapes."
}
