{
  "ref": "def_3_6",
  "title": "Acyclicity",
  "type": "definition",
  "def_or_claim": "def",
  "section": "3.1",
  "lean_file_path": "leanification/Chapter3_GraphTheory/Section3_1/Acyclicity.lean",
  "lean_code_with_comments": "def IsAcyclic (G : CDMG Node) : Prop :=\n  ∀ v ∈ G, ¬ ∃ p : Walk G v v, p.IsDirectedWalk ∧ p.length ≥ 1",
  "lean_code_without_comments": "def IsAcyclic (G : CDMG Node) : Prop :=\n  ∀ v ∈ G, ¬ ∃ p : Walk G v v, p.IsDirectedWalk ∧ p.length ≥ 1",
  "lean_explanation": "The predicate `IsAcyclic` takes one explicit argument `G : CDMG Node` — the CDMG whose acyclicity is being asserted — and lives over the ambient implicit binders `Node : Type*` with `[DecidableEq Node]`. The type parameter `Node` is the carrier the LN treats as the silent universe of vertex labels; the decidability instance is load-bearing because the downstream `Walk.IsDirectedWalk` predicate (from `def_3_4`) recurses on per-edge `a ∈ G.E` membership checks, and the `Membership Node (CDMG Node)` instance from `def_3_2` reduces `v ∈ G` to `Finset.mem` on `G.J ∪ G.V` — both require `DecidableEq Node` to type-check.\n\nThe body reads `∀ v ∈ G, ¬ ∃ p : Walk G v v, p.IsDirectedWalk ∧ p.length ≥ 1`. Three Mathlib-style idioms are worth flagging. First, `∀ v ∈ G` is sugar for `∀ v, v ∈ G → …`; the `v ∈ G` notation is *not* Lean primitive but is supplied by the `Membership Node (CDMG Node)` instance defined in `CDMGNotation.lean`, where it unfolds to `v ∈ G.J ∪ G.V`. The LN's literal phrase \"for any node $v \\in J \\cup V$\" therefore transports verbatim onto the Lean text. Second, `Walk G v v` is the dependent type of walks indexed on both endpoints: \"a walk from $v$ to itself\" is encoded directly in the type, with no side condition threading a \"starts at $v$, ends at $v$\" hypothesis through. Third, `p.IsDirectedWalk` is a `Prop`-valued predicate witnessing that every edge of `p` is a directed edge (from `G.E`), and `p.length` is the edge count via `Walk.nil ↦ 0` / `Walk.cons ↦ length p + 1`.\n\nThe overall shape: a single existential is *negated* under the `∀ v ∈ G` quantifier, and the existential bundles the walk together with two independent constraints — directedness and non-triviality. \"Non-trivial\" is pinned to $n \\ge 1$ per the operator clarification accompanying the LN block, so a length-$1$ directed self-loop $v \\rightharpoonup v$ counts as a forbidden cycle.",
  "design_choices": "**Quantifier scope `∀ v ∈ G`, mirroring the LN's literal `v \\in J \\cup V`.** The `Membership Node (CDMG Node)` instance unfolds `v ∈ G` to `v ∈ G.J ∪ G.V`, so the Lean text matches the LN word-for-word with no helper notation. A narrower scope `∀ v ∈ G.V` would be logically equivalent — the `J`-half is vacuous because `def_3_1`'s `hE_subset` forces every directed edge's target into `G.V`, and `hJV_disj` rules out a return to any `j ∈ G.J` — but it would drift away from the LN's literal phrasing and force downstream rows to carry an extra $J$-versus-$V$ case split whenever they destructure a hypothesis about \"every node of $G$\".\n\n**\"Non-trivial\" encoded as `p.length ≥ 1`.** The LN block uses \"non-trivial\" without a definition; the operator's addition pins it to walks of length $n \\ge 1$ — at least one edge traversed — and admits a self-loop $v \\rightharpoonup v$ as a non-trivial walk of length $1$. The wording surfaced two plausible readings (length $\\ge 1$ versus length $\\ge 2$, only the latter would let an acyclic CDMG carry self-loops); we resolve to the first, and the rewritten tex spec spells out the no-self-loop consequence in its \"Consequence\" paragraph. Alternative encodings `0 < p.length`, `¬ p.length = 0`, or `p ≠ Walk.nil v hv` are logically equivalent but `p.length ≥ 1` reads most directly off the LN's `$n \\ge 1$`.\n\n**`Walk G v v` rather than a fresh `Cycle` or `Path` type.** The upstream `Walk` inductive (`def_3_4`) indexes on endpoints, so \"from $v$ to itself\" lives in the *type* — no side condition required. A separate `Cycle G v` type would force a forgetful coercion at every downstream site that mixes acyclicity with general-walk reasoning (topological order in `def_3_8`, the acyclic-iff-topological-order claim in `claim_3_2`, and the $d$-/$\\sigma$-separation arguments in later chapters). A `Path` type with no repeated vertices would *exclude* the LN-admitted length-$1$ self-loop case — the wrong shape for this row.\n\n**Conjunction under the existential, not a subtype or bundled `DirectedCycle` struct.** The LN prose says \"a non-trivial directed walk\" — two independent constraints on the same walk. A plain `∧` keeps inversion ergonomic: a hypothesis `¬ ∃ p, p.IsDirectedWalk ∧ p.length ≥ 1` decomposes by `rintro ⟨p, hp_dir, hp_len⟩` with no `Subtype.mk` / `Subtype.property` unpacking, exactly the shape that downstream consumers want when constructing or rejecting witness cycles.\n\n**Mathlib re-use.** Built on this project's own `Walk` from `def_3_4`. Mathlib's `SimpleGraph.Walk` is for undirected single-channel graphs and has no notion of directed-versus-bidirected edges or a $J$/$V$ input/output partition, so it cannot encode a CDMG walk. Mathlib's `SimpleGraph.IsAcyclic` forbids only undirected cycles of length $\\ge 3$, missing both the directed-walk constraint and the length-$1$ self-loop case. Rolling our own predicate on top of our own `Walk` is the only option that stays close to the LN.",
  "lean_source_urls": [
    {
      "title": "Acyclicity.lean",
      "url": "https://github.com/schipeltaper/causality-leanified/blob/server_setting_up_scaffold/leanification/Chapter3_GraphTheory/Section3_1/Acyclicity.lean#L185-L186"
    }
  ]
}
