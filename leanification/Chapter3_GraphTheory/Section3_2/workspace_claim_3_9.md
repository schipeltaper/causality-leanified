# Workspace for claim_3_9 — SwigAcyclicTopologicalOrder

This file is the manager's scratchpad for this row.

## Source block (LN graphs.tex, line 612, immediately after def_3_12)

```latex
\begin{claimmark}
\begin{Rem}
    For a CADMG $G=(J,V,E,L)$, also $G_{\swig(W)}$ is acyclic.
    If $<$ is any topological order of $G$ given by enumerating all nodes $v \in J \cup V$ via:
    \[ v_1 < v_2 < \cdots < v_n,\]
    then, for instance,
    a topological order for $G_{\swig(W)}$ can be achieved by assigning for a node $v_j \in W$ with index $j$ the node
    $v_j^o$ the index $j-\frac{1}{3}$
    and $v_j^i$ the index $j+\frac{1}{3}$, and then ordering all nodes according to their index value.
\end{Rem}
\end{claimmark}
```

## Plan — overview

This row is the **SWIG analogue of claim_3_6** (which is the same statement for `nodeSplittingOn`). Since `G_{\swig(W)} = (G.nodeSplittingOn W hW).hardInterventionOn (range Sum.inr)` (see `NodeSplittingHard.lean` def_3_12), and the LN's `±1/3` interleave recipe is *identical* between claim_3_6 (`v_j^0`, `v_j^1`) and claim_3_9 (`v_j^o`, `v_j^i`), this row is a near-direct **corollary chain** of claim_3_3 + claim_3_6:

* **Part A (acyclicity)** = `isAcyclic_hardInterventionOn` applied to the result of `isAcyclic_nodeSplittingOn`.
* **Part B (topological order)** = the *same* `splitOrder W r` from `SplitTopologicalOrder.lean` (reused by name) — `isTopologicalOrder_hardInterventionOn` applied to the result of `isTopologicalOrder_nodeSplittingOn`. The HI's precondition `W' ⊆ G'.J ∪ G'.V` reduces to `Set.range Sum.inr ⊆ (nodeSplittingOn).J ∪ (nodeSplittingOn).V`, which holds by `nodeSplittingOn_V`.

Mirrors claim_3_6's two-theorem decomposition exactly (analogous to claim_3_3's structure for HI).

## Target Lean file

`Section3_2/SwigAcyclicTopologicalOrder.lean` — name matches the row title.

Imports needed:
* `Chapter3_GraphTheory.Section3_2.NodeSplittingHard` (for the SWIG construction)
* `Chapter3_GraphTheory.Section3_2.AcyclicUnderIntervention` (for `isAcyclic_hardInterventionOn` and `isTopologicalOrder_hardInterventionOn`)
* `Chapter3_GraphTheory.Section3_2.SplitTopologicalOrder` (for `splitOrder W r`, `isAcyclic_nodeSplittingOn`, `isTopologicalOrder_nodeSplittingOn`)

## Statement-phase notes (this manager)

Two theorems, mirroring claim_3_6's split:

1. `isTopologicalOrder_nodeSplittingHardInterventionOn` — constructive half.
2. `isAcyclic_nodeSplittingHardInterventionOn` — side-condition half.

Both signatures should follow claim_3_6's style: `{G}`, `{W}` implicit, `hW : W ⊆ G.V` explicit, `r` implicit, `hr`/`h` explicit.

## Plan — actions

1. ✅ Write workspace notes.
2. ✅ `spawn_agent_sub_task` → `formalize_claim_in_lean.md` — both theorem signatures written with `sorry`; `lake build` clean (only the two expected `sorry` warnings).
3. ✅ `review_design` — PASS. Reviewer confirmed the two-theorem split mirrors claim_3_6 / claim_3_3, the reuse of `splitOrder W r` by name is correct (same carrier `α ⊕ ↑W`, same `±1/3` recipe under `^o = ^0 = Sum.inl`, `^i = ^1 = Sum.inr` convention), binders are natural, no `[Finite α]`.
4. ✅ `verify_equivalence` — PASS. Equivalence verifier walked the checklist: hypotheses (`hW`, `hr`/`h`) and conclusion shapes match the LN block on both halves. The split into two theorems matches the LN's two sentences. No silent strengthening / weakening.
5. ✅ `add_design_choice_comments` — Done. Two extra bullets added (J/V partition preservation point, and the long-form naming rationale); file-level docstring + per-theorem docstrings already covered the rest. Comments-only edit; no Lean code changed.
6. ⏳ `new_manager` handoff to proof-phase manager (Manager B). Statement phase complete.

## Statement-phase outcome

- **Lean file:** `Section3_2/SwigAcyclicTopologicalOrder.lean` — two theorems, both `sorry`.
- **TeX statement subfile:** `Section3_2/tex/claim_3_9_statement_SwigAcyclicTopologicalOrder.tex` (auto-generated by orchestrator at row start).
- **TeX proof subfile:** `Section3_2/tex/claim_3_9_proof_SwigAcyclicTopologicalOrder.tex` — currently a stub with the `\Rem` statement restated and an empty `proof` environment ready for Manager B's tex writer.
- **Build state:** `lake build` clean from the repo root, modulo the two expected `sorry` warnings.

## What's been tried

_(nothing yet — first turn pipeline, no failures encountered.)_
