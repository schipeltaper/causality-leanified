{
  "ref": "claim_3_14",
  "title": "AddingInterventionNodes",
  "type": "lemma",
  "def_or_claim": "claim",
  "section": "3.2",
  "lean_file_path": "leanification/Chapter3_GraphTheory/Section3_2/AddingInterventionNodesCommute.lean",
  "lean_statement": [
    {
      "name": "subset_extendingCDMGWithInterventionNodes_J_union_V_of_subset",
      "kind": "theorem",
      "code": "-- helper required to state the chain-1 rows\n/-- Chain-1 helper: if `W₂ ⊆ G.J ∪ G.V`, then `Sum.inl '' W₂` is\ncontained in the node-set union of `G.extendingCDMGWithInterventionNodes\nW₁ hW₁`. Discharges the outer-`ext` precondition for\n`((G.ext W₁ hW₁).ext (Sum.inl '' W₂) _)` in chain 1. -/\ntheorem subset_extendingCDMGWithInterventionNodes_J_union_V_of_subset\n    {G : CDMG α} {W₁ W₂ : Set α} (hW₁ : W₁ ⊆ G.J ∪ G.V)\n    (hW₂ : W₂ ⊆ G.J ∪ G.V) :\n    Sum.inl '' W₂ ⊆\n      (G.extendingCDMGWithInterventionNodes W₁ hW₁).J ∪\n        (G.extendingCDMGWithInterventionNodes W₁ hW₁).V := by"
    },
    {
      "name": "subset_hardInterventionOn_J_union_V_of_disjoint",
      "kind": "theorem",
      "code": "-- helper required to state the chain-2 row\n/-- Chain-2 helper: if `W₁ ⊆ G.J ∪ G.V` and `Disjoint W₁ W₂`, then\n`W₁ ⊆ (G.hardInterventionOn W₂).J ∪ (G.hardInterventionOn W₂).V`.\nDischarges the outer-`ext` precondition for\n`((G.hardInterventionOn W₂).ext W₁ _)` in chain 2. -/\ntheorem subset_hardInterventionOn_J_union_V_of_disjoint\n    {G : CDMG α} {W₁ W₂ : Set α} (hW₁ : W₁ ⊆ G.J ∪ G.V)\n    (hdisj : Disjoint W₁ W₂) :\n    W₁ ⊆ (G.hardInterventionOn W₂).J ∪ (G.hardInterventionOn W₂).V := by"
    },
    {
      "name": "extendingCDMGWithInterventionNodes_fusion_equiv",
      "kind": "def",
      "code": "/-- claim_3_14 chain 1, part 1/2 (fusion lemma): iterating\n`extendingCDMGWithInterventionNodes` on disjoint `W₁`, `W₂` is\n`CDMGEquiv`-equivalent to a single extension on `W₁ ∪ W₂`. Mirrors\nthe second `=` of chain 1 in the LN's `\\Lem` at\n`lecture-notes/lecture_notes/graphs.tex` line 836:\n`(G_{do(I_{W₁})})_{do(I_{W₂})} = G_{do(I_{W₁ ∪ W₂})}`. -/\nnoncomputable def extendingCDMGWithInterventionNodes_fusion_equiv\n    {G : CDMG α} {W₁ W₂ : Set α}\n    (hW₁ : W₁ ⊆ G.J ∪ G.V) (hW₂ : W₂ ⊆ G.J ∪ G.V)\n    (hdisj : Disjoint W₁ W₂) :\n    CDMGEquiv\n      ((G.extendingCDMGWithInterventionNodes W₁ hW₁).extendingCDMGWithInterventionNodes\n          (Sum.inl '' W₂)\n          (subset_extendingCDMGWithInterventionNodes_J_union_V_of_subset hW₁ hW₂))\n      (G.extendingCDMGWithInterventionNodes (W₁ ∪ W₂)\n          (Set.union_subset hW₁ hW₂)) := by"
    },
    {
      "name": "extendingCDMGWithInterventionNodes_comm_equiv",
      "kind": "def",
      "code": "/-- claim_3_14 chain 1, part 2/2 (commute corollary): swapping `W₁` and\n`W₂` in the iterated extension is `CDMGEquiv`-symmetric. Mirrors the\nfirst `=` of chain 1:\n`(G_{do(I_{W₁})})_{do(I_{W₂})} = (G_{do(I_{W₂})})_{do(I_{W₁})}`. -/\nnoncomputable def extendingCDMGWithInterventionNodes_comm_equiv\n    {G : CDMG α} {W₁ W₂ : Set α}\n    (hW₁ : W₁ ⊆ G.J ∪ G.V) (hW₂ : W₂ ⊆ G.J ∪ G.V)\n    (hdisj : Disjoint W₁ W₂) :\n    CDMGEquiv\n      ((G.extendingCDMGWithInterventionNodes W₁ hW₁).extendingCDMGWithInterventionNodes\n          (Sum.inl '' W₂)\n          (subset_extendingCDMGWithInterventionNodes_J_union_V_of_subset hW₁ hW₂))\n      ((G.extendingCDMGWithInterventionNodes W₂ hW₂).extendingCDMGWithInterventionNodes\n          (Sum.inl '' W₁)\n          (subset_extendingCDMGWithInterventionNodes_J_union_V_of_subset hW₂ hW₁)) := by"
    },
    {
      "name": "extendingCDMGWithInterventionNodes_hardInterventionOn_comm_equiv",
      "kind": "def",
      "code": "/-- claim_3_14 chain 2: hard intervention on `W₂` and extension by\nintervention nodes on `W₁` commute when `Disjoint W₁ W₂`. Mirrors the\nfirst `=` of chain 2 in the LN's `\\Lem` at\n`lecture-notes/lecture_notes/graphs.tex` line 839:\n`(G_{do(I_{W₁})})_{do(W₂)} = (G_{do(W₂)})_{do(I_{W₁})}`. -/\nnoncomputable def extendingCDMGWithInterventionNodes_hardInterventionOn_comm_equiv\n    {G : CDMG α} {W₁ W₂ : Set α}\n    (hW₁ : W₁ ⊆ G.J ∪ G.V) (hdisj : Disjoint W₁ W₂) :\n    CDMGEquiv\n      ((G.extendingCDMGWithInterventionNodes W₁ hW₁).hardInterventionOn\n          (Sum.inl '' W₂))\n      ((G.hardInterventionOn W₂).extendingCDMGWithInterventionNodes W₁\n          (subset_hardInterventionOn_J_union_V_of_disjoint hW₁ hdisj)) := by"
    }
  ],
  "lean_explanation": "The Lean transcription works over `α : Type u`, the ambient vertex type matching the LN's silent assumption that the graph lives over some carrier; `G : CDMG α` is the LN's $G = (J, V, E, L)$, and `W₁ W₂ : Set α` are the two LN subsets $W_1, W_2 \\subseteq J \\cup V$. The hypotheses `hW₁`, `hW₂` express the LN's $W_i \\subseteq J \\cup V$ assumptions, and `hdisj : Disjoint W₁ W₂` is the LN's disjointness assumption (Mathlib's order-theoretic `Disjoint`, specialised here to sets).\n\nThe LN's $\\doit(I_W)$ is the Lean operator `G.extendingCDMGWithInterventionNodes W _`, which mints a fresh intervention-input node $I_w$ for each $w \\in W \\setminus J$ and rebases the carrier to a `Sum`: `α ⊕ ↑(W \\ G.J)`. The LN's $\\doit(W)$ (hard intervention) is `G.hardInterventionOn W`, which promotes `W ∩ V` into the input layer and is carrier-preserving. Because iterating a carrier-changing operator like `extendingCDMGWithInterventionNodes` produces CDMGs over *different* `Sum`-typed carriers on the two sides of each LN equality, the LN's `=` signs cannot be stated as Lean `Eq` — they ship as `CDMGEquiv`, a structure (defined in `TwoDisjointNodeSplittingsCommute.lean` and imported here) bundling a carrier-rewriting `Equiv` plus four `image`-equality fields for `J`, `V`, `E`, and `L`.\n\nSeveral Mathlib idioms appear in the signatures: `Sum.inl '' W₂` is the canonical embedding of the original `W₂ : Set α` into the extended carrier (the LN's implicit identification of $W_2$ with its image in the new graph); `Set.union_subset hW₁ hW₂` combines the two preconditions for the merged third term in chain 1; subtypes like `↑(W₁ \\ G.J)` (carrier-level encoding of the LN's $I_j := j$ convention for $j \\in J$) appear inside the `Sum`. Implicit `{G W₁ W₂}` binders let the conclusion pin down the sets, with the hypotheses explicit.\n\nThe row delivers three `CDMGEquiv`-valued declarations — chain-1 fusion (collapsing two iterated intervention-node extensions into one on $W_1 \\cup W_2$), chain-1 commute (swapping $W_1 \\leftrightarrow W_2$, derived from fusion), and chain-2 commute (swapping a hard intervention with an intervention-node extension) — plus two named subset helpers (`subset_extendingCDMGWithInterventionNodes_J_union_V_of_subset`, `subset_hardInterventionOn_J_union_V_of_disjoint`) that discharge the type-level preconditions of the iterated extensions.",
  "design_choices": "**`CDMGEquiv` rather than literal `Eq` — forced by typing, not stylistic.** Both LN chains read as set-equalities at the CDMG-data level, but the iterated CDMGs live over different carriers: chain 1's iterated side has a nested `Sum` (`(α ⊕ ↑(W₁ \\ G.J)) ⊕ ↑(...)`) while its joint third term has a flat `Sum` (`α ⊕ ↑((W₁ ∪ W₂) \\ G.J)`); chain 2's two sides both have shape `α ⊕ ↑(W₁ \\ ?)` but with `?` differing between `G.J` and `G.J ∪ W₂`. Under `Disjoint W₁ W₂` the two predicates denote the same set, but they are not definitionally equal as predicates, so a literal Lean `Eq` is not type-correct even with the disjointness hypothesis in scope. `CDMGEquiv` (with its `refl`/`symm`/`trans` groupoid laws) is the categorified version: a carrier-rewriting `Equiv` plus four image-equality fields for `J`, `V`, `E`, and `L`. The alternative — unfolding the `Sum`s and subtype predicates to bring the carriers into definitional equality — would require modifying the underlying `extendingCDMGWithInterventionNodes` definition, far out of scope.\n\n**Fusion + commute split for chain 1, mirroring the LN's \"by symmetry\" close.** The LN states a chained equality $(G_{\\doit(I_{W_1})})_{\\doit(I_{W_2})} = (G_{\\doit(I_{W_2})})_{\\doit(I_{W_1})} = G_{\\doit(I_{W_1 \\cup W_2})}$ and only proves the *fusion* direction (the second `=`); the first is closed \"by symmetry\". The Lean factoring matches the LN: `extendingCDMGWithInterventionNodes_fusion_equiv` is the load-bearing fusion lemma, and `extendingCDMGWithInterventionNodes_comm_equiv` is a short corollary composing two fusion calls through a `Set.union_comm` bridge (`Equiv.subtypeEquivRight` absorbing the def-distinct subtype carriers `↑((W₁ ∪ W₂) \\ G.J)` vs `↑((W₂ ∪ W₁) \\ G.J)`). Folding the commute into the fusion lemma's conclusion would force every consumer who only wants to swap $W_1 \\leftrightarrow W_2$ (without collapsing) to chain two fusion calls themselves at each site, with the union-comm bridge re-derived locally.\n\n**Joint third term materialised for chain 1, but *not* for chain 2.** The LN's chain-1 third term $G_{\\doit(I_{W_1 \\cup W_2})}$ is a literal single extension on the union — the same operator `extendingCDMGWithInterventionNodes` evaluated at `W₁ ∪ W₂` — so the fusion lemma's right-hand side is a real Lean term in the conclusion. By contrast, the LN's chain-2 third term $G_{\\doit(I_{W_1}, W_2)}$ is *notation* for the unambiguous value of either side (as the commented-out LN lines just below the `\\Lem` make explicit), not a separate operator. Minting a fresh `do(I_·, ·)` joint operator just to materialise it in Lean would duplicate the API for no payoff: a new CDMG definition with its own four `@[simp]` characterisations, threaded through every downstream caller that currently chains `hardInterventionOn` and `extendingCDMGWithInterventionNodes` directly. The chain-2 `CDMGEquiv` consequently has only the two iterates in its conclusion, and any consumer who wants \"the joint value\" picks one side by `.symm` if needed.\n\n**Named subset helpers, not inlined preconditions.** Both chains' iterated extensions need to type-check a precondition of shape `W ⊆ inner.J ∪ inner.V` where `inner` is itself an `extendingCDMGWithInterventionNodes` (chain 1) or a `hardInterventionOn` (chain 2). Discharging these inline as `by simp; ...` blocks in the headline `CDMGEquiv`-valued signatures would drown the conclusion in tactic noise and force every consumer to re-prove the same subset fact under any goal-state perturbation. The two named helpers — `subset_extendingCDMGWithInterventionNodes_J_union_V_of_subset` for chain 1, `subset_hardInterventionOn_J_union_V_of_disjoint` for chain 2 — factor this out once. The chain-1 helper does *not* need `Disjoint W₁ W₂`, because intervention-node extension is purely additive on the input layer (original nodes survive untouched into the extended graph); the chain-2 helper *does* need disjointness, because hard intervention is a *deletion* on the `V` side (`hardInterventionOn_V = G.V \\ W₂`), so a vertex `w ∈ W₁ ∩ G.V ∩ W₂` would fall out of the extended `V` without disjointness. This is the formal transcription of the LN's implicit $W_1 \\cap (V \\setminus W_2) = W_1 \\cap V$ step under disjointness.\n\n**`Sum.inl ''`-lift on iterated-extension targets.** Each chain's iterated side applies an outer operator (extension or hard intervention) on top of an inner `extendingCDMGWithInterventionNodes W₁`, whose carrier is `α ⊕ ↑(W₁ \\ G.J)`. The outer operator's target set must therefore be a `Set (α ⊕ ↑(W₁ \\ G.J))`, not the bare `Set α` of the LN. The LN-faithful lift is `Sum.inl '' W₂` — the canonical embedding under the convention `Sum.inl = original-α-side` established by the extension construction — and any alternative lift would either be type-incorrect or obscure the LN's implicit $\\alpha \\cong \\mathrm{Sum.inl}\\,''\\,\\alpha$ identification.\n\n**Foundation reuse: `CDMGEquiv` imported from `TwoDisjointNodeSplittingsCommute.lean`.** This row is the third consumer of the `CDMGEquiv` structure (after `claim_3_7` and `claim_3_10`). Promoting it to a shared `Section3_1/` foundation file was considered as the natural \"third-consumer\" trigger, but rejected: the import cost from each consumer is a single line, no fourth consumer is queued in Section 3.2 with a pinned Lean shape, and the reverse move (merging a promoted file back) is cheap if the promotion turns out wrong. The promotion will be revisited if `claim_3_15` or a chapter-4 row needs the same shape."
}