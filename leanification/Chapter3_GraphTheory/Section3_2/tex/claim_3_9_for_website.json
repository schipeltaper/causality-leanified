{
  "ref": "claim_3_9",
  "title": "SwigAcyclicTopologicalOrder",
  "type": "remark",
  "def_or_claim": "claim",
  "section": "3.2",
  "lean_file_path": "leanification/Chapter3_GraphTheory/Section3_2/SwigAcyclicTopologicalOrder.lean",
  "lean_statement": [
    {
      "name": "isTopologicalOrder_nodeSplittingHardInterventionOn",
      "kind": "theorem",
      "code": "/-- claim_3_9 part A: if `W ⊆ G.V` and `r` is a topological order of\n`G`, then `splitOrder W r` is a topological order of\n`G.nodeSplittingHardInterventionOn W hW` (the SWIG). Mirrors the\nconstructive half of the `\\Rem` immediately after def_3_12 in\n`lecture-notes/lecture_notes/graphs.tex` (line 612), with the LN's\n`± 1/3` interleaving recipe for `v_j^o` / `v_j^i` encoded as the\nfour-case `splitOrder` pattern match -- the *same* `splitOrder W r`\nrelation produced by claim_3_6 (whose recipe for `v_j^0` / `v_j^1`\nagrees with the SWIG's `v_j^o` / `v_j^i` under our\n`^o = ^0 = Sum.inl`, `^i = ^1 = Sum.inr` convention).\n\nThe `W ⊆ G.V` precondition is structurally required by\n`nodeSplittingHardInterventionOn` itself (def_3_12), inherited from\n`nodeSplittingOn` def_3_11; it is *not* a topological-order\ncondition.\n\nSee `isAcyclic_nodeSplittingHardInterventionOn` below for the\nacyclicity half of the LN remark and the file-level docstring for\nthe rationale behind splitting the LN's single `\\Rem` into two\ntheorems. This row is the corollary chain of claim_3_3 + claim_3_6;\nboth halves are short composites of the existing theorems. -/\ntheorem isTopologicalOrder_nodeSplittingHardInterventionOn\n    {G : CDMG α} {W : Set α} (hW : W ⊆ G.V)\n    {r : α → α → Prop} (hr : G.IsTopologicalOrder r) :\n    (G.nodeSplittingHardInterventionOn W hW).IsTopologicalOrder\n      (splitOrder W r) := by"
    },
    {
      "name": "isAcyclic_nodeSplittingHardInterventionOn",
      "kind": "theorem",
      "code": "/-- claim_3_9 part B: if `W ⊆ G.V` and `G` is acyclic, then\n`G.nodeSplittingHardInterventionOn W hW` (the SWIG) is acyclic.\nMirrors the acyclicity half of the `\\Rem` immediately after def_3_12\nin `lecture-notes/lecture_notes/graphs.tex` (line 612); see\n`isTopologicalOrder_nodeSplittingHardInterventionOn` above for the\nconstructive half and the file-level docstring for the rationale\nbehind splitting the LN's single `\\Rem` into two theorems.\n\nThe `W ⊆ G.V` precondition is structurally required by\n`nodeSplittingHardInterventionOn` itself (def_3_12); see\n`isTopologicalOrder_nodeSplittingHardInterventionOn`. No `[Finite α]`\nhypothesis is added at the statement phase, keeping both proof routes\nopen (composite via claim_3_6 part B + claim_3_3 part A, or direct\nwalk-lifting on the SWIG). -/\ntheorem isAcyclic_nodeSplittingHardInterventionOn\n    {G : CDMG α} {W : Set α} (hW : W ⊆ G.V)\n    (h : G.IsAcyclic) :\n    (G.nodeSplittingHardInterventionOn W hW).IsAcyclic := by"
    }
  ],
  "lean_explanation": "The two theorems share the same ambient setup. `α : Type*` is the carrier type of vertex names (matching the LN's silent assumption that the graph is over some fixed vertex set), and `G : CDMG α` is the LN's conditional directed mixed graph $G = (J, V, E, L)$ — the unconditional-and-not-necessarily-acyclic carrier; the LN's CADMG refinement is modelled here by carrying the acyclicity hypothesis `h : G.IsAcyclic` explicitly rather than baking it into the type. `W : Set α` is the LN's intervention target $W \\subseteq V$, and `hW : W ⊆ G.V` is the structural precondition that `nodeSplittingHardInterventionOn` (the formal name for $G_{\\swig(W)}$, def_3_12) demands; it is not a topological-order condition.\n\nIn Part A, `r : α → α → Prop` is the LN's `<`, with `hr : G.IsTopologicalOrder r` asserting that it enumerates $J \\cup V$ as a topological order of $G$. The conclusion produces `splitOrder W r`, a single named binary relation on the SWIG's carrier `α ⊕ ↑W` (`α ⊕ ↑W` is the disjoint sum of the original vertex type and the subtype of `W`, used to keep the fresh $W^o$ and $W^i$ copies distinct from the original nodes — `Sum.inl` injects the originals, `Sum.inr` carries the fresh copies). The same `splitOrder W r` relation is the one produced by claim_3_6 part A on the node-split graph; here it is reused unchanged because the hard-intervention step preserves the carrier. In Part B the carrier and binders are identical except that `hr` is replaced by `h : G.IsAcyclic` and the conclusion is `(G.nodeSplittingHardInterventionOn W hW).IsAcyclic`. Both theorems use Lean's implicit binders `{G}` / `{W}` / `{r}` for arguments recoverable by unification from the conclusion or from `hW` / `hr`.",
  "design_choices": "**The single LN remark splits into two theorems.** The LN's $\\Rem$ block packs together a *named-construction* claim (the topological order on $G_{\\swig(W)}$ is the explicit $\\pm 1/3$ interleave) with a *side-condition* claim (the SWIG is acyclic). Bundling these into a single `IsAcyclic ∧ IsTopologicalOrder` conjunction would force every consumer that only wants one side to project (`.1` / `.2`) or to drag in data they do not need: a caller proving an acyclicity fact about the SWIG should not need to invent a topological order on $G$ first, and a caller plugging the topological order into a downstream construction should not be projecting through a conjunction. The same two-theorem split is used by claim_3_6 (the node-splitting analogue) and claim_3_3 (the hard-intervention analogue).\n\n**`splitOrder W r` is reused by name, not redefined.** The SWIG is definitionally a node-splitting followed by a hard intervention on the fresh inner copies, and the hard-intervention step preserves the underlying carrier `α ⊕ ↑W`. So the `splitOrder W r` relation introduced by claim_3_6 — a four-case pattern match on `Sum.inl` / `Sum.inr` that realises the LN's $\\pm 1/3$ index recipe — is already typed on the SWIG's carrier and is the *exact* relation described by the SWIG's $v_j^o$ at index $j - 1/3$, $v_j^i$ at index $j + 1/3$ recipe (under the convention $^o = ^0 = $ `Sum.inl`, $^i = ^1 = $ `Sum.inr` fixed when node-splitting was formalised). Redefining a separate `swigOrder` relation would have produced a syntactic alias of the existing one and forced downstream counterfactual chapters to talk about two named orders instead of one.\n\n**`G.IsAcyclic` is an explicit hypothesis, not a typeclass on `CDMG`.** `CDMG α` is the LN's unconditional carrier (def_3_1) — it does not bake in acyclicity. The LN's CADMG refinement is modelled here by passing `G.IsAcyclic` as an explicit `Prop`-valued hypothesis (defined in `Acyclicity.lean`). Bundling acyclicity into a typeclass on `CDMG` would force every chapter-3 lemma to restate the instance, and would commit to a refinement that some later chapter-4-to-6 rows want to relax again.\n\n**No `[Finite α]` hypothesis at the statement phase.** Both theorems are stated without finiteness. Part A's topological-order argument is purely relational, and Part B's acyclicity argument (via the proof-time composite of `isAcyclic_nodeSplittingOn` and `isAcyclic_hardInterventionOn`) does not introduce finiteness either. Leaving `[Finite α]` out at this level keeps the result usable in later chapters that work with potentially infinite vertex types."
}