{
  "ref": "claim_3_4",
  "kind": "claim",
  "section": "3.2",
  "title": "HardInterventionsCommute",
  "type": "lemma",
  "status": {
    "formalized": "yes",
    "proven": "proven",
    "solved": "yes"
  },
  "tex_block_html": "<p>Let $G:=(J,V,E,L)$ be a CDMG and $W_1, W_2 \\ins J \\cup V$ two  subsets of nodes from $G$. \n      Then we have:\n      \\[ \\lp G_{\\doit(W_1)} \\rp_{\\doit(W_2)} = \\lp G_{\\doit(W_2)} \\rp_{\\doit(W_1)} =  G_{\\doit(W_1 \\cup W_2)}. \\]</p>",
  "tex_statement": {
    "raw": "% Claim statement file for row claim_3_4 -- \"HardInterventionsCommute\".\n% Auto-generated stub by scaffold/solve_chapter.py at row start. The\n% manager agent (or a worker it dispatches) fills the body below with the\n% claim's statement(s) from the lecture notes -- statement only, no proof\n% (proofs live in the sibling `claim_3_4_proof_*.tex` file).\n%\n% This file is a sibling of `main.tex` inside `<subsection>/tex/`. The\n% `subfiles` package lets it render both standalone and as part of\n% `main.tex`. Note: `main.tex` does NOT \\subfile this statement file -- the\n% sibling `_proof_` file restates the statement above the proof, so\n% including both in `main.tex` would be redundant. This file exists for\n% standalone reading / grep convenience.\n\n\\documentclass[main]{subfiles}\n\n\\begin{document}\n% ref: claim_3_4 (statement)\n% title: HardInterventionsCommute\n\\def\\rowref{claim\\_3\\_4}\n\\phantomsection\\label{claim_3_4}\n\n\\begin{Lem}[Hard interventions commute]\\label{hard-interventions-commute}\n    Let $G = (J, V, E, L)$ be a CDMG (def \\ref{def-cdmg}); throughout we use the notation of def \\ref{not-cdmg} and recall the typing and axioms of def \\ref{def-cdmg}, namely $J \\cap V = \\emptyset$, $E \\subseteq (J \\cup V) \\times V$, $L \\subseteq V \\times V$, $L$ irreflexive ($(v_1, v_2) \\in L \\Rightarrow v_1 \\neq v_2$), and $L$ symmetric ($(v_1, v_2) \\in L \\Leftrightarrow (v_2, v_1) \\in L$). Let\n    \\[ W_1 \\subseteq J \\cup V \\quad\\text{and}\\quad W_2 \\subseteq J \\cup V \\]\n    be two subsets of nodes (no disjointness between $W_1$ and $W_2$ is assumed, and overlap with $J$ is permitted, as per def \\ref{def:G_hard_intervention}'s precondition). All three quantifiers ``for every CDMG $G$'', ``for every $W_1 \\subseteq J \\cup V$'', and ``for every $W_2 \\subseteq J \\cup V$'' are read \\emph{universally}.\n\n    \\emph{Carrier matching for the iterated hard interventions.} The inner two terms of the equality below are iterated hard interventions of the form $(G_{\\doit(W_i)})_{\\doit(W_j)}$, in which $\\doit(W_j)$ is applied to the CDMG $G_{\\doit(W_i)}$ produced by def \\ref{def:G_hard_intervention}. For this outer application to be well-typed per def \\ref{def:G_hard_intervention}'s precondition ``$W \\subseteq J \\cup V$ of the input CDMG'', we need $W_j$ to be a subset of the carrier of $G_{\\doit(W_i)}$, i.e.\\\n    \\[ W_j \\;\\subseteq\\; J_{\\doit(W_i)} \\cup V_{\\doit(W_i)}. \\]\n    This holds because hard intervention preserves the carrier $J \\cup V$ as a set: by items i.\\ and ii.\\ of def \\ref{def:G_hard_intervention},\n    \\[\n        J_{\\doit(W_i)} \\cup V_{\\doit(W_i)} \\;=\\; (J \\cup W_i) \\cup (V \\sm W_i) \\;=\\; J \\cup V,\n    \\]\n    where the second equality uses $W_i \\subseteq J \\cup V$ (as in the carrier-matching paragraph of \\ref{claim_3_3}). Hence $W_j \\subseteq J \\cup V = J_{\\doit(W_i)} \\cup V_{\\doit(W_i)}$, so $(G_{\\doit(W_i)})_{\\doit(W_j)}$ is defined. The same observation applied to $W_1 \\cup W_2 \\subseteq J \\cup V$ shows that the right-hand side $G_{\\doit(W_1 \\cup W_2)}$ is also defined per def \\ref{def:G_hard_intervention}.\n\n    \\emph{The conclusion.} The CDMGs $(G_{\\doit(W_1)})_{\\doit(W_2)}$, $(G_{\\doit(W_2)})_{\\doit(W_1)}$, and $G_{\\doit(W_1 \\cup W_2)}$ (all constructed via def \\ref{def:G_hard_intervention} as just verified) coincide. Concretely, the LN's displayed triple equality\n    \\[ \\lp G_{\\doit(W_1)} \\rp_{\\doit(W_2)} = \\lp G_{\\doit(W_2)} \\rp_{\\doit(W_1)} = G_{\\doit(W_1 \\cup W_2)} \\]\n    is the conjunction of the two following equalities of CDMGs:\n    \\begin{enumerate}[label=(\\alph*)]\n        \\item \\emph{Composition with $W_1$ then $W_2$.}\n              \\[ \\lp G_{\\doit(W_1)} \\rp_{\\doit(W_2)} \\;=\\; G_{\\doit(W_1 \\cup W_2)}. \\]\n        \\item \\emph{Composition with $W_2$ then $W_1$.}\n              \\[ \\lp G_{\\doit(W_2)} \\rp_{\\doit(W_1)} \\;=\\; G_{\\doit(W_1 \\cup W_2)}. \\]\n    \\end{enumerate}\n    Together (a) and (b) yield the LN's ``symmetry under swap'' reading $\\lp G_{\\doit(W_1)} \\rp_{\\doit(W_2)} = \\lp G_{\\doit(W_2)} \\rp_{\\doit(W_1)}$ by transitivity of equality. The equality of CDMGs in (a) and (b) is understood componentwise on the four components $(J, V, E, L)$ of def \\ref{def-cdmg}, but the conjunctive unpacking into the four field-by-field equalities is deferred to the proof.\n\n      %This shows that our notion:\n      %\\[ G(V \\sm (W_1 \\cup W_2) | \\doit(W_1 \\cup W_2 \\cup J))   \\]\n      %is unambiguous.\n\\end{Lem}\n\n\\end{document}\n",
    "html": "<p>Let $G = (J, V, E, L)$ be a CDMG (def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>); throughout we use the notation of def <a href=\"#def_3_2\" class=\"refrow\"><code>not-cdmg</code></a> and recall the typing and axioms of def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>, namely $J \\cap V = \\emptyset$, $E \\subseteq (J \\cup V) \\times V$, $L \\subseteq V \\times V$, $L$ irreflexive ($(v_1, v_2) \\in L \\Rightarrow v_1 \\neq v_2$), and $L$ symmetric ($(v_1, v_2) \\in L \\Leftrightarrow (v_2, v_1) \\in L$). Let\n    \\[ W_1 \\subseteq J \\cup V \\quad\\text{and}\\quad W_2 \\subseteq J \\cup V \\]\n    be two subsets of nodes (no disjointness between $W_1$ and $W_2$ is assumed, and overlap with $J$ is permitted, as per def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a>'s precondition). All three quantifiers “for every CDMG $G$”, “for every $W_1 \\subseteq J \\cup V$”, and “for every $W_2 \\subseteq J \\cup V$” are read <em>universally</em>.</p>\n<p><em>Carrier matching for the iterated hard interventions.</em> The inner two terms of the equality below are iterated hard interventions of the form $(G_{\\doit(W_i)})_{\\doit(W_j)}$, in which $\\doit(W_j)$ is applied to the CDMG $G_{\\doit(W_i)}$ produced by def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a>. For this outer application to be well-typed per def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a>'s precondition “$W \\subseteq J \\cup V$ of the input CDMG”, we need $W_j$ to be a subset of the carrier of $G_{\\doit(W_i)}$, i.e.\\\n    \\[ W_j \\;\\subseteq\\; J_{\\doit(W_i)} \\cup V_{\\doit(W_i)}. \\]\n    This holds because hard intervention preserves the carrier $J \\cup V$ as a set: by items i.\\ and ii.\\ of def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a>,\n    \\[\n        J_{\\doit(W_i)} \\cup V_{\\doit(W_i)} \\;=\\; (J \\cup W_i) \\cup (V \\sm W_i) \\;=\\; J \\cup V,\n    \\]\n    where the second equality uses $W_i \\subseteq J \\cup V$ (as in the carrier-matching paragraph of <a href=\"#claim_3_3\" class=\"refrow\"><code>claim_3_3</code></a>). Hence $W_j \\subseteq J \\cup V = J_{\\doit(W_i)} \\cup V_{\\doit(W_i)}$, so $(G_{\\doit(W_i)})_{\\doit(W_j)}$ is defined. The same observation applied to $W_1 \\cup W_2 \\subseteq J \\cup V$ shows that the right-hand side $G_{\\doit(W_1 \\cup W_2)}$ is also defined per def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a>.</p>\n<p><em>The conclusion.</em> The CDMGs $(G_{\\doit(W_1)})_{\\doit(W_2)}$, $(G_{\\doit(W_2)})_{\\doit(W_1)}$, and $G_{\\doit(W_1 \\cup W_2)}$ (all constructed via def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a> as just verified) coincide. Concretely, the LN's displayed triple equality\n    \\[ \\lp G_{\\doit(W_1)} \\rp_{\\doit(W_2)} = \\lp G_{\\doit(W_2)} \\rp_{\\doit(W_1)} = G_{\\doit(W_1 \\cup W_2)} \\]\n    is the conjunction of the two following equalities of CDMGs:</p>\n<ol class=\"enum\"><li><em>Composition with $W_1$ then $W_2$.</em>\n              \\[ \\lp G_{\\doit(W_1)} \\rp_{\\doit(W_2)} \\;=\\; G_{\\doit(W_1 \\cup W_2)}. \\]</li><li><em>Composition with $W_2$ then $W_1$.</em>\n              \\[ \\lp G_{\\doit(W_2)} \\rp_{\\doit(W_1)} \\;=\\; G_{\\doit(W_1 \\cup W_2)}. \\]</li></ol>\n<p>Together (a) and (b) yield the LN's “symmetry under swap” reading $\\lp G_{\\doit(W_1)} \\rp_{\\doit(W_2)} = \\lp G_{\\doit(W_2)} \\rp_{\\doit(W_1)}$ by transitivity of equality. The equality of CDMGs in (a) and (b) is understood componentwise on the four components $(J, V, E, L)$ of def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>, but the conjunctive unpacking into the four field-by-field equalities is deferred to the proof.</p>",
    "env": "Lem",
    "env_title": "Hard interventions commute",
    "source_path": "leanification/Chapter3_GraphTheory/Section3_2/tex/claim_3_4_statement_HardInterventionsCommute.tex"
  },
  "tex_proof": {
    "raw": "% Claim proof file for row claim_3_4 -- \"HardInterventionsCommute\".\n% Refactor twin of `claim_3_4_proof_HardInterventionsCommute.tex` for the\n% `cdmg_typed_edges` refactor.  The LN statement is unchanged; the proof\n% body is identical to the original on the (J, V, E) components, and the\n% L-component paragraph plus its closing \"Remark on the Lean encoding of\n% L\" are revised to reflect the post-refactor `Sym2 Node` encoding of L\n% (under which the previously-registered deviation\n% hard_intervention_l_symmetrized_removal is structurally resolved at the\n% def_3_10 row).\n%\n% This file is a sibling of `main.tex` inside `<subsection>/tex/`. The\n% `subfiles` package lets it render both standalone and as part of\n% `main.tex` via `\\subfile{...}`.\n%\n% The statement is restated above the proof so the file is self-contained\n% when rendered on its own. The proof must:\n%   - Stay close to the lecture notes' proof if one exists (always search\n%     the LN first for a `\\begin{proof}` block immediately following the\n%     claim; copy / lightly edit when present).\n%   - Otherwise use the LN paradigm and cite prior claims by their `ref`.\n%   - Be self-contained enough that a mathematician can follow it without\n%     re-reading the LN.\n%   - Have no `sorry`, `omitted`, or \"obvious\" shortcuts.\n\n\\documentclass[main]{subfiles}\n\n\\begin{document}\n% ref: claim_3_4 (proof, with statement restated for readability)\n% title: HardInterventionsCommute\n\\def\\rowref{claim\\_3\\_4}\n\\phantomsection\\label{claim_3_4}\n\n% --- statement (restated) ---------------------------------------------------\n\\begin{Lem}[Hard interventions commute]\\label{hard-interventions-commute}\n    Let $G = (J, V, E, L)$ be a CDMG (def \\ref{def-cdmg}); throughout we use the notation of def \\ref{not-cdmg} and recall the typing and axioms of def \\ref{def-cdmg}, namely $J \\cap V = \\emptyset$, $E \\subseteq (J \\cup V) \\times V$, $L \\subseteq V \\times V$, $L$ irreflexive ($(v_1, v_2) \\in L \\Rightarrow v_1 \\neq v_2$), and $L$ symmetric ($(v_1, v_2) \\in L \\Leftrightarrow (v_2, v_1) \\in L$). Let\n    \\[ W_1 \\subseteq J \\cup V \\quad\\text{and}\\quad W_2 \\subseteq J \\cup V \\]\n    be two subsets of nodes (no disjointness between $W_1$ and $W_2$ is assumed, and overlap with $J$ is permitted, as per def \\ref{def:G_hard_intervention}'s precondition). All three quantifiers ``for every CDMG $G$'', ``for every $W_1 \\subseteq J \\cup V$'', and ``for every $W_2 \\subseteq J \\cup V$'' are read \\emph{universally}.\n\n    \\emph{Carrier matching for the iterated hard interventions.} The inner two terms of the equality below are iterated hard interventions of the form $(G_{\\doit(W_i)})_{\\doit(W_j)}$, in which $\\doit(W_j)$ is applied to the CDMG $G_{\\doit(W_i)}$ produced by def \\ref{def:G_hard_intervention}. For this outer application to be well-typed per def \\ref{def:G_hard_intervention}'s precondition ``$W \\subseteq J \\cup V$ of the input CDMG'', we need $W_j$ to be a subset of the carrier of $G_{\\doit(W_i)}$, i.e.\\\n    \\[ W_j \\;\\subseteq\\; J_{\\doit(W_i)} \\cup V_{\\doit(W_i)}. \\]\n    This holds because hard intervention preserves the carrier $J \\cup V$ as a set: by items i.\\ and ii.\\ of def \\ref{def:G_hard_intervention},\n    \\[\n        J_{\\doit(W_i)} \\cup V_{\\doit(W_i)} \\;=\\; (J \\cup W_i) \\cup (V \\sm W_i) \\;=\\; J \\cup V,\n    \\]\n    where the second equality uses $W_i \\subseteq J \\cup V$ (as in the carrier-matching paragraph of \\ref{claim_3_3}). Hence $W_j \\subseteq J \\cup V = J_{\\doit(W_i)} \\cup V_{\\doit(W_i)}$, so $(G_{\\doit(W_i)})_{\\doit(W_j)}$ is defined. The same observation applied to $W_1 \\cup W_2 \\subseteq J \\cup V$ shows that the right-hand side $G_{\\doit(W_1 \\cup W_2)}$ is also defined per def \\ref{def:G_hard_intervention}.\n\n    \\emph{The conclusion.} The CDMGs $(G_{\\doit(W_1)})_{\\doit(W_2)}$, $(G_{\\doit(W_2)})_{\\doit(W_1)}$, and $G_{\\doit(W_1 \\cup W_2)}$ (all constructed via def \\ref{def:G_hard_intervention} as just verified) coincide. Concretely, the LN's displayed triple equality\n    \\[ \\lp G_{\\doit(W_1)} \\rp_{\\doit(W_2)} = \\lp G_{\\doit(W_2)} \\rp_{\\doit(W_1)} = G_{\\doit(W_1 \\cup W_2)} \\]\n    is the conjunction of the two following equalities of CDMGs:\n    \\begin{enumerate}[label=(\\alph*)]\n        \\item \\emph{Composition with $W_1$ then $W_2$.}\n              \\[ \\lp G_{\\doit(W_1)} \\rp_{\\doit(W_2)} \\;=\\; G_{\\doit(W_1 \\cup W_2)}. \\]\n        \\item \\emph{Composition with $W_2$ then $W_1$.}\n              \\[ \\lp G_{\\doit(W_2)} \\rp_{\\doit(W_1)} \\;=\\; G_{\\doit(W_1 \\cup W_2)}. \\]\n    \\end{enumerate}\n    Together (a) and (b) yield the LN's ``symmetry under swap'' reading $\\lp G_{\\doit(W_1)} \\rp_{\\doit(W_2)} = \\lp G_{\\doit(W_2)} \\rp_{\\doit(W_1)}$ by transitivity of equality. The equality of CDMGs in (a) and (b) is understood componentwise on the four components $(J, V, E, L)$ of def \\ref{def-cdmg}, but the conjunctive unpacking into the four field-by-field equalities is deferred to the proof.\n\n      %This shows that our notion:\n      %\\[ G(V \\sm (W_1 \\cup W_2) | \\doit(W_1 \\cup W_2 \\cup J))   \\]\n      %is unambiguous.\n\\end{Lem}\n\n% --- proof ------------------------------------------------------------------\n% Proof body adapted from the LN's \\Claude{...} block at\n% lecture-notes/lecture_notes/graphs.tex lines 326-355, with three light edits:\n% (i) the opening is re-framed to reference the rewrite's (a) and (b) labels,\n% (ii) the closing \"by symmetry\" remark for (b) is expanded into an explicit\n% W_1 <-> W_2 swap + commutativity-of-union step, and\n% (iii) the bidirected-edge paragraph carries an inline note that, under the\n% cdmg_typed_edges refactor's `Sym2 Node` encoding of L, the previously-\n% registered deviation `hard_intervention_l_symmetrized_removal` is\n% structurally resolved at the def_3_10 row (no symmetrisation step in either\n% encoding is needed at this proof's L-component step).\n\\begin{proof}\nWe prove sub-claim (a), $\\lp G_{\\doit(W_1)} \\rp_{\\doit(W_2)} = G_{\\doit(W_1 \\cup W_2)}$, by checking the equality of the two CDMGs componentwise on each of the four components $(J, V, E, L)$ of def \\ref{def-cdmg} in turn; sub-claim (b), $\\lp G_{\\doit(W_2)} \\rp_{\\doit(W_1)} = G_{\\doit(W_1 \\cup W_2)}$, is then obtained by an explicit symmetry argument at the end of the proof. Both sub-claims rely on the carrier-matching observation already established in the statement block above to license the outer $\\doit(\\cdot)$ application.\n\n\\medskip\\noindent\\textbf{Proof of (a): composition with $W_1$ then $W_2$.}\nWe verify the equality $\\lp G_{\\doit(W_1)} \\rp_{\\doit(W_2)} = G_{\\doit(W_1 \\cup W_2)}$ on each of the four components.\n\n\\medskip\\noindent\\emph{Input nodes.}\nBy item i.\\ of def \\ref{def:G_hard_intervention} applied twice (first to $G$ with $W_1$, then to $G_{\\doit(W_1)}$ with $W_2$),\n\\[\n    J_{\\lp G_{\\doit(W_1)} \\rp_{\\doit(W_2)}} \\;=\\; J_{\\doit(W_1)} \\cup W_2 \\;=\\; (J \\cup W_1) \\cup W_2 \\;=\\; J \\cup (W_1 \\cup W_2) \\;=\\; J_{\\doit(W_1 \\cup W_2)},\n\\]\nwhere the middle equality is associativity of $\\cup$. \\checkmark\n\n\\medskip\\noindent\\emph{Output nodes.}\nBy item ii.\\ of def \\ref{def:G_hard_intervention} applied twice,\n\\[\n    V_{\\lp G_{\\doit(W_1)} \\rp_{\\doit(W_2)}} \\;=\\; V_{\\doit(W_1)} \\sm W_2 \\;=\\; (V \\sm W_1) \\sm W_2 \\;=\\; V \\sm (W_1 \\cup W_2) \\;=\\; V_{\\doit(W_1 \\cup W_2)},\n\\]\nwhere the middle equality is the standard identity $(A \\sm B) \\sm C = A \\sm (B \\cup C)$ for set difference (an element $x$ survives both removals iff $x \\notin B$ and $x \\notin C$, iff $x \\notin B \\cup C$). \\checkmark\n\n\\medskip\\noindent\\emph{Directed edges.}\nBy item iii.\\ of def \\ref{def:G_hard_intervention} applied twice,\n\\begin{align*}\n    E_{\\lp G_{\\doit(W_1)} \\rp_{\\doit(W_2)}}\n    &= E_{\\doit(W_1)} \\sm \\lC v \\tuh w \\mid w \\in W_2 \\rC \\\\\n    &= \\lp E \\sm \\lC v \\tuh w \\mid w \\in W_1 \\rC \\rp\n       \\sm \\lC v \\tuh w \\mid w \\in W_2 \\rC \\\\\n    &= E \\sm \\lC v \\tuh w \\mid w \\in W_1 \\cup W_2 \\rC\n     = E_{\\doit(W_1 \\cup W_2)}.\n\\end{align*}\nThe penultimate equality is the same set-difference identity used for the output nodes, instantiated at the directed-edge set: an ordered pair $(v, w) \\in E$ survives both removal steps iff its head $w$ lies neither in $W_1$ nor in $W_2$, iff $w \\notin W_1 \\cup W_2$; that condition is exactly the one matching the removal set of a single application of item iii.\\ at the union $W_1 \\cup W_2$. \\checkmark\n\n\\medskip\\noindent\\emph{Bidirected edges.}\nBy item iv.\\ of def \\ref{def:G_hard_intervention} applied twice,\n\\begin{align*}\n    L_{\\lp G_{\\doit(W_1)} \\rp_{\\doit(W_2)}}\n    &= L_{\\doit(W_1)} \\sm \\lC v \\huh w \\mid w \\in W_2 \\rC \\\\\n    &= \\lp L \\sm \\lC v \\huh w \\mid w \\in W_1 \\rC \\rp\n       \\sm \\lC v \\huh w \\mid w \\in W_2 \\rC \\\\\n    &= L \\sm \\lC v \\huh w \\mid w \\in W_1 \\cup W_2 \\rC\n     = L_{\\doit(W_1 \\cup W_2)}.\n\\end{align*}\nThe penultimate equality combines the two removal clauses by the same union-of-conditions step used for the directed-edge case, with $v \\huh w$ in place of $v \\tuh w$. \\checkmark\n\n\\smallskip\\noindent\\emph{Remark on the Lean encoding of $L$ under the \\texttt{cdmg\\_typed\\_edges} refactor.} Under the post-refactor Lean encoding of def \\ref{def-cdmg} (the \\texttt{cdmg\\_typed\\_edges} refactor; see \\texttt{leanification/refactors/refactor\\_cdmg\\_typed\\_edges.md} for the design block), $L$ is typed as a $\\Finset$ of \\emph{unordered} pairs $\\Sym_2(\\Node)$ rather than as a $\\Finset$ of ordered pairs $\\Node \\times \\Node$ paired with a separate $\\Sym$-symmetry axiom. Consequently, item iv.\\ of def \\ref{def:G_hard_intervention} reads literally: a bidirected edge $s \\in L$ (an unordered pair) is removed iff some endpoint $v$ of $s$ lies in $W$ (kept-condition: $\\forall v \\in s,\\ v \\notin W$). There is no ``second component'' on an unordered pair to test in isolation, so the LN's literal one-sided phrasing $v_2 \\in W$ collapses to ``any endpoint in $W$'', and no symmetrisation step is required to satisfy any CDMG axiom (swap-symmetry is definitional on $\\Sym_2$, so $s(v_1, v_2) = s(v_2, v_1)$ holds by construction). The previously-registered deviation \\emph{hard\\_intervention\\_l\\_symmetrized\\_removal} -- attached to the pre-refactor encoding of def \\ref{def:G_hard_intervention} -- is therefore \\emph{structurally resolved at the def\\_3\\_10 row} under the refactor, and does not surface at this row. The composition step of this proof, expressed at the Lean level, reads as ``filter-by-$p_1$ then filter-by-$p_2 = $ filter-by-$(p_1 \\wedge p_2)$'' with $p_i(s) := (\\forall v \\in s,\\ v \\notin W_i)$: applied at the union $W_1 \\cup W_2$, the pointwise predicate equivalence\n\\[\n    (\\forall v \\in s,\\ v \\notin W_1) \\wedge (\\forall v \\in s,\\ v \\notin W_2) \\;\\Longleftrightarrow\\; \\forall v \\in s,\\ v \\notin W_1 \\cup W_2\n\\]\nfollows by pulling the conjunction inside the bounded universal (each disjoint-$v$ instance is one $\\wedge$-pair) plus the membership-in-union rewrite $v \\notin W_1 \\cup W_2 \\Leftrightarrow v \\notin W_1 \\wedge v \\notin W_2$. Both the LN's set-difference algebra in the displayed line and the Lean-level composition step land on the same final bidirected-edge set $L_{\\doit(W_1 \\cup W_2)}$.\n\n\\smallskip\nCombining the four componentwise equalities above, the CDMGs $\\lp G_{\\doit(W_1)} \\rp_{\\doit(W_2)}$ and $G_{\\doit(W_1 \\cup W_2)}$ agree field by field on each of $(J, V, E, L)$ and are therefore equal as CDMGs (def \\ref{def-cdmg}). This establishes sub-claim (a).\n\n\\medskip\\noindent\\textbf{Proof of (b): composition with $W_2$ then $W_1$.}\nSub-claim (b) is obtained by swapping the roles of $W_1$ and $W_2$ in the argument for (a). Concretely, re-running the four-component check above with the substitution $W_1 \\leftrightarrow W_2$ -- the proof depends on the pair of subsets only through their symbolic roles, and the displayed identities $(J \\cup W_1) \\cup W_2 = J \\cup (W_1 \\cup W_2)$, $(V \\sm W_1) \\sm W_2 = V \\sm (W_1 \\cup W_2)$, and the analogous edge-set identities all retain their truth under the swap -- yields\n\\[\n    \\lp G_{\\doit(W_2)} \\rp_{\\doit(W_1)} \\;=\\; G_{\\doit(W_2 \\cup W_1)}.\n\\]\nThis is precisely sub-claim (a) instantiated at the swapped pair $(W_2, W_1)$. We emphasise that sub-claim (b) is \\emph{not} a literal instance of (a): the iterated hard intervention on the left-hand side of (b) is built from the swapped pair $(W_2, W_1)$, and the right-hand-side union appearing in (a) for that swapped pair is $W_2 \\cup W_1$, not the $W_1 \\cup W_2$ in the statement of (b). The two right-hand sides agree by commutativity of union, $W_2 \\cup W_1 = W_1 \\cup W_2$, which gives\n\\[\n    G_{\\doit(W_2 \\cup W_1)} \\;=\\; G_{\\doit(W_1 \\cup W_2)}\n\\]\nas CDMGs: def \\ref{def:G_hard_intervention} depends on $W$ only through the underlying set $W \\subseteq J \\cup V$ (each of items i.\\ -- iv.\\ refers to $W$ only via the operations $J \\cup W$, $V \\sm W$, and the membership predicate ``$\\cdot \\in W$''), and equal sets produce equal CDMGs componentwise. Chaining the two equalities,\n\\[\n    \\lp G_{\\doit(W_2)} \\rp_{\\doit(W_1)} \\;=\\; G_{\\doit(W_2 \\cup W_1)} \\;=\\; G_{\\doit(W_1 \\cup W_2)},\n\\]\nestablishes sub-claim (b).\n\n\\medskip\\noindent\\emph{Recovery of the LN's triple equality.}\nBy sub-claims (a) and (b) -- both of which equate their respective iterated hard intervention with the single right-hand side $G_{\\doit(W_1 \\cup W_2)}$ -- transitivity of equality on that shared right-hand side yields\n\\[\n    \\lp G_{\\doit(W_1)} \\rp_{\\doit(W_2)} \\;=\\; G_{\\doit(W_1 \\cup W_2)} \\;=\\; \\lp G_{\\doit(W_2)} \\rp_{\\doit(W_1)},\n\\]\nwhich is the LN's stated triple equality and in particular recovers the swap-symmetry reading $\\lp G_{\\doit(W_1)} \\rp_{\\doit(W_2)} = \\lp G_{\\doit(W_2)} \\rp_{\\doit(W_1)}$.\n\\end{proof}\n\n\\end{document}\n",
    "html": "<div class=\"theorem-block\"><div class=\"theorem-label\">Lemma — <span class=\"theorem-title\">Hard interventions commute</span></div><p>Let $G = (J, V, E, L)$ be a CDMG (def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>); throughout we use the notation of def <a href=\"#def_3_2\" class=\"refrow\"><code>not-cdmg</code></a> and recall the typing and axioms of def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>, namely $J \\cap V = \\emptyset$, $E \\subseteq (J \\cup V) \\times V$, $L \\subseteq V \\times V$, $L$ irreflexive ($(v_1, v_2) \\in L \\Rightarrow v_1 \\neq v_2$), and $L$ symmetric ($(v_1, v_2) \\in L \\Leftrightarrow (v_2, v_1) \\in L$). Let\n    \\[ W_1 \\subseteq J \\cup V \\quad\\text{and}\\quad W_2 \\subseteq J \\cup V \\]\n    be two subsets of nodes (no disjointness between $W_1$ and $W_2$ is assumed, and overlap with $J$ is permitted, as per def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a>'s precondition). All three quantifiers “for every CDMG $G$”, “for every $W_1 \\subseteq J \\cup V$”, and “for every $W_2 \\subseteq J \\cup V$” are read <em>universally</em>.</p>\n<p><em>Carrier matching for the iterated hard interventions.</em> The inner two terms of the equality below are iterated hard interventions of the form $(G_{\\doit(W_i)})_{\\doit(W_j)}$, in which $\\doit(W_j)$ is applied to the CDMG $G_{\\doit(W_i)}$ produced by def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a>. For this outer application to be well-typed per def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a>'s precondition “$W \\subseteq J \\cup V$ of the input CDMG”, we need $W_j$ to be a subset of the carrier of $G_{\\doit(W_i)}$, i.e.\\\n    \\[ W_j \\;\\subseteq\\; J_{\\doit(W_i)} \\cup V_{\\doit(W_i)}. \\]\n    This holds because hard intervention preserves the carrier $J \\cup V$ as a set: by items i.\\ and ii.\\ of def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a>,\n    \\[\n        J_{\\doit(W_i)} \\cup V_{\\doit(W_i)} \\;=\\; (J \\cup W_i) \\cup (V \\sm W_i) \\;=\\; J \\cup V,\n    \\]\n    where the second equality uses $W_i \\subseteq J \\cup V$ (as in the carrier-matching paragraph of <a href=\"#claim_3_3\" class=\"refrow\"><code>claim_3_3</code></a>). Hence $W_j \\subseteq J \\cup V = J_{\\doit(W_i)} \\cup V_{\\doit(W_i)}$, so $(G_{\\doit(W_i)})_{\\doit(W_j)}$ is defined. The same observation applied to $W_1 \\cup W_2 \\subseteq J \\cup V$ shows that the right-hand side $G_{\\doit(W_1 \\cup W_2)}$ is also defined per def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a>.</p>\n<p><em>The conclusion.</em> The CDMGs $(G_{\\doit(W_1)})_{\\doit(W_2)}$, $(G_{\\doit(W_2)})_{\\doit(W_1)}$, and $G_{\\doit(W_1 \\cup W_2)}$ (all constructed via def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a> as just verified) coincide. Concretely, the LN's displayed triple equality\n    \\[ \\lp G_{\\doit(W_1)} \\rp_{\\doit(W_2)} = \\lp G_{\\doit(W_2)} \\rp_{\\doit(W_1)} = G_{\\doit(W_1 \\cup W_2)} \\]\n    is the conjunction of the two following equalities of CDMGs:</p>\n<ol class=\"enum\"><li><em>Composition with $W_1$ then $W_2$.</em>\n              \\[ \\lp G_{\\doit(W_1)} \\rp_{\\doit(W_2)} \\;=\\; G_{\\doit(W_1 \\cup W_2)}. \\]</li><li><em>Composition with $W_2$ then $W_1$.</em>\n              \\[ \\lp G_{\\doit(W_2)} \\rp_{\\doit(W_1)} \\;=\\; G_{\\doit(W_1 \\cup W_2)}. \\]</li></ol>\n<p>Together (a) and (b) yield the LN's “symmetry under swap” reading $\\lp G_{\\doit(W_1)} \\rp_{\\doit(W_2)} = \\lp G_{\\doit(W_2)} \\rp_{\\doit(W_1)}$ by transitivity of equality. The equality of CDMGs in (a) and (b) is understood componentwise on the four components $(J, V, E, L)$ of def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>, but the conjunctive unpacking into the four field-by-field equalities is deferred to the proof.</p></div>\n<div class=\"tex-proof\"><span class=\"proof-label\">Proof.</span><p>We prove sub-claim (a), $\\lp G_{\\doit(W_1)} \\rp_{\\doit(W_2)} = G_{\\doit(W_1 \\cup W_2)}$, by checking the equality of the two CDMGs componentwise on each of the four components $(J, V, E, L)$ of def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a> in turn; sub-claim (b), $\\lp G_{\\doit(W_2)} \\rp_{\\doit(W_1)} = G_{\\doit(W_1 \\cup W_2)}$, is then obtained by an explicit symmetry argument at the end of the proof. Both sub-claims rely on the carrier-matching observation already established in the statement block above to license the outer $\\doit(\\cdot)$ application.</p>\n<p><strong>Proof of (a): composition with $W_1$ then $W_2$.</strong>\nWe verify the equality $\\lp G_{\\doit(W_1)} \\rp_{\\doit(W_2)} = G_{\\doit(W_1 \\cup W_2)}$ on each of the four components.</p>\n<p><em>Input nodes.</em>\nBy item i.\\ of def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a> applied twice (first to $G$ with $W_1$, then to $G_{\\doit(W_1)}$ with $W_2$),\n\\[\n    J_{\\lp G_{\\doit(W_1)} \\rp_{\\doit(W_2)}} \\;=\\; J_{\\doit(W_1)} \\cup W_2 \\;=\\; (J \\cup W_1) \\cup W_2 \\;=\\; J \\cup (W_1 \\cup W_2) \\;=\\; J_{\\doit(W_1 \\cup W_2)},\n\\]\nwhere the middle equality is associativity of $\\cup$. ✓</p>\n<p><em>Output nodes.</em>\nBy item ii.\\ of def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a> applied twice,\n\\[\n    V_{\\lp G_{\\doit(W_1)} \\rp_{\\doit(W_2)}} \\;=\\; V_{\\doit(W_1)} \\sm W_2 \\;=\\; (V \\sm W_1) \\sm W_2 \\;=\\; V \\sm (W_1 \\cup W_2) \\;=\\; V_{\\doit(W_1 \\cup W_2)},\n\\]\nwhere the middle equality is the standard identity $(A \\sm B) \\sm C = A \\sm (B \\cup C)$ for set difference (an element $x$ survives both removals iff $x \\notin B$ and $x \\notin C$, iff $x \\notin B \\cup C$). ✓</p>\n<p><em>Directed edges.</em>\nBy item iii.\\ of def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a> applied twice,\n\\[\\begin{aligned}\n    E_{\\lp G_{\\doit(W_1)} \\rp_{\\doit(W_2)}}\n    &= E_{\\doit(W_1)} \\sm \\lC v \\tuh w \\mid w \\in W_2 \\rC \\\\\n    &= \\lp E \\sm \\lC v \\tuh w \\mid w \\in W_1 \\rC \\rp\n       \\sm \\lC v \\tuh w \\mid w \\in W_2 \\rC \\\\\n    &= E \\sm \\lC v \\tuh w \\mid w \\in W_1 \\cup W_2 \\rC\n     = E_{\\doit(W_1 \\cup W_2)}.\n\\end{aligned}\\]\nThe penultimate equality is the same set-difference identity used for the output nodes, instantiated at the directed-edge set: an ordered pair $(v, w) \\in E$ survives both removal steps iff its head $w$ lies neither in $W_1$ nor in $W_2$, iff $w \\notin W_1 \\cup W_2$; that condition is exactly the one matching the removal set of a single application of item iii.\\ at the union $W_1 \\cup W_2$. ✓</p>\n<p><em>Bidirected edges.</em>\nBy item iv.\\ of def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a> applied twice,\n\\[\\begin{aligned}\n    L_{\\lp G_{\\doit(W_1)} \\rp_{\\doit(W_2)}}\n    &= L_{\\doit(W_1)} \\sm \\lC v \\huh w \\mid w \\in W_2 \\rC \\\\\n    &= \\lp L \\sm \\lC v \\huh w \\mid w \\in W_1 \\rC \\rp\n       \\sm \\lC v \\huh w \\mid w \\in W_2 \\rC \\\\\n    &= L \\sm \\lC v \\huh w \\mid w \\in W_1 \\cup W_2 \\rC\n     = L_{\\doit(W_1 \\cup W_2)}.\n\\end{aligned}\\]\nThe penultimate equality combines the two removal clauses by the same union-of-conditions step used for the directed-edge case, with $v \\huh w$ in place of $v \\tuh w$. ✓</p>\n<p><em>Remark on the Lean encoding of $L$ under the <code>cdmg_typed_edges</code> refactor.</em> Under the post-refactor Lean encoding of def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a> (the <code>cdmg_typed_edges</code> refactor; see <code>leanification/refactors/refactor_cdmg_typed_edges.md</code> for the design block), $L$ is typed as a $\\Finset$ of <em>unordered</em> pairs $\\Sym_2(\\Node)$ rather than as a $\\Finset$ of ordered pairs $\\Node \\times \\Node$ paired with a separate $\\Sym$-symmetry axiom. Consequently, item iv.\\ of def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a> reads literally: a bidirected edge $s \\in L$ (an unordered pair) is removed iff some endpoint $v$ of $s$ lies in $W$ (kept-condition: $\\forall v \\in s,\\ v \\notin W$). There is no “second component” on an unordered pair to test in isolation, so the LN's literal one-sided phrasing $v_2 \\in W$ collapses to “any endpoint in $W$”, and no symmetrisation step is required to satisfy any CDMG axiom (swap-symmetry is definitional on $\\Sym_2$, so $s(v_1, v_2) = s(v_2, v_1)$ holds by construction). The previously-registered deviation <em>hard\\_intervention\\_l\\_symmetrized\\_removal</em> – attached to the pre-refactor encoding of def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a> – is therefore <em>structurally resolved at the def\\_3\\_10 row</em> under the refactor, and does not surface at this row. The composition step of this proof, expressed at the Lean level, reads as “filter-by-$p_1$ then filter-by-$p_2 = $ filter-by-$(p_1 \\wedge p_2)$” with $p_i(s) := (\\forall v \\in s,\\ v \\notin W_i)$: applied at the union $W_1 \\cup W_2$, the pointwise predicate equivalence\n\\[\n    (\\forall v \\in s,\\ v \\notin W_1) \\wedge (\\forall v \\in s,\\ v \\notin W_2) \\;\\Longleftrightarrow\\; \\forall v \\in s,\\ v \\notin W_1 \\cup W_2\n\\]\nfollows by pulling the conjunction inside the bounded universal (each disjoint-$v$ instance is one $\\wedge$-pair) plus the membership-in-union rewrite $v \\notin W_1 \\cup W_2 \\Leftrightarrow v \\notin W_1 \\wedge v \\notin W_2$. Both the LN's set-difference algebra in the displayed line and the Lean-level composition step land on the same final bidirected-edge set $L_{\\doit(W_1 \\cup W_2)}$.</p>\n<p>Combining the four componentwise equalities above, the CDMGs $\\lp G_{\\doit(W_1)} \\rp_{\\doit(W_2)}$ and $G_{\\doit(W_1 \\cup W_2)}$ agree field by field on each of $(J, V, E, L)$ and are therefore equal as CDMGs (def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>). This establishes sub-claim (a).</p>\n<p><strong>Proof of (b): composition with $W_2$ then $W_1$.</strong>\nSub-claim (b) is obtained by swapping the roles of $W_1$ and $W_2$ in the argument for (a). Concretely, re-running the four-component check above with the substitution $W_1 \\leftrightarrow W_2$ – the proof depends on the pair of subsets only through their symbolic roles, and the displayed identities $(J \\cup W_1) \\cup W_2 = J \\cup (W_1 \\cup W_2)$, $(V \\sm W_1) \\sm W_2 = V \\sm (W_1 \\cup W_2)$, and the analogous edge-set identities all retain their truth under the swap – yields\n\\[\n    \\lp G_{\\doit(W_2)} \\rp_{\\doit(W_1)} \\;=\\; G_{\\doit(W_2 \\cup W_1)}.\n\\]\nThis is precisely sub-claim (a) instantiated at the swapped pair $(W_2, W_1)$. We emphasise that sub-claim (b) is <em>not</em> a literal instance of (a): the iterated hard intervention on the left-hand side of (b) is built from the swapped pair $(W_2, W_1)$, and the right-hand-side union appearing in (a) for that swapped pair is $W_2 \\cup W_1$, not the $W_1 \\cup W_2$ in the statement of (b). The two right-hand sides agree by commutativity of union, $W_2 \\cup W_1 = W_1 \\cup W_2$, which gives\n\\[\n    G_{\\doit(W_2 \\cup W_1)} \\;=\\; G_{\\doit(W_1 \\cup W_2)}\n\\]\nas CDMGs: def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a> depends on $W$ only through the underlying set $W \\subseteq J \\cup V$ (each of items i.\\ – iv.\\ refers to $W$ only via the operations $J \\cup W$, $V \\sm W$, and the membership predicate “$\\cdot \\in W$”), and equal sets produce equal CDMGs componentwise. Chaining the two equalities,\n\\[\n    \\lp G_{\\doit(W_2)} \\rp_{\\doit(W_1)} \\;=\\; G_{\\doit(W_2 \\cup W_1)} \\;=\\; G_{\\doit(W_1 \\cup W_2)},\n\\]\nestablishes sub-claim (b).</p>\n<p><em>Recovery of the LN's triple equality.</em>\nBy sub-claims (a) and (b) – both of which equate their respective iterated hard intervention with the single right-hand side $G_{\\doit(W_1 \\cup W_2)}$ – transitivity of equality on that shared right-hand side yields\n\\[\n    \\lp G_{\\doit(W_1)} \\rp_{\\doit(W_2)} \\;=\\; G_{\\doit(W_1 \\cup W_2)} \\;=\\; \\lp G_{\\doit(W_2)} \\rp_{\\doit(W_1)},\n\\]\nwhich is the LN's stated triple equality and in particular recovers the swap-symmetry reading $\\lp G_{\\doit(W_1)} \\rp_{\\doit(W_2)} = \\lp G_{\\doit(W_2)} \\rp_{\\doit(W_1)}$.</p></div>",
    "source_path": "leanification/Chapter3_GraphTheory/Section3_2/tex/claim_3_4_proof_HardInterventionsCommute.tex"
  },
  "lean_blocks": [
    {
      "kind": "helper",
      "code": "variable {Node : Type*} [DecidableEq Node]",
      "explanation": "This is a `variable` declaration: Lean auto-binds `Node : Type*` and the instance `[DecidableEq Node]` as implicit arguments to every subsequent declaration in the surrounding `CDMG` namespace, sparing us from rewriting the same parameters on `subset_carrier_of_hardInterventionOn` and on `hardInterventionsCommute`. The `Type*` notation makes `Node` universe-polymorphic, so callers can instantiate it at any universe level. The `DecidableEq Node` assumption is load-bearing for this row in particular: the statement references `Finset.union` (forming `G.J ∪ W` and `W₁ ∪ W₂`), `Finset.sdiff` (the `\\` in `G.V \\ W`), and `Finset.filter` (used inside `hardInterventionOn` and reasoned about via `Finset.filter_filter` and `Finset.filter_congr` in the proof) — all of which need decidable equality on the element type to compute the resulting `Finset`. Stronger structure on `Node` (e.g.\\ `Fintype`, `LinearOrder`) is not required here and is deferred to downstream consumers that actually use it. Same convention as the rest of `Chapter3_GraphTheory`'s files.",
      "code_annotated": "-- Auto-binds `Node` + `DecidableEq Node` as implicits; decidability drives `Finset.union`/`sdiff`/`filter` used below.\nvariable {Node : Type*} [DecidableEq Node]"
    },
    {
      "kind": "helper",
      "code": "variable {Node : Type*} [DecidableEq Node]",
      "explanation": "This is a `variable` declaration: Lean auto-binds `Node : Type*` and the instance `[DecidableEq Node]` as implicit arguments to every subsequent declaration in the surrounding `CDMG` namespace, sparing us from rewriting the same parameters on `subset_carrier_of_hardInterventionOn` and on `hardInterventionsCommute`. The `Type*` notation makes `Node` universe-polymorphic, so callers can instantiate it at any universe level. The `DecidableEq Node` assumption is load-bearing for this row in particular: the statement references `Finset.union` (forming `G.J ∪ W` and `W₁ ∪ W₂`), `Finset.sdiff` (the `\\` in `G.V \\ W`), and `Finset.filter` (used inside `hardInterventionOn` and reasoned about via `Finset.filter_filter` and `Finset.filter_congr` in the proof) — all of which need decidable equality on the element type to compute the resulting `Finset`. Stronger structure on `Node` (e.g.\\ `Fintype`, `LinearOrder`) is not required here and is deferred to downstream consumers that actually use it. Same convention as the rest of `Chapter3_GraphTheory`'s files.",
      "code_annotated": "-- Auto-binds `Node` + `DecidableEq Node` as implicits; decidability drives `Finset.union`/`sdiff`/`filter` used below.\nvariable {Node : Type*} [DecidableEq Node]"
    },
    {
      "kind": "helper",
      "code": "private lemma subset_carrier_of_hardInterventionOn\n    {G : CDMG Node} {W : Finset Node} (hW : W ⊆ G.J ∪ G.V)\n    {S : Finset Node} (hS : S ⊆ G.J ∪ G.V) :\n    S ⊆ (G.hardInterventionOn W hW).J ∪\n          (G.hardInterventionOn W hW).V",
      "explanation": "",
      "code_annotated": ""
    },
    {
      "kind": "main",
      "code": "theorem hardInterventionsCommute (G : CDMG Node)\n    (W₁ W₂ : Finset Node)\n    (hW₁ : W₁ ⊆ G.J ∪ G.V) (hW₂ : W₂ ⊆ G.J ∪ G.V) :\n    (G.hardInterventionOn W₁ hW₁).hardInterventionOn W₂\n        (subset_carrier_of_hardInterventionOn hW₁ hW₂)\n      = G.hardInterventionOn (W₁ ∪ W₂) (Finset.union_subset hW₁ hW₂)\n    ∧\n    (G.hardInterventionOn W₂ hW₂).hardInterventionOn W₁\n        (subset_carrier_of_hardInterventionOn hW₂ hW₁)\n      = G.hardInterventionOn (W₁ ∪ W₂) (Finset.union_subset hW₁ hW₂)",
      "explanation": "",
      "code_annotated": ""
    }
  ],
  "lean_source_url": "https://github.com/schipeltaper/causality-leanified/blob/main/leanification/Chapter3_GraphTheory/Section3_2/HardInterventionsCommute.lean",
  "lean_file_path": "leanification/Chapter3_GraphTheory/Section3_2/HardInterventionsCommute.lean",
  "addition_to_the_LN": "",
  "design_choices": "**One theorem returning a conjunction, not two separate top-level theorems.** The LN packages its triple equality $(G_{\\mathrm{do}(W_1)})_{\\mathrm{do}(W_2)} = (G_{\\mathrm{do}(W_2)})_{\\mathrm{do}(W_1)} = G_{\\mathrm{do}(W_1 \\cup W_2)}$ inside a single lemma block, and the canonical statement decomposes it into two binary equalities (a) and (b). We mirror that single-lemma packaging by returning `(a) ∧ (b)` from one theorem. Consumers reach `.1` for (a) and `.2` for (b); the LN's swap-symmetry reading $(G_{\\mathrm{do}(W_1)})_{\\mathrm{do}(W_2)} = (G_{\\mathrm{do}(W_2)})_{\\mathrm{do}(W_1)}$ is then recovered as `.1.trans .2.symm`, with no need to bake a third sub-claim into the statement. Splitting into two top-level theorems was rejected because it would duplicate the antecedents `hW₁`, `hW₂` at both theorem heads.\n\n**No disjointness hypothesis on `W₁` and `W₂`.** The LN's wording is \"two subsets of nodes\", with no clause requiring $W_1 \\cap W_2 = \\emptyset$ or excluding overlap with the input set $J$. This makes the lemma a *free composition* result: hard intervention commutes with itself unconditionally, and the union `W₁ ∪ W₂` on the right-hand side handles overlap correctly. Downstream consumers — do-calculus reductions, iterated-intervention algebras — can compose hard interventions without first discharging a side condition. Adding a disjointness binder would strictly weaken the statement and is therefore omitted; sibling rows that genuinely require disjointness (because they combine hard intervention with node-splitting) introduce it locally.\n\n**Carrier-subset transport via a dedicated helper lemma.** The nested intervention `(G.hardInterventionOn W₁ hW₁).hardInterventionOn W₂ ?_` requires a proof that `W₂ ⊆ (G.hardInterventionOn W₁ hW₁).J ∪ (G.hardInterventionOn W₁ hW₁).V`, not merely `W₂ ⊆ G.J ∪ G.V`. We expose the transport as the private helper `subset_carrier_of_hardInterventionOn` rather than inlining a `by`-block in the theorem type. Inlining was rejected because it would bloat the rendered statement on the website and duplicate the carrier-matching reasoning at every nested-intervention call site.\n\n**CDMG equality unfolded inline as `cdmgExt`, not as a top-level extensionality lemma.** The proof opens with `have cdmgExt : ∀ {G₁ G₂ : CDMG Node}, G₁.J = G₂.J → … → G₁ = G₂`, destructured by `rintro` on both CDMGs and four `obtain rfl` substitutions. A top-level `@[ext]` lemma for `CDMG` is plausible but was not introduced here: this is the first row that needs componentwise CDMG equality, and a single-use local lemma keeps the proof self-contained. Future rows that need the same fact can promote it to a top-level lemma at that point.\n\n**The bidirected-edge filter is two-sided, not one-sided.** This inherits the registered deviation from the underlying hard-intervention definition: an edge $(v_1, v_2) \\in L$ is retained iff $v_1 \\notin W$ *and* $v_2 \\notin W$, rather than the LN's literal one-sided $v_2 \\notin W$. The two-sided convention is forced by the symmetry axiom on $L$ — a one-sided filter would delete only one of the two ordered pairs encoding the same bidirected edge and so break symmetry. The composition step at $L$ then fuses two two-sided filters into a single two-sided filter for $W_1 \\cup W_2$, landing on the same final bidirected-edge set the LN's one-sided convention would."
}
