{
  "ref": "def_3_8",
  "kind": "def",
  "section": "3.1",
  "title": "TopologicalOrder",
  "type": "definition",
  "status": {
    "formalized": "yes",
    "proven": "n/a",
    "solved": "yes"
  },
  "tex_block_html": "<p>Let $G=(J,V,E,L)$ be a CDMG.\n    A <em>topological order</em> of $G$ is a total order $<$ of $J \\cup V$ such that for all $v,w \\in G$:\n    \\[ v \\in \\Pa^G(w) \\; \\implies \\; v < w.\\]\n    Equivalently, it can be described as an indexing of the nodes $J \\cup V = \\{v_1,\\dots,v_K\\}$ where parents always precede their children.</p>",
  "tex_statement": {
    "raw": "% Definition file for row def_3_8 -- \"TopologicalOrder\".\n% Auto-generated stub by scaffold/solve_chapter.py at row start. The\n% manager agent (or a worker dispatched by it) fills the body below with the\n% Lean-faithful definition statement(s). Stays close to the lecture notes.\n%\n% This file is a sibling of `main.tex` inside `<subsection>/tex/`. The\n% `subfiles` package lets it compile both standalone (resolving its\n% preamble via `main.tex`) and as part of `main.tex` via `\\subfile{...}`.\n\n\\documentclass[main]{subfiles}\n\n\\begin{document}\n% ref: def_3_8\n% title: TopologicalOrder\n\\def\\rowref{def\\_3\\_8}\n\\phantomsection\\label{def_3_8}\n\n\\begin{Def}[Topological order]\n    \\label{def-topological-order}\n    Let $G = (J, V, E, L)$ be a CDMG (def \\ref{def-cdmg}); throughout we use the notation of def \\ref{not-cdmg} and the family-relationship notation of def \\ref{def_3_5}. By def \\ref{def-cdmg}, items~i.--ii., the node set $J \\cup V$ is finite (as a union of two finite sets).\n\n    A \\emph{topological order} of $G$ is a \\emph{strict} total order $<$ on $J \\cup V$ -- i.e.\\ a binary relation $<$ on $J \\cup V$ that is irreflexive, transitive, and satisfies trichotomy ($v < w$, $v = w$, or $w < v$ for every $v, w \\in J \\cup V$); the lecture notes' order symbol $<$ is read strictly throughout -- such that\n    \\[\n        \\forall\\, v, w \\in J \\cup V :\\quad v \\in \\Pa^G(w) \\;\\Longrightarrow\\; v < w.\n    \\]\n    The displayed implication is well-typed because, by the set-builder defining clause $\\Pa^G(w) := \\lC u \\in J \\cup V \\,|\\, (u, w) \\in E \\rC$ of def \\ref{def_3_5}, item~i., any $v \\in \\Pa^G(w)$ automatically lies in $J \\cup V$, so the comparison $v < w$ is defined.\n\n    \\emph{Equivalent indexing form.}\n    Set $K := |J \\cup V|$, the cardinality of the (finite) node set. Because $<$ is a strict total order on the finite set $J \\cup V$, its order type is the finite ordinal $K$, so $<$ may equivalently be presented as a bijection $\\{1, 2, \\dots, K\\} \\to J \\cup V$, $i \\mapsto v_i$, with $v_1 < v_2 < \\cdots < v_K$ under $<$. Under this presentation, the parents-precede-children condition above becomes\n    \\[\n        \\forall\\, i, j \\in \\{1, 2, \\dots, K\\} :\\quad v_i \\in \\Pa^G(v_j) \\;\\Longrightarrow\\; i < j,\n    \\]\n    i.e.\\ \\emph{parents always precede their children} in the enumeration $v_1, v_2, \\dots, v_K$. The equivalence between the primary (strict-total-order) form and this indexed form relies essentially on the finiteness of $J \\cup V$ guaranteed by def \\ref{def-cdmg}: without finiteness, the indexed form would be strictly stronger than the primary form (it forces order type at most $\\omega$).\n\\end{Def}\n\n\\end{document}\n",
    "html": "<p>Let $G = (J, V, E, L)$ be a CDMG (def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>); throughout we use the notation of def <a href=\"#def_3_2\" class=\"refrow\"><code>not-cdmg</code></a> and the family-relationship notation of def <a href=\"#def_3_5\" class=\"refrow\"><code>def_3_5</code></a>. By def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>, items&nbsp;i.–ii., the node set $J \\cup V$ is finite (as a union of two finite sets).</p>\n<p>A <em>topological order</em> of $G$ is a <em>strict</em> total order $<$ on $J \\cup V$ – i.e.\\ a binary relation $<$ on $J \\cup V$ that is irreflexive, transitive, and satisfies trichotomy ($v < w$, $v = w$, or $w < v$ for every $v, w \\in J \\cup V$); the lecture notes' order symbol $<$ is read strictly throughout – such that\n    \\[\n        \\forall\\, v, w \\in J \\cup V :\\quad v \\in \\Pa^G(w) \\;\\Longrightarrow\\; v < w.\n    \\]\n    The displayed implication is well-typed because, by the set-builder defining clause $\\Pa^G(w) := \\lC u \\in J \\cup V \\,|\\, (u, w) \\in E \\rC$ of def <a href=\"#def_3_5\" class=\"refrow\"><code>def_3_5</code></a>, item&nbsp;i., any $v \\in \\Pa^G(w)$ automatically lies in $J \\cup V$, so the comparison $v < w$ is defined.</p>\n<p><em>Equivalent indexing form.</em>\n    Set $K := |J \\cup V|$, the cardinality of the (finite) node set. Because $<$ is a strict total order on the finite set $J \\cup V$, its order type is the finite ordinal $K$, so $<$ may equivalently be presented as a bijection $\\{1, 2, \\dots, K\\} \\to J \\cup V$, $i \\mapsto v_i$, with $v_1 < v_2 < \\cdots < v_K$ under $<$. Under this presentation, the parents-precede-children condition above becomes\n    \\[\n        \\forall\\, i, j \\in \\{1, 2, \\dots, K\\} :\\quad v_i \\in \\Pa^G(v_j) \\;\\Longrightarrow\\; i < j,\n    \\]\n    i.e.\\ <em>parents always precede their children</em> in the enumeration $v_1, v_2, \\dots, v_K$. The equivalence between the primary (strict-total-order) form and this indexed form relies essentially on the finiteness of $J \\cup V$ guaranteed by def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>: without finiteness, the indexed form would be strictly stronger than the primary form (it forces order type at most $\\omega$).</p>",
    "env": "Def",
    "env_title": "Topological order",
    "source_path": "leanification/Chapter3_GraphTheory/Section3_1/tex/def_3_8_TopologicalOrder.tex"
  },
  "tex_proof": null,
  "lean_blocks": [
    {
      "kind": "helper",
      "code": "variable {Node : Type*} [DecidableEq Node]",
      "explanation": "This single line is statement-typing infrastructure rather than part of the LN definition itself. The `variable` command auto-binds `Node` (as an implicit `{Node : Type*}`) and the instance `[DecidableEq Node]` into every declaration that follows in this section, so we never have to repeat them on the `IsTopologicalOrder` signature below. The `Type*` annotation is universe-polymorphic — `Node` may live at any universe level, matching the convention set in `def_3_1`'s `CDMG`. The `[DecidableEq Node]` instance is load-bearing for this row in two distinct places: first, the `Membership Node (CDMG Node)` instance from `def_3_2` makes `v ∈ G` reduce to `Finset.mem` on `G.J ∪ G.V`, which needs decidable equality on `Node`; second, `G.Pa w` from `def_3_5` builds its set body around the same `Finset` membership predicate. Drop `DecidableEq Node` and the main statement below stops type-checking.",
      "code_annotated": "-- Auto-binds `Node` + `DecidableEq Node` as implicits for every declaration below;\n-- decidability is load-bearing twice (via `v ∈ G` from def_3_2 and `G.Pa w` from def_3_5).\nvariable {Node : Type*} [DecidableEq Node]"
    },
    {
      "kind": "helper",
      "code": "def IsTotalOrder (G : CDMG Node) (lt : Node → Node → Prop) : Prop :=\n  (∀ v ∈ G, ¬ lt v v) ∧\n  (∀ u ∈ G, ∀ v ∈ G, ∀ w ∈ G, lt u v → lt v w → lt u w) ∧\n  (∀ v ∈ G, ∀ w ∈ G, lt v w ∨ v = w ∨ lt w v)",
      "explanation": "This helper carves out the LN's \"strict total order on `J ∪ V`\" premise as its own named predicate, so `IsTopologicalOrder` below can read literally as \"total order *and* parents precede children\". The body is a three-way conjunction restricting `lt` to the carrier `J ∪ V`: `∀ v ∈ G, ¬ lt v v` says `lt` is irreflexive on nodes of `G`; the next conjunct says it is transitive there (a fresh `∀ u ∈ G` block per variable, since Lean's bounded `∀ … ∈ …` does not chain across multiple binders); the last is trichotomy with equality in the middle slot, following Mathlib's `Trichotomous` disjunct convention. The bounded quantifier `∀ v ∈ G, …` desugars to `∀ v, v ∈ G → …` and routes membership through the `Membership Node (CDMG Node)` instance from `def_3_2`, which unfolds `v ∈ G` to `v ∈ G.J ∪ G.V`. Existing as a separate definition lets `def_3_9`'s `Pred` / `PredLE` take `(h : G.IsTotalOrder lt)` directly without pulling in parent-precedence.",
      "code_annotated": "-- Named helper carved out of the old flat 4-way ∧ so callers can refer to \"total order on J ∪ V\" alone.\ndef IsTotalOrder (G : CDMG Node) (lt : Node → Node → Prop) : Prop :=\n  -- (i) irreflexive on the carrier J ∪ V; ∀ v ∈ G desugars via def_3_2's Membership instance.\n  (∀ v ∈ G, ¬ lt v v) ∧\n  -- (ii) transitive on J ∪ V; three separate bounded ∀'s since `∀ … ∈ …` does not chain across binders.\n  (∀ u ∈ G, ∀ v ∈ G, ∀ w ∈ G, lt u v → lt v w → lt u w) ∧\n  -- (iii) trichotomy on J ∪ V with equality in the middle slot — Mathlib's `Trichotomous` convention.\n  (∀ v ∈ G, ∀ w ∈ G, lt v w ∨ v = w ∨ lt w v)"
    },
    {
      "kind": "main",
      "code": "def IsTopologicalOrder (G : CDMG Node) (lt : Node → Node → Prop) : Prop :=\n  G.IsTotalOrder lt ∧ (∀ v w, v ∈ G.Pa w → lt v w)",
      "explanation": "This is the LN's two-tier reading rendered as a nested 2-way `∧`: a topological order of `G` is a strict total order on `J ∪ V` (the `G.IsTotalOrder lt` half, from the helper above) *and* a parent-precedence clause `∀ v w, v ∈ G.Pa w → lt v w` layered on top. Logically equivalent to the older flat 4-conjunct shape — `G.IsTotalOrder lt` unfolds definitionally to the same three atomic propositions — but the destructure pattern is now nested: `obtain ⟨⟨h_irrefl, h_trans, h_total⟩, h_topo⟩ := h` or, more often, `obtain ⟨h_to, h_topo⟩ := h` followed by further unpacking. `G.Pa w` uses dot-notation through `def_3_5`'s `CDMG.Pa`, matching the LN's $\\Pa^G(w)$ notation; no `v ∈ G` / `w ∈ G` guards are needed because `Pa`'s body already forces both, and omitting `v ≠ w` is load-bearing so a directed self-loop forces `lt v v`, contradicting irreflexivity. The predicate is `Prop`-valued over an external `lt`, leaving existence for `claim_3_2`'s `G.IsAcyclic ↔ ∃ lt, G.IsTopologicalOrder lt` to quantify over.",
      "code_annotated": "-- Post-refactor 2-way ∧: total-order content + parent-precedence; logically equivalent to the old flat 4-tuple.\ndef IsTopologicalOrder (G : CDMG Node) (lt : Node → Node → Prop) : Prop :=\n  -- LHS reuses the IsTotalOrder helper; RHS is the parent-precedence clause via def_3_5's G.Pa dot-notation.\n  G.IsTotalOrder lt ∧ (∀ v w, v ∈ G.Pa w → lt v w)"
    }
  ],
  "lean_source_url": "https://github.com/schipeltaper/causality-leanified/blob/main/leanification/Chapter3_GraphTheory/Section3_1/TopologicalOrder.lean",
  "lean_file_path": "leanification/Chapter3_GraphTheory/Section3_1/TopologicalOrder.lean",
  "addition_to_the_LN": "",
  "design_choices": "**Typing context `Node : Type*` with `[DecidableEq Node]`, inherited from `def_3_1`.** Both fixtures are load-bearing here. The bounded quantifier `∀ v ∈ G, …` driving every conjunct of `IsTotalOrder` routes through the `Membership Node (CDMG Node)` instance from `def_3_2` (`CDMGNotation.lean`), which unfolds `v ∈ G` to `v ∈ G.J ∪ G.V` — a `Finset` membership check that reduces to decidable equality on `Node`. The parent-precedence clause's `v ∈ G.Pa w` reaches into `def_3_5`'s parent-set body `{u | u ∈ G ∧ (u, w) ∈ G.E}`, whose first conjunct is the same `Finset`-membership predicate and whose second tests pair membership in the `Finset (Node × Node)` edge set; both routes go through `DecidableEq Node`. Dropping either fixture would make the statement fail to type-check. Stronger instances (`Fintype`, `LinearOrder`) are not needed here and are deferred to consumers — `claim_3_2`'s constructive topological-sort proof may need them at the algorithmic step, but they would over-commit this row's interface.\n\n**Total-order content carved out as a named helper `IsTotalOrder`, joined to the parent-precedence clause by a nested 2-way `∧`.** The LN reads \"Let `<` be a *total order* of `J ∪ V` *such that* whenever `v ∈ Pa^G(w)` we have `v < w`\" — a two-tier presentation that names \"total order on `J ∪ V`\" as a substantive sub-concept and then layers parent-precedence on top. The original encoding silently merged the three total-order conjuncts (irreflexive, transitive, trichotomous on `J ∪ V`) into one flat 4-way `∧` inside `IsTopologicalOrder`, erasing that structure at the type level. The refactor extracts those three conjuncts into `def IsTotalOrder (G) (lt) : Prop := …` (wrapped with three-dash `-- def_3_8 --- start helper` / `end helper` markers, the helper-marker convention used throughout the chapter). All three signals from the helper-predicates checklist are met: *referenced by spec* (the canonical tex literally writes \"a *strict* total order `<` on `J ∪ V`\"), *substantive content* (three atomic conditions, not a one-liner), and *reused by a downstream row* — `def_3_9`'s `Pred` / `PredLE` consume `Pred^G_<(v) = {w ∈ J ∪ V ∣ w < v}`, meaningful for any total order on `J ∪ V` regardless of parent-precedence, and ch. 5's ID-algorithm and factorisation steps slice or reverse `J ∪ V` via total-order content alone. Without the helper, every such consumer would either re-spell the three atomic conditions or over-import `IsTopologicalOrder`. `IsTopologicalOrder` is then `G.IsTotalOrder lt ∧ (∀ v w, v ∈ G.Pa w → lt v w)`.\n\n**The destructure / constructor pattern flips symmetrically from flat 4-tuple to nested 2-tuple.** The two shapes are logically equivalent — `G.IsTotalOrder lt` unfolds *definitionally* to the same three atomic propositions in the same order — but the pattern-matching syntax changes. The old `obtain ⟨h_irrefl, h_trans, h_total, h_topo⟩ := h` becomes either `obtain ⟨⟨h_irrefl, h_trans, h_total⟩, h_topo⟩ := h` in one step or `obtain ⟨h_to, h_topo⟩ := h; obtain ⟨h_irrefl, h_trans, h_total⟩ := h_to` in two; constructors flip the same way to `refine ⟨⟨irrefl_pf, trans_pf, total_pf⟩, topo_pf⟩`. `claim_3_2` (`AcyclicIffTopologicalOrder.lean`) is the test case for both directions — forward arm constructs, backward arm destructures — and its theorem statement is unchanged; only the proof body's pattern sites move. `def_3_9` benefits as well: it can now take `(h : G.IsTotalOrder lt)` as an explicit hypothesis on `Pred` / `PredLE`, closing the loosened-domain leak where the old shape forced consumers to accept any binary relation or pull in the whole `IsTopologicalOrder` and discard its parent-precedence half.\n\n**`lt : Node → Node → Prop` as an explicit external argument, not a `[LT Node]` typeclass or a bundled structure.** A `[LT Node]` instance would force *exactly one* canonical `<` per `Node` type, but a single `Node` underlies every CDMG in the codebase and each CDMG may admit many topological orders; locking `<` to the type level would make `claim_3_2`'s existence claim either vacuous or unsatisfiable. A typeclass `[CDMG.TopologicalOrder G]` faces the same uniqueness pressure. A bundled `structure TopologicalOrder (G) where lt + props` was rejected on three grounds: it conflates the property (`G.IsTopologicalOrder lt`) with the witness data (`lt`) `claim_3_2` quantifies over, forces callers who just want to *check* a candidate `lt` to package it first, and tempts downstream code to single out \"the\" canonical order the LN never picks. The same reasoning rules out making `IsTotalOrder` a typeclass: the LN's \"total order of `J ∪ V`\" is a *property of a chosen relation*, not an instance the type system resolves silently, and a class would force consumers (`def_3_9`'s `Pred`, ch. 4–7 factorisation and d-separation, ch. 8–10 iSCMs) to either thread `[G.IsTotalOrder lt]` brackets through every signature or rely on the resolver to surface the witness — neither matches the LN's pattern of naming `<` explicitly and quantifying over it.\n\n**Domain restricted to `J ∪ V` via `∀ v ∈ G, …`, not to the ambient `Node` type via Mathlib's `IsStrictTotalOrder`.** Mathlib's `IsIrrefl`, `IsTrans`, `IsTrichotomous`, and the bundled `IsStrictTotalOrder` all assert their axioms on the *entire* ambient type. The LN's order only needs to relate nodes in `J ∪ V`; using the typeclass would either over-commit `lt` on nodes outside the carrier (a strictly stronger requirement than the LN, ruling out perfectly valid orders that leave non-nodes unrelated) or force a subtype wrapper `↥(G.J ∪ G.V)` that every consumer would have to coerce across. A custom helper `IsStrictTotalOrderOn (S : Finset Node) (lt) : Prop` was considered and rejected as premature scope creep — bundling the three conjuncts under yet another fresh name would obscure the LN's plain reading without offering anything `∀ v ∈ G, …` does not already. The trichotomy disjunct order `lt v w ∨ v = w ∨ lt w v` (equality in the middle) follows Mathlib's `Trichotomous` convention, easing any later lift to a subtype `Trichotomous` instance.\n\n**`G.Pa w` via dot-notation through `def_3_5`'s namespaced parent operator, no `v ∈ G`/`w ∈ G` guard, no `v ≠ w` precondition.** The parent clause spells the relation as `G.Pa w` rather than `Pa G w`: `def_3_5` defines `Pa` inside `namespace CDMG`, so `G.Pa w` reads naturally as \"the parents of `w` in `G`\" through Lean's dot-notation, matching the LN's $\\Pa^G(w)$ shorthand letter-for-letter. Spelling the implication directly as `(v, w) ∈ G.E → lt v w` would lose the `v ∈ G` witness baked into `Pa`'s body. The universal `∀ v w, v ∈ G.Pa w → lt v w` needs no bounded prefix because `def_3_5`'s `Pa` body already forces `v ∈ G` and `def_3_1`'s edge-endpoint subset clause forces `w ∈ G` whenever `(v, w) ∈ G.E`. Omitting `v ≠ w` is load-bearing: when `(v, v) ∈ G.E` (a directed self-loop), the implication forces `lt v v`, which contradicts irreflexivity (the first conjunct of `IsTotalOrder`). The mere existence of any topological order therefore rules out directed self-loops on `J ∪ V` — exactly what `claim_3_2`'s `⇐` direction needs, and the reason a `v ≠ w` guard would silently break it.\n\n**Scope choice: primary strict-total-order form only; the indexed reformulation is a downstream theorem.** The canonical tex spells out both the strict-total-order form and the equivalent \"`J ∪ V = {v_1, …, v_K}` with `v_1 < … < v_K`\" indexing, and proves them equivalent under finiteness — any strict total order on a finite set has order type a finite ordinal and so can be presented as an enumeration. We encode only the primary form: finiteness of `J ∪ V` is available *when needed* via `def_3_1`'s `Finset`-valued `J, V`, so any consumer can derive the indexed form on demand (via `Finset.toList` ordered by `lt`, or a `Fin K → Node` bijection) without polluting this row's API. A parallel `IsTopologicalOrderIndexed` predicate would invite spurious case-splits at every downstream site over which form is in scope, while the LN treats the two as logically identical presentations of one notion. No Mathlib re-use beyond what `IsTotalOrder` itself uses (`Membership Node (CDMG Node)` from `def_3_2`, `Set` membership on `G.Pa w` from `def_3_5`); no `IsStrictTotalOrder` / `LinearOrder` / `Preorder` for the domain-restriction reasons above."
}
