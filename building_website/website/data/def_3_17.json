{
  "ref": "def_3_17",
  "kind": "def",
  "section": "3.3",
  "title": "SigmaBlockedWalks",
  "type": "definition",
  "status": {
    "formalized": "yes",
    "proven": "n/a",
    "solved": "yes"
  },
  "tex_block_html": "<p>Let $G=(J,V,E,L)$ be a CDMG and $C \\ins J \\cup V$ a subset of nodes and $\\pi$ a walk in $G$:\n  \\[ \\pi =\\lp  v_0 \\sus \\cdots \\sus v_n \\rp.  \\]\n  We say that the walk $\\pi$ is:</p>\n<ol class=\"enum\"><li><em>$C$-$\\sigma$-open</em> (or <em>$\\sigma$-open given $C$</em>) if and only if:\n<ol class=\"roman-paren\"><li>all colliders $v_k$ on $\\pi$ are in $\\Anc^G(C)$, <em>and</em>:</li><li>all blockable non-colliders $v_k$ on $\\pi$ are <em>not</em> in $C$.</li></ol></li><li><em>$C$-$\\sigma$-blocked</em>  (or <em>$\\sigma$-blocked given $C$</em>) if and only if:\n<ol class=\"roman-paren\"><li>there exists a collider $v_k$ on $\\pi$ that is <em>not</em> in $\\Anc^G(C)$, <em>or</em>:</li><li>there exists a blockable non-collider $v_k$ on $\\pi$ in $C$.</li></ol></li></ol>\n<p>Note that unblockable non-colliders are always $C$-$\\sigma$-open, regardless of the subset $C \\ins V \\cup J$.</p>",
  "tex_statement": {
    "raw": "% Definition file for row def_3_17 -- \"SigmaBlockedWalks\".\n% Auto-generated stub by scaffold/solve_chapter.py at row start. The\n% manager agent (or a worker dispatched by it) fills the body below with the\n% Lean-faithful definition statement(s). Stays close to the lecture notes.\n%\n% This file is a sibling of `main.tex` inside `<subsection>/tex/`. The\n% `subfiles` package lets it compile both standalone (resolving its\n% preamble via `main.tex`) and as part of `main.tex` via `\\subfile{...}`.\n\n\\documentclass[main]{subfiles}\n\n\\begin{document}\n% ref: def_3_17\n% title: SigmaBlockedWalks\n\\def\\rowref{def\\_3\\_17}\n\\phantomsection\\label{def_3_17}\n\n\\begin{Def}[$\\sigma$-blocked walks]\\label{def:sigma_blocking}\n    Let $G = (J, V, E, L)$ be a CDMG (def \\ref{def-cdmg}); throughout we use the notation of def \\ref{not-cdmg}, the family-relationships definition of def \\ref{def_3_5}, the collider/non-collider machinery of def \\ref{def:collider_noncollider}, and the blockable/unblockable refinement of def \\ref{def:unblockable_noncollider}. Let $C \\subseteq J \\cup V$ be a subset of nodes, and let\n    \\[\n        \\pi = (v_0, a_0, v_1, a_1, \\dots, v_{n-1}, a_{n-1}, v_n)\n    \\]\n    be a walk in $G$ (def \\ref{def:walks}, item~i.) for some integer $n \\ge 0$, with $v_0, \\dots, v_n \\in J \\cup V$ and $a_0, \\dots, a_{n-1} \\in E \\cup L$ satisfying the walk constraint at every $k \\in \\{0, 1, \\dots, n - 1\\}$:\n    \\[\n        \\bigl(a_k = (v_k, v_{k+1}) \\;\\land\\; a_k \\in E \\cup L\\bigr)\n        \\;\\lor\\;\n        \\bigl(a_k = (v_{k+1}, v_k) \\;\\land\\; a_k \\in E\\bigr).\n    \\]\n\n    \\emph{Ancestor set of $C$.}\n    The \\emph{ancestor set} $\\Anc^G(C) \\subseteq J \\cup V$ is the indexed union of the per-vertex ancestor sets of def \\ref{def_3_5}, item~iv.:\n    \\[\n        \\Anc^G(C) := \\bigcup_{c \\in C} \\Anc^G(c).\n    \\]\n    Equivalently, for any $w \\in J \\cup V$,\n    \\[\n        w \\in \\Anc^G(C) \\quad\\iff\\quad \\exists\\, c \\in C \\text{ such that } w \\in \\Anc^G(c).\n    \\]\n    In the clauses below we use $\\Anc^G(C)$ in this union sense.\n\n    \\emph{$C$-$\\sigma$-open walk.}\n    The walk $\\pi$ is said to be \\emph{$C$-$\\sigma$-open} (also called \\emph{$\\sigma$-open given $C$}) iff both of the following universal conditions hold:\n    \\begin{enumerate}[label=\\roman*.)]\n        \\item for every position $k \\in \\{0, 1, \\dots, n\\}$, if $k$ is a collider on $\\pi$ in the sense of def \\ref{def:collider_noncollider}, item~ii.\\ (equivalently, $\\mathrm{ah}_\\pi(k) = 2$), then $v_k \\in \\Anc^G(C)$;\n        \\item for every position $k \\in \\{0, 1, \\dots, n\\}$, if $k$ is a blockable non-collider on $\\pi$ in the sense of def \\ref{def:unblockable_noncollider} (paragraph ``Blockable non-collider on $\\pi$''), then $v_k \\notin C$.\n    \\end{enumerate}\n    Both clauses are universally quantified over the position index set $\\{0, 1, \\dots, n\\}$; non-collider positions that are \\emph{unblockable} (def \\ref{def:unblockable_noncollider}, paragraph ``Unblockable non-collider on $\\pi$'') are not constrained by clause~ii., and collider positions are not constrained by clause~ii.\\ either (clause~i.\\ handles them and clause~ii.\\ is vacuous on collider positions since ``blockable non-collider'' carries ``non-collider'' as part of its precondition per def \\ref{def:unblockable_noncollider}).\n\n    \\emph{$C$-$\\sigma$-blocked walk.}\n    The walk $\\pi$ is said to be \\emph{$C$-$\\sigma$-blocked} (also called \\emph{$\\sigma$-blocked given $C$}) iff at least one of the following existential conditions holds:\n    \\begin{enumerate}[label=\\roman*.)]\n        \\item there exists a position $k \\in \\{0, 1, \\dots, n\\}$ such that $k$ is a collider on $\\pi$ (def \\ref{def:collider_noncollider}, item~ii.) and $v_k \\notin \\Anc^G(C)$;\n        \\item there exists a position $k \\in \\{0, 1, \\dots, n\\}$ such that $k$ is a blockable non-collider on $\\pi$ (def \\ref{def:unblockable_noncollider}, paragraph ``Blockable non-collider on $\\pi$'') and $v_k \\in C$.\n    \\end{enumerate}\n\n    \\emph{De Morgan duality of the two classifications.}\n    Inspecting the two clause-pairs above, clause~i.\\ of ``$C$-$\\sigma$-blocked'' is precisely the De Morgan negation of clause~i.\\ of ``$C$-$\\sigma$-open'' (``there exists a collider on $\\pi$ with $v_k \\notin \\Anc^G(C)$'' is the negation of ``every collider on $\\pi$ has $v_k \\in \\Anc^G(C)$''), and clause~ii.\\ of ``$C$-$\\sigma$-blocked'' is precisely the De Morgan negation of clause~ii.\\ of ``$C$-$\\sigma$-open'' (``there exists a blockable non-collider on $\\pi$ with $v_k \\in C$'' is the negation of ``every blockable non-collider on $\\pi$ has $v_k \\notin C$''). Disjoining the two negated clauses thus gives the negation of the conjunction of the two original clauses; consequently\n    \\[\n        \\pi \\text{ is } C\\text{-}\\sigma\\text{-blocked} \\quad\\iff\\quad \\pi \\text{ is not } C\\text{-}\\sigma\\text{-open},\n    \\]\n    and the two notions are by construction mutually exclusive and jointly exhaustive over walks in $G$.\n\n    \\emph{Treatment of the trailing LN remark.}\n    The LN source block (\\texttt{graphs.tex}, \\texttt{\\textbackslash label\\{def:sigma\\_blocking\\}}) follows the two definitions above with an embedded \\texttt{claimmark} reading ``Note that unblockable non-colliders are always $C$-$\\sigma$-open, regardless of the subset $C \\subseteq J \\cup V$.'' Per the row's authoritative \\texttt{addition\\_to\\_the\\_LN} clause \\texttt{[claim\\_type\\_mismatch\\_vertex\\_vs\\_walk]}, that remark is \\emph{intentionally excluded} from the formalization of def \\ref{def:sigma_blocking}: it is informal commentary, not part of the definition proper, and it applies the predicate ``$C$-$\\sigma$-open'' to \\emph{vertices} (specifically, to unblockable non-collider occurrences on a walk), whereas the formal definition above only ever introduces ``$C$-$\\sigma$-open'' as a property of \\emph{walks}. No Lean obligation is to be derived from the embedded remark within this definition. Any walk-level reformulation of its underlying content is handled separately as the dedicated claim row \\texttt{claim\\_3\\_21}; the present definition treats the embedded remark as out of scope.\n\\end{Def}\n\n\\end{document}\n",
    "html": "<p>Let $G = (J, V, E, L)$ be a CDMG (def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>); throughout we use the notation of def <a href=\"#def_3_2\" class=\"refrow\"><code>not-cdmg</code></a>, the family-relationships definition of def <a href=\"#def_3_5\" class=\"refrow\"><code>def_3_5</code></a>, the collider/non-collider machinery of def <a href=\"#def_3_15\" class=\"refrow\"><code>def:collider_noncollider</code></a>, and the blockable/unblockable refinement of def <a href=\"#def_3_16\" class=\"refrow\"><code>def:unblockable_noncollider</code></a>. Let $C \\subseteq J \\cup V$ be a subset of nodes, and let\n    \\[\n        \\pi = (v_0, a_0, v_1, a_1, \\dots, v_{n-1}, a_{n-1}, v_n)\n    \\]\n    be a walk in $G$ (def <a href=\"#def_3_4\" class=\"refrow\"><code>def:walks</code></a>, item&nbsp;i.) for some integer $n \\ge 0$, with $v_0, \\dots, v_n \\in J \\cup V$ and $a_0, \\dots, a_{n-1} \\in E \\cup L$ satisfying the walk constraint at every $k \\in \\{0, 1, \\dots, n - 1\\}$:\n    \\[\n        \\bigl(a_k = (v_k, v_{k+1}) \\;\\land\\; a_k \\in E \\cup L\\bigr)\n        \\;\\lor\\;\n        \\bigl(a_k = (v_{k+1}, v_k) \\;\\land\\; a_k \\in E\\bigr).\n    \\]</p>\n<p><em>Ancestor set of $C$.</em>\n    The <em>ancestor set</em> $\\Anc^G(C) \\subseteq J \\cup V$ is the indexed union of the per-vertex ancestor sets of def <a href=\"#def_3_5\" class=\"refrow\"><code>def_3_5</code></a>, item&nbsp;iv.:\n    \\[\n        \\Anc^G(C) := \\bigcup_{c \\in C} \\Anc^G(c).\n    \\]\n    Equivalently, for any $w \\in J \\cup V$,\n    \\[\n        w \\in \\Anc^G(C) \\quad\\iff\\quad \\exists\\, c \\in C \\text{ such that } w \\in \\Anc^G(c).\n    \\]\n    In the clauses below we use $\\Anc^G(C)$ in this union sense.</p>\n<p><em>$C$-$\\sigma$-open walk.</em>\n    The walk $\\pi$ is said to be <em>$C$-$\\sigma$-open</em> (also called <em>$\\sigma$-open given $C$</em>) iff both of the following universal conditions hold:</p>\n<ol class=\"roman-paren\"><li>for every position $k \\in \\{0, 1, \\dots, n\\}$, if $k$ is a collider on $\\pi$ in the sense of def <a href=\"#def_3_15\" class=\"refrow\"><code>def:collider_noncollider</code></a>, item&nbsp;ii.\\ (equivalently, $\\mathrm{ah}_\\pi(k) = 2$), then $v_k \\in \\Anc^G(C)$;</li><li>for every position $k \\in \\{0, 1, \\dots, n\\}$, if $k$ is a blockable non-collider on $\\pi$ in the sense of def <a href=\"#def_3_16\" class=\"refrow\"><code>def:unblockable_noncollider</code></a> (paragraph “Blockable non-collider on $\\pi$”), then $v_k \\notin C$.</li></ol>\n<p>Both clauses are universally quantified over the position index set $\\{0, 1, \\dots, n\\}$; non-collider positions that are <em>unblockable</em> (def <a href=\"#def_3_16\" class=\"refrow\"><code>def:unblockable_noncollider</code></a>, paragraph “Unblockable non-collider on $\\pi$”) are not constrained by clause&nbsp;ii., and collider positions are not constrained by clause&nbsp;ii.\\ either (clause&nbsp;i.\\ handles them and clause&nbsp;ii.\\ is vacuous on collider positions since “blockable non-collider” carries “non-collider” as part of its precondition per def <a href=\"#def_3_16\" class=\"refrow\"><code>def:unblockable_noncollider</code></a>).</p>\n<p><em>$C$-$\\sigma$-blocked walk.</em>\n    The walk $\\pi$ is said to be <em>$C$-$\\sigma$-blocked</em> (also called <em>$\\sigma$-blocked given $C$</em>) iff at least one of the following existential conditions holds:</p>\n<ol class=\"roman-paren\"><li>there exists a position $k \\in \\{0, 1, \\dots, n\\}$ such that $k$ is a collider on $\\pi$ (def <a href=\"#def_3_15\" class=\"refrow\"><code>def:collider_noncollider</code></a>, item&nbsp;ii.) and $v_k \\notin \\Anc^G(C)$;</li><li>there exists a position $k \\in \\{0, 1, \\dots, n\\}$ such that $k$ is a blockable non-collider on $\\pi$ (def <a href=\"#def_3_16\" class=\"refrow\"><code>def:unblockable_noncollider</code></a>, paragraph “Blockable non-collider on $\\pi$”) and $v_k \\in C$.</li></ol>\n<p><em>De Morgan duality of the two classifications.</em>\n    Inspecting the two clause-pairs above, clause&nbsp;i.\\ of “$C$-$\\sigma$-blocked” is precisely the De Morgan negation of clause&nbsp;i.\\ of “$C$-$\\sigma$-open” (“there exists a collider on $\\pi$ with $v_k \\notin \\Anc^G(C)$” is the negation of “every collider on $\\pi$ has $v_k \\in \\Anc^G(C)$”), and clause&nbsp;ii.\\ of “$C$-$\\sigma$-blocked” is precisely the De Morgan negation of clause&nbsp;ii.\\ of “$C$-$\\sigma$-open” (“there exists a blockable non-collider on $\\pi$ with $v_k \\in C$” is the negation of “every blockable non-collider on $\\pi$ has $v_k \\notin C$”). Disjoining the two negated clauses thus gives the negation of the conjunction of the two original clauses; consequently\n    \\[\n        \\pi \\text{ is } C\\text{-}\\sigma\\text{-blocked} \\quad\\iff\\quad \\pi \\text{ is not } C\\text{-}\\sigma\\text{-open},\n    \\]\n    and the two notions are by construction mutually exclusive and jointly exhaustive over walks in $G$.</p>\n<p><em>Treatment of the trailing LN remark.</em>\n    The LN source block (<code>graphs.tex</code>, \\texttt{\\textbackslash label\\{def:sigma\\_blocking\\}}) follows the two definitions above with an embedded <code>claimmark</code> reading “Note that unblockable non-colliders are always $C$-$\\sigma$-open, regardless of the subset $C \\subseteq J \\cup V$.” Per the row's authoritative <code>addition_to_the_LN</code> clause <code>[claim_type_mismatch_vertex_vs_walk]</code>, that remark is <em>intentionally excluded</em> from the formalization of def <a href=\"#def_3_17\" class=\"refrow\"><code>def:sigma_blocking</code></a>: it is informal commentary, not part of the definition proper, and it applies the predicate “$C$-$\\sigma$-open” to <em>vertices</em> (specifically, to unblockable non-collider occurrences on a walk), whereas the formal definition above only ever introduces “$C$-$\\sigma$-open” as a property of <em>walks</em>. No Lean obligation is to be derived from the embedded remark within this definition. Any walk-level reformulation of its underlying content is handled separately as the dedicated claim row <code>claim_3_21</code>; the present definition treats the embedded remark as out of scope.</p>",
    "env": "Def",
    "env_title": "$\\sigma$-blocked walks",
    "source_path": "leanification/Chapter3_GraphTheory/Section3_3/tex/def_3_17_SigmaBlockedWalks.tex"
  },
  "tex_proof": null,
  "lean_blocks": [
    {
      "kind": "helper",
      "code": "variable {Node : Type*} [DecidableEq Node]",
      "explanation": "This is a `variable` declaration: Lean auto-binds `Node : Type*` and the instance `[DecidableEq Node]` as implicit arguments to every subsequent declaration in the surrounding `CDMG` namespace, sparing us from rewriting the same parameters on `IsSigmaOpenGiven` and `IsSigmaBlockedGiven`. The `Type*` notation makes `Node` universe-polymorphic, so callers can instantiate it at any universe level. The `DecidableEq Node` assumption is inherited from the chapter-wide convention: although the two predicates in this row are propositional and do not themselves run any decision procedure, they reach into `G.AncSet C` and `p.IsCollider k` / `p.IsBlockableNonCollider k`, which in turn unfold through `Finset.filter`, `Finset.image`, and option-valued list lookups on `p.vertices` — all of which need decidable equality on the element type. Stronger structure on `Node` (e.g.\\ `Fintype`, `LinearOrder`) is not required here and is deferred to downstream consumers that actually use it. Same convention as the rest of `Chapter3_GraphTheory`'s files.",
      "code_annotated": "-- Auto-binds `Node` + `DecidableEq Node` as implicits; decidability is inherited by `G.AncSet`, `p.IsCollider`, `p.IsBlockableNonCollider` below.\nvariable {Node : Type*} [DecidableEq Node]"
    },
    {
      "kind": "helper",
      "code": "variable {G : CDMG Node}",
      "explanation": "A second `variable` line, scoped to the `Walk` sub-namespace, that auto-binds the ambient CDMG `G : CDMG Node` as an implicit argument to every subsequent declaration. Both `IsSigmaOpenGiven` and `IsSigmaBlockedGiven` take a walk `p : Walk G u v` whose graph parameter must come from somewhere; without this binder each signature would have to spell out `{G : CDMG Node}` explicitly. Lifting it once at the namespace top mirrors the LN's once-at-the-top quantifier \"let $G = (J, V, E, L)$ be a CDMG\" and keeps the bodies focused on the walk and the conditioning set. `G` is kept implicit because downstream callers reach into it via dot-notation on the walk (`p.IsSigmaOpenGiven C`) — Lean recovers `G` from the type of `p`, so the user never types it. The two main predicates also call `G.AncSet C` on the bound `G`, so it must be in scope when their bodies type-check.",
      "code_annotated": "-- Namespace-wide implicit `G : CDMG Node`; recovered from the walk's type via dot-notation at every use site.\nvariable {G : CDMG Node}"
    },
    {
      "kind": "main",
      "code": "def IsSigmaOpenGiven {u v : Node} (p : Walk G u v) (C : Set Node) : Prop :=\n  (∀ (k : ℕ) (vk : Node), p.vertices[k]? = some vk → p.IsCollider k →\n      vk ∈ G.AncSet C) ∧\n  (∀ (k : ℕ) (vk : Node), p.vertices[k]? = some vk →\n      p.IsBlockableNonCollider k → vk ∉ C)",
      "explanation": "This block defines the LN's first classifier — a walk $\\pi$ is *$C$-$\\sigma$-open* — as a `Prop`-valued predicate on `(p : Walk G u v) (C : Set Node)`. The body is a conjunction of two parallel universals, one per LN clause. Each universal ranges over position indices `k : ℕ` together with the vertex `vk : Node` at that position, pinned down by the hypothesis `p.vertices[k]? = some vk`; this is the standard `getElem?` lookup that returns `none` past the end of the walk, so the LN's index range $k \\in \\{0, \\dots, n\\}$ is implicit — out-of-range `k` falsify the antecedent and make the implication vacuous. Clause (i) says every collider position has its vertex inside the ancestor set $\\Anc^G(C)$, written here as `G.AncSet C` (the indexed-union form from `def_3_5`). Clause (ii) says every *blockable* non-collider position has its vertex outside `C`; using `p.IsBlockableNonCollider k` rather than the broader `p.IsNonCollider k` is load-bearing — unblockable non-collider positions are silently outside both clauses, matching the LN's intent. Dot-notation `p.IsSigmaOpenGiven C` reads as the LN's \"$\\pi$ is $C$-$\\sigma$-open\".",
      "code_annotated": "-- ref: def_3_17 (main, LN paragraph \"C-σ-open walk\") — walk-level `Prop` predicate matching the LN's two-clause universal.\ndef IsSigmaOpenGiven {u v : Node} (p : Walk G u v) (C : Set Node) : Prop :=\n  -- LN clause (i): universal over `(k, vk)` pairs; `getElem?` lookup implicitly bounds `k ≤ p.length`.\n  (∀ (k : ℕ) (vk : Node), p.vertices[k]? = some vk → p.IsCollider k →\n      -- Collider positions must have their vertex inside the indexed-union ancestor set `Anc^G(C)`.\n      vk ∈ G.AncSet C) ∧\n  -- LN clause (ii): same `(k, vk)` shape, restricted to *blockable* non-colliders only (unblockable positions silently excluded).\n  (∀ (k : ℕ) (vk : Node), p.vertices[k]? = some vk →\n      -- Blockable non-collider positions must have their vertex outside the conditioning set `C`.\n      p.IsBlockableNonCollider k → vk ∉ C)"
    },
    {
      "kind": "main",
      "code": "def IsSigmaBlockedGiven {u v : Node} (p : Walk G u v) (C : Set Node) : Prop :=\n  (∃ (k : ℕ) (vk : Node),\n      p.vertices[k]? = some vk ∧ p.IsCollider k ∧ vk ∉ G.AncSet C) ∨\n  (∃ (k : ℕ) (vk : Node),\n      p.vertices[k]? = some vk ∧ p.IsBlockableNonCollider k ∧ vk ∈ C)",
      "explanation": "This block defines the LN's second classifier — a walk $\\pi$ is *$C$-$\\sigma$-blocked* — as the positive existential disjunction dual to `IsSigmaOpenGiven`, not as `¬ p.IsSigmaOpenGiven C`. The body is a `∨` of two existentials, each pinning a position index `k : ℕ` and the vertex `vk` at that position via `p.vertices[k]? = some vk`. Disjunct (i) is a collider witness: some position `k` is a collider on `p` whose vertex `vk` escapes `G.AncSet C`. Disjunct (ii) is a blockable-non-collider witness: some position `k` is a blockable non-collider on `p` whose vertex `vk` lies in `C`. Encoding the predicate in this positive form lets downstream proofs build a blocking witness directly as `Or.inl ⟨k, vk, h_lookup, h_collider, h_anc⟩` or `Or.inr ⟨k, vk, h_lookup, h_blockable, h_mem⟩`, instead of pushing a double negation through nested universals. The classical De Morgan equivalence `¬ p.IsSigmaOpenGiven C ↔ p.IsSigmaBlockedGiven C` is left as a downstream lemma rather than a definitional reduction.",
      "code_annotated": "-- ref: def_3_17 (main, LN paragraph \"C-σ-blocked walk\") — positive existential disjunction, NOT `¬ IsSigmaOpenGiven`.\ndef IsSigmaBlockedGiven {u v : Node} (p : Walk G u v) (C : Set Node) : Prop :=\n  -- LN clause (i) dual: a collider witness whose vertex escapes the ancestor set `Anc^G(C)`.\n  (∃ (k : ℕ) (vk : Node),\n      -- Triple-conjunct destructures cleanly as `⟨h_lookup, h_collider, h_anc⟩` at the use site.\n      p.vertices[k]? = some vk ∧ p.IsCollider k ∧ vk ∉ G.AncSet C) ∨\n  -- LN clause (ii) dual: a *blockable* non-collider witness whose vertex lies in `C` (unblockable positions cannot witness).\n  (∃ (k : ℕ) (vk : Node),\n      p.vertices[k]? = some vk ∧ p.IsBlockableNonCollider k ∧ vk ∈ C)"
    }
  ],
  "lean_source_url": "https://github.com/schipeltaper/causality-leanified/blob/main/leanification/Chapter3_GraphTheory/Section3_3/SigmaBlockedWalks.lean",
  "lean_file_path": "leanification/Chapter3_GraphTheory/Section3_3/SigmaBlockedWalks.lean",
  "addition_to_the_LN": "[claim_type_mismatch_vertex_vs_walk] The trailing claim asserting that unblockable non-colliders are always $C$-$\\sigma$-open is excluded from the formalization. It is informal commentary, not part of Definition~\\ref{def:sigma_blocking}, and no Lean obligation should be derived from it.",
  "design_choices": "**Walk-level `Prop` predicates over `(p : Walk G u v) (C : Set Node)`.** The LN classifies *walks* relative to a conditioning set, so the predicates take the walk and the set directly and dot-notation `p.IsSigmaOpenGiven C` mirrors the LN's \"$\\pi$ is $C$-$\\sigma$-open\" writing. No vertex-level σ-open predicate is introduced: the trailing LN remark that \"unblockable non-colliders are always $C$-$\\sigma$-open\" applies the walk-level predicate to vertices, which is a type mismatch this row records but does not formalise; any walk-level reformulation of its underlying intent is left to a separate claim.\n\n**Position–vertex coupling via `p.vertices[k]? = some vk`.** The LN writes $v_k$ for the vertex at position $k$; the option-valued lookup pins down `vk` while implicitly bounding $k \\le n$, since out-of-range lookups are `none`. The universal antecedents of `IsSigmaOpenGiven` then become vacuous on out-of-range positions, and the existential witnesses of `IsSigmaBlockedGiven` cannot be formed — matching the LN's scoping to $\\{0, \\dots, n\\}$ without an explicit upper-bound hypothesis. This mirrors the idiom already used in `def_3_15` and `def_3_16`.\n\n**`p.IsBlockableNonCollider k`, not `p.IsNonCollider k`.** Clause (ii) of the LN restricts to *blockable* non-colliders specifically: unblockable positions do not constrain $C$-$\\sigma$-openness, and they do not witness $C$-$\\sigma$-blockedness either. The broader `IsNonCollider` would over-fire on the open side (forcing unblockable positions outside $C$) and under-fire on the blocked side (admitting unblockable positions in $C$ as blocking witnesses) — neither of which the LN says. The restriction is the predicate-level realisation of the LN's intent.\n\n**Positive existential disjunction for `IsSigmaBlockedGiven`, not `¬ IsSigmaOpenGiven`.** The LN writes $C$-$\\sigma$-blocked as $\\exists \\dots \\notin \\Anc^G(C) \\;\\lor\\; \\exists \\dots \\in C$, the De Morgan dual of the open definition. Encoding it directly as that positive existential disjunction (rather than as a negation of `IsSigmaOpenGiven`) mirrors the LN's literal writing, lets downstream proofs construct a blocking witness as an immediate `Or.inl ⟨k, vk, _, _, _⟩` term, and avoids pushing a double negation through nested universals and conjunctions every time a witness is needed. The equivalence `¬ p.IsSigmaOpenGiven C ↔ p.IsSigmaBlockedGiven C` is a downstream (classical) De Morgan lemma to be proved when a row needs it, not a definitional reduction the def-shape forces.\n\n**Two parallel universals/existentials, not a single quantifier over a combined predicate.** The LN spells each classification as two parallel clauses, so the formalisation matches: `IsSigmaOpenGiven` destructures as `⟨h_collider, h_blockable⟩` and `IsSigmaBlockedGiven` destructures into one of the two clause witnesses. A single quantifier over a sum-typed predicate would compile but would force every downstream proof to recover the bullet-list shape by hand.\n\n**Reusing `G.AncSet` from `def_3_5`.** `CDMG.AncSet : CDMG Node → Set Node → Set Node` already encodes the indexed-union ancestor set $\\Anc^G(C)$. Out-of-graph elements of $C$ contribute $G.\\mathrm{Anc}\\,c = \\emptyset$ via the `w ∈ G` guard inside `Anc`, so the LN's $C \\subseteq J \\cup V$ hypothesis is not needed at the definition site — an escaping $C$ simply restricts the effective ancestor set to $\\Anc^G(C \\cap (J \\cup V))$. Downstream consumers that genuinely need the subset hypothesis pass it at the use site."
}
