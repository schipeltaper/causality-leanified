{
  "ref": "def_3_2",
  "kind": "def",
  "section": "3.1",
  "title": "CDMGNotation",
  "type": "notation",
  "status": {
    "formalized": "yes",
    "proven": "n/a",
    "solved": "yes"
  },
  "tex_statement": {
    "raw": "% Definition file for row def_3_2 -- \"CDMGNotation\".\n% Auto-generated stub by scaffold/solve_chapter.py at row start. The\n% manager agent (or a worker dispatched by it) fills the body below with the\n% Lean-faithful definition statement(s). Stays close to the lecture notes.\n%\n% This file is a sibling of `main.tex` inside `<subsection>/tex/`. The\n% `subfiles` package lets it compile both standalone (resolving its\n% preamble via `main.tex`) and as part of `main.tex` via `\\subfile{...}`.\n\n\\documentclass[main]{subfiles}\n\n\\begin{document}\n% ref: def_3_2\n% title: CDMGNotation\n\\def\\rowref{def\\_3\\_2}\n\\phantomsection\\label{def_3_2}\n\n\\begin{Not}[CDMGNotation]\n    \\label{not-cdmg}\n    Let $G = (J, V, E, L)$ be a CDMG (def \\ref{def-cdmg}).\n    We introduce the following seven shorthand notations, each defined as a literal abbreviation for a set-theoretic statement over the four components $J, V, E, L$ of $G$.\n    The visual macros $\\tuh, \\hut, \\huh, \\suh, \\hus, \\sus$ are notation only; the load-bearing definitional content is the right-hand side of each abbreviation below.\n    \\begin{enumerate}\n        %\\item $G(V|\\doit(J))$ to represent the graph $(J,V,E,L)$, where $E$ and $L$ are kept implicit in this notation.\n        \\item $v \\in G \\;:\\Longleftrightarrow\\; v \\in J \\cup V$.\n        \\item $v_1 \\tuh v_2 \\in G \\;:\\Longleftrightarrow\\; (v_1, v_2) \\in E$.\n        \\item $v_1 \\hut v_2 \\in G \\;:\\Longleftrightarrow\\; (v_2, v_1) \\in E$.\n        \\item $v_1 \\huh v_2 \\in G \\;:\\Longleftrightarrow\\; (v_1, v_2) \\in L$.\n              Because $L$ is required to be symmetric by the CDMG definition (def \\ref{def-cdmg}: $(v_1, v_2) \\in L \\Longleftrightarrow (v_2, v_1) \\in L$ for all $v_1, v_2$), this abbreviation is itself symmetric in its two arguments:\n              \\[ v_1 \\huh v_2 \\in G \\;\\Longleftrightarrow\\; v_2 \\huh v_1 \\in G. \\]\n        \\item $v_1 \\suh v_2 \\in G \\;:\\Longleftrightarrow\\; \\bigl(v_1 \\tuh v_2 \\in G\\bigr) \\,\\lor\\, \\bigl(v_1 \\huh v_2 \\in G\\bigr)$, equivalently $(v_1, v_2) \\in E \\,\\lor\\, (v_1, v_2) \\in L$.\n              Because the $\\huh$-disjunct is symmetric in its arguments (item~4), swapping $v_1 \\leftrightarrow v_2$ in this disjunction turns $\\suh$ into $\\hus$:\n              \\[ v_1 \\suh v_2 \\in G \\;\\Longleftrightarrow\\; v_2 \\hus v_1 \\in G. \\]\n        \\item $v_1 \\hus v_2 \\in G \\;:\\Longleftrightarrow\\; \\bigl(v_1 \\hut v_2 \\in G\\bigr) \\,\\lor\\, \\bigl(v_1 \\huh v_2 \\in G\\bigr)$, equivalently $(v_2, v_1) \\in E \\,\\lor\\, (v_1, v_2) \\in L$.\n              By the same $\\huh$-symmetry of item~4, $\\hus$ becomes $\\suh$ under swap of its arguments:\n              \\[ v_1 \\hus v_2 \\in G \\;\\Longleftrightarrow\\; v_2 \\suh v_1 \\in G. \\]\n        \\item $v_1 \\sus v_2 \\in G \\;:\\Longleftrightarrow\\; \\bigl(v_1 \\tuh v_2 \\in G\\bigr) \\,\\lor\\, \\bigl(v_1 \\hut v_2 \\in G\\bigr) \\,\\lor\\, \\bigl(v_1 \\huh v_2 \\in G\\bigr)$, equivalently $(v_1, v_2) \\in E \\,\\lor\\, (v_2, v_1) \\in E \\,\\lor\\, (v_1, v_2) \\in L$.\n\n              \\emph{Exhaustiveness of the three disjuncts.}\n              These three configurations enumerate every edge configuration a CDMG admits between an ordered pair of nodes $(v_1, v_2)$.\n              Although mechanically reading the placeholder convention ``$\\star$ stands for arrowhead or tail'' would suggest a fourth, tail-tail case ``$v_1 \\tut v_2$'', no edge type of a CDMG carries tails on both ends: per def \\ref{def-cdmg}, directed edges in $E$ have one tail and one head, and bidirected edges in $L$ have two heads, so no tail-tail edge type exists.\n              The case $v_1 \\tut v_2$ is therefore vacuous as a CDMG edge and is excluded by definition, not omitted by oversight.\n              Any case analysis on ``$v_1 \\sus v_2 \\in G$'' may assume exhaustiveness over exactly the three listed alternatives $\\{\\tuh, \\hut, \\huh\\}$.\n\n              \\emph{Symmetry of $\\sus$.}\n              Under the swap $v_1 \\leftrightarrow v_2$, the three disjuncts pair off: $v_1 \\tuh v_2 \\in G$ exchanges with $v_2 \\hut v_1 \\in G$ (items~2~$\\leftrightarrow$~3), and $v_1 \\huh v_2 \\in G$ exchanges with $v_2 \\huh v_1 \\in G$ by the $\\huh$-symmetry of item~4. Hence $\\sus$ is symmetric in its two arguments:\n              \\[ v_1 \\sus v_2 \\in G \\;\\Longleftrightarrow\\; v_2 \\sus v_1 \\in G. \\]\n    \\end{enumerate}\n    A ``$\\star$'' appearing in the diagrammatic shorthands ($\\suh$, $\\hus$, $\\sus$) is a placeholder reading ``arrowhead or tail''; in $\\sus$ the would-be tail-tail combination is vacuous as a CDMG edge type (see item~7), so $\\sus$ ranges over exactly the three configurations enumerated there.\n\\end{Not}\n\n\\end{document}\n",
    "html": "<p>Let $G = (J, V, E, L)$ be a CDMG (def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>).\n    We introduce the following seven shorthand notations, each defined as a literal abbreviation for a set-theoretic statement over the four components $J, V, E, L$ of $G$.\n    The visual macros $\\tuh, \\hut, \\huh, \\suh, \\hus, \\sus$ are notation only; the load-bearing definitional content is the right-hand side of each abbreviation below.</p>\n<ol class=\"enum\"><li>$v \\in G \\;:\\Longleftrightarrow\\; v \\in J \\cup V$.</li><li>$v_1 \\tuh v_2 \\in G \\;:\\Longleftrightarrow\\; (v_1, v_2) \\in E$.</li><li>$v_1 \\hut v_2 \\in G \\;:\\Longleftrightarrow\\; (v_2, v_1) \\in E$.</li><li>$v_1 \\huh v_2 \\in G \\;:\\Longleftrightarrow\\; (v_1, v_2) \\in L$.\n              Because $L$ is required to be symmetric by the CDMG definition (def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>: $(v_1, v_2) \\in L \\Longleftrightarrow (v_2, v_1) \\in L$ for all $v_1, v_2$), this abbreviation is itself symmetric in its two arguments:\n              \\[ v_1 \\huh v_2 \\in G \\;\\Longleftrightarrow\\; v_2 \\huh v_1 \\in G. \\]</li><li>$v_1 \\suh v_2 \\in G \\;:\\Longleftrightarrow\\; \\bigl(v_1 \\tuh v_2 \\in G\\bigr) \\,\\lor\\, \\bigl(v_1 \\huh v_2 \\in G\\bigr)$, equivalently $(v_1, v_2) \\in E \\,\\lor\\, (v_1, v_2) \\in L$.\n              Because the $\\huh$-disjunct is symmetric in its arguments (item&nbsp;4), swapping $v_1 \\leftrightarrow v_2$ in this disjunction turns $\\suh$ into $\\hus$:\n              \\[ v_1 \\suh v_2 \\in G \\;\\Longleftrightarrow\\; v_2 \\hus v_1 \\in G. \\]</li><li>$v_1 \\hus v_2 \\in G \\;:\\Longleftrightarrow\\; \\bigl(v_1 \\hut v_2 \\in G\\bigr) \\,\\lor\\, \\bigl(v_1 \\huh v_2 \\in G\\bigr)$, equivalently $(v_2, v_1) \\in E \\,\\lor\\, (v_1, v_2) \\in L$.\n              By the same $\\huh$-symmetry of item&nbsp;4, $\\hus$ becomes $\\suh$ under swap of its arguments:\n              \\[ v_1 \\hus v_2 \\in G \\;\\Longleftrightarrow\\; v_2 \\suh v_1 \\in G. \\]</li><li>$v_1 \\sus v_2 \\in G \\;:\\Longleftrightarrow\\; \\bigl(v_1 \\tuh v_2 \\in G\\bigr) \\,\\lor\\, \\bigl(v_1 \\hut v_2 \\in G\\bigr) \\,\\lor\\, \\bigl(v_1 \\huh v_2 \\in G\\bigr)$, equivalently $(v_1, v_2) \\in E \\,\\lor\\, (v_2, v_1) \\in E \\,\\lor\\, (v_1, v_2) \\in L$.\n<p><em>Exhaustiveness of the three disjuncts.</em>\n              These three configurations enumerate every edge configuration a CDMG admits between an ordered pair of nodes $(v_1, v_2)$.\n              Although mechanically reading the placeholder convention “$\\star$ stands for arrowhead or tail” would suggest a fourth, tail-tail case “$v_1 \\tut v_2$”, no edge type of a CDMG carries tails on both ends: per def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>, directed edges in $E$ have one tail and one head, and bidirected edges in $L$ have two heads, so no tail-tail edge type exists.\n              The case $v_1 \\tut v_2$ is therefore vacuous as a CDMG edge and is excluded by definition, not omitted by oversight.\n              Any case analysis on “$v_1 \\sus v_2 \\in G$” may assume exhaustiveness over exactly the three listed alternatives $\\{\\tuh, \\hut, \\huh\\}$.</p>\n<p><em>Symmetry of $\\sus$.</em>\n              Under the swap $v_1 \\leftrightarrow v_2$, the three disjuncts pair off: $v_1 \\tuh v_2 \\in G$ exchanges with $v_2 \\hut v_1 \\in G$ (items&nbsp;2&nbsp;$\\leftrightarrow$&nbsp;3), and $v_1 \\huh v_2 \\in G$ exchanges with $v_2 \\huh v_1 \\in G$ by the $\\huh$-symmetry of item&nbsp;4. Hence $\\sus$ is symmetric in its two arguments:\n              \\[ v_1 \\sus v_2 \\in G \\;\\Longleftrightarrow\\; v_2 \\sus v_1 \\in G. \\]</li></ol></p>\n<p>A “$\\star$” appearing in the diagrammatic shorthands ($\\suh$, $\\hus$, $\\sus$) is a placeholder reading “arrowhead or tail”; in $\\sus$ the would-be tail-tail combination is vacuous as a CDMG edge type (see item&nbsp;7), so $\\sus$ ranges over exactly the three configurations enumerated there.</p>",
    "env": "Not",
    "env_title": "CDMGNotation",
    "source_path": "leanification/Chapter3_GraphTheory/Section3_1/tex/def_3_2_CDMGNotation.tex"
  },
  "tex_proof": null,
  "lean_blocks": [
    {
      "kind": "helper",
      "code": "variable {Node : Type*} [DecidableEq Node]"
    },
    {
      "kind": "main",
      "code": "instance instMembership : Membership Node (CDMG Node) where\n  mem G v := v ∈ G.J ∪ G.V"
    },
    {
      "kind": "main",
      "code": "def tuh (G : CDMG Node) (v1 v2 : Node) : Prop := (v1, v2) ∈ G.E"
    },
    {
      "kind": "main",
      "code": "def hut (G : CDMG Node) (v1 v2 : Node) : Prop := (v2, v1) ∈ G.E"
    },
    {
      "kind": "main",
      "code": "def huh (G : CDMG Node) (v1 v2 : Node) : Prop := (v1, v2) ∈ G.L"
    },
    {
      "kind": "main",
      "code": "def suh (G : CDMG Node) (v1 v2 : Node) : Prop :=\n  G.tuh v1 v2 ∨ G.huh v1 v2"
    },
    {
      "kind": "main",
      "code": "def hus (G : CDMG Node) (v1 v2 : Node) : Prop :=\n  G.hut v1 v2 ∨ G.huh v1 v2"
    },
    {
      "kind": "main",
      "code": "def sus (G : CDMG Node) (v1 v2 : Node) : Prop :=\n  G.tuh v1 v2 ∨ G.hut v1 v2 ∨ G.huh v1 v2"
    }
  ],
  "lean_source_url": "https://github.com/schipeltaper/causality-leanified/blob/main/leanification/Chapter3_GraphTheory/Section3_1/CDMGNotation.lean",
  "lean_file_path": "leanification/Chapter3_GraphTheory/Section3_1/CDMGNotation.lean",
  "addition_to_the_LN": "[sus_omits_tail_tail_despite_star_eq_head_or_tail] CDMGs do not admit a tail-tail edge type; consequently the placeholder rule \"the star stands for arrowhead or tail\" yields only the three enumerated configurations in item 7 (the tail-tail case \"$v_1 \\tut v_2$\" is excluded by definition, not omitted by oversight). Any case analysis on \"$v_1 \\sus v_2 \\in G$\" may assume exhaustiveness over exactly the three listed alternatives $\\{\\tuh, \\hut, \\huh\\}$.\n\n[huh_visual_symmetry_vs_ordered_pair_in_L] The relation $L$ is required to be symmetric: for all $v_1, v_2$, $(v_1, v_2) \\in L$ if and only if $(v_2, v_1) \\in L$. Consequently, $v_1 \\huh v_2 \\in G \\iff v_2 \\huh v_1 \\in G$, and the symmetry of the $\\huh$-disjunct propagates to items 5–7 accordingly.",
  "lean_explanation": "The six edge-relation names are two-letter sketches of what sits at the endpoints of an edge: `t` is a *tail*, `h` is an *arrowhead*, and `s` is the LN's *star* placeholder meaning \"either head or tail\" (the middle `u` is a connector chosen so the Lean names match the LaTeX macros `\\tuh`, `\\hut`, `\\huh`, `\\suh`, `\\hus`, `\\sus` letter-for-letter). The convention is: first letter = endpoint at the LEFT argument, second letter = endpoint at the RIGHT argument. So `tuh v1 v2` is tail-at-`v1`, head-at-`v2` (the forward $v_1 \\to v_2$); `hut` is its mirror $v_1 \\leftarrow v_2$; `huh` is head-on-both (the bidirected $v_1 \\leftrightarrow v_2$); and `suh`, `hus`, `sus` are the `∨`-fanouts you get by expanding each star into its head/tail alternatives.\n\nThe `variable {Node : Type*} [DecidableEq Node]` line is a Lean *section-style declaration*, not a definition. It tells the elaborator: in every subsequent `def`/`instance` of this namespace that mentions `Node`, silently insert these two binders into the signature. The curly braces make `Node` *implicit* (Lean infers it from the `CDMG Node` argument at each call site), and the square-bracketed `[DecidableEq Node]` is an *instance binder*: it threads a `DecidableEq` typeclass dictionary through without naming it, so `Finset` operations on `Node × Node` — which need decidable equality to check membership and union — just work in every body below.\n\nA few Mathlib idioms worth flagging. The first block is registered as an `instance` of the `Membership` typeclass, which is exactly what makes the literal `v ∈ G` typecheck — Lean resolves `∈` by typeclass search on the container type, so `Finset` membership (used inside the body as `v ∈ G.J ∪ G.V`) and CDMG membership coexist without clashing. The remaining six are `Prop`-valued (logical propositions, not `Bool` computations), which is what lets downstream proofs use `rcases h with h | h` directly on the `∨`-disjunctions. Finally, because everything lives inside `namespace CDMG`, *dot-notation* `G.tuh v1 v2` is sugar for `CDMG.tuh G v1 v2`, lining up the Lean reading with the LN's $v_1 \\tuh v_2 \\in G$.",
  "design_choices": "**Encode `v ∈ G` as a `Membership Node (CDMG Node)` instance rather than a plain `def CDMG.Mem`.** The lecture notes write `v ∈ G` verbatim — both in this notation block itself and at every downstream use site, including parent sets of the form $\\{w \\in G \\mid w \\to v \\in G\\}$, walk-endpoint constraints, and the `v_1 \\tuh v_2 \\in G` phrasings of items 2 through 7. A plain `def` would force every consumer to write `G.Mem v` or `CDMG.Mem G v`, neither of which mirrors the notes. Lean's `Membership` typeclass dispatches `∈` to the supplied `mem` function, so `v ∈ G` becomes literal Lean text downstream with no notation macro and no clash with mathlib's own `∈`.\n\n**Rely on typeclass resolution to disambiguate `v ∈ G` from `v ∈ G.J ∪ G.V`.** Mathlib already provides `Membership α (Finset α)`, and the body of this instance itself uses `∈` against a `Finset Node`. The two instances coexist because their second type arguments differ — `Finset Node` versus `CDMG Node` — so Lean picks the right instance at each call site without ascriptions or `@`-spelled-out invocations. The outer `v ∈ G` resolves to the new `CDMG` instance, and the inner `v ∈ G.J ∪ G.V` resolves to the mathlib `Finset` instance, transparently.\n\n**Define membership as `v ∈ G.J ∪ G.V`, not as a pair of disjoint membership tests.** The notes say $v \\in J \\cup V$, and `Finset.union` lifts this verbatim. Threading the structural disjointness witness `hJV_disj` into the membership body was rejected because membership only needs to ask whether `v` lies in either set, not in exactly one; downstream rows that require the exclusive form (for instance the hard-intervention rewrite `V \\setminus W` to `J \\cup W`) invoke `hJV_disj` directly at the use site, keeping the data-versus-axiom distinction clean.\n\n**Introduce `Node` and `DecidableEq Node` once via a `variable` block rather than as explicit parameters on every definition.** Every following declaration in the file (the membership instance and the six edge predicates) shares the same `Node` carrier and needs `DecidableEq Node` for `Finset` membership to compute. Repeating `{Node : Type*} [DecidableEq Node]` on each `def` and `instance` would be uniform boilerplate that adds no information; a single `variable` line lets Lean auto-bind the parameters in exactly the declarations that use them. The `DecidableEq Node` assumption is what makes `v ∈ G.J ∪ G.V` and the `(v1, v2) ∈ G.E` / `(v1, v2) ∈ G.L` clauses elaborate without further instance arguments, since `Finset` membership is itself decidable only when the element type has decidable equality."
}
