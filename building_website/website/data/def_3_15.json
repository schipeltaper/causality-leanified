{
  "ref": "def_3_15",
  "kind": "def",
  "section": "3.3",
  "title": "CollidersAndNon",
  "type": "definition",
  "status": {
    "formalized": "yes",
    "proven": "n/a",
    "solved": "yes"
  },
  "tex_block_html": "<p>Let $G=(J,V,E,L)$ be a CDMG and $\\pi$ a walk in $G$:\n  \\[ \\pi =\\lp  v_0 \\sus \\cdots \\sus v_n \\rp.  \\]\n  A node $v_k$, or more precisely, the position $k \\in \\lC 0,\\dots, n \\rC$,\n  on the walk $\\pi$ is called:</p>\n<ol class=\"enum\"><li>a <em>non-collider</em> on $\\pi$, if there is at most one arrowhead pointing towards $v_k$, i.e.\\ if it falls into one of the following cases:\n  \\[\\begin{array}{rccl}\n    \\text{ end-node: } & k \\in \\lC 0,n \\rC, \\\\\n    \\text{ left chain: } & v_{k-1} \\hut v_k \\hus v_{k+1}, \\\\\n    \\text{ right chain: } & v_{k-1} \\suh v_k \\tuh v_{k+1},\\\\\n    \\text{ fork: } & v_{k-1} \\hut v_k \\tuh v_{k+1};\n    \\end{array}\\]</li><li>a <em>collider</em> on $\\pi$, if it is of the form: \n             \\[v_{k-1} \\suh v_k \\hus v_{k+1},\\] \n             i.e.\\ if there are two arrowheads pointing towards $v_k$ on the walk $\\pi$.</li></ol>",
  "tex_statement": {
    "raw": "% Definition file for row def_3_15 -- \"CollidersAndNon\".\n% Auto-generated stub by scaffold/solve_chapter.py at row start. The\n% manager agent (or a worker dispatched by it) fills the body below with the\n% Lean-faithful definition statement(s). Stays close to the lecture notes.\n%\n% This file is a sibling of `main.tex` inside `<subsection>/tex/`. The\n% `subfiles` package lets it compile both standalone (resolving its\n% preamble via `main.tex`) and as part of `main.tex` via `\\subfile{...}`.\n\n\\documentclass[main]{subfiles}\n\n\\begin{document}\n% ref: def_3_15\n% title: CollidersAndNon\n\\def\\rowref{def\\_3\\_15}\n\\phantomsection\\label{def_3_15}\n\n\\begin{Def}[Colliders and non-colliders]\\label{def:collider_noncollider}\n    Let $G = (J, V, E, L)$ be a CDMG (def \\ref{def-cdmg}); throughout we use the notation of def \\ref{not-cdmg} and the edge classifications of def \\ref{def-edge-relations}. Let\n    \\[\n        \\pi = (v_0, a_0, v_1, a_1, \\dots, v_{n-1}, a_{n-1}, v_n)\n    \\]\n    be a walk in $G$ (def \\ref{def:walks}, item~i.) for some integer $n \\ge 0$, with $v_0, \\dots, v_n \\in J \\cup V$ and $a_0, \\dots, a_{n-1} \\in E \\cup L$ satisfying the walk constraint at every $k \\in \\{0, 1, \\dots, n - 1\\}$:\n    \\[\n        \\bigl(a_k = (v_k, v_{k+1}) \\;\\land\\; a_k \\in E \\cup L\\bigr)\n        \\;\\lor\\;\n        \\bigl(a_k = (v_{k+1}, v_k) \\;\\land\\; a_k \\in E\\bigr).\n    \\]\n\n    \\emph{Arrowhead count at a position.}\n    For a position $k \\in \\{0, 1, \\dots, n\\}$, the \\emph{arrowhead count at $v_k$ on $\\pi$}, denoted $\\mathrm{ah}_\\pi(k)$, is the cardinality\n    \\[\n        \\mathrm{ah}_\\pi(k)\n            := \\Bigl|\\Bigl\\{\\, i \\in \\{k - 1, k\\} \\,\\Bigm|\\, 0 \\le i \\le n - 1 \\,\\land\\, a_i \\text{ is an edge into } v_k \\text{ in } G \\,\\Bigr\\}\\Bigr|,\n    \\]\n    where ``$a_i$ is an edge into $v_k$ in $G$'' is in the sense of def \\ref{def-edge-relations}, item~ii.; spelled out, for any $i \\in \\{0, 1, \\dots, n - 1\\}$ writing $a_i = (e_1, e_2)$,\n    \\[\n        a_i \\text{ is an edge into } v_k\n        \\quad\\iff\\quad\n        \\bigl(a_i \\in E \\;\\land\\; e_2 = v_k\\bigr)\n            \\;\\lor\\;\n        \\bigl(a_i \\in L \\;\\land\\; (e_1 = v_k \\,\\lor\\, e_2 = v_k)\\bigr).\n    \\]\n    The index set defining $\\mathrm{ah}_\\pi(k)$ ranges only over the (at most two) walk-incident edges at position $k$, namely $a_{k-1}$ (when $k \\ge 1$) and $a_k$ (when $k \\le n - 1$); edges of $G$ that are not incident to $v_k$ along $\\pi$ are excluded by construction. At an end-position $k \\in \\{0, n\\}$ at most one such index is admissible, so $\\mathrm{ah}_\\pi(k) \\le 1$ automatically (and $\\mathrm{ah}_\\pi(0) = 0$ on the trivial walk $n = 0$, where neither index is admissible). At an interior position $1 \\le k \\le n - 1$ both indices are admissible, so $\\mathrm{ah}_\\pi(k) \\in \\{0, 1, 2\\}$. (The stored-pair test ``$a_i$ is an edge into $v_k$'' above is ambiguous in two corner configurations admitted by def \\ref{def-cdmg}: at a walk-incident directed self-loop $a_i \\in E$ with $v_i = v_{i+1}$, where its $E$-clause fires unconditionally at both adjacent positions $k \\in \\{i, i + 1\\}$; and at a walk-incident writing-mirror walk-step whose underlying stored pair sits simultaneously in $E$ and in $L$, where its $L$-clause fires unconditionally at both adjacent positions regardless of which channel the walk-step actually traverses. The canonical disambiguation at such positions is the side-aware reading of the addition tag \\texttt{[collider\\_side\\_aware\\_walkstep\\_predicates]}, spelled out in the Encoding note and Treatment of directed self-loops and writing-mirror walk-steps paragraphs below.)\n\n    \\emph{Encoding note: side-aware reading via a typed walk-step representation.}\n    At a walk-step $a_i$ whose endpoints are distinct ($v_i \\ne v_{i+1}$), the walk constraint of def \\ref{def:walks}, item~i.\\ is typically satisfied by a unique channel assignment: the underlying stored pair lives in exactly one of $E$, $L$, in exactly one orientation, and only one walk-constraint disjunct fires. But def \\ref{def-cdmg} admits \\emph{writing-mirror} pairs: the irreflexivity restriction applies to $L$ only, and no graph-theoretic mutual-exclusion is imposed between the directed channel $E$ and the bidirected channel $L$, so a single underlying vertex pair $\\{u, v\\}$ may simultaneously support a directed edge $(u, v) \\in E$ (or $(v, u) \\in E$) \\emph{and} a bidirected edge $s(u, v) \\in L$. At a walk-step traversing such a writing-mirror pair, more than one channel assignment to $a_i$ satisfies the walk constraint, and the literal stored-pair ``$a_i$ is an edge into $v_k$'' test of def \\ref{def-edge-relations}, item~ii.\\ fires its $L$-clause at both adjacent positions $k \\in \\{i, i+1\\}$ irrespective of which channel the walk-step actually traverses. The same kind of literal-test ambiguity arises at a walk-incident directed self-loop $a_i \\in E$ with $v_i = v_{i+1} = v$, where both walk-constraint disjuncts apply syntactically and the stored-pair test's $E$-clause fires unconditionally at both adjacent positions.\n\n    The side-aware reading introduced by the addition tag \\texttt{[collider\\_side\\_aware\\_walkstep\\_predicates]} resolves both corners by committing to the walk-step's recorded traversal channel as the \\emph{sole} signal for which channel contributes to the arrowhead test at this step. Each walk-step $a_i$ on $\\pi$ carries an explicit channel tag $c_i \\in \\{\\textsf{forward-}E, \\textsf{backward-}E, \\textsf{bidir-}L\\}$, recorded as part of $\\pi$ at any walk-step where the underlying vertex/edge sequence alone would leave the channel choice ambiguous -- writing-mirror walk-steps, and directed self-loops where both orientations $v_i \\to v_{i+1}$ and $v_{i+1} \\to v_i$ satisfy the walk constraint; at all other walk-steps the channel is forced by the walk's data. The per-step contribution of $a_i$ to $\\mathrm{ah}_\\pi(k)$ at each adjacent position $k \\in \\{i, i+1\\}$ is determined \\emph{solely} by $c_i$, never consulting whether the underlying stored pair also sits in the opposite channel of $G$:\n    \\begin{itemize}\n        \\item $c_i = \\textsf{forward-}E$ (underlying $(v_i, v_{i+1}) \\in E$, traversed forward $v_i \\to v_{i+1}$): contributes $1$ at $k = i + 1$ and $0$ at $k = i$;\n        \\item $c_i = \\textsf{backward-}E$ (underlying $(v_{i+1}, v_i) \\in E$, traversed backward $v_{i+1} \\to v_i$): contributes $1$ at $k = i$ and $0$ at $k = i + 1$;\n        \\item $c_i = \\textsf{bidir-}L$ (underlying $s(v_i, v_{i+1}) \\in L$): contributes $1$ at both adjacent positions $k \\in \\{i, i+1\\}$.\n    \\end{itemize}\n    A directed step ($c_i \\in \\{\\textsf{forward-}E, \\textsf{backward-}E\\}$) commits to its $E$-channel reading even when a coexisting $L$-edge $s(v_i, v_{i+1}) \\in L$ would, under the literal stored-pair test, additionally contribute an arrowhead at the otherwise-zero adjacent position (the writing-mirror case). Symmetrically, a bidirected step ($c_i = \\textsf{bidir-}L$) commits to its $L$-channel bidirected reading even when a coexisting $E$-edge is present in $G$. This refinement strictly overrides the literal stored-pair test of def \\ref{def-edge-relations}, item~ii.\\ at both corner kinds (writing-mirror walk-steps traversed via the $E$-channel, and directed self-loops), and is taken as the canonical reading of $\\mathrm{ah}_\\pi(k)$ used by the collider/non-collider classification below. On walks that traverse neither corner configuration, the side-aware and literal stored-pair readings coincide pointwise.\n\n    The Lean realisation of this definition implements the side-aware reading faithfully via a typed walk-step representation: each walk-step carries a constructor tag -- one of \\emph{forward-$E$}, \\emph{backward-$E$}, or \\emph{bidir-$L$} -- recording the traversal channel directly, alongside typed source/target end indices at the type level. The per-step contribution to $\\mathrm{ah}_\\pi(k)$ is read off the constructor tag and the type-level ends alone, with \\emph{no} consultation of the opposite channel of $G$: a forward-$E$ step contributes $1$ at the type-level target end and $0$ at the type-level source end; a backward-$E$ step mirrors, contributing $1$ at the type-level source end and $0$ at the type-level target end; a bidir-$L$ step contributes $1$ at both type-level ends. There is \\emph{no} additional opposite-channel $L$-disjunct on the directed branches and \\emph{no} additional $E$-disjunct on the bidirected branch -- the constructor tag alone carries the side-aware commitment, exactly realising the per-step contribution formula above. In particular, the strict deviation from the literal stored-pair test is preserved at writing-mirror walk-steps traversed via the $E$-channel as well as at directed self-loops.\n\n    \\emph{Classification.}\n    The position $k \\in \\{0, 1, \\dots, n\\}$ on the walk $\\pi$ -- or, by abuse, the node $v_k$ at position $k$ -- is called:\n    \\begin{enumerate}[label=\\roman*.)]\n        \\item a \\emph{non-collider on $\\pi$} iff $\\mathrm{ah}_\\pi(k) \\le 1$;\n        \\item a \\emph{collider on $\\pi$} iff $\\mathrm{ah}_\\pi(k) = 2$.\n    \\end{enumerate}\n    By construction these two classifications are mutually exclusive and jointly exhaustive over $\\{0, 1, \\dots, n\\}$: every position $k$ on $\\pi$ is exactly one of a non-collider or a collider on $\\pi$.\n\n    \\emph{Reconciliation with the source-block pattern writings.}\n    The source block characterises non-colliders by a list of four pattern-cases (end-node, left chain, right chain, fork) glossed as ``at most one arrowhead pointing towards $v_k$'', and colliders by a single pattern-case glossed as ``two arrowheads pointing towards $v_k$ on the walk $\\pi$''. Below we reconcile these visual writings with the count-based classification, taking the count-based classification (the LN's ``i.e.''\\ gloss) as canonical. The visual writings $v_{k-1} \\tuh v_k$, $v_{k-1} \\hut v_k$, $v_{k-1} \\huh v_k$, $v_{k-1} \\suh v_k$, $v_{k-1} \\hus v_k$ are unfolded per def \\ref{not-cdmg}, items~2--6.\n    \\begin{itemize}\n        \\item \\textbf{end-node} ($k \\in \\{0, n\\}$): $\\mathrm{ah}_\\pi(k) \\le 1$ holds automatically, hence the position is a non-collider on $\\pi$. (For the trivial walk $n = 0$, the unique position $k = 0 = n$ has $\\mathrm{ah}_\\pi(0) = 0$ and is a non-collider.)\n        \\item \\textbf{left chain} ($1 \\le k \\le n - 1$; LN writing $v_{k-1} \\hut v_k \\hus v_{k+1}$): the walk's $a_{k-1}$ is \\emph{not} an edge into $v_k$, while its $a_k$ \\emph{is} an edge into $v_k$. Spelled out via the walk constraint, $a_{k-1} = (v_k, v_{k-1}) \\in E$ (a directed edge with arrowhead at $v_{k-1}$ and tail at $v_k$), and either $a_k = (v_{k+1}, v_k) \\in E$ or $a_k = (v_k, v_{k+1}) \\in L$. Count: $0 + 1 = 1$; non-collider.\n        \\item \\textbf{right chain} ($1 \\le k \\le n - 1$; LN writing $v_{k-1} \\suh v_k \\tuh v_{k+1}$): the walk's $a_{k-1}$ \\emph{is} an edge into $v_k$, while its $a_k$ is \\emph{not} an edge into $v_k$. Spelled out, either $a_{k-1} = (v_{k-1}, v_k) \\in E$ or $a_{k-1} = (v_{k-1}, v_k) \\in L$, and $a_k = (v_k, v_{k+1}) \\in E$. Count: $1 + 0 = 1$; non-collider.\n        \\item \\textbf{fork} ($1 \\le k \\le n - 1$; LN writing $v_{k-1} \\hut v_k \\tuh v_{k+1}$): neither walk-incident edge is an edge into $v_k$. Spelled out, $a_{k-1} = (v_k, v_{k-1}) \\in E$ and $a_k = (v_k, v_{k+1}) \\in E$. Count: $0 + 0 = 0$; non-collider.\n        \\item \\textbf{collider} ($1 \\le k \\le n - 1$; LN writing $v_{k-1} \\suh v_k \\hus v_{k+1}$): both walk-incident edges $a_{k-1}, a_k$ are edges into $v_k$. Spelled out, $a_{k-1} \\in E \\cup L$ with right-component $v_k$ if directed (or either component $v_k$ if bidirected), and analogously for $a_k$ with left-component $v_k$ if directed (or either component $v_k$ if bidirected). Count: $1 + 1 = 2$; collider.\n    \\end{itemize}\n    The four non-collider patterns realise exactly the interior counts $\\{0, 1\\}$, and the collider pattern realises exactly the interior count $2$; together with the end-node case they exhaust the positions of $\\pi$. The pattern-cases consult \\emph{walk-incident} edges $a_{k-1}, a_k$ only -- arrowheads contributed by edges of $G$ that are not on $\\pi$ at $v_k$ are excluded from $\\mathrm{ah}_\\pi(k)$ by construction. (This fixes the scope of the LN's gloss ``at most one (resp.\\ two) arrowheads pointing towards $v_k$''.) For walks that traverse a directed self-loop $a_i \\in E$ with $v_i = v_{i+1} = v$ -- where the literal pattern writings would otherwise admit any of the four conjunctions above simultaneously -- or a writing-mirror walk-step whose underlying stored pair sits simultaneously in $E$ and in $L$ -- where the literal pattern writings admit both the directed and the bidirected channel interpretations -- the recorded walk-traversal channel of $a_i$ ($\\textsf{forward-}E$, $\\textsf{backward-}E$, or $\\textsf{bidir-}L$) selects exactly one pattern per adjacent position, per the side-aware reading of the addition tag \\texttt{[collider\\_side\\_aware\\_walkstep\\_predicates]}; see the Treatment of directed self-loops and writing-mirror walk-steps paragraph below for the per-position contribution.\n\n    \\emph{Treatment of directed self-loops and writing-mirror walk-steps.}\n    The literal stored-pair test ``$a_i$ is an edge into $v_k$'' of def \\ref{def-edge-relations}, item~ii.\\ is ambiguous in two corner configurations admitted by def \\ref{def-cdmg} but not excluded by the walk constraint of def \\ref{def:walks}, item~i.:\n    \\begin{itemize}\n        \\item \\textbf{Directed self-loop walk-steps.} A directed self-loop $(v, v) \\in E$ is admitted by def \\ref{def-cdmg} (the irreflexivity restriction applies to $L$, not to $E$). At a walk-incident self-loop edge $a_i = (v_i, v_{i+1}) = (v, v) \\in E$ with $v_i = v_{i+1} = v$, both walk-constraint disjuncts of def \\ref{def:walks}, item~i.\\ apply syntactically, and both the LN's literal pattern writings $v_{k-1} \\tuh v_k$ and $v_{k-1} \\hut v_k$ (def \\ref{not-cdmg}, items~2--3) and the stored-pair test collapse to the single condition $(v, v) \\in E$, firing unconditionally at both adjacent positions $k \\in \\{i, i + 1\\}$ for which $v_k = v$. The stored-pair count formula above would commit, at such a self-loop traversal, to contribution $1$ from $a_i$ at both adjacent positions, and a single interior position $k$ traversing the self-loop would simultaneously match the ``left chain'', ``right chain'', ``fork'', and ``collider'' pattern conjunctions of the Reconciliation paragraph above.\n        \\item \\textbf{Writing-mirror walk-steps.} At a non-self-loop walk-step ($v_i \\ne v_{i+1}$) whose underlying stored pair $(v_i, v_{i+1})$ (or $(v_{i+1}, v_i)$) is in $E$ \\emph{and} whose unordered version $s(v_i, v_{i+1})$ is in $L$ -- the writing-mirror configuration permitted by def \\ref{def-cdmg}, which does not impose $E \\cap L = \\emptyset$ -- the walk constraint of def \\ref{def:walks}, item~i.\\ is satisfied by more than one channel assignment to $a_i$, and the stored-pair test's $L$-clause fires unconditionally at both adjacent positions $k \\in \\{i, i + 1\\}$ regardless of which channel the walk-step actually traverses. The literal pattern writings of def \\ref{not-cdmg} similarly admit both the directed and the bidirected channel interpretations at such a walk-step. (The writing-mirror self-loop sibling -- $(v, v) \\in E$ and $s(v, v) \\in L$ simultaneously -- is structurally impossible by def \\ref{def-cdmg}'s \\texttt{hL\\_irrefl}, which forbids self-pair elements in $L$.)\n    \\end{itemize}\n\n    The canonical disambiguation in both cases is the \\emph{side-aware} reading introduced by the addition tag \\texttt{[collider\\_side\\_aware\\_walkstep\\_predicates]}, as spelled out in the Encoding note paragraph above: each walk-step $a_i$ on $\\pi$ carries an explicit traversal channel $c_i \\in \\{\\textsf{forward-}E, \\textsf{backward-}E, \\textsf{bidir-}L\\}$, with the choice recorded explicitly at any walk-step where the underlying vertex/edge sequence alone would leave the channel ambiguous, and the per-step contribution to $\\mathrm{ah}_\\pi(k)$ is read \\emph{solely} off $c_i$. At a directed self-loop $a_i \\in E$, the side-aware reading commits the arrowhead contribution to a single adjacent position -- $k = i + 1$ when $c_i = \\textsf{forward-}E$, $k = i$ when $c_i = \\textsf{backward-}E$ -- and contributes $0$ at the other adjacent position; the $\\textsf{bidir-}L$ channel cannot fire at a self-loop because def \\ref{def-cdmg}'s irreflexivity restriction on $L$ rules out $s(v, v) \\in L$ outright, so the strict side-aware refinement at self-loops is preserved by construction. At a writing-mirror walk-step traversed via $c_i \\in \\{\\textsf{forward-}E, \\textsf{backward-}E\\}$, the side-aware reading ignores the coexisting $L$-edge entirely, contributing only at the directed channel's arrowhead-receiving end (per the per-step contribution formula in the Encoding note); analogously, a writing-mirror walk-step traversed via $c_i = \\textsf{bidir-}L$ commits to the bidirected contribution (arrowheads at both adjacent positions) and ignores the coexisting $E$-edge. This explicitly replaces, at both corner kinds, the literal stored-pair test's reading of $\\mathrm{ah}_\\pi(k)$; on walks that traverse neither a directed self-loop nor a writing-mirror walk-step the side-aware and stored-pair readings coincide pointwise, so no other part of the definition is affected by the refinement. The recorded walk-traversal channel also selects exactly one of the LN's literal $\\tuh / \\hut / \\huh$ pattern conjunctions per walk-step at each adjacent position, resolving the apparent multi-pattern overlap at both corner kinds.\n\\end{Def}\n\n\\end{document}\n",
    "html": "<p>Let $G = (J, V, E, L)$ be a CDMG (def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>); throughout we use the notation of def <a href=\"#def_3_2\" class=\"refrow\"><code>not-cdmg</code></a> and the edge classifications of def <a href=\"#def_3_3\" class=\"refrow\"><code>def-edge-relations</code></a>. Let\n    \\[\n        \\pi = (v_0, a_0, v_1, a_1, \\dots, v_{n-1}, a_{n-1}, v_n)\n    \\]\n    be a walk in $G$ (def <a href=\"#def_3_4\" class=\"refrow\"><code>def:walks</code></a>, item&nbsp;i.) for some integer $n \\ge 0$, with $v_0, \\dots, v_n \\in J \\cup V$ and $a_0, \\dots, a_{n-1} \\in E \\cup L$ satisfying the walk constraint at every $k \\in \\{0, 1, \\dots, n - 1\\}$:\n    \\[\n        \\bigl(a_k = (v_k, v_{k+1}) \\;\\land\\; a_k \\in E \\cup L\\bigr)\n        \\;\\lor\\;\n        \\bigl(a_k = (v_{k+1}, v_k) \\;\\land\\; a_k \\in E\\bigr).\n    \\]</p>\n<p><em>Arrowhead count at a position.</em>\n    For a position $k \\in \\{0, 1, \\dots, n\\}$, the <em>arrowhead count at $v_k$ on $\\pi$</em>, denoted $\\mathrm{ah}_\\pi(k)$, is the cardinality\n    \\[\n        \\mathrm{ah}_\\pi(k)\n            := \\Bigl|\\Bigl\\{\\, i \\in \\{k - 1, k\\} \\,\\Bigm|\\, 0 \\le i \\le n - 1 \\,\\land\\, a_i \\text{ is an edge into } v_k \\text{ in } G \\,\\Bigr\\}\\Bigr|,\n    \\]\n    where “$a_i$ is an edge into $v_k$ in $G$” is in the sense of def <a href=\"#def_3_3\" class=\"refrow\"><code>def-edge-relations</code></a>, item&nbsp;ii.; spelled out, for any $i \\in \\{0, 1, \\dots, n - 1\\}$ writing $a_i = (e_1, e_2)$,\n    \\[\n        a_i \\text{ is an edge into } v_k\n        \\quad\\iff\\quad\n        \\bigl(a_i \\in E \\;\\land\\; e_2 = v_k\\bigr)\n            \\;\\lor\\;\n        \\bigl(a_i \\in L \\;\\land\\; (e_1 = v_k \\,\\lor\\, e_2 = v_k)\\bigr).\n    \\]\n    The index set defining $\\mathrm{ah}_\\pi(k)$ ranges only over the (at most two) walk-incident edges at position $k$, namely $a_{k-1}$ (when $k \\ge 1$) and $a_k$ (when $k \\le n - 1$); edges of $G$ that are not incident to $v_k$ along $\\pi$ are excluded by construction. At an end-position $k \\in \\{0, n\\}$ at most one such index is admissible, so $\\mathrm{ah}_\\pi(k) \\le 1$ automatically (and $\\mathrm{ah}_\\pi(0) = 0$ on the trivial walk $n = 0$, where neither index is admissible). At an interior position $1 \\le k \\le n - 1$ both indices are admissible, so $\\mathrm{ah}_\\pi(k) \\in \\{0, 1, 2\\}$. (The stored-pair test “$a_i$ is an edge into $v_k$” above is ambiguous in two corner configurations admitted by def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>: at a walk-incident directed self-loop $a_i \\in E$ with $v_i = v_{i+1}$, where its $E$-clause fires unconditionally at both adjacent positions $k \\in \\{i, i + 1\\}$; and at a walk-incident writing-mirror walk-step whose underlying stored pair sits simultaneously in $E$ and in $L$, where its $L$-clause fires unconditionally at both adjacent positions regardless of which channel the walk-step actually traverses. The canonical disambiguation at such positions is the side-aware reading of the addition tag <code>[collider_side_aware_walkstep_predicates]</code>, spelled out in the Encoding note and Treatment of directed self-loops and writing-mirror walk-steps paragraphs below.)</p>\n<p><em>Encoding note: side-aware reading via a typed walk-step representation.</em>\n    At a walk-step $a_i$ whose endpoints are distinct ($v_i \\ne v_{i+1}$), the walk constraint of def <a href=\"#def_3_4\" class=\"refrow\"><code>def:walks</code></a>, item&nbsp;i.\\ is typically satisfied by a unique channel assignment: the underlying stored pair lives in exactly one of $E$, $L$, in exactly one orientation, and only one walk-constraint disjunct fires. But def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a> admits <em>writing-mirror</em> pairs: the irreflexivity restriction applies to $L$ only, and no graph-theoretic mutual-exclusion is imposed between the directed channel $E$ and the bidirected channel $L$, so a single underlying vertex pair $\\{u, v\\}$ may simultaneously support a directed edge $(u, v) \\in E$ (or $(v, u) \\in E$) <em>and</em> a bidirected edge $s(u, v) \\in L$. At a walk-step traversing such a writing-mirror pair, more than one channel assignment to $a_i$ satisfies the walk constraint, and the literal stored-pair “$a_i$ is an edge into $v_k$” test of def <a href=\"#def_3_3\" class=\"refrow\"><code>def-edge-relations</code></a>, item&nbsp;ii.\\ fires its $L$-clause at both adjacent positions $k \\in \\{i, i+1\\}$ irrespective of which channel the walk-step actually traverses. The same kind of literal-test ambiguity arises at a walk-incident directed self-loop $a_i \\in E$ with $v_i = v_{i+1} = v$, where both walk-constraint disjuncts apply syntactically and the stored-pair test's $E$-clause fires unconditionally at both adjacent positions.</p>\n<p>The side-aware reading introduced by the addition tag <code>[collider_side_aware_walkstep_predicates]</code> resolves both corners by committing to the walk-step's recorded traversal channel as the <em>sole</em> signal for which channel contributes to the arrowhead test at this step. Each walk-step $a_i$ on $\\pi$ carries an explicit channel tag $c_i \\in \\{\\textsf{forward-}E, \\textsf{backward-}E, \\textsf{bidir-}L\\}$, recorded as part of $\\pi$ at any walk-step where the underlying vertex/edge sequence alone would leave the channel choice ambiguous – writing-mirror walk-steps, and directed self-loops where both orientations $v_i \\to v_{i+1}$ and $v_{i+1} \\to v_i$ satisfy the walk constraint; at all other walk-steps the channel is forced by the walk's data. The per-step contribution of $a_i$ to $\\mathrm{ah}_\\pi(k)$ at each adjacent position $k \\in \\{i, i+1\\}$ is determined <em>solely</em> by $c_i$, never consulting whether the underlying stored pair also sits in the opposite channel of $G$:</p>\n<ul><li>$c_i = \\textsf{forward-}E$ (underlying $(v_i, v_{i+1}) \\in E$, traversed forward $v_i \\to v_{i+1}$): contributes $1$ at $k = i + 1$ and $0$ at $k = i$;</li><li>$c_i = \\textsf{backward-}E$ (underlying $(v_{i+1}, v_i) \\in E$, traversed backward $v_{i+1} \\to v_i$): contributes $1$ at $k = i$ and $0$ at $k = i + 1$;</li><li>$c_i = \\textsf{bidir-}L$ (underlying $s(v_i, v_{i+1}) \\in L$): contributes $1$ at both adjacent positions $k \\in \\{i, i+1\\}$.</li></ul>\n<p>A directed step ($c_i \\in \\{\\textsf{forward-}E, \\textsf{backward-}E\\}$) commits to its $E$-channel reading even when a coexisting $L$-edge $s(v_i, v_{i+1}) \\in L$ would, under the literal stored-pair test, additionally contribute an arrowhead at the otherwise-zero adjacent position (the writing-mirror case). Symmetrically, a bidirected step ($c_i = \\textsf{bidir-}L$) commits to its $L$-channel bidirected reading even when a coexisting $E$-edge is present in $G$. This refinement strictly overrides the literal stored-pair test of def <a href=\"#def_3_3\" class=\"refrow\"><code>def-edge-relations</code></a>, item&nbsp;ii.\\ at both corner kinds (writing-mirror walk-steps traversed via the $E$-channel, and directed self-loops), and is taken as the canonical reading of $\\mathrm{ah}_\\pi(k)$ used by the collider/non-collider classification below. On walks that traverse neither corner configuration, the side-aware and literal stored-pair readings coincide pointwise.</p>\n<p>The Lean realisation of this definition implements the side-aware reading faithfully via a typed walk-step representation: each walk-step carries a constructor tag – one of <em>forward-$E$</em>, <em>backward-$E$</em>, or <em>bidir-$L$</em> – recording the traversal channel directly, alongside typed source/target end indices at the type level. The per-step contribution to $\\mathrm{ah}_\\pi(k)$ is read off the constructor tag and the type-level ends alone, with <em>no</em> consultation of the opposite channel of $G$: a forward-$E$ step contributes $1$ at the type-level target end and $0$ at the type-level source end; a backward-$E$ step mirrors, contributing $1$ at the type-level source end and $0$ at the type-level target end; a bidir-$L$ step contributes $1$ at both type-level ends. There is <em>no</em> additional opposite-channel $L$-disjunct on the directed branches and <em>no</em> additional $E$-disjunct on the bidirected branch – the constructor tag alone carries the side-aware commitment, exactly realising the per-step contribution formula above. In particular, the strict deviation from the literal stored-pair test is preserved at writing-mirror walk-steps traversed via the $E$-channel as well as at directed self-loops.</p>\n<p><em>Classification.</em>\n    The position $k \\in \\{0, 1, \\dots, n\\}$ on the walk $\\pi$ – or, by abuse, the node $v_k$ at position $k$ – is called:</p>\n<ol class=\"roman-paren\"><li>a <em>non-collider on $\\pi$</em> iff $\\mathrm{ah}_\\pi(k) \\le 1$;</li><li>a <em>collider on $\\pi$</em> iff $\\mathrm{ah}_\\pi(k) = 2$.</li></ol>\n<p>By construction these two classifications are mutually exclusive and jointly exhaustive over $\\{0, 1, \\dots, n\\}$: every position $k$ on $\\pi$ is exactly one of a non-collider or a collider on $\\pi$.</p>\n<p><em>Reconciliation with the source-block pattern writings.</em>\n    The source block characterises non-colliders by a list of four pattern-cases (end-node, left chain, right chain, fork) glossed as “at most one arrowhead pointing towards $v_k$”, and colliders by a single pattern-case glossed as “two arrowheads pointing towards $v_k$ on the walk $\\pi$”. Below we reconcile these visual writings with the count-based classification, taking the count-based classification (the LN's “i.e.”\\ gloss) as canonical. The visual writings $v_{k-1} \\tuh v_k$, $v_{k-1} \\hut v_k$, $v_{k-1} \\huh v_k$, $v_{k-1} \\suh v_k$, $v_{k-1} \\hus v_k$ are unfolded per def <a href=\"#def_3_2\" class=\"refrow\"><code>not-cdmg</code></a>, items&nbsp;2–6.</p>\n<ul><li><strong>end-node</strong> ($k \\in \\{0, n\\}$): $\\mathrm{ah}_\\pi(k) \\le 1$ holds automatically, hence the position is a non-collider on $\\pi$. (For the trivial walk $n = 0$, the unique position $k = 0 = n$ has $\\mathrm{ah}_\\pi(0) = 0$ and is a non-collider.)</li><li><strong>left chain</strong> ($1 \\le k \\le n - 1$; LN writing $v_{k-1} \\hut v_k \\hus v_{k+1}$): the walk's $a_{k-1}$ is <em>not</em> an edge into $v_k$, while its $a_k$ <em>is</em> an edge into $v_k$. Spelled out via the walk constraint, $a_{k-1} = (v_k, v_{k-1}) \\in E$ (a directed edge with arrowhead at $v_{k-1}$ and tail at $v_k$), and either $a_k = (v_{k+1}, v_k) \\in E$ or $a_k = (v_k, v_{k+1}) \\in L$. Count: $0 + 1 = 1$; non-collider.</li><li><strong>right chain</strong> ($1 \\le k \\le n - 1$; LN writing $v_{k-1} \\suh v_k \\tuh v_{k+1}$): the walk's $a_{k-1}$ <em>is</em> an edge into $v_k$, while its $a_k$ is <em>not</em> an edge into $v_k$. Spelled out, either $a_{k-1} = (v_{k-1}, v_k) \\in E$ or $a_{k-1} = (v_{k-1}, v_k) \\in L$, and $a_k = (v_k, v_{k+1}) \\in E$. Count: $1 + 0 = 1$; non-collider.</li><li><strong>fork</strong> ($1 \\le k \\le n - 1$; LN writing $v_{k-1} \\hut v_k \\tuh v_{k+1}$): neither walk-incident edge is an edge into $v_k$. Spelled out, $a_{k-1} = (v_k, v_{k-1}) \\in E$ and $a_k = (v_k, v_{k+1}) \\in E$. Count: $0 + 0 = 0$; non-collider.</li><li><strong>collider</strong> ($1 \\le k \\le n - 1$; LN writing $v_{k-1} \\suh v_k \\hus v_{k+1}$): both walk-incident edges $a_{k-1}, a_k$ are edges into $v_k$. Spelled out, $a_{k-1} \\in E \\cup L$ with right-component $v_k$ if directed (or either component $v_k$ if bidirected), and analogously for $a_k$ with left-component $v_k$ if directed (or either component $v_k$ if bidirected). Count: $1 + 1 = 2$; collider.</li></ul>\n<p>The four non-collider patterns realise exactly the interior counts $\\{0, 1\\}$, and the collider pattern realises exactly the interior count $2$; together with the end-node case they exhaust the positions of $\\pi$. The pattern-cases consult <em>walk-incident</em> edges $a_{k-1}, a_k$ only – arrowheads contributed by edges of $G$ that are not on $\\pi$ at $v_k$ are excluded from $\\mathrm{ah}_\\pi(k)$ by construction. (This fixes the scope of the LN's gloss “at most one (resp.\\ two) arrowheads pointing towards $v_k$”.) For walks that traverse a directed self-loop $a_i \\in E$ with $v_i = v_{i+1} = v$ – where the literal pattern writings would otherwise admit any of the four conjunctions above simultaneously – or a writing-mirror walk-step whose underlying stored pair sits simultaneously in $E$ and in $L$ – where the literal pattern writings admit both the directed and the bidirected channel interpretations – the recorded walk-traversal channel of $a_i$ ($\\textsf{forward-}E$, $\\textsf{backward-}E$, or $\\textsf{bidir-}L$) selects exactly one pattern per adjacent position, per the side-aware reading of the addition tag <code>[collider_side_aware_walkstep_predicates]</code>; see the Treatment of directed self-loops and writing-mirror walk-steps paragraph below for the per-position contribution.</p>\n<p><em>Treatment of directed self-loops and writing-mirror walk-steps.</em>\n    The literal stored-pair test “$a_i$ is an edge into $v_k$” of def <a href=\"#def_3_3\" class=\"refrow\"><code>def-edge-relations</code></a>, item&nbsp;ii.\\ is ambiguous in two corner configurations admitted by def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a> but not excluded by the walk constraint of def <a href=\"#def_3_4\" class=\"refrow\"><code>def:walks</code></a>, item&nbsp;i.:</p>\n<ul><li><strong>Directed self-loop walk-steps.</strong> A directed self-loop $(v, v) \\in E$ is admitted by def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a> (the irreflexivity restriction applies to $L$, not to $E$). At a walk-incident self-loop edge $a_i = (v_i, v_{i+1}) = (v, v) \\in E$ with $v_i = v_{i+1} = v$, both walk-constraint disjuncts of def <a href=\"#def_3_4\" class=\"refrow\"><code>def:walks</code></a>, item&nbsp;i.\\ apply syntactically, and both the LN's literal pattern writings $v_{k-1} \\tuh v_k$ and $v_{k-1} \\hut v_k$ (def <a href=\"#def_3_2\" class=\"refrow\"><code>not-cdmg</code></a>, items&nbsp;2–3) and the stored-pair test collapse to the single condition $(v, v) \\in E$, firing unconditionally at both adjacent positions $k \\in \\{i, i + 1\\}$ for which $v_k = v$. The stored-pair count formula above would commit, at such a self-loop traversal, to contribution $1$ from $a_i$ at both adjacent positions, and a single interior position $k$ traversing the self-loop would simultaneously match the “left chain”, “right chain”, “fork”, and “collider” pattern conjunctions of the Reconciliation paragraph above.</li><li><strong>Writing-mirror walk-steps.</strong> At a non-self-loop walk-step ($v_i \\ne v_{i+1}$) whose underlying stored pair $(v_i, v_{i+1})$ (or $(v_{i+1}, v_i)$) is in $E$ <em>and</em> whose unordered version $s(v_i, v_{i+1})$ is in $L$ – the writing-mirror configuration permitted by def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>, which does not impose $E \\cap L = \\emptyset$ – the walk constraint of def <a href=\"#def_3_4\" class=\"refrow\"><code>def:walks</code></a>, item&nbsp;i.\\ is satisfied by more than one channel assignment to $a_i$, and the stored-pair test's $L$-clause fires unconditionally at both adjacent positions $k \\in \\{i, i + 1\\}$ regardless of which channel the walk-step actually traverses. The literal pattern writings of def <a href=\"#def_3_2\" class=\"refrow\"><code>not-cdmg</code></a> similarly admit both the directed and the bidirected channel interpretations at such a walk-step. (The writing-mirror self-loop sibling – $(v, v) \\in E$ and $s(v, v) \\in L$ simultaneously – is structurally impossible by def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>'s <code>hL_irrefl</code>, which forbids self-pair elements in $L$.)</li></ul>\n<p>The canonical disambiguation in both cases is the <em>side-aware</em> reading introduced by the addition tag <code>[collider_side_aware_walkstep_predicates]</code>, as spelled out in the Encoding note paragraph above: each walk-step $a_i$ on $\\pi$ carries an explicit traversal channel $c_i \\in \\{\\textsf{forward-}E, \\textsf{backward-}E, \\textsf{bidir-}L\\}$, with the choice recorded explicitly at any walk-step where the underlying vertex/edge sequence alone would leave the channel ambiguous, and the per-step contribution to $\\mathrm{ah}_\\pi(k)$ is read <em>solely</em> off $c_i$. At a directed self-loop $a_i \\in E$, the side-aware reading commits the arrowhead contribution to a single adjacent position – $k = i + 1$ when $c_i = \\textsf{forward-}E$, $k = i$ when $c_i = \\textsf{backward-}E$ – and contributes $0$ at the other adjacent position; the $\\textsf{bidir-}L$ channel cannot fire at a self-loop because def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>'s irreflexivity restriction on $L$ rules out $s(v, v) \\in L$ outright, so the strict side-aware refinement at self-loops is preserved by construction. At a writing-mirror walk-step traversed via $c_i \\in \\{\\textsf{forward-}E, \\textsf{backward-}E\\}$, the side-aware reading ignores the coexisting $L$-edge entirely, contributing only at the directed channel's arrowhead-receiving end (per the per-step contribution formula in the Encoding note); analogously, a writing-mirror walk-step traversed via $c_i = \\textsf{bidir-}L$ commits to the bidirected contribution (arrowheads at both adjacent positions) and ignores the coexisting $E$-edge. This explicitly replaces, at both corner kinds, the literal stored-pair test's reading of $\\mathrm{ah}_\\pi(k)$; on walks that traverse neither a directed self-loop nor a writing-mirror walk-step the side-aware and stored-pair readings coincide pointwise, so no other part of the definition is affected by the refinement. The recorded walk-traversal channel also selects exactly one of the LN's literal $\\tuh / \\hut / \\huh$ pattern conjunctions per walk-step at each adjacent position, resolving the apparent multi-pattern overlap at both corner kinds.</p>",
    "env": "Def",
    "env_title": "Colliders and non-colliders",
    "source_path": "leanification/Chapter3_GraphTheory/Section3_3/tex/def_3_15_CollidersAndNon.tex"
  },
  "tex_proof": null,
  "lean_blocks": [
    {
      "kind": "helper",
      "code": "variable {Node : Type*} [DecidableEq Node]",
      "explanation": "This is a `variable` declaration: Lean auto-binds `Node : Type*` and the instance `[DecidableEq Node]` as implicit arguments to every subsequent declaration in the surrounding `CDMG` namespace, sparing us from rewriting the same parameters on `IsCollider` and `IsNonCollider`. The `Type*` notation makes `Node` universe-polymorphic, so callers can instantiate it at any universe level. The `DecidableEq Node` assumption is load-bearing for the ambient `CDMG Node` carrier: the structure stores `J, V, E, L` as `Finset`-valued fields, and `Finset` needs decidable equality on the element type for membership, union, and filter. Stronger structure on `Node` (e.g.\\ `Fintype`, `LinearOrder`) is not required here and is deferred to downstream consumers that actually use it. The same `variable` line wraps every prior chapter-3 file (`CDMG.lean`, `CDMGNotation.lean`, `EdgeRelations.lean`, `Walks.lean`), keeping the section-3.3 signatures continuous with the section-3.1 chapter convention.",
      "code_annotated": "-- Auto-binds `Node` + `DecidableEq Node` as implicits; decidability is required by every `Finset` field of `CDMG Node`.\nvariable {Node : Type*} [DecidableEq Node]"
    },
    {
      "kind": "helper",
      "code": "variable {G : CDMG Node}",
      "explanation": "This second `variable` line sits inside `namespace Walk` and auto-binds the ambient CDMG `G : CDMG Node` as an implicit argument to every subsequent declaration in the namespace. It is the Lean encoding of the LN's once-at-the-top quantifier ``let $G = (J, V, E, L)$ be a CDMG'', threaded through `IsCollider` and `IsNonCollider` without each signature having to repeat `{G : CDMG Node}` explicitly. The binder is implicit (`{G}` rather than `(G)`) because downstream consumers always reach into `G` via dot-notation on a walk — `p.IsCollider k` rather than `Walk.IsCollider G p k` — so the CDMG is recovered from the walk's type `Walk G u v` by unification at the use site. Pairing this with the first helper, the chapter's standard prelude is in place: `Node` provides the abstract vertex carrier with decidable equality, and `G` fixes the ambient graph against which positions on walks are classified.",
      "code_annotated": "-- Namespace-wide implicit `G`; recovered by unification from each walk's `Walk G u v` type, no explicit binder on the defs.\nvariable {G : CDMG Node}"
    },
    {
      "kind": "helper",
      "code": "def IsInto : ∀ {u v : Node}, WalkStep G u v → Node → Prop\n  | u, v, .forwardE _,  w => w = v ∨ (s(u, v) ∈ G.L ∧ (w = u ∨ w = v))\n  | u, v, .backwardE _, w => w = u ∨ (s(u, v) ∈ G.L ∧ (w = u ∨ w = v))\n  | u, v, .bidir _,     w => w = u ∨ w = v",
      "explanation": "`WalkStep.IsInto s w` is the typed-WalkStep transcription of `def_3_3` item ii's ``edge into a node'' primitive: it asks whether the step `s : WalkStep G u v` places an arrowhead at the node `w` when traversed. A `WalkStep` carries its channel as a constructor tag (`.forwardE`, `.backwardE`, `.bidir`) and its endpoints as Type-level indices `u v`, so the per-step classification can be read off by pattern-matching rather than through an `(a : Node × Node)`-style membership-disjunction. The naming follows the LN's ``edge into $v_k$'' phrasing: `IsInto vk` reads ``has its arrowhead at `vk`''. Three subtleties are absorbed here: (1) tests use node-equality on type indices, so a directed self-loop with `u = v` correctly fires `IsInto v = True`; (2) the writing-mirror union disjunct `s(u, v) ∈ G.L ∧ …` on `.forwardE` / `.backwardE` preserves constructor-choice invariance when the stored pair simultaneously lives in `G.L`; (3) `.bidir` needs no symmetric E-disjunct because its L-clause already places arrowheads at both endpoints. The sole consumer is `Walk.IsCollider` below.",
      "code_annotated": "-- `IsInto s w`: does WalkStep `s` place an arrowhead at node `w`? Per-channel pattern match.\ndef IsInto : ∀ {u v : Node}, WalkStep G u v → Node → Prop\n  -- Forward directed edge `u → v`: arrowhead at target `v`; writing-mirror disjunct re-injects L-channel if pair also in `G.L`.\n  | u, v, .forwardE _,  w => w = v ∨ (s(u, v) ∈ G.L ∧ (w = u ∨ w = v))\n  -- Backward directed edge `v → u`: arrowhead at source-index `u` (target of underlying directed edge); same writing-mirror disjunct.\n  | u, v, .backwardE _, w => w = u ∨ (s(u, v) ∈ G.L ∧ (w = u ∨ w = v))\n  -- Bidirected edge `s(u, v) ∈ G.L`: arrowheads at BOTH endpoints; no extra E-disjunct needed (L-clause dominates).\n  | u, v, .bidir _,     w => w = u ∨ w = v"
    },
    {
      "kind": "helper",
      "code": "def HeadAtTarget : ∀ {u v : Node}, WalkStep G u v → Prop\n  | _, _, .forwardE _  => True\n  | _, _, .backwardE _ => False\n  | _, _, .bidir _     => True",
      "explanation": "`WalkStep.HeadAtTarget s` is a side-aware predicate that asks: does the typed walk-step `s : WalkStep G u v` place an arrowhead at its walk-traversal target end `v`? The body is a pure constructor-tag pattern with no node argument and no `G.L`-membership consultation. `.forwardE _` carries a witness $(u, v) \\in G.E$ — the directed edge runs $u \\to v$, so the target `v` is the arrowhead-receiving end (`True`). `.backwardE _` carries $(v, u) \\in G.E$ — the edge runs $v \\to u$, so the walk-traversal target `v` is the tail end (`False`). `.bidir _` carries $s(u, v) \\in G.L$ — bidirected semantics places arrowheads at both endpoints (`True`). The constructor tag is the SOLE signal: a directed step commits to its $E$-channel reading even at a writing-mirror pair where $s(u, v) \\in G.L$ also holds, and `.bidir` commits to the bidirected reading even when a coexisting $E$-edge sits on the same vertex pair. Paired with `HeadAtSource`, this realises the per-step arrowhead-contribution formula at the two adjacent walk positions of `s`.",
      "code_annotated": "-- Side-aware: does walk-step `s` place an arrowhead at its walk-traversal target `v`? Pure constructor-tag pattern.\ndef HeadAtTarget : ∀ {u v : Node}, WalkStep G u v → Prop\n  -- Forward directed edge `u → v`: arrowhead at target `v`.\n  | _, _, .forwardE _  => True\n  -- Backward directed edge `v → u`: tail at walk-traversal target `v`; no opposite-channel L-disjunct.\n  | _, _, .backwardE _ => False\n  -- Bidirected edge `s(u, v) ∈ G.L`: arrowheads at both endpoints.\n  | _, _, .bidir _     => True"
    },
    {
      "kind": "helper",
      "code": "def HeadAtSource : ∀ {u v : Node}, WalkStep G u v → Prop\n  | _, _, .forwardE _  => False\n  | _, _, .backwardE _ => True\n  | _, _, .bidir _     => True",
      "explanation": "`WalkStep.HeadAtSource s` is the mirror of `HeadAtTarget`: it asks whether the walk-step `s : WalkStep G u v` places an arrowhead at its walk-traversal source end `u`. The body is a pure constructor-tag pattern with no node argument and no `G.L`-consultation. `.forwardE _` encodes $(u, v) \\in G.E$ running $u \\to v$, so the source `u` is the tail (`False`); `.backwardE _` encodes $(v, u) \\in G.E$ running $v \\to u$, so the source `u` is the arrowhead-receiving end (`True`); `.bidir _` encodes $s(u, v) \\in G.L$, contributing arrowheads at both endpoints (`True`). The constructor tag carries the side-aware commitment alone — a directed step ignores any coexisting bidirected edge $s(u, v) \\in G.L$ even at writing-mirror pairs. At a directed self-loop encoded as `.forwardE _ : WalkStep G v v`, the source-side predicate evaluates to `False` at the loop vertex, distinguishing tail from head precisely where node-equality on type indices cannot. Together with `HeadAtTarget`, this discharges the per-step contribution at adjacent walk positions consumed by `Walk.IsCollider`.",
      "code_annotated": "-- Side-aware mirror: does walk-step `s` place an arrowhead at its walk-traversal source `u`? Pure constructor-tag pattern.\ndef HeadAtSource : ∀ {u v : Node}, WalkStep G u v → Prop\n  -- Forward directed edge `u → v`: tail at source `u`; no opposite-channel L-disjunct.\n  | _, _, .forwardE _  => False\n  -- Backward directed edge `v → u`: arrowhead at source-index `u` (target of underlying directed edge).\n  | _, _, .backwardE _ => True\n  -- Bidirected edge `s(u, v) ∈ G.L`: arrowheads at both endpoints.\n  | _, _, .bidir _     => True"
    },
    {
      "kind": "helper",
      "code": "variable {G : CDMG Node}",
      "explanation": "This second `variable` line sits inside `namespace Walk` and auto-binds the ambient CDMG `G : CDMG Node` as an implicit argument to every subsequent declaration in the namespace. It is the Lean encoding of the LN's once-at-the-top quantifier ``let $G = (J, V, E, L)$ be a CDMG'', threaded through `IsCollider` and `IsNonCollider` without each signature having to repeat `{G : CDMG Node}` explicitly. The binder is implicit (`{G}` rather than `(G)`) because downstream consumers always reach into `G` via dot-notation on a walk — `p.IsCollider k` rather than `Walk.IsCollider G p k` — so the CDMG is recovered from the walk's type `Walk G u v` by unification at the use site. Pairing this with the first helper, the chapter's standard prelude is in place: `Node` provides the abstract vertex carrier with decidable equality, and `G` fixes the ambient graph against which positions on walks are classified.",
      "code_annotated": "-- Namespace-wide implicit `G`; recovered by unification from each walk's `Walk G u v` type, no explicit binder on the defs.\nvariable {G : CDMG Node}"
    },
    {
      "kind": "main",
      "code": "def IsCollider : ∀ {u v : Node}, Walk G u v → ℕ → Prop\n  | _, _, .nil _ _, _ => False\n  | _, _, .cons _ _ (.nil _ _), _ => False\n  | _, _, .cons _ _ (.cons _ _ _), 0 => False\n  | _, _, .cons _ s₀ (.cons _ s₁ _), 1 =>\n      s₀.HeadAtTarget ∧ s₁.HeadAtSource\n  | _, _, .cons _ _ (p@(.cons _ _ _)), k + 2 =>\n      p.IsCollider (k + 1)",
      "explanation": "`Walk.IsCollider p k` is the position-level collider predicate: it holds iff position `k` on the walk `p` has side-aware arrowhead count $\\mathrm{ah}_\\pi(k) = 2$ — both walk-incident steps `s_{k-1}` and `s_k` contribute arrowheads at `v_k`. The five-clause structural recursion on `Walk × ℕ` encodes the LN's classification: the trivial walk `.nil _ _` and the length-1 walk `.cons _ _ (.nil _ _)` carry no interior position and fall through to `False`; the `k = 0` end-position branch on length-$\\ge 2$ walks returns `False`; the `k = 1` branch is the dedicated arrowhead test on the head pair, `s₀.HeadAtTarget ∧ s₁.HeadAtSource`, since `s₀ : WalkStep G _ v_k` has its type-level target at `v_k` and `s₁ : WalkStep G v_k _` has its type-level source at `v_k`; the `k + 2` branch descends one cons-cell into the tail with index shift `k + 2 → k + 1`. Pattern-match exhaustiveness handles out-of-range `k > p.length` by falling through to `False` automatically. The middle-vertex slot is a wildcard `_` because the WalkStep indices already pin which end of each step sits at `v_k`. Side-awareness, not node-equality, prevents a directed self-loop at the right slot of an `a \\to b \\to b` walk from being spuriously classified as a collider at `b`.",
      "code_annotated": "-- Position-level collider: side-aware `ah_π(k) = 2` test, structural recursion on `Walk × ℕ`.\ndef IsCollider : ∀ {u v : Node}, Walk G u v → ℕ → Prop\n  -- Trivial walk `n = 0`: no positions to classify; every `k` is non-collider.\n  | _, _, .nil _ _, _ => False\n  -- Length-1 walk `n = 1`: only end-positions exist; every `k` is non-collider.\n  | _, _, .cons _ _ (.nil _ _), _ => False\n  -- End-position `k = 0` on a length-≥2 walk: walk start, automatically non-collider.\n  | _, _, .cons _ _ (.cons _ _ _), 0 => False\n  -- Interior position `k = 1`: head-pair test using side-aware predicates on `s₀`, `s₁`.\n  | _, _, .cons _ s₀ (.cons _ s₁ _), 1 =>\n      -- `s₀ : WalkStep G _ v_k` target at `v_k`; `s₁ : WalkStep G v_k _` source at `v_k`; no node binder needed.\n      s₀.HeadAtTarget ∧ s₁.HeadAtSource\n  -- Position `k + 2`: descend one cons-cell with index shift `k + 2 → k + 1`.\n  | _, _, .cons _ _ (p@(.cons _ _ _)), k + 2 =>\n      p.IsCollider (k + 1)"
    },
    {
      "kind": "main",
      "code": "def IsNonCollider {u v : Node} (p : Walk G u v) (k : ℕ) : Prop :=\n  k ≤ p.length ∧ ¬ p.IsCollider k",
      "explanation": "This block defines the position-level non-collider predicate `Walk.IsNonCollider p k` as the negation of `IsCollider` on the in-range fragment $k \\in \\{0, \\dots, p.length\\}$. Since the arrowhead count $\\mathrm{ah}_\\pi(k)$ lives in $\\{0, 1, 2\\}$, the LN classification ``$\\mathrm{ah}_\\pi(k) \\le 1$'' is equivalent to ``$\\mathrm{ah}_\\pi(k) \\neq 2$'', i.e.\\ to `¬ p.IsCollider k`, which makes the LN's mutual-exclusivity statement (every in-range position is exactly one of collider / non-collider) definitional rather than a separate theorem. The explicit bound `k ≤ p.length` is load-bearing: without it, every out-of-range `k > p.length` would vacuously satisfy `¬ p.IsCollider k` (since `IsCollider` is `False` off-range through the Option-`some` conjuncts) and would be misclassified. The asymmetry with the sibling block — `IsCollider`'s in-range condition is implicit via Option-membership, while `IsNonCollider` carries it explicitly — is the cost of the negation-based encoding; the payoff is that the two predicates are pointwise complementary on $\\{0, \\dots, p.length\\}$ by unfolding alone.",
      "code_annotated": "-- Non-collider at position `k`: in-range AND not a collider; mutual exclusivity with `IsCollider` becomes definitional.\ndef IsNonCollider {u v : Node} (p : Walk G u v) (k : ℕ) : Prop :=\n  -- Explicit in-range bound; `IsCollider`'s implicit Option-membership scoping is not available on the negation side.\n  k ≤ p.length ∧ ¬ p.IsCollider k"
    }
  ],
  "lean_source_url": "https://github.com/schipeltaper/causality-leanified/blob/main/leanification/Chapter3_GraphTheory/Section3_3/CollidersAndNon.lean",
  "lean_file_path": "leanification/Chapter3_GraphTheory/Section3_3/CollidersAndNon.lean",
  "addition_to_the_LN": "\\textbf{Addition tag \\texttt{[collider\\_side\\_aware\\_walkstep\\_predicates]}.}\nThis addition refines the literal LN definition by introducing a \\emph{side-aware}\nreading of the per-walk-step contribution to $\\mathrm{ah}_\\pi(k)$ at the two adjacent\nwalk-positions $k \\in \\{i, i+1\\}$ of a walk-step $a_i$ of $\\pi$. The refinement is\nauthorised because the literal LN definition is ambiguous in two corner configurations:\n(i) at a walk-incident directed self-loop $a_i \\in E$ with $v_i = v_{i+1}$, where both\nwalk-constraint disjuncts of def \\ref{def:walks}, item~i.\\ apply syntactically and the\nstored-pair test of def \\ref{def-edge-relations}, item~ii.\\ fires unconditionally at\nboth adjacent positions; and (ii) at a walk-step traversing a pair that\nsimultaneously sits in $E$ as an ordered pair and in $L$ as an unordered pair -- the\n\\emph{writing-mirror} configuration permitted by def \\ref{def-cdmg} (which does not\nimpose $E \\cap L = \\emptyset$) -- where the literal stored-pair test's L-disjunct fires\nat both adjacent positions regardless of which channel the walk-step actually traverses.\nThe side-aware reading introduced here resolves both corners by committing to the\nwalk-step's recorded traversal channel as the \\emph{sole} signal for which channel\ncontributes to the arrowhead test at this step, refining (and strictly overriding) the\nliteral stored-pair test as the canonical reading of $\\mathrm{ah}_\\pi(k)$ used by the\ncollider/non-collider classification.\n\n\\medskip\\noindent\\textbf{(a) Per-step walk-traversal channel and direction are recorded\nexplicitly.} Each walk-step $a_i$ of $\\pi$ carries an explicit walk-traversal channel\n$c_i \\in \\{\\textsf{forward-}E, \\textsf{backward-}E, \\textsf{bidir-}L\\}$, with the\nconvention\n\\[\n    c_i = \\textsf{forward-}E \\iff \\bigl(a_i = (v_i, v_{i+1}) \\;\\land\\; a_i \\in E\\bigr),\n\\]\n\\[\n    c_i = \\textsf{backward-}E \\iff \\bigl(a_i = (v_{i+1}, v_i) \\;\\land\\; a_i \\in E\\bigr),\n\\]\n\\[\n    c_i = \\textsf{bidir-}L \\iff a_i \\in L \\;\\text{(taken with}\\; a_i = (v_i, v_{i+1})\n    \\;\\text{by convention)}.\n\\]\nAt a walk-step whose endpoints are distinct and that does not sit in a writing-mirror\npair, the channel $c_i$ is forced by the data of $\\pi$. At a writing-mirror walk-step\n($(v_i, v_{i+1}) \\in E$ \\emph{and} $s(v_i, v_{i+1}) \\in L$, with $v_i \\ne v_{i+1}$\nor $v_i = v_{i+1}$), the choice of $c_i \\in \\{\\textsf{forward-}E, \\textsf{backward-}E,\n\\textsf{bidir-}L\\}$ is \\emph{recorded explicitly} as part of $\\pi$ -- i.e.\\ $\\pi$\ncarries strictly more data at such steps than its underlying vertex/edge sequence alone.\nAt a walk-incident directed self-loop $a_i \\in E$ with $v_i = v_{i+1}$, the choice\nbetween $c_i = \\textsf{forward-}E$ and $c_i = \\textsf{backward-}E$ is similarly\nrecorded explicitly.\n\n\\medskip\\noindent\\textbf{(b) Side-aware per-step contribution formula at adjacent positions.}\nFor each walk-step $a_i$ of $\\pi$ ($0 \\le i \\le n - 1$) and each adjacent position\n$k \\in \\{i, i+1\\}$, the contribution of $a_i$ to $\\mathrm{ah}_\\pi(k)$ is determined\n\\emph{solely} by the recorded channel $c_i$, never consulting whether the underlying\nstored pair also sits in the opposite channel:\n\\begin{itemize}\n    \\item \\emph{Forward-$E$ step} ($c_i = \\textsf{forward-}E$): contributes $1$ at the\n        walk-traversal target $k = i + 1$ and $0$ at the walk-traversal source $k = i$.\n    \\item \\emph{Backward-$E$ step} ($c_i = \\textsf{backward-}E$): contributes $1$ at\n        the walk-traversal target $k = i$ and $0$ at the walk-traversal source $k = i + 1$.\n    \\item \\emph{Bidir-$L$ step} ($c_i = \\textsf{bidir-}L$): contributes $1$ at both\n        adjacent positions $k \\in \\{i, i+1\\}$.\n\\end{itemize}\nA directed step (forward-$E$ or backward-$E$) commits to its $E$-side traversal even\nwhen a coexisting $L$-edge $s(v_i, v_{i+1}) \\in L$ would, under the literal stored-pair\ntest, contribute additional arrowheads at the source position. A bidirected step\n($\\textsf{bidir-}L$) commits to the $L$-side bidirected contribution even when a\ncoexisting $E$-edge would, under the literal test, contribute differently. The\narrowhead count $\\mathrm{ah}_\\pi(k)$ at a position $k \\in \\{0, 1, \\dots, n\\}$ is then\nthe sum of these per-step contributions, summed over the (at most two) walk-incident\nsteps $i \\in \\{k - 1, k\\} \\cap \\{0, 1, \\dots, n - 1\\}$.\n\n\\medskip\\noindent\\textbf{(c) Deviation from the literal stored-pair test is strict at\nwriting-mirror walk-steps and at directed self-loops.}\nThe side-aware contribution formula of item~(b) coincides pointwise with the literal\nstored-pair ``$a_i$ is an edge into $v_k$'' test of def \\ref{def-edge-relations},\nitem~ii.\\ on any walk $\\pi$ that traverses neither a writing-mirror configuration nor\na directed self-loop. Where the two readings disagree:\n\\begin{itemize}\n    \\item \\emph{Directed self-loop steps} ($a_i \\in E$ with $v_i = v_{i+1}$, no\n        coexisting $L$-edge): the literal test fires unconditionally at both adjacent\n        positions, contributing $1 + 1 = 2$; the side-aware reading commits via $c_i$\n        to a single position (the walk-traversal target) for the contribution,\n        contributing $1$ at that position and $0$ at the other.\n    \\item \\emph{Writing-mirror walk-steps} ($(v_i, v_{i+1}) \\in E$ \\emph{and}\n        $s(v_i, v_{i+1}) \\in L$, with $v_i \\ne v_{i+1}$): the literal test fires the\n        L-disjunct at both adjacent positions regardless of which channel the\n        walk-step traverses; the side-aware reading ignores the coexisting $L$-edge\n        when $c_i \\in \\{\\textsf{forward-}E, \\textsf{backward-}E\\}$, contributing only\n        at the walk-traversal target of the directed channel.\n    \\item \\emph{Writing-mirror self-loop steps} ($(v, v) \\in E$ \\emph{and}\n        $s(v, v) \\in L$): structurally impossible by def \\ref{def-cdmg}'s\n        \\texttt{hL\\_irrefl}, which forbids self-pair elements in $L$.\n\\end{itemize}\nThe strict-equivalence gate accepts this deviation as the canonical disambiguation of\nthe LN's ``$\\mathrm{ah}_\\pi(k)$ counts arrowheads on the walk $\\pi$'' phrasing -- a\nwalk-step \\emph{is} the traversal of a single channel of the graph, and an arrowhead\nthat the walk's channel does not place at $v_k$ is not part of the walk's count, even\nwhen the same pair sits in the opposite channel of the graph as a separate edge object.\n\n\\medskip\\noindent\\textbf{Canonical classification.}\nThe collider / non-collider classification of def \\ref{def:collider_noncollider} is\ndefined using the side-aware $\\mathrm{ah}_\\pi(k)$ of items~(a)--(b): position\n$k \\in \\{0, 1, \\dots, n\\}$ is a non-collider on $\\pi$ iff its side-aware arrowhead\ncount satisfies $\\mathrm{ah}_\\pi(k) \\le 1$, and a collider iff $\\mathrm{ah}_\\pi(k) = 2$.\nThe side-aware reading is the canonical disambiguation of the LN's ``i.e.''\\ gloss\n``at most one arrowhead'' (resp.\\ ``two arrowheads pointing towards $v_k$ on the walk\n$\\pi$''), taking precedence over the literal stored-pair test at both corner kinds\nlisted in item~(c). On walks avoiding both corners, the classification coincides with\nthe count obtained from the literal stored-pair test.\n",
  "design_choices": "**Side-aware per-step arrowhead predicates, not a single node-equality test.** `WalkStep.HeadAtTarget` and `WalkStep.HeadAtSource` are zero-argument predicates that read the per-step contribution off the constructor tag alone. The collider test at position `k = 1` reads `s₀.HeadAtTarget ∧ s₁.HeadAtSource` — no middle vertex `v_k` binder — because the typed indices `s₀ : WalkStep G _ v_k` and `s₁ : WalkStep G v_k _` already pin which end of each step sits at `v_k`. A node-equality reading via `IsInto vk` would collapse on directed self-loops: at `.forwardE _ : WalkStep G b b`, the indices `u = v = b` make the test `w = b` fire on both source and target sides indistinguishably, spuriously classifying a position-1 walk `a → b → b` (right step a self-loop) as a collider at `b`. The split prevents that pathology at the type-and-pattern-match level rather than via a runtime guard.\n\n**No opposite-channel disjunct on the side-aware predicates.** The bodies of `HeadAtTarget` and `HeadAtSource` consult neither `G.L` nor `G.E` membership; the constructor tag is the SOLE signal for which channel contributes. A directed step (`.forwardE` or `.backwardE`) commits to its $E$-channel reading even at a writing-mirror pair where $s(u, v) \\in G.L$ also holds, and `.bidir` commits to its bidirected reading even when a coexisting $E$-edge is present. This is the strict refinement authorised by the addition tag `[collider_side_aware_walkstep_predicates]`: a walk-step IS the traversal of a single channel, and an arrowhead the walk's channel does not place at `v_k` is not part of the walk's count.\n\n**`IsInto` retained alongside as a coarser, non-side-aware predicate.** `WalkStep.IsInto s w` asks the union-flavoured question \"is `w` ANY arrowhead-endpoint of `s`?\" via node-equality on type indices plus a writing-mirror $L$-disjunct. It serves consumers that need constructor-choice-invariance across writing-mirror encodings of the same walk-step — notably the reversal symmetry lemma `s.reverse.IsInto w ↔ s.IsInto w` consumed by `claim_3_22`. The two readings coincide on walks avoiding both directed self-loops and writing-mirror walk-steps traversed via the $E$-channel.\n\n**Structural recursion on `Walk × ℕ`, no `p.edges`-indexing.** `Walk` carries no `.edges` projection, so the five clauses fan out the position semantics: `nil _ _` and `cons _ _ (nil _ _)` send every `k` to `False` (no interior positions); `cons _ _ (cons _ _ _), 0` handles the start end-position; `cons _ s₀ (cons _ s₁ _), 1` is the head-pair arrowhead test; `cons _ _ (p@(cons _ _ _)), k + 2` recurses into the tail with index shift. Out-of-range `k > p.length` falls through to `False` by pattern-match exhaustiveness; no explicit `1 ≤ k ≤ p.length - 1` guard is needed.\n\n**Dedicated position-1 clause, recursion only from `k + 2`.** Position 1 reads both head steps `s₀` and `s₁` simultaneously, both bound at the outer cons-cons pattern. Recursing into the tail at position 1 would drop `s₀` and lose the left conjunct. The recursion's first opportunity is therefore outer position `k + 2`, where the relevant pair lives in the tail's first two steps.\n\n**`IsCollider` primary, `IsNonCollider` as bounded de Morgan dual.** `IsNonCollider p k := k ≤ p.length ∧ ¬ p.IsCollider k` makes the LN's mutual-exclusivity / joint-exhaustiveness claim definitional on the in-range fragment $\\{0, 1, \\dots, p.\\mathrm{length}\\}$ rather than a separate theorem. The explicit bound `k ≤ p.length` is load-bearing: without it, every out-of-range `k > p.length` would vacuously satisfy `¬ p.IsCollider k` and be misclassified. `IsCollider`'s in-range condition is instead implicit in its recursive cons-cell structure — the asymmetry is the price for the negation-based encoding.\n\n**`Prop`, not `Bool`.** Walks live at `Type _`; per-position classification at `Prop`. A decidable `Bool` form would demand `DecidableEq Node` discharges at every elaboration site for no downstream benefit — `def_3_16` blockability and `def_3_17` $\\sigma$-blocking consume these predicates as hypotheses, where `Prop` is the natural fit.\n\n**Namespace-level `variable {G : CDMG Node}` in both `WalkStep` and `Walk`.** Auto-binds `G` as an implicit on every subsequent declaration, encoding the LN's once-at-the-top \"let $G = (J, V, E, L)$ be a CDMG\" quantifier. Implicit because downstream consumers reach into `G` via dot-notation on a walk (`p.IsCollider k`), recovering `G` by unification from the walk's type `Walk G u v`."
}
