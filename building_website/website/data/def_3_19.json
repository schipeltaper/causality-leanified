{
  "ref": "def_3_19",
  "kind": "def",
  "section": "3.4",
  "title": "DBlockedWalks",
  "type": "definition",
  "status": {
    "formalized": "yes",
    "proven": "n/a",
    "solved": "yes"
  },
  "tex_statement": {
    "raw": "% Definition file for row def_3_19 -- \"DBlockedWalks\".\n% Auto-generated stub by scaffold/solving_current_row.py at row start. The\n% manager agent (or a worker dispatched by it) fills the body below with the\n% Lean-faithful definition statement(s). Stays close to the lecture notes.\n%\n% Compiles standalone via the `subfiles` package -- the parent main.tex in\n% this folder provides the preamble.\n\n\\documentclass[main]{subfiles}\n\n\\begin{document}\n% ref: def_3_19\n% title: DBlockedWalks\n\\def\\rowref{def\\_3\\_19}\n\\phantomsection\\label{def_3_19}\n\n\\begin{Def}[$d$-blocked walks]\\label{def:d-blocking}\n      Let $G=(J,V,E,L)$ be a CDMG and $C \\ins J \\cup V$ a subset of nodes and $\\pi$ a walk in $G$:\n      \\[ \\pi =\\lp  v_0 \\sus \\cdots \\sus v_n \\rp.  \\]\n      \\begin{enumerate}\n          \\item\n              We say that the walk $\\pi$ is \\emph{$C$-$d$-blocked} or \\emph{$d$-blocked by $C$} to emphasize the use of the bidirected edges\\footnote{The ``d'' here stands for ``directional''. d-separation was first only used for DAGs (without bidirected edges).  For ADMGs it was then called ``m-separation'' in \\cite{Ric03}. But since the notion of m-separation is arguably the natural extension of d-separation and to avoid introducing more definitions, we will just call it d-separation as well, which will not create any ambiguity.}\n              if either:\n      \\begin{enumerate}[label=\\roman*.)]\n          \\item $v_0 \\in C$ or $v_n \\in C$ or:\n          \\item there are two adjacent edges in $\\pi$ of one of the following forms:\n              \\[\\begin{array}{rccl}\n           \\text{ left chain: } & v_{k-1} \\hut v_k \\hus v_{k+1} & \\text{ with } & v_k \\in C,\\\\\n           \\text{ right chain: } & v_{k-1} \\suh v_k \\tuh v_{k+1} & \\text{ with } & v_k \\in C,\\\\\n           \\text{ fork: } & v_{k-1} \\hut v_k \\tuh v_{k+1} & \\text{ with } & v_k \\in C,\\\\\n           \\text{ collider: } & v_{k-1} \\suh v_k \\hus v_{k+1} & \\text{ with } & v_k \\notin \\Anc^G(C).\n              \\end{array}\\]\n      \\end{enumerate}\n          \\item We say that the walk $\\pi$ is \\emph{$C$-$d$-open} if it is not $C$-$d$-blocked.\n      \\end{enumerate}\n\\end{Def}\n\n\\end{document}\n",
    "html": "<p>Let $G=(J,V,E,L)$ be a CDMG and $C \\ins J \\cup V$ a subset of nodes and $\\pi$ a walk in $G$:\n      \\[ \\pi =\\lp  v_0 \\sus \\cdots \\sus v_n \\rp.  \\]</p>\n<ol class=\"enum\"><li>We say that the walk $\\pi$ is <em>$C$-$d$-blocked</em> or <em>$d$-blocked by $C$</em> to emphasize the use of the bidirected edges\\footnote{The “d” here stands for “directional”. d-separation was first only used for DAGs (without bidirected edges).  For ADMGs it was then called “m-separation” in \\cite{Ric03}. But since the notion of m-separation is arguably the natural extension of d-separation and to avoid introducing more definitions, we will just call it d-separation as well, which will not create any ambiguity.}\n              if either:\n      \\begin{enumerate}[label=\\roman*.)]</li><li>$v_0 \\in C$ or $v_n \\in C$ or:</li><li>there are two adjacent edges in $\\pi$ of one of the following forms:\n              \\[\\begin{array}{rccl}\n           \\text{ left chain: } & v_{k-1} \\hut v_k \\hus v_{k+1} & \\text{ with } & v_k \\in C,\\\\\n           \\text{ right chain: } & v_{k-1} \\suh v_k \\tuh v_{k+1} & \\text{ with } & v_k \\in C,\\\\\n           \\text{ fork: } & v_{k-1} \\hut v_k \\tuh v_{k+1} & \\text{ with } & v_k \\in C,\\\\\n           \\text{ collider: } & v_{k-1} \\suh v_k \\hus v_{k+1} & \\text{ with } & v_k \\notin \\Anc^G(C).\n              \\end{array}\\]</li></ol>\n<p>\\item We say that the walk $\\pi$ is <em>$C$-$d$-open</em> if it is not $C$-$d$-blocked.\n      \\end{enumerate}</p>",
    "env": "Def",
    "env_title": "$d$-blocked walks",
    "source_path": "leanification/Chapter3_GraphTheory/Section3_4/tex/def_3_19_DBlockedWalks.tex"
  },
  "tex_proof": null,
  "lean_code_with_comments": "/-- The walk `π` is *$C$-$d$-blocked* (LN def 3.19, item 1):\neither $v_0$ or $v_n$ lies in `C`, or some interior\nnon-collider position on `π` lies in `C`, or some collider\nposition on `π` lies outside $\\Anc^G(C)$. -/\ndef IsDBlocked {v w : α} (π : Walk G v w) (C : Set α) : Prop :=\n  (π.nodeAt 0 ∈ C) ∨\n  (π.nodeAt π.length ∈ C) ∨\n  (∃ k, 0 < k ∧ k < π.length ∧ π.IsNonColliderAt k ∧ π.nodeAt k ∈ C) ∨\n  (∃ k, π.IsColliderAt k ∧ π.nodeAt k ∉ G.AncSet C)\n\n/-- The walk `π` is *$C$-$d$-open* (LN def 3.19, item 2):\nthe literal negation of `π.IsDBlocked C`. -/\ndef IsDOpen {v w : α} (π : Walk G v w) (C : Set α) : Prop :=\n  ¬ π.IsDBlocked C\n\n/-- Defining equation for `IsDOpen`. The LN-original\ncharacterisation: \"$\\pi$ is $C$-$d$-open iff it is not\n$C$-$d$-blocked\". Holds by definition. -/\ntheorem isDOpen_iff_not_isDBlocked {v w : α}\n    (π : Walk G v w) (C : Set α) :\n    π.IsDOpen C ↔ ¬ π.IsDBlocked C :=\n  Iff.rfl",
  "lean_code_without_comments": "def IsDBlocked {v w : α} (π : Walk G v w) (C : Set α) : Prop :=\n  (π.nodeAt 0 ∈ C) ∨\n  (π.nodeAt π.length ∈ C) ∨\n  (∃ k, 0 < k ∧ k < π.length ∧ π.IsNonColliderAt k ∧ π.nodeAt k ∈ C) ∨\n  (∃ k, π.IsColliderAt k ∧ π.nodeAt k ∉ G.AncSet C)\n\ndef IsDOpen {v w : α} (π : Walk G v w) (C : Set α) : Prop :=\n  ¬ π.IsDBlocked C\n\ntheorem isDOpen_iff_not_isDBlocked {v w : α}\n    (π : Walk G v w) (C : Set α) :\n    π.IsDOpen C ↔ ¬ π.IsDBlocked C :=\n  Iff.rfl",
  "lean_source_urls": [
    {
      "title": "DBlockedWalks.lean",
      "url": "https://github.com/schipeltaper/causality-leanified/blob/server_setting_up_scaffold/leanification/Chapter3_GraphTheory/Section3_4/DBlockedWalks.lean#L332-L449"
    }
  ],
  "lean_explanation": "The walk `π : Walk G v w` runs between endpoints `v, w : α`, where `α : Type*` is the ambient vertex type. The graph `G : CDMG α` is a chain directed mixed graph (the LN's $G = (J, V, E, L)$); the conditioning set is `C : Set α`, with no subtype wrapper enforcing the LN's prose-level preamble $C \\subseteq J \\cup V$. Interior positions are indexed by `k : ℕ` and bounded by `0 < k ∧ k < π.length`, picking out the genuinely interior positions of $\\pi = (v_0, v_1, \\ldots, v_n)$.\n\n`π.IsDBlocked C` is a `Prop`-valued four-way disjunction matching the LN's reading order. The two endpoint clauses use `π.nodeAt 0 ∈ C` and `π.nodeAt π.length ∈ C` (the LN's $v_0 \\in C$ and $v_n \\in C$). The interior non-collider clause is an existential over positions $k$ satisfying both `π.IsNonColliderAt k` — which collapses the LN's left-chain, right-chain, and fork sub-patterns into a single non-collider predicate — together with $v_k \\in C$. The collider clause is an existential over positions $k$ with `π.IsColliderAt k` and $v_k \\notin G.\\mathrm{AncSet}\\ C$, Lean's name for the LN's $\\Anc^G(C)$ supplied by the family-reachability module `Section3_1.FamilyReachability`.\n\nLean's dot-notation `π.IsDBlocked C` works because `π` is the first explicit argument, the same convention used throughout chapter 3 (`π.IsColliderAt`, `π.IsSigmaOpen`, and so on).\n\n`π.IsDOpen C` is the literal Lean negation `¬ π.IsDBlocked C`; the LN itself defines $d$-open as 'not $d$-blocked', and the companion lemma `isDOpen_iff_not_isDBlocked` is consequently `Iff.rfl`.",
  "design_choices": "**The four-disjunct shape mirrors the LN's reading order rather than the collapsed two-disjunct form.** A reader of the lecture notes sees clause (i) — two endpoint sub-conditions — followed by clause (ii) — four sub-patterns (left chain, right chain, fork, collider). The first three sub-patterns of (ii) all read '$v_k \\in C$' under a non-collider shape and together exhaust 'interior non-collider in $C$'; the fourth is the collider sub-clause. The Lean definition keeps this one-to-one correspondence: endpoint $v_0$, endpoint $v_n$, interior non-collider, collider. The natural alternative is a two-disjunct form ('some non-collider position is in $C$, or some collider position is not in $\\Anc^G(C)$'), equivalent under the observation that endpoints are always non-colliders. We reject the collapse because the very next row (claim 3.28) is the LN's own statement of that equivalence; baking it into the definition would silently delete an articulated theorem.\n\n**The interior bound `0 < k ∧ k < π.length` appears on the non-collider disjunct but not the collider disjunct.** With our `IsNonColliderAt`, endpoint positions are unconditionally non-colliders, so dropping the bound would let any walk with $v_0 \\in C$ trigger the non-collider clause and collapse the four-disjunct shape. The collider predicate, by contrast, is already `False` at $k = 0$, $k = \\pi.\\mathrm{length}$, and at out-of-range positions, so any witness is automatically interior; a redundant bound would only burden downstream proofs with an extra discharge step.\n\n**`IsNonColliderAt` rather than `IsBlockableNonColliderAt`.** This is the principal shape-level difference between $d$-blocking and $\\sigma$-blocking (def 3.17). $\\sigma$-blocking restricts attention to *blockable* non-colliders — a sub-classification driven by the coreflexive-edge boundary of the CDMG — and treats non-blockable non-colliders as '$\\sigma$-open for free'. $d$-blocking imposes no such restriction: every non-collider position is a potential blocker. Using `IsBlockableNonColliderAt` here would silently identify def 3.19 with def 3.17, which is wrong even though under acyclicity (claim 3.20) the two collapse to the same family of walks.\n\n**`IsDOpen` is the syntactic negation of `IsDBlocked`, not a parallel positive predicate.** The LN itself flips the asymmetry it used for $\\sigma$-separation: $d$-open is defined in one line as 'not $d$-blocked'. We mirror that — `IsDOpen := ¬ IsDBlocked` — which makes `isDOpen_iff_not_isDBlocked` definitionally `Iff.rfl` and eliminates the classical-reasoning step the $\\sigma$-version's De-Morgan dual requires. A caller wanting the positive conjunctive shape ('every endpoint not in $C$, every interior non-collider not in $C$, every collider in $\\Anc^G(C)$') applies De Morgan on demand; we judged that cost worth paying to keep the LN's framing as the primitive shape.\n\n**`C : Set α` rather than a `{ C // C ⊆ G.J ∪ G.V }` subtype.** The LN's preamble '$C \\subseteq J \\cup V$' is stated in prose, not enforced at the type level. `AncSet G C` already silently ignores members outside the graph, and threading a subtype through every downstream signature (def 3.20, the claim 3.28 / 29 / 30 chain, chapter $\\ge 4$ Markov properties, do-calculus) would impose a real ergonomic cost for no proof benefit. Consumers who genuinely depend on the side-condition state it as a separate hypothesis at the call site."
}
