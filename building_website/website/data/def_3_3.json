{
  "ref": "def_3_3",
  "kind": "def",
  "section": "3.1",
  "title": "EdgeRelations",
  "type": "definition",
  "status": {
    "formalized": "yes",
    "proven": "n/a",
    "solved": "yes"
  },
  "tex_statement": {
    "raw": "% Definition file for row def_3_3 -- \"EdgeRelations\".\n% Auto-generated stub by scaffold/solve_chapter.py at row start. The\n% manager agent (or a worker dispatched by it) fills the body below with the\n% Lean-faithful definition statement(s). Stays close to the lecture notes.\n%\n% This file is a sibling of `main.tex` inside `<subsection>/tex/`. The\n% `subfiles` package lets it compile both standalone (resolving its\n% preamble via `main.tex`) and as part of `main.tex` via `\\subfile{...}`.\n\n\\documentclass[main]{subfiles}\n\n\\begin{document}\n% ref: def_3_3\n% title: EdgeRelations\n\\def\\rowref{def\\_3\\_3}\n\\phantomsection\\label{def_3_3}\n\n\\begin{Def}[EdgeRelations]\n  Let $G=(J,V,E,L)$ be a CDMG.\n  \\begin{enumerate}\n    \\item If $v_1 \\sus v_2 \\in G$ then we call $v_1$ and $v_2$ \\emph{adjacent in $G$}.\n    \\item Edges of the form $v_1 \\hut v_2$ or $v_1 \\huh v_2$ are called \\emph{into $v_1$}.  \\\\\n          Edges of the form $v_1 \\tuh v_2$ or $v_1 \\huh v_2$ are called \\emph{into $v_2$}. \n    \\item Edges of the form $v_1 \\tuh v_2$ or $v_2 \\hut v_1$ are called \\emph{out of $v_1$}.\n  \\end{enumerate}\n\\end{Def}\n\n\\end{document}\n",
    "html": "<p>Let $G=(J,V,E,L)$ be a CDMG.</p>\n<ol class=\"enum\"><li>If $v_1 \\sus v_2 \\in G$ then we call $v_1$ and $v_2$ <em>adjacent in $G$</em>.</li><li>Edges of the form $v_1 \\hut v_2$ or $v_1 \\huh v_2$ are called <em>into $v_1$</em>.  <br>\n          Edges of the form $v_1 \\tuh v_2$ or $v_1 \\huh v_2$ are called <em>into $v_2$</em>.</li><li>Edges of the form $v_1 \\tuh v_2$ or $v_2 \\hut v_1$ are called <em>out of $v_1$</em>.</li></ol>",
    "env": "Def",
    "env_title": "EdgeRelations",
    "source_path": "leanification/Chapter3_GraphTheory/Section3_1/tex/def_3_3_EdgeRelations.tex"
  },
  "tex_proof": null,
  "lean_blocks": [
    {
      "kind": "main",
      "code": "def adjacent (G : CDMG Node) (v1 v2 : Node) : Prop := G.sus v1 v2"
    },
    {
      "kind": "main",
      "code": "def edgeInto (G : CDMG Node) (v1 v2 : Node) : Prop := G.hus v1 v2"
    },
    {
      "kind": "main",
      "code": "def edgeOutOf (G : CDMG Node) (v1 v2 : Node) : Prop := G.tuh v1 v2"
    }
  ],
  "lean_source_url": "https://github.com/schipeltaper/causality-leanified/blob/main/leanification/Chapter3_GraphTheory/Section3_1/EdgeRelations.lean",
  "lean_file_path": "leanification/Chapter3_GraphTheory/Section3_1/EdgeRelations.lean",
  "addition_to_the_LN": "[inconsistent_writing_order_enumeration_into_vs_out_of] Throughout the enumeration above, the writings $v_1 \\tuh v_2$ and $v_2 \\hut v_1$ denote the same edge (and the bidirected writings $v_1 \\huh v_2$ and $v_2 \\huh v_1$ likewise coincide). Membership of an edge in the categories \"into $v$\" or \"out of $v$\" depends only on the underlying element of $E$, not on which of these textual writings is used; item 3's explicit listing of both writings and item 2's single-writing enumeration are therefore equivalent under this identification.",
  "lean_explanation": "The three definitions live in the `CDMG` namespace, so the user-facing names are `CDMG.adjacent`, `CDMG.edgeInto`, and `CDMG.edgeOutOf`; once a `G : CDMG Node` is in scope, Lean 4 dot-notation lets you write `G.adjacent v1 v2` instead of `CDMG.adjacent G v1 v2`.\n\nThe right-hand sides are *thin renames* of three primitive operators imported from `CDMGNotation`: `sus` (a symmetric \"some edge of any type\", spelling the LN macro $\\sus$), `hus` (a directed-or-bidirected edge with an *arrowhead at the left argument*, spelling $\\hus$), and `tuh` (a directed edge from left to right, spelling $\\tuh$). Each of these unfolds to a disjunction of `Finset` memberships in `G.E` / `G.L`; the wrappers here exist purely to give those disjunctions the LN's *names*.\n\nThe signatures use Lean 4's implicit binders: `{Node : Type*}` is an implicit universe-polymorphic node type and `[DecidableEq Node]` is an instance argument needed because edge membership is decided over `Finset`s. Both are inherited from a `variable` block, so they do not appear in the `def` headers explicitly.\n\nCrucially, `edgeOutOf` is defined by the *single* operator `G.tuh v1 v2`, not by a disjunction `tuh ∨ hut`. The LN's two writings $v_1 \\tuh v_2$ and $v_2 \\hut v_1$ denote the *same* directed edge (`G.hut v1 v2` is by definition `G.tuh v2 v1`), so a disjunction would wrongly include the *reverse* edge $v_2 \\to v_1$ as \"out of $v_1$\".\n\nThe choice of `def` (rather than `abbrev`) keeps the names opaque to `simp`, so downstream proofs see `adjacent`, `edgeInto`, `edgeOutOf` as primitives until explicitly unfolded with `simp only [CDMG.adjacent]` etc.",
  "design_choices": "## Design choices for `def_3_3` (EdgeRelations)\n\n**Three two-argument predicates, one per LN item, with the focal vertex first.** The three numbered LN clauses become three separate definitions — `adjacent`, `edgeInto`, `edgeOutOf` — each taking the graph and two `Node` arguments, with the *first* node argument always being the \"focal\" vertex (the one the relation is *about*). This unifies the LN's stylistic variation, where item 2 writes the focal vertex on either side and item 3 lists two textual writings of the same edge, into a single uniform Lean shape.\n\n**`def`, not `abbrev`, to keep the named concepts opaque.** All three relations are thin wrappers over the primitive operators from `CDMGNotation` (`sus`, `hus`, `tuh`). Using `abbrev` would cause `simp` to immediately unfold them into their underlying disjunctions and then into `Finset` memberships, defeating the purpose of *naming* the LN's edge-relation concepts. With `def`, downstream proofs in `def_3_4` (walks), `def_3_5` (parent/child/sibling), `def_3_6` (acyclicity), and `claim_3_1` can reason about these as primitives, and a controlled `simp only [CDMG.adjacent]` opens one layer when needed.\n\n**Thin renames of existing operators, not fresh disjunctions.** Each relation is defined as a *single* application of an existing `CDMGNotation` primitive: `adjacent := G.sus v1 v2`, `edgeInto := G.hus v1 v2`, `edgeOutOf := G.tuh v1 v2`. Spelling out the disjuncts would diverge from those primitives and force any future refactor (e.g. adding an edge type) to update multiple sites — exactly the failure mode the `CDMGNotation.sus` design guards against.\n\n**Item 3 collapses to a single `tuh`, deliberately *not* `tuh ∨ hut`.** This is the load-bearing call for the row. The LN lists $v_1 \tuh v_2$ and $v_2 \\hut v_1$ as two writings, but the addition `[inconsistent_writing_order_enumeration_into_vs_out_of]` clarifies that these denote the *same edge*. A naive disjunction would be wrong: `G.hut v1 v2` unfolds to `G.tuh v2 v1` — the *opposite* directed edge — which would falsely classify a `v_2 → v_1` edge as \"out of `v_1`\". Only `tuh` is used, because bidirected edges have no tail and so cannot be \"out of\" anything.\n\n**Item 2 picks the focal-on-left `hus` spelling.** Item 2's two sub-clauses (\"into $v_1$\" vs \"into $v_2$\") collapse to one predicate parameterised by the focal vertex. The `hus` (focal on the left) spelling was chosen over `suh` because it matches item 2's primary sub-clause orientation, reuses the existing primitive verbatim, and aligns with the walk-into-$v_0$ convention `a_0 = v_0 \\hus v_1` in `def_3_4`.\n\n**Edge-as-object abstraction rejected.** A cleaner reformulation taking an edge `Node × Node` plus a focal vertex was considered and rejected: every downstream consumer pattern-matches on the LN macros (`\\hus`, `\\suh`, `\tuh`, `\\hut`) directly, and an edge-object would also force an arbitrary choice between `E`, `L`, or their union.\n\n**Naming and symmetry.** Lowercase camelCase (`adjacent`, `edgeInto`, `edgeOutOf`) mirrors the LN prose and the existing operator names. `adjacent` is symmetric (inherited from `sus`); the two `edge*` relations are *not* symmetric — the focal vertex is fixed. Symmetry / equivalence lemmas are deferred to wherever downstream rows actually need them."
}
