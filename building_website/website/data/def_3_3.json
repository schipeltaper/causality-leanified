{
  "ref": "def_3_3",
  "kind": "def",
  "section": "3.1",
  "title": "EdgeRelations",
  "type": "definition",
  "status": {
    "formalized": "yes",
    "proven": "n/a",
    "solved": "yes"
  },
  "tex_statement": {
    "raw": "% Definition file for row def_3_3 -- \"EdgeRelations\".\n% Auto-generated stub by scaffold/solving_current_row.py at row start. The\n% manager agent (or a worker dispatched by it) fills the body below with the\n% Lean-faithful definition statement(s). Stays close to the lecture notes.\n%\n% Compiles standalone via the `subfiles` package -- the parent main.tex in\n% this folder provides the preamble.\n\n\\documentclass[main]{subfiles}\n\n\\begin{document}\n% ref: def_3_3\n% title: EdgeRelations\n\\def\\rowref{def\\_3\\_3}\n\\phantomsection\\label{def_3_3}\n\n\\begin{Def}[EdgeRelations]\n  Let $G=(J,V,E,L)$ be a CDMG.\n  \\begin{enumerate}\n    \\item If $v_1 \\sus v_2 \\in G$ then we call $v_1$ and $v_2$ \\emph{adjacent in $G$}.\n    \\item Edges of the form $v_1 \\hut v_2$ or $v_1 \\huh v_2$ are called \\emph{into $v_1$}.  \\\\\n          Edges of the form $v_1 \\tuh v_2$ or $v_1 \\huh v_2$ are called \\emph{into $v_2$}. \n    \\item Edges of the form $v_1 \\tuh v_2$ or $v_2 \\hut v_1$ are called \\emph{out of $v_1$}.\n  \\end{enumerate}\n\\end{Def}\n\n\\end{document}\n",
    "html": "<p>Let $G=(J,V,E,L)$ be a CDMG.</p>\n<ol class=\"enum\"><li>If $v_1 \\sus v_2 \\in G$ then we call $v_1$ and $v_2$ <em>adjacent in $G$</em>.</li><li>Edges of the form $v_1 \\hut v_2$ or $v_1 \\huh v_2$ are called <em>into $v_1$</em>.  <br>\n          Edges of the form $v_1 \\tuh v_2$ or $v_1 \\huh v_2$ are called <em>into $v_2$</em>.</li><li>Edges of the form $v_1 \\tuh v_2$ or $v_2 \\hut v_1$ are called <em>out of $v_1$</em>.</li></ol>",
    "env": "Def",
    "env_title": "EdgeRelations",
    "source_path": "leanification/Chapter3_GraphTheory/Section3_1/tex/def_3_3_EdgeRelations.tex"
  },
  "tex_proof": null,
  "lean": [
    {
      "name": "Adjacent",
      "kind": "def",
      "code": "/-- `v₁` and `v₂` are *adjacent in `G`*: there is some edge of any\norientation between them. Definitionally `sus G v₁ v₂` from\ndef_3_2; the readable name exists so downstream prose statements\nsuch as claim 3.1's \"no two nodes in $J$ are adjacent\" can be cited\nwithout unfolding to `sus`. -/\ndef Adjacent (G : CDMG α) (v₁ v₂ : α) : Prop := sus G v₁ v₂"
    },
    {
      "name": "EdgeInto",
      "kind": "def",
      "code": "/-- `EdgeInto G v₁ v₂` -- the edge between `v₁` and `v₂` is *into\n`v₁`*, i.e. has an arrowhead at `v₁` (either a directed edge\n`v₁ ⟵[G] v₂` or a bidirected edge `v₁ ⟷[G] v₂`). Definitionally\n`hus G v₁ v₂` from def_3_2. -/\ndef EdgeInto (G : CDMG α) (v₁ v₂ : α) : Prop := hus G v₁ v₂"
    },
    {
      "name": "EdgeOutOf",
      "kind": "def",
      "code": "/-- `EdgeOutOf G v₁ v₂` -- the edge between `v₁` and `v₂` is *out\nof `v₁`*: a directed edge `v₁ ⟶[G] v₂`. Definitionally\n`tuh G v₁ v₂` from def_3_2. Note the deliberate asymmetry with\n`EdgeInto`: bidirected edges are excluded here, because a\nbidirected edge has arrowheads at both endpoints and so is \"into\"\nboth vertices but \"out of\" neither. -/\ndef EdgeOutOf (G : CDMG α) (v₁ v₂ : α) : Prop := tuh G v₁ v₂"
    }
  ],
  "lean_file_path": "leanification/Chapter3_GraphTheory/Section3_1/EdgeRelations.lean",
  "lean_explanation": "The file equips def 3.2's primitive edge relations with three readable prose names matching the LN's English phrasings. Throughout, `α : Type*` is the ambient vertex type, `G : CDMG α` is the graph under discussion, and `v₁ v₂ : α` are arbitrary vertices.\n\nEach of `Adjacent`, `EdgeInto`, and `EdgeOutOf` is a `Prop`-valued one-line `def` that aliases one of def 3.2's underlying relations. `Adjacent G v₁ v₂` (LN: \"$v_1$ and $v_2$ adjacent in $G$\") unfolds to `sus G v₁ v₂`, the \"any edge of any orientation between $v_1$ and $v_2$\" relation. `EdgeInto G v₁ v₂` (LN: \"edge into $v_1$\") unfolds to `hus G v₁ v₂`, which holds when there is an arrowhead at $v_1$ — either a directed edge $v_1 \\hut v_2$ or a bidirected edge $v_1 \\huh v_2$. The LN's companion phrasing \"edge into $v_2$\" is the *same* predicate with the arguments swapped: `EdgeInto G v₂ v₁`. `EdgeOutOf G v₁ v₂` (LN: \"edge out of $v_1$\") unfolds to `tuh G v₁ v₂`, which holds exactly when there is a directed edge $v_1 \\tuh v_2$ — equivalently $(v_1, v_2) \\in G.E$ — and deliberately excludes bidirected edges.\n\nThe file also ships a `@[simp]` characterisation lemma for each of the three names (`adjacent_iff`, `edgeInto_iff`, `edgeOutOf_iff`), each just `Iff.rfl`, so callers can rewrite freely between the prose name and its def 3.2 underlying form. A small companion lemma `edgeOutOf_iff_hut` records that the LN's two equivalent spellings of \"out of $v_1$\" — $v_1 \\tuh v_2$ and $v_2 \\hut v_1$ — collapse to the same Lean proposition by definition. Finally, `Adjacent.symm` proves the (LN-implicit) symmetry of adjacency by case-splitting on the three possible edge orientations and invoking `G.L_symm` from def 3.1 on the bidirected branch.",
  "design_choices": "**A `def`, not an `abbrev` or new notation.** The goal of this row is to introduce *readable Lean identifiers* for prose-level use, not new graph structure. An `abbrev` would dissolve into `sus` / `hus` / `tuh` on sight, defeating the purpose: downstream statements such as claim 3.1's \"no two nodes in $J$ are adjacent\" would render in goals and error messages as the def 3.2 primitive rather than as the LN's English name. A `notation` token would pile yet another arrow on top of the six already introduced in def 3.2, when what we want at this layer is precisely the English. A `def` is the right unit: it survives intact in goal output, it can be unfolded explicitly via the `@[simp]` `_iff` lemmas when a proof needs the underlying relation, and it keeps the def 3.2 arrows as the formal-syntax layer with these new identifiers as the prose-syntax layer on top.\n\n**Convention: arrowhead at the *first* argument.** Item 2 of the LN lists two sentences — \"into $v_1$\" and \"into $v_2$\" — that share the same predicate, differing only in which vertex argument the arrowhead lands on. Rather than introducing two predicates, we follow the LN's natural wording and let `EdgeInto G v₁ v₂` mean \"into $v_1$\", with the arrowhead at the first listed vertex; the LN's \"into $v_2$\" sentence becomes `EdgeInto G v₂ v₁`. This convention also matches the underlying def 3.2 relation `hus`, which already picks out the first vertex as the arrowhead site, so no swapping is needed when unfolding.\n\n**Bidirected edges deliberately excluded from `EdgeOutOf`.** A bidirected edge $v_1 \\huh v_2$ has arrowheads at *both* endpoints, so it is \"into\" both vertices but \"out of\" neither. The LN encodes this asymmetry by listing $\\huh$ only on the \"into\" side of item 2 and never on the \"out of\" side of item 3. This is load-bearing downstream: the directed-walk and ancestor relations introduced in later rows all condition on *directed* edges only, never bidirected ones, and baking that asymmetry into the prose names now keeps those later definitions clean.\n\n**The LN's two spellings of \"out of\" collapse to one Lean proposition.** Item 3 offers both $v_1 \\tuh v_2$ and $v_2 \\hut v_1$ as ways to express \"out of $v_1$\". In Lean these are literally the same proposition — both unfold to $(v_1, v_2) \\in G.E$ — so we pick `tuh G v₁ v₂` as the canonical form and record the alternative spelling as a separate `Iff.rfl` lemma `edgeOutOf_iff_hut`, giving readers who arrive via the $\\hut$ side an immediate bridge to the canonical form."
}
