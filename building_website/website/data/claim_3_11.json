{
  "ref": "claim_3_11",
  "kind": "claim",
  "section": "3.2",
  "title": "DisjointHardInterventions",
  "type": "lemma",
  "status": {
    "formalized": "yes",
    "proven": "proven",
    "solved": "yes"
  },
  "tex_block_html": "<p>Let $G=(J,V,E,L)$ be a CADMG and $W_1 \\ins J \\cup V$ and $W_2 \\ins V$ two disjoint subsets of nodes from $G$.\n   Then the CADMG obtained from first hard intervening on $W_1$ and then node-splitting on $W_2$ is the same CADMG that arises from first node-splitting on $W_2$ and then hard intervening on $W_1$.\n      \\[ \\lp G_{\\doit(W_1)} \\rp_{\\swig(W_2)} =  \\lp G_{\\swig(W_2)} \\rp_{\\doit(W_1)}.   \\]</p>",
  "tex_statement": {
    "raw": "% Claim statement file for row claim_3_11 -- \"DisjointHardInterventions\".\n% Auto-generated stub by scaffold/solve_chapter.py at row start. The\n% manager agent (or a worker it dispatches) fills the body below with the\n% claim's statement(s) from the lecture notes -- statement only, no proof\n% (proofs live in the sibling `claim_3_11_proof_*.tex` file).\n%\n% This file is a sibling of `main.tex` inside `<subsection>/tex/`. The\n% `subfiles` package lets it render both standalone and as part of\n% `main.tex`. Note: `main.tex` does NOT \\subfile this statement file -- the\n% sibling `_proof_` file restates the statement above the proof, so\n% including both in `main.tex` would be redundant. This file exists for\n% standalone reading / grep convenience.\n\n\\documentclass[main]{subfiles}\n\n\\begin{document}\n% ref: claim_3_11 (statement)\n% title: DisjointHardInterventions\n\\def\\rowref{claim\\_3\\_11}\n\\phantomsection\\label{claim_3_11}\n\n\\begin{Lem}[Disjoint hard interventions and node-splitting hard interventions commute]\\label{disjoint-hard-interventions-and-node-splitting-hard-interventions-commute}\n    Let $G = (J, V, E, L)$ be a CADMG (def \\ref{def-cdmg-types}, item~i., equivalently a CDMG of def \\ref{def-cdmg} that is acyclic in the sense of def \\ref{def-acylic}); throughout we use the notation of def \\ref{not-cdmg} and recall the typing and axioms of def \\ref{def-cdmg}, namely $J \\cap V = \\emptyset$, $E \\subseteq (J \\cup V) \\times V$, $L \\subseteq V \\times V$, $L$ irreflexive ($(v_1, v_2) \\in L \\Rightarrow v_1 \\neq v_2$), and $L$ symmetric ($(v_1, v_2) \\in L \\Leftrightarrow (v_2, v_1) \\in L$). We rely throughout on the hard-intervention construction $\\doit$ of def \\ref{def:G_hard_intervention} and the node-splitting hard-intervention (SWIG) construction $\\swig$ of def \\ref{def:G_node-splitting_intervention}. Let\n    \\[ W_1 \\subseteq J \\cup V \\quad\\text{and}\\quad W_2 \\subseteq V \\]\n    be two subsets of nodes of $G$ (both automatically finite, since $J \\cup V$ is finite by def \\ref{def-cdmg}; this matches the project's Lean encoding of $W_1$ and $W_2$ as \\texttt{Finset}s), subject to the disjointness side condition\n    \\[ W_1 \\cap W_2 \\;=\\; \\emptyset. \\]\n    All three quantifiers ``for every CADMG $G$'', ``for every $W_1 \\subseteq J \\cup V$'', and ``for every $W_2 \\subseteq V$ with $W_1 \\cap W_2 = \\emptyset$'' are read \\emph{universally}; in particular the (vacuously disjoint) case $W_1 = W_2 = \\emptyset$ is admitted.\n\n    \\emph{Reading of ``CADMG'' in the LN's prose.}\n    The LN's literal text opens with ``Let $G = (J, V, E, L)$ be a CADMG'' and refers in the prose to ``the CADMG obtained from $\\ldots$'' for both sides of the displayed equality. Both sides of the equality are produced by applying $\\doit$ (def \\ref{def:G_hard_intervention}) and $\\swig$ (def \\ref{def:G_node-splitting_intervention}) to $G$. The constructor of def \\ref{def:G_node-splitting_intervention} requires the CADMG hypothesis on its input -- on $G$ for the RHS's inner $\\swig(W_2)$, and on $G_{\\doit(W_1)}$ for the LHS's outer $\\swig(W_2)$. Acyclicity of $G_{\\doit(W_1)}$ under the CADMG hypothesis on $G$ is established by \\ref{claim_3_3} (\\emph{AcyclicPreservedUnderDo}); acyclicity of $G_{\\swig(W_2)}$ under the same hypothesis is established by \\ref{claim_3_9} (\\emph{SwigAcyclic}). Hence both sides of the displayed equality are CADMGs. At the underlying CDMG-type level, the asserted equality below is first an equality of CDMGs (def \\ref{def-cdmg}) -- componentwise on the four components $(J, V, E, L)$ -- and the upgrade to an equality of CADMGs is automatic once both sides are known to be acyclic CDMGs (acyclicity is a propositional condition on a fixed CDMG, transported along the CDMG-equality).\n\n    \\emph{Well-typedness of the inner SWIG on the LHS.}\n    The LHS $\\lp G_{\\doit(W_1)} \\rp_{\\swig(W_2)}$ requires its outer $\\swig(W_2)$ to act on a CADMG whose output-node set contains $W_2$, i.e.\\ $W_2 \\subseteq V_{\\doit(W_1)}$ per def \\ref{def:G_node-splitting_intervention}'s precondition (the CADMG-ness of $G_{\\doit(W_1)}$ itself comes from \\ref{claim_3_3} on top of $G$ being a CADMG). By item~ii.\\ of def \\ref{def:G_hard_intervention} we have $V_{\\doit(W_1)} = V \\sm W_1$, and the inclusion\n    \\[ W_2 \\;\\subseteq\\; V \\sm W_1 \\;=\\; V_{\\doit(W_1)} \\]\n    follows from $W_2 \\subseteq V$ together with $W_1 \\cap W_2 = \\emptyset$. Hence the LHS is defined per def \\ref{def:G_node-splitting_intervention}. Well-typedness of the RHS $\\lp G_{\\swig(W_2)} \\rp_{\\doit(W_1)}$ is spelled out in the carrier-reading paragraph below the displayed equality.\n\n    \\emph{The conclusion.}\n    The CADMG obtained from first hard intervening on $W_1$ and then node-splitting (in the SWIG sense of def \\ref{def:G_node-splitting_intervention}) on $W_2$ equals the CADMG obtained from first node-splitting (in the same sense) on $W_2$ and then hard intervening on $W_1$:\n    \\[ \\lp G_{\\doit(W_1)} \\rp_{\\swig(W_2)} \\;=\\; \\lp G_{\\swig(W_2)} \\rp_{\\doit(W_1)}. \\]\n    The equality is understood componentwise on the four components $(J, V, E, L)$ of def \\ref{def-cdmg}: equal input-node sets ($J_{\\text{LHS}} = J_{\\text{RHS}}$), equal output-node sets ($V_{\\text{LHS}} = V_{\\text{RHS}}$), equal directed-edge sets ($E_{\\text{LHS}} = E_{\\text{RHS}}$), and equal bidirected-edge sets ($L_{\\text{LHS}} = L_{\\text{RHS}}$); the conjunctive unpacking into these four field-by-field equalities is deferred to the proof.\n\n    \\medskip\\emph{Carrier reading of the equality.}\n    By def \\ref{def:G_node-splitting_intervention} the operation $\\swig(W_2)$ shifts the carrier from $J \\cup V$ to the tagged-disjoint-union carrier\n    \\[ J_{\\swig(W_2)} \\cup V_{\\swig(W_2)} \\;=\\; \\lp J \\dcup W_2^i \\rp \\;\\cup\\; \\lp (V \\sm W_2) \\dcup W_2^o \\rp, \\]\n    where $W_2^o := \\lC w^o \\st w \\in W_2 \\rC$ and $W_2^i := \\lC w^i \\st w \\in W_2 \\rC$ are the two type-level tagged copies of $W_2$ introduced in def \\ref{def:G_node-splitting_intervention} (so $W_2^o \\cap W_2^i = \\emptyset$ and both $W_2^o$ and $W_2^i$ are disjoint from every element of $J \\cup V$). Crucially, the input-side tagged copy $W_2^i$ sits in $J_{\\swig(W_2)}$ and the output-side tagged copy $W_2^o$ sits in $V_{\\swig(W_2)}$ -- this is the distinguishing feature of $\\swig$ versus the regular node-splitting $\\spl$ of def \\ref{def:G_node-splitting}, whose output-node set carries \\emph{both} tagged copies. By def \\ref{def:G_hard_intervention} the operation $\\doit$ preserves the carrier $J \\cup V$ of its input as a set (items~i.\\ and~ii.\\ give $J_{\\doit(W)} \\cup V_{\\doit(W)} = (J \\cup W) \\cup (V \\sm W) = J \\cup V$ whenever $W \\subseteq J \\cup V$). Thus both sides of the displayed equality live in a CDMG over the same $\\swig(W_2)$-carrier just displayed, and the asserted equality is a \\emph{literal} $=$ of CDMGs (def \\ref{def-cdmg}) -- \\emph{not} an equality up to a carrier-relabelling map. On the right-hand side, $\\doit(W_1)$ is read as hard-intervening on the \\emph{unsplit copies} of the nodes of $W_1$ inside the $\\swig(W_2)$-carrier: every $w \\in W_1$ satisfies $w \\in J \\cup V$ (by $W_1 \\subseteq J \\cup V$) and $w \\notin W_2$ (by $W_1 \\cap W_2 = \\emptyset$), so $w \\in J \\cup (V \\sm W_2)$, which is the untagged piece of $J_{\\swig(W_2)} \\cup V_{\\swig(W_2)}$ under def \\ref{def:G_node-splitting_intervention}'s notational shorthand ``$v^o := v^i := v$ for $v \\in J \\cup (V \\sm W_2)$'' (i.e.\\ $W_1$ injects into the $\\swig(W_2)$-carrier as the unsplit injection $w \\mapsto w$). The precondition $W_1 \\subseteq J_{\\swig(W_2)} \\cup V_{\\swig(W_2)}$ of def \\ref{def:G_hard_intervention} is thereby met by these unsplit copies, so the RHS is also defined per def \\ref{def:G_hard_intervention}, and the LN's $G_{\\swig(W_2)}$ carrier reading agrees with the unique LN-faithful reading of $\\doit(W_1)$ on it.\n\\end{Lem}\n\n\\end{document}\n",
    "html": "<p>Let $G = (J, V, E, L)$ be a CADMG (def <a href=\"#def_3_7\" class=\"refrow\"><code>def-cdmg-types</code></a>, item&nbsp;i., equivalently a CDMG of def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a> that is acyclic in the sense of def <a href=\"#def_3_6\" class=\"refrow\"><code>def-acylic</code></a>); throughout we use the notation of def <a href=\"#def_3_2\" class=\"refrow\"><code>not-cdmg</code></a> and recall the typing and axioms of def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>, namely $J \\cap V = \\emptyset$, $E \\subseteq (J \\cup V) \\times V$, $L \\subseteq V \\times V$, $L$ irreflexive ($(v_1, v_2) \\in L \\Rightarrow v_1 \\neq v_2$), and $L$ symmetric ($(v_1, v_2) \\in L \\Leftrightarrow (v_2, v_1) \\in L$). We rely throughout on the hard-intervention construction $\\doit$ of def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a> and the node-splitting hard-intervention (SWIG) construction $\\swig$ of def <a href=\"#def_3_12\" class=\"refrow\"><code>def:G_node-splitting_intervention</code></a>. Let\n    \\[ W_1 \\subseteq J \\cup V \\quad\\text{and}\\quad W_2 \\subseteq V \\]\n    be two subsets of nodes of $G$ (both automatically finite, since $J \\cup V$ is finite by def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>; this matches the project's Lean encoding of $W_1$ and $W_2$ as <code>Finset</code>s), subject to the disjointness side condition\n    \\[ W_1 \\cap W_2 \\;=\\; \\emptyset. \\]\n    All three quantifiers “for every CADMG $G$”, “for every $W_1 \\subseteq J \\cup V$”, and “for every $W_2 \\subseteq V$ with $W_1 \\cap W_2 = \\emptyset$” are read <em>universally</em>; in particular the (vacuously disjoint) case $W_1 = W_2 = \\emptyset$ is admitted.</p>\n<p><em>Reading of “CADMG” in the LN's prose.</em>\n    The LN's literal text opens with “Let $G = (J, V, E, L)$ be a CADMG” and refers in the prose to “the CADMG obtained from $\\ldots$” for both sides of the displayed equality. Both sides of the equality are produced by applying $\\doit$ (def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a>) and $\\swig$ (def <a href=\"#def_3_12\" class=\"refrow\"><code>def:G_node-splitting_intervention</code></a>) to $G$. The constructor of def <a href=\"#def_3_12\" class=\"refrow\"><code>def:G_node-splitting_intervention</code></a> requires the CADMG hypothesis on its input – on $G$ for the RHS's inner $\\swig(W_2)$, and on $G_{\\doit(W_1)}$ for the LHS's outer $\\swig(W_2)$. Acyclicity of $G_{\\doit(W_1)}$ under the CADMG hypothesis on $G$ is established by <a href=\"#claim_3_3\" class=\"refrow\"><code>claim_3_3</code></a> (<em>AcyclicPreservedUnderDo</em>); acyclicity of $G_{\\swig(W_2)}$ under the same hypothesis is established by <a href=\"#claim_3_9\" class=\"refrow\"><code>claim_3_9</code></a> (<em>SwigAcyclic</em>). Hence both sides of the displayed equality are CADMGs. At the underlying CDMG-type level, the asserted equality below is first an equality of CDMGs (def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>) – componentwise on the four components $(J, V, E, L)$ – and the upgrade to an equality of CADMGs is automatic once both sides are known to be acyclic CDMGs (acyclicity is a propositional condition on a fixed CDMG, transported along the CDMG-equality).</p>\n<p><em>Well-typedness of the inner SWIG on the LHS.</em>\n    The LHS $\\lp G_{\\doit(W_1)} \\rp_{\\swig(W_2)}$ requires its outer $\\swig(W_2)$ to act on a CADMG whose output-node set contains $W_2$, i.e.\\ $W_2 \\subseteq V_{\\doit(W_1)}$ per def <a href=\"#def_3_12\" class=\"refrow\"><code>def:G_node-splitting_intervention</code></a>'s precondition (the CADMG-ness of $G_{\\doit(W_1)}$ itself comes from <a href=\"#claim_3_3\" class=\"refrow\"><code>claim_3_3</code></a> on top of $G$ being a CADMG). By item&nbsp;ii.\\ of def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a> we have $V_{\\doit(W_1)} = V \\sm W_1$, and the inclusion\n    \\[ W_2 \\;\\subseteq\\; V \\sm W_1 \\;=\\; V_{\\doit(W_1)} \\]\n    follows from $W_2 \\subseteq V$ together with $W_1 \\cap W_2 = \\emptyset$. Hence the LHS is defined per def <a href=\"#def_3_12\" class=\"refrow\"><code>def:G_node-splitting_intervention</code></a>. Well-typedness of the RHS $\\lp G_{\\swig(W_2)} \\rp_{\\doit(W_1)}$ is spelled out in the carrier-reading paragraph below the displayed equality.</p>\n<p><em>The conclusion.</em>\n    The CADMG obtained from first hard intervening on $W_1$ and then node-splitting (in the SWIG sense of def <a href=\"#def_3_12\" class=\"refrow\"><code>def:G_node-splitting_intervention</code></a>) on $W_2$ equals the CADMG obtained from first node-splitting (in the same sense) on $W_2$ and then hard intervening on $W_1$:\n    \\[ \\lp G_{\\doit(W_1)} \\rp_{\\swig(W_2)} \\;=\\; \\lp G_{\\swig(W_2)} \\rp_{\\doit(W_1)}. \\]\n    The equality is understood componentwise on the four components $(J, V, E, L)$ of def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>: equal input-node sets ($J_{\\text{LHS}} = J_{\\text{RHS}}$), equal output-node sets ($V_{\\text{LHS}} = V_{\\text{RHS}}$), equal directed-edge sets ($E_{\\text{LHS}} = E_{\\text{RHS}}$), and equal bidirected-edge sets ($L_{\\text{LHS}} = L_{\\text{RHS}}$); the conjunctive unpacking into these four field-by-field equalities is deferred to the proof.</p>\n<p><em>Carrier reading of the equality.</em>\n    By def <a href=\"#def_3_12\" class=\"refrow\"><code>def:G_node-splitting_intervention</code></a> the operation $\\swig(W_2)$ shifts the carrier from $J \\cup V$ to the tagged-disjoint-union carrier\n    \\[ J_{\\swig(W_2)} \\cup V_{\\swig(W_2)} \\;=\\; \\lp J \\dcup W_2^i \\rp \\;\\cup\\; \\lp (V \\sm W_2) \\dcup W_2^o \\rp, \\]\n    where $W_2^o := \\lC w^o \\st w \\in W_2 \\rC$ and $W_2^i := \\lC w^i \\st w \\in W_2 \\rC$ are the two type-level tagged copies of $W_2$ introduced in def <a href=\"#def_3_12\" class=\"refrow\"><code>def:G_node-splitting_intervention</code></a> (so $W_2^o \\cap W_2^i = \\emptyset$ and both $W_2^o$ and $W_2^i$ are disjoint from every element of $J \\cup V$). Crucially, the input-side tagged copy $W_2^i$ sits in $J_{\\swig(W_2)}$ and the output-side tagged copy $W_2^o$ sits in $V_{\\swig(W_2)}$ – this is the distinguishing feature of $\\swig$ versus the regular node-splitting $\\spl$ of def <a href=\"#def_3_11\" class=\"refrow\"><code>def:G_node-splitting</code></a>, whose output-node set carries <em>both</em> tagged copies. By def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a> the operation $\\doit$ preserves the carrier $J \\cup V$ of its input as a set (items&nbsp;i.\\ and&nbsp;ii.\\ give $J_{\\doit(W)} \\cup V_{\\doit(W)} = (J \\cup W) \\cup (V \\sm W) = J \\cup V$ whenever $W \\subseteq J \\cup V$). Thus both sides of the displayed equality live in a CDMG over the same $\\swig(W_2)$-carrier just displayed, and the asserted equality is a <em>literal</em> $=$ of CDMGs (def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>) – <em>not</em> an equality up to a carrier-relabelling map. On the right-hand side, $\\doit(W_1)$ is read as hard-intervening on the <em>unsplit copies</em> of the nodes of $W_1$ inside the $\\swig(W_2)$-carrier: every $w \\in W_1$ satisfies $w \\in J \\cup V$ (by $W_1 \\subseteq J \\cup V$) and $w \\notin W_2$ (by $W_1 \\cap W_2 = \\emptyset$), so $w \\in J \\cup (V \\sm W_2)$, which is the untagged piece of $J_{\\swig(W_2)} \\cup V_{\\swig(W_2)}$ under def <a href=\"#def_3_12\" class=\"refrow\"><code>def:G_node-splitting_intervention</code></a>'s notational shorthand “$v^o := v^i := v$ for $v \\in J \\cup (V \\sm W_2)$” (i.e.\\ $W_1$ injects into the $\\swig(W_2)$-carrier as the unsplit injection $w \\mapsto w$). The precondition $W_1 \\subseteq J_{\\swig(W_2)} \\cup V_{\\swig(W_2)}$ of def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a> is thereby met by these unsplit copies, so the RHS is also defined per def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a>, and the LN's $G_{\\swig(W_2)}$ carrier reading agrees with the unique LN-faithful reading of $\\doit(W_1)$ on it.</p>",
    "env": "Lem",
    "env_title": "Disjoint hard interventions and node-splitting hard interventions commute",
    "source_path": "leanification/Chapter3_GraphTheory/Section3_2/tex/claim_3_11_statement_DisjointHardInterventions.tex"
  },
  "tex_proof": {
    "raw": "% Claim proof file (REFACTOR TWIN) for row claim_3_11 -- \"DisjointHardInterventions\".\n% This is the twin file produced during the `cdmg_typed_edges` refactor;\n% the original `claim_3_11_proof_DisjointHardInterventions.tex` stays\n% untouched until cleanup eventually renames this twin over it.\n%\n% Mathematically the LN-level proof is unchanged by the refactor: at the\n% LN level `L` is `a set of unordered pairs of distinct vertices in V`,\n% which both the original `Finset (Node × Node) + hL_symm` Lean encoding\n% and the refactored `Finset (Sym2 Node)` Lean encoding faithfully\n% capture. There are two prose adjustments versus the original:\n%\n% (i) the L-axiom recitation in the statement-restatement block drops the\n%     explicit ordered-pair phrasing ($L \\subseteq V \\times V$, $L$\n%     irreflexive on ordered pairs, $L$ symmetric in the swap of\n%     components) and uses the unordered-pair phrasing instead;\n%\n% (ii) the \"Bidirected edges\" section of the proof body no longer needs\n%      the \"Registered two-sided removal of $L$\" deviation paragraph: in\n%      the post-refactor `Sym2`-typed encoding the LN's literal one-sided\n%      removal clause $L \\sm \\lC v \\huh w \\mid w \\in W_1 \\rC$ translates\n%      directly to \"all endpoints of the unordered pair lie outside\n%      $W_1$\" (there is no ordered \"second component\" to filter on), so\n%      the LN reading is by construction the same as the Lean encoding\n%      and no symmetrisation step is required. We replace the deviation\n%      paragraph with a short refactor-context note, and use\n%      endpoint-universal wording (\"$v_1, v_2 \\notin W_1$\" read as\n%      \"every endpoint of the bidirected edge lies outside $W_1$\")\n%      throughout the calculation that follows. The calculation itself\n%      is otherwise unchanged, and all SWIG-specific content -- the\n%      \\ref{claim_3_3} citation for inner-LHS-acyclicity (the SWIG\n%      construction of def \\ref{def:G_node-splitting_intervention}\n%      requires a CADMG input), the \"no transfer edges\" remark in the\n%      $E$ section, the carrier-arithmetic observations\n%      $W_1 \\cap W_2^o = W_1 \\cap W_2^i = \\emptyset$, the\n%      $W_2^i \\subseteq J_{\\swig}$ / $W_2^o \\subseteq V_{\\swig}$\n%      placement, and the closing acyclicity-preservation argument via\n%      \\ref{claim_3_9} (\\emph{SwigAcyclic}) for the SWIG side -- is\n%      carried over verbatim from the pre-refactor SWIG twin.\n%\n% This file is a sibling of `main.tex` inside `<subsection>/tex/`. The\n% `subfiles` package lets it render both standalone and as part of\n% `main.tex` via `\\subfile{...}`.\n\n\\documentclass[main]{subfiles}\n\n\\begin{document}\n% ref: claim_3_11 (proof, with statement restated for readability)\n% title: DisjointHardInterventions\n\\def\\rowref{claim\\_3\\_11}\n\\phantomsection\\label{claim_3_11}\n\n% --- statement (restated) ---------------------------------------------------\n% Statement body below is synced from the canonical statement file\n% `tex/claim_3_11_statement_DisjointHardInterventions.tex`, with one\n% light refactor-aware rewording: the L-axiom recitation drops the\n% explicit-ordered-pair phrasing (\"$L \\subseteq V \\times V$, $L$\n% irreflexive ($(v_1, v_2) \\in L \\Rightarrow v_1 \\neq v_2$), and $L$\n% symmetric ($(v_1, v_2) \\in L \\Leftrightarrow (v_2, v_1) \\in L$)\") and\n% replaces it with the unordered-pair phrasing \"$L$ consists of\n% unordered pairs of distinct vertices in $V$\". The mathematics is\n% unchanged and the same as in the LN.\n\n\\begin{Lem}[Disjoint hard interventions and node-splitting hard interventions commute]\\label{disjoint-hard-interventions-and-node-splitting-hard-interventions-commute}\n    Let $G = (J, V, E, L)$ be a CADMG (def \\ref{def-cdmg-types}, item~i., equivalently a CDMG of def \\ref{def-cdmg} that is acyclic in the sense of def \\ref{def-acylic}); throughout we use the notation of def \\ref{not-cdmg} and recall the typing and axioms of def \\ref{def-cdmg}, namely $J \\cap V = \\emptyset$, $E \\subseteq (J \\cup V) \\times V$, and $L$ consists of unordered pairs of distinct vertices in $V$. We rely throughout on the hard-intervention construction $\\doit$ of def \\ref{def:G_hard_intervention} and the node-splitting hard-intervention (SWIG) construction $\\swig$ of def \\ref{def:G_node-splitting_intervention}. Let\n    \\[ W_1 \\subseteq J \\cup V \\quad\\text{and}\\quad W_2 \\subseteq V \\]\n    be two subsets of nodes of $G$ (both automatically finite, since $J \\cup V$ is finite by def \\ref{def-cdmg}; this matches the project's Lean encoding of $W_1$ and $W_2$ as \\texttt{Finset}s), subject to the disjointness side condition\n    \\[ W_1 \\cap W_2 \\;=\\; \\emptyset. \\]\n    All three quantifiers ``for every CADMG $G$'', ``for every $W_1 \\subseteq J \\cup V$'', and ``for every $W_2 \\subseteq V$ with $W_1 \\cap W_2 = \\emptyset$'' are read \\emph{universally}; in particular the (vacuously disjoint) case $W_1 = W_2 = \\emptyset$ is admitted.\n\n    \\emph{Reading of ``CADMG'' in the LN's prose.}\n    The LN's literal text opens with ``Let $G = (J, V, E, L)$ be a CADMG'' and refers in the prose to ``the CADMG obtained from $\\ldots$'' for both sides of the displayed equality. Both sides of the equality are produced by applying $\\doit$ (def \\ref{def:G_hard_intervention}) and $\\swig$ (def \\ref{def:G_node-splitting_intervention}) to $G$. The constructor of def \\ref{def:G_node-splitting_intervention} requires the CADMG hypothesis on its input -- on $G$ for the RHS's inner $\\swig(W_2)$, and on $G_{\\doit(W_1)}$ for the LHS's outer $\\swig(W_2)$. Acyclicity of $G_{\\doit(W_1)}$ under the CADMG hypothesis on $G$ is established by \\ref{claim_3_3} (\\emph{AcyclicPreservedUnderDo}); acyclicity of $G_{\\swig(W_2)}$ under the same hypothesis is established by \\ref{claim_3_9} (\\emph{SwigAcyclic}). Hence both sides of the displayed equality are CADMGs. At the underlying CDMG-type level, the asserted equality below is first an equality of CDMGs (def \\ref{def-cdmg}) -- componentwise on the four components $(J, V, E, L)$ -- and the upgrade to an equality of CADMGs is automatic once both sides are known to be acyclic CDMGs (acyclicity is a propositional condition on a fixed CDMG, transported along the CDMG-equality).\n\n    \\emph{Well-typedness of the inner SWIG on the LHS.}\n    The LHS $\\lp G_{\\doit(W_1)} \\rp_{\\swig(W_2)}$ requires its outer $\\swig(W_2)$ to act on a CADMG whose output-node set contains $W_2$, i.e.\\ $W_2 \\subseteq V_{\\doit(W_1)}$ per def \\ref{def:G_node-splitting_intervention}'s precondition (the CADMG-ness of $G_{\\doit(W_1)}$ itself comes from \\ref{claim_3_3} on top of $G$ being a CADMG). By item~ii.\\ of def \\ref{def:G_hard_intervention} we have $V_{\\doit(W_1)} = V \\sm W_1$, and the inclusion\n    \\[ W_2 \\;\\subseteq\\; V \\sm W_1 \\;=\\; V_{\\doit(W_1)} \\]\n    follows from $W_2 \\subseteq V$ together with $W_1 \\cap W_2 = \\emptyset$. Hence the LHS is defined per def \\ref{def:G_node-splitting_intervention}. Well-typedness of the RHS $\\lp G_{\\swig(W_2)} \\rp_{\\doit(W_1)}$ is spelled out in the carrier-reading paragraph below the displayed equality.\n\n    \\emph{The conclusion.}\n    The CADMG obtained from first hard intervening on $W_1$ and then node-splitting (in the SWIG sense of def \\ref{def:G_node-splitting_intervention}) on $W_2$ equals the CADMG obtained from first node-splitting (in the same sense) on $W_2$ and then hard intervening on $W_1$:\n    \\[ \\lp G_{\\doit(W_1)} \\rp_{\\swig(W_2)} \\;=\\; \\lp G_{\\swig(W_2)} \\rp_{\\doit(W_1)}. \\]\n    The equality is understood componentwise on the four components $(J, V, E, L)$ of def \\ref{def-cdmg}: equal input-node sets ($J_{\\text{LHS}} = J_{\\text{RHS}}$), equal output-node sets ($V_{\\text{LHS}} = V_{\\text{RHS}}$), equal directed-edge sets ($E_{\\text{LHS}} = E_{\\text{RHS}}$), and equal bidirected-edge sets ($L_{\\text{LHS}} = L_{\\text{RHS}}$); the conjunctive unpacking into these four field-by-field equalities is deferred to the proof.\n\n    \\medskip\\emph{Carrier reading of the equality.}\n    By def \\ref{def:G_node-splitting_intervention} the operation $\\swig(W_2)$ shifts the carrier from $J \\cup V$ to the tagged-disjoint-union carrier\n    \\[ J_{\\swig(W_2)} \\cup V_{\\swig(W_2)} \\;=\\; \\lp J \\dcup W_2^i \\rp \\;\\cup\\; \\lp (V \\sm W_2) \\dcup W_2^o \\rp, \\]\n    where $W_2^o := \\lC w^o \\st w \\in W_2 \\rC$ and $W_2^i := \\lC w^i \\st w \\in W_2 \\rC$ are the two type-level tagged copies of $W_2$ introduced in def \\ref{def:G_node-splitting_intervention} (so $W_2^o \\cap W_2^i = \\emptyset$ and both $W_2^o$ and $W_2^i$ are disjoint from every element of $J \\cup V$). Crucially, the input-side tagged copy $W_2^i$ sits in $J_{\\swig(W_2)}$ and the output-side tagged copy $W_2^o$ sits in $V_{\\swig(W_2)}$ -- this is the distinguishing feature of $\\swig$ versus the regular node-splitting $\\spl$ of def \\ref{def:G_node-splitting}, whose output-node set carries \\emph{both} tagged copies. By def \\ref{def:G_hard_intervention} the operation $\\doit$ preserves the carrier $J \\cup V$ of its input as a set (items~i.\\ and~ii.\\ give $J_{\\doit(W)} \\cup V_{\\doit(W)} = (J \\cup W) \\cup (V \\sm W) = J \\cup V$ whenever $W \\subseteq J \\cup V$). Thus both sides of the displayed equality live in a CDMG over the same $\\swig(W_2)$-carrier just displayed, and the asserted equality is a \\emph{literal} $=$ of CDMGs (def \\ref{def-cdmg}) -- \\emph{not} an equality up to a carrier-relabelling map. On the right-hand side, $\\doit(W_1)$ is read as hard-intervening on the \\emph{unsplit copies} of the nodes of $W_1$ inside the $\\swig(W_2)$-carrier: every $w \\in W_1$ satisfies $w \\in J \\cup V$ (by $W_1 \\subseteq J \\cup V$) and $w \\notin W_2$ (by $W_1 \\cap W_2 = \\emptyset$), so $w \\in J \\cup (V \\sm W_2)$, which is the untagged piece of $J_{\\swig(W_2)} \\cup V_{\\swig(W_2)}$ under def \\ref{def:G_node-splitting_intervention}'s notational shorthand ``$v^o := v^i := v$ for $v \\in J \\cup (V \\sm W_2)$'' (i.e.\\ $W_1$ injects into the $\\swig(W_2)$-carrier as the unsplit injection $w \\mapsto w$). The precondition $W_1 \\subseteq J_{\\swig(W_2)} \\cup V_{\\swig(W_2)}$ of def \\ref{def:G_hard_intervention} is thereby met by these unsplit copies, so the RHS is also defined per def \\ref{def:G_hard_intervention}, and the LN's $G_{\\swig(W_2)}$ carrier reading agrees with the unique LN-faithful reading of $\\doit(W_1)$ on it.\n\\end{Lem}\n\n% --- proof ------------------------------------------------------------------\n% Proof body adapted from the LN's \\Claude{...} block at\n% lecture-notes/lecture_notes/graphs.tex lines 672-700, with the following\n% refinements (carried over from the pre-refactor SWIG twin, except for\n% the bidirected-edge refactor-context note flagged in (iv)):\n% (i) explicit citation of \\ref{claim_3_3} (AcyclicPreservedUnderDo) for\n%     the CADMG-hood of G_{\\doit(W_1)} required by the inner \\swig(W_2) on\n%     the LHS (the LN's terse opening sentence \"both operations are\n%     well-defined in either order\" glossed over this; the SWIG construction\n%     of def_3_12 -- in contrast to the spl construction of def_3_11 used in\n%     claim_3_8 -- requires its input to be a CADMG, so an acyclicity\n%     witness must be exhibited);\n% (ii) the LN's terse parenthetical \"with W_1 \\cap W_2^o = \\emptyset\" is\n%      unpacked into an explicit decomposition of ((V \\sm W_2) \\dcup W_2^o)\n%      \\sm W_1 that splits the intersection across the two disjoint pieces,\n%      citing the SWIG-specific tagged-construction disjointness\n%      W_1 \\cap W_2^o = W_1 \\cap W_2^i = \\emptyset at the tagged pieces and\n%      the hypothesis W_1 \\cap W_2 = \\emptyset only on the untagged piece;\n% (iii) the LN's \"head v_2^o \\in W_1 iff v_2 \\in W_1\" claim is unpacked\n%       into two sub-cases on whether v_2 \\in W_2 or v_2 \\in V \\sm W_2, with\n%       the disjointness W_1 \\cap W_2 = \\emptyset cross-checked in the\n%       v_2 \\in W_2 branch;\n% (iv) in the post-refactor `Sym2`-typed encoding the LN's literal\n%      one-sided removal clause translates directly to \"every endpoint\n%      of the unordered pair lies outside $W_1$\" (there is no ordered\n%      \"second component\" to filter on), so the \"Registered two-sided\n%      removal of $L$\" deviation paragraph carried by the pre-refactor\n%      SWIG twin is no longer needed; it is replaced here by a short\n%      refactor-context note, and the calculation that follows uses\n%      endpoint-universal wording for the single removal condition\n%      \"$v_1, v_2 \\notin W_1$\" and unordered-pair notation\n%      \"$v_1 \\huh v_2$\" in place of the pre-refactor ordered-pair\n%      notation \"$(v_1, v_2)$\".\n% The proof is organised under the LN's three section headings\n% (Node sets / Directed edges / Bidirected edges); the \"Node sets\" section\n% covers the J and V fields jointly, matching the LN's grouping and\n% the four-field componentwise check of the downstream Lean proof.\n\\begin{proof}\nWe verify the equality $\\lp G_{\\doit(W_1)} \\rp_{\\swig(W_2)} = \\lp G_{\\swig(W_2)} \\rp_{\\doit(W_1)}$ componentwise on the four fields $(J, V, E, L)$ of def \\ref{def-cdmg}, organised as in the LN under three section headings: ``Node sets'' (covering $J$ and $V$ jointly), ``Directed edges'' (the $E$ field), and ``Bidirected edges'' (the $L$ field).\n\n\\smallskip\\noindent\\emph{Well-typedness of both iteration orders.}\nFor the LHS $\\lp G_{\\doit(W_1)} \\rp_{\\swig(W_2)}$: the outer $\\swig(W_2)$ of def \\ref{def:G_node-splitting_intervention} requires (a) its input to be a CADMG and (b) $W_2 \\subseteq V_{\\doit(W_1)}$. For (a), $G_{\\doit(W_1)}$ is itself a CADMG by \\ref{claim_3_3} (\\emph{AcyclicPreservedUnderDo}) applied to the standing CADMG hypothesis on $G$ -- this is the SWIG-specific acyclicity step that the spl analog (\\refrow{claim_3_8}) did not need, since the regular node-splitting $\\spl$ of def \\ref{def:G_node-splitting} does not require a CADMG input. For (b), by def \\ref{def:G_hard_intervention} item~ii., $V_{\\doit(W_1)} = V \\sm W_1$, and the inclusion $W_2 \\subseteq V \\sm W_1$ follows from $W_2 \\subseteq V$ together with $W_1 \\cap W_2 = \\emptyset$. For the RHS $\\lp G_{\\swig(W_2)} \\rp_{\\doit(W_1)}$: the outer $\\doit(W_1)$ of def \\ref{def:G_hard_intervention} requires only $W_1 \\subseteq J_{\\swig(W_2)} \\cup V_{\\swig(W_2)}$, which holds by the carrier-reading paragraph of the statement above: every $w \\in W_1$ satisfies $w \\in J \\cup V$ (from $W_1 \\subseteq J \\cup V$) and $w \\notin W_2$ (from $W_1 \\cap W_2 = \\emptyset$), so $w \\in J \\cup (V \\sm W_2)$, the untagged piece of $J_{\\swig(W_2)} \\cup V_{\\swig(W_2)}$ under def \\ref{def:G_node-splitting_intervention}'s unsplit shorthand $v^o := v^i := v$.\n\n\\smallskip\\noindent\\emph{Notational reminder.}\nFor $w \\in W_2$ we write $w^o$, $w^i$ for the two tagged copies produced by $\\swig(W_2)$ per def \\ref{def:G_node-splitting_intervention}; these live in the tagged-disjoint-union pieces $W_2^o$ and $W_2^i$ respectively, and are formally distinct from every element of $J \\cup V$ (and from each other), per def \\ref{def:G_node-splitting_intervention}'s tagged-copies disjointness assertions $W_2^o \\cap V = W_2^i \\cap V = W_2^o \\cap J = W_2^i \\cap J = W_2^o \\cap W_2^i = \\emptyset$. For $v \\in J \\cup (V \\sm W_2)$ we use def \\ref{def:G_node-splitting_intervention}'s unsplit shorthand $v^o := v^i := v$, which leaves $v$ in the untagged piece of $J_{\\swig(W_2)} \\cup V_{\\swig(W_2)}$. \\emph{Crucially for SWIG, the input copies $W_2^i$ sit in $J_{\\swig(W_2)}$ and the output copies $W_2^o$ sit in $V_{\\swig(W_2)}$} -- the distinguishing feature of $\\swig$ versus the regular node-splitting $\\spl$ of def \\ref{def:G_node-splitting}, whose output-node set carries \\emph{both} tagged copies.\n\nOn the right-hand side, the outer $\\doit(W_1)$ is read on the lifted intervention set ``$W_1$ inside the $\\swig(W_2)$-carrier'', which by the carrier-reading paragraph of the statement is the unsplit image $\\lC w \\st w \\in W_1 \\rC \\subseteq J \\cup (V \\sm W_2)$ (using $W_1 \\subseteq J \\cup V$ and $W_1 \\cap W_2 = \\emptyset$); throughout the proof we write $W_1$ for this lifted set, using the unsplit shorthand $w = w^o = w^i$ for $w \\in W_1$. The SWIG-specific carrier-arithmetic observation we use repeatedly below is\n\\[ W_1 \\cap W_2^o \\;=\\; \\emptyset \\qquad\\text{and}\\qquad W_1 \\cap W_2^i \\;=\\; \\emptyset, \\]\nboth of which follow from $W_1 \\subseteq J \\cup V$ together with the tagged-construction disjointness $W_2^o \\cap (J \\cup V) = W_2^i \\cap (J \\cup V) = \\emptyset$ of def \\ref{def:G_node-splitting_intervention}.\n\n``Head'' of a directed edge $(a, b) \\in E$ means its second component $b$; ``endpoints'' of a bidirected edge $v_1 \\huh v_2 \\in L$ means the unordered pair $\\{v_1, v_2\\}$ of its two vertices.\n\n\\medskip\\noindent\\emph{Node sets.}\n\n\\smallskip\\noindent\\emph{Input nodes ($J$).}\nWe show\n\\[ J_{\\lp G_{\\doit(W_1)} \\rp_{\\swig(W_2)}} \\;=\\; J_{\\lp G_{\\swig(W_2)} \\rp_{\\doit(W_1)}}. \\]\nBy def \\ref{def:G_hard_intervention} item~i.\\ applied to $G$ with intervention set $W_1$,\n\\[ J_{G_{\\doit(W_1)}} \\;=\\; J \\cup W_1. \\]\nBy def \\ref{def:G_node-splitting_intervention} item~i.\\ applied to $G_{\\doit(W_1)}$ with split set $W_2$ (well-typed by $W_2 \\subseteq V \\sm W_1 = V_{\\doit(W_1)}$),\n\\[ J_{\\lp G_{\\doit(W_1)} \\rp_{\\swig(W_2)}} \\;=\\; J_{G_{\\doit(W_1)}} \\dcup W_2^i \\;=\\; (J \\cup W_1) \\dcup W_2^i. \\]\nThe $\\dcup$ here is a literal disjoint union: $W_2^i$ is disjoint from $J \\cup W_1$ since $W_1 \\subseteq J \\cup V$ gives $(J \\cup W_1) \\subseteq J \\cup V$, and $W_2^i \\cap (J \\cup V) = \\emptyset$ by the tagged-construction disjointness from the notational reminder.\n\nFor the RHS, by def \\ref{def:G_node-splitting_intervention} item~i.\\ applied to $G$ with split set $W_2$,\n\\[ J_{G_{\\swig(W_2)}} \\;=\\; J \\dcup W_2^i. \\]\nBy def \\ref{def:G_hard_intervention} item~i.\\ applied to $G_{\\swig(W_2)}$ with intervention set $W_1$ (lifted via the unsplit injection per the well-typedness paragraph),\n\\[ J_{\\lp G_{\\swig(W_2)} \\rp_{\\doit(W_1)}} \\;=\\; J_{G_{\\swig(W_2)}} \\cup W_1 \\;=\\; (J \\dcup W_2^i) \\cup W_1. \\]\nBy $W_1 \\cap W_2^i = \\emptyset$ (from the notational reminder), adjoining $W_1$ to $J \\dcup W_2^i$ folds $W_1$ into the $J$ piece while leaving the $W_2^i$ piece untouched:\n\\[ (J \\dcup W_2^i) \\cup W_1 \\;=\\; (J \\cup W_1) \\dcup W_2^i. \\]\nHence\n\\[ J_{\\lp G_{\\doit(W_1)} \\rp_{\\swig(W_2)}} \\;=\\; (J \\cup W_1) \\dcup W_2^i \\;=\\; J_{\\lp G_{\\swig(W_2)} \\rp_{\\doit(W_1)}}. \\]\n\\checkmark\n\n\\smallskip\\noindent\\emph{Output nodes ($V$).}\nWe show\n\\[ V_{\\lp G_{\\doit(W_1)} \\rp_{\\swig(W_2)}} \\;=\\; V_{\\lp G_{\\swig(W_2)} \\rp_{\\doit(W_1)}}. \\]\nBy def \\ref{def:G_hard_intervention} item~ii.\\ applied to $G$ with intervention set $W_1$ and then def \\ref{def:G_node-splitting_intervention} item~ii.\\ applied to $G_{\\doit(W_1)}$ with split set $W_2 \\subseteq V \\sm W_1 = V_{\\doit(W_1)}$,\n\\[\n    V_{\\lp G_{\\doit(W_1)} \\rp_{\\swig(W_2)}}\n    \\;=\\; \\lp V_{G_{\\doit(W_1)}} \\sm W_2 \\rp \\;\\dcup\\; W_2^o\n    \\;=\\; \\lp (V \\sm W_1) \\sm W_2 \\rp \\;\\dcup\\; W_2^o,\n\\]\nwhich simplifies (by $(V \\sm W_1) \\sm W_2 = V \\sm W_1 \\sm W_2$, the standard ``two-step removal'' identity for set-difference) to\n\\[\n    V_{\\lp G_{\\doit(W_1)} \\rp_{\\swig(W_2)}}\n    \\;=\\; (V \\sm W_1 \\sm W_2) \\;\\dcup\\; W_2^o.\n\\]\n\nFor the RHS, by def \\ref{def:G_node-splitting_intervention} item~ii.\\ applied to $G$ with split set $W_2$ and then def \\ref{def:G_hard_intervention} item~ii.\\ applied to $G_{\\swig(W_2)}$ with intervention set $W_1$ (lifted to the SWIG carrier via the unsplit injection),\n\\[\n    V_{\\lp G_{\\swig(W_2)} \\rp_{\\doit(W_1)}}\n    \\;=\\; V_{G_{\\swig(W_2)}} \\sm W_1\n    \\;=\\; \\lp (V \\sm W_2) \\;\\dcup\\; W_2^o \\rp \\sm W_1.\n\\]\nWe simplify the latter by decomposing the set-difference across the two disjoint pieces of the disjoint union, using both the disjointness hypothesis $W_1 \\cap W_2 = \\emptyset$ (on the untagged piece) and the SWIG-specific tagged-construction disjointness (on the tagged piece):\n\\begin{itemize}\n    \\item $W_1 \\cap W_2^o = \\emptyset$: this is the SWIG-specific carrier-arithmetic observation from the notational reminder. Since the lifted $W_1$ sits in the untagged piece $J \\cup (V \\sm W_2) \\subseteq J \\cup V$, and $W_2^o \\cap (J \\cup V) = \\emptyset$ by def \\ref{def:G_node-splitting_intervention}'s tagged-construction disjointness, the lifted $W_1$ does not meet $W_2^o$. Hence the set-difference acts as the identity on $W_2^o$.\n    \\item $(V \\sm W_2) \\sm W_1 = V \\sm W_1 \\sm W_2$: indeed, for any $x$, the chain of equivalences $x \\in (V \\sm W_2) \\sm W_1$ iff $x \\in V$, $x \\notin W_2$, $x \\notin W_1$ iff $x \\in V$, $x \\notin W_1$, $x \\notin W_2$ iff $x \\in V \\sm W_1 \\sm W_2$ holds. (Here the disjointness $W_1 \\cap W_2 = \\emptyset$ is \\emph{not} needed for this equality; the two iterated removals commute by associativity of $\\sm$ alone.)\n\\end{itemize}\nCombining the two piecewise calculations,\n\\[\n    \\lp (V \\sm W_2) \\;\\dcup\\; W_2^o \\rp \\sm W_1\n    \\;=\\; \\lp (V \\sm W_2) \\sm W_1 \\rp \\;\\dcup\\; W_2^o\n    \\;=\\; (V \\sm W_1 \\sm W_2) \\;\\dcup\\; W_2^o.\n\\]\nHence\n\\[\n    V_{\\lp G_{\\doit(W_1)} \\rp_{\\swig(W_2)}}\n    \\;=\\; (V \\sm W_1 \\sm W_2) \\;\\dcup\\; W_2^o\n    \\;=\\; V_{\\lp G_{\\swig(W_2)} \\rp_{\\doit(W_1)}}.\n\\]\n\\checkmark\n\n\\medskip\\noindent\\emph{Directed edges.}\nWe show\n\\[ E_{\\lp G_{\\doit(W_1)} \\rp_{\\swig(W_2)}} \\;=\\; E_{\\lp G_{\\swig(W_2)} \\rp_{\\doit(W_1)}}. \\]\nBy def \\ref{def:G_hard_intervention} item~iii.\\ applied to $G$ with intervention set $W_1$,\n\\[\n    E_{G_{\\doit(W_1)}}\n    \\;=\\; E \\sm \\lC (v_1, v_2) \\in E \\st v_2 \\in W_1 \\rC\n    \\;=\\; \\lC (v_1, v_2) \\in E \\st v_2 \\notin W_1 \\rC.\n\\]\nBy def \\ref{def:G_node-splitting_intervention} item~iii.\\ applied to $G_{\\doit(W_1)}$ with split set $W_2$ (every surviving directed edge $(v_1, v_2) \\in E_{G_{\\doit(W_1)}}$ is lifted to $(v_1^i, v_2^o)$; note that the SWIG construction of def \\ref{def:G_node-splitting_intervention} introduces \\emph{no} transfer edges, in contrast to the regular node-splitting $\\spl$ of def \\ref{def:G_node-splitting}),\n\\[\n    E_{\\lp G_{\\doit(W_1)} \\rp_{\\swig(W_2)}}\n    \\;=\\; \\lC (v_1^i, v_2^o) \\st (v_1, v_2) \\in E_{G_{\\doit(W_1)}} \\rC\n    \\;=\\; \\lC (v_1^i, v_2^o) \\st (v_1, v_2) \\in E,\\; v_2 \\notin W_1 \\rC.\n\\]\n\nFor the RHS, by def \\ref{def:G_node-splitting_intervention} item~iii.\\ applied to $G$ with split set $W_2$,\n\\[\n    E_{G_{\\swig(W_2)}}\n    \\;=\\; \\lC (v_1^i, v_2^o) \\st (v_1, v_2) \\in E \\rC.\n\\]\nBy def \\ref{def:G_hard_intervention} item~iii.\\ applied to $G_{\\swig(W_2)}$ with intervention set $W_1$ (lifted to the SWIG carrier via the unsplit injection),\n\\[\n    E_{\\lp G_{\\swig(W_2)} \\rp_{\\doit(W_1)}}\n    \\;=\\; E_{G_{\\swig(W_2)}} \\sm \\lC (a, b) \\in E_{G_{\\swig(W_2)}} \\st b \\in W_1 \\rC.\n\\]\nThe removal condition ``$b \\in W_1$'' inspects the head $b = v_2^o$ of each candidate generator $(v_1^i, v_2^o)$ arising from $(v_1, v_2) \\in E$. By def \\ref{def-cdmg}'s typing $E \\subseteq (J \\cup V) \\times V$ we have $v_2 \\in V$, so we case-split on whether $v_2 \\in W_2$:\n\\begin{itemize}\n    \\item \\emph{If $v_2 \\in W_2$:} $v_2^o \\in W_2^o$ is a tagged copy, lying in the disjoint-union piece $W_2^o$. By $W_1 \\cap W_2^o = \\emptyset$ (from the notational reminder), $v_2^o \\notin W_1$ (read in the SWIG carrier via the unsplit lift). The edge \\emph{survives} the RHS removal. Cross-check with the LHS: by the disjointness hypothesis $W_1 \\cap W_2 = \\emptyset$, $v_2 \\in W_2$ also forces $v_2 \\notin W_1$, so the LHS-side condition ``$v_2 \\notin W_1$'' is automatically met -- both sides keep this edge.\n    \\item \\emph{If $v_2 \\in V \\sm W_2$:} $v_2^o = v_2$ (unsplit shorthand, since $v_2 \\in V \\sm W_2 \\subseteq J \\cup (V \\sm W_2)$). The lifted $W_1$ in the SWIG carrier is the unsplit image $\\lC w \\st w \\in W_1 \\rC$, so $v_2^o \\in W_1$ iff $v_2 \\in W_1$ (the unsplit injection $J \\cup (V \\sm W_2) \\hookrightarrow J_{\\swig(W_2)} \\cup V_{\\swig(W_2)}$ is injective). The edge is removed by the RHS iff $v_2 \\in W_1$, equivalently kept iff $v_2 \\notin W_1$. This matches the LHS condition ``$v_2 \\notin W_1$'' verbatim.\n\\end{itemize}\nCombining the two sub-cases, the generator $(v_1^i, v_2^o)$ from $(v_1, v_2) \\in E$ survives the RHS removal iff $v_2 \\notin W_1$, matching the LHS surviving condition exactly. (In the $v_2 \\in W_2$ branch this is supplied automatically by the disjointness $W_1 \\cap W_2 = \\emptyset$; in the $v_2 \\in V \\sm W_2$ branch it is read off the unsplit identification.) Therefore\n\\[\n    E_{\\lp G_{\\swig(W_2)} \\rp_{\\doit(W_1)}}\n    \\;=\\; \\lC (v_1^i, v_2^o) \\st (v_1, v_2) \\in E,\\; v_2 \\notin W_1 \\rC.\n\\]\nComparing with the LHS,\n\\[\n    E_{\\lp G_{\\doit(W_1)} \\rp_{\\swig(W_2)}}\n    \\;=\\; \\lC (v_1^i, v_2^o) \\st (v_1, v_2) \\in E,\\; v_2 \\notin W_1 \\rC\n    \\;=\\; E_{\\lp G_{\\swig(W_2)} \\rp_{\\doit(W_1)}}.\n\\]\n\\checkmark\n\n\\medskip\\noindent\\emph{Bidirected edges.}\nWe show\n\\[ L_{\\lp G_{\\doit(W_1)} \\rp_{\\swig(W_2)}} \\;=\\; L_{\\lp G_{\\swig(W_2)} \\rp_{\\doit(W_1)}}. \\]\n\n\\smallskip\\noindent\\emph{Refactor-context note on the removal clause of $\\doit$ for $L$.}\nAt the LN level the bidirected-edge set $L$ consists of unordered pairs of distinct vertices in $V$, and def \\ref{def:G_hard_intervention} item~iv.\\ removes those pairs that touch $W_1$. The LN's literal one-sided gloss ``$L \\sm \\lC v \\huh w \\mid w \\in W_1 \\rC$'' selects an ordered-pair representative of each unordered pair and tests its second component, which presumes an ordered-pair encoding of $L$. The post-refactor encoding of def \\ref{def-cdmg} (and downstream def \\ref{def:G_hard_intervention}) carries $L$ as a set of \\emph{unordered} pairs, so there is no ordered ``second component'' to single out: the structurally well-defined reading of the LN's removal clause is the endpoint-universal form\n\\[\n    L_{\\doit(W)} \\;=\\; \\lC v_1 \\huh v_2 \\in L \\st v_1 \\notin W,\\; v_2 \\notin W \\rC,\n\\]\ni.e.\\ ``$v_1 \\huh v_2$ survives iff every endpoint of the unordered pair lies outside $W$''. This is precisely the LN's intended ``remove all edges into nodes from $W$'' applied to bidirected edges (which are incident to \\emph{both} endpoints) and agrees on the survivor set with the LN's literal one-sided gloss after closing under the implicit swap of representatives. The pre-refactor encoding of def \\ref{def:G_hard_intervention} had to spell this symmetrisation out as a registered deviation; under the post-refactor unordered-pair encoding the LN-faithful reading is the literal one and no symmetrisation step is required. We use the endpoint-universal form throughout the calculations below.\n\nBy def \\ref{def:G_hard_intervention} item~iv.\\ (endpoint-universal form), applied to $G$ with intervention set $W_1$,\n\\[\n    L_{G_{\\doit(W_1)}}\n    \\;=\\; \\lC v_1 \\huh v_2 \\in L \\st v_1 \\notin W_1,\\; v_2 \\notin W_1 \\rC.\n\\]\nBy def \\ref{def:G_node-splitting_intervention} item~iv.\\ applied to $G_{\\doit(W_1)}$ with split set $W_2$ (every surviving bidirected edge $v_1 \\huh v_2 \\in L_{G_{\\doit(W_1)}}$ is lifted to $v_1^o \\huh v_2^o$, with \\emph{both} endpoints carrying the output-side $^o$ superscript -- in particular no element of $W_2^i$ appears as an endpoint of any bidirected edge),\n\\[\n    L_{\\lp G_{\\doit(W_1)} \\rp_{\\swig(W_2)}}\n    \\;=\\; \\lC v_1^o \\huh v_2^o \\st v_1 \\huh v_2 \\in L_{G_{\\doit(W_1)}} \\rC\n    \\;=\\; \\lC v_1^o \\huh v_2^o \\st v_1 \\huh v_2 \\in L,\\; v_1 \\notin W_1,\\; v_2 \\notin W_1 \\rC.\n\\]\n\nFor the RHS, by def \\ref{def:G_node-splitting_intervention} item~iv.\\ applied to $G$ with split set $W_2$,\n\\[\n    L_{G_{\\swig(W_2)}}\n    \\;=\\; \\lC v_1^o \\huh v_2^o \\st v_1 \\huh v_2 \\in L \\rC.\n\\]\nBy def \\ref{def:G_hard_intervention} item~iv.\\ (endpoint-universal form), applied to $G_{\\swig(W_2)}$ with intervention set $W_1$ (lifted to the SWIG carrier via the unsplit injection),\n\\[\n    L_{\\lp G_{\\swig(W_2)} \\rp_{\\doit(W_1)}}\n    \\;=\\; \\lC a \\huh b \\in L_{G_{\\swig(W_2)}} \\st a \\notin W_1,\\; b \\notin W_1 \\rC.\n\\]\nWe compute the surviving pairs by analysing each endpoint of the generator $v_1^o \\huh v_2^o$ arising from $v_1 \\huh v_2 \\in L$. Since $L$ consists of unordered pairs of distinct vertices in $V$, both $v_1, v_2 \\in V$. For each $k \\in \\{1, 2\\}$:\n\\begin{itemize}\n    \\item \\emph{If $v_k \\in W_2$:} $v_k^o \\in W_2^o$ is a tagged copy. By $W_1 \\cap W_2^o = \\emptyset$ (from the notational reminder), $v_k^o \\notin W_1$ (in the SWIG carrier). Cross-check with the LHS: by the disjointness hypothesis $W_1 \\cap W_2 = \\emptyset$, $v_k \\in W_2$ also forces $v_k \\notin W_1$, so both the RHS-side condition ``$v_k^o \\notin W_1$'' and the LHS-side condition ``$v_k \\notin W_1$'' are satisfied automatically in this branch.\n    \\item \\emph{If $v_k \\in V \\sm W_2$:} $v_k^o = v_k$ (unsplit shorthand, since $v_k \\in V \\sm W_2 \\subseteq J \\cup (V \\sm W_2)$). The lifted $W_1$ in the SWIG carrier is the unsplit image $\\lC w \\st w \\in W_1 \\rC$, so $v_k^o \\in W_1$ iff $v_k \\in W_1$ (the unsplit injection is injective). Hence the RHS-side condition ``$v_k^o \\notin W_1$'' is equivalent to the LHS-side condition ``$v_k \\notin W_1$''.\n\\end{itemize}\nIn both sub-cases the condition ``$v_k^o \\notin W_1$'' is equivalent to ``$v_k \\notin W_1$''. Applying this to both endpoints $v_1$ and $v_2$ independently,\n\\[\n    L_{\\lp G_{\\swig(W_2)} \\rp_{\\doit(W_1)}}\n    \\;=\\; \\lC v_1^o \\huh v_2^o \\st v_1 \\huh v_2 \\in L,\\; v_1 \\notin W_1,\\; v_2 \\notin W_1 \\rC.\n\\]\nComparing with the LHS,\n\\[\n    L_{\\lp G_{\\doit(W_1)} \\rp_{\\swig(W_2)}}\n    \\;=\\; \\lC v_1^o \\huh v_2^o \\st v_1 \\huh v_2 \\in L,\\; v_1 \\notin W_1,\\; v_2 \\notin W_1 \\rC\n    \\;=\\; L_{\\lp G_{\\swig(W_2)} \\rp_{\\doit(W_1)}}.\n\\]\n\\checkmark\n\n\\smallskip\nCombining the four componentwise equalities ($J$, $V$, $E$, $L$) above, the CDMGs $\\lp G_{\\doit(W_1)} \\rp_{\\swig(W_2)}$ and $\\lp G_{\\swig(W_2)} \\rp_{\\doit(W_1)}$ agree field by field on each of $(J, V, E, L)$ and are therefore equal as CDMGs (def \\ref{def-cdmg}). The upgrade to an equality of CADMGs is automatic: acyclicity is a propositional condition on a fixed CDMG and transports along the CDMG-equality (acyclicity of the LHS follows from \\ref{claim_3_9} (\\emph{SwigAcyclic}) applied to $G_{\\doit(W_1)}$, which is itself a CADMG by \\ref{claim_3_3}; acyclicity of the RHS follows from \\ref{claim_3_3} applied to $G_{\\swig(W_2)}$, which is itself a CADMG by \\ref{claim_3_9}).\n\\end{proof}\n\n\\end{document}\n",
    "html": "<div class=\"theorem-block\"><div class=\"theorem-label\">Lemma — <span class=\"theorem-title\">Disjoint hard interventions and node-splitting hard interventions commute</span></div><p>Let $G = (J, V, E, L)$ be a CADMG (def <a href=\"#def_3_7\" class=\"refrow\"><code>def-cdmg-types</code></a>, item&nbsp;i., equivalently a CDMG of def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a> that is acyclic in the sense of def <a href=\"#def_3_6\" class=\"refrow\"><code>def-acylic</code></a>); throughout we use the notation of def <a href=\"#def_3_2\" class=\"refrow\"><code>not-cdmg</code></a> and recall the typing and axioms of def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>, namely $J \\cap V = \\emptyset$, $E \\subseteq (J \\cup V) \\times V$, and $L$ consists of unordered pairs of distinct vertices in $V$. We rely throughout on the hard-intervention construction $\\doit$ of def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a> and the node-splitting hard-intervention (SWIG) construction $\\swig$ of def <a href=\"#def_3_12\" class=\"refrow\"><code>def:G_node-splitting_intervention</code></a>. Let\n    \\[ W_1 \\subseteq J \\cup V \\quad\\text{and}\\quad W_2 \\subseteq V \\]\n    be two subsets of nodes of $G$ (both automatically finite, since $J \\cup V$ is finite by def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>; this matches the project's Lean encoding of $W_1$ and $W_2$ as <code>Finset</code>s), subject to the disjointness side condition\n    \\[ W_1 \\cap W_2 \\;=\\; \\emptyset. \\]\n    All three quantifiers “for every CADMG $G$”, “for every $W_1 \\subseteq J \\cup V$”, and “for every $W_2 \\subseteq V$ with $W_1 \\cap W_2 = \\emptyset$” are read <em>universally</em>; in particular the (vacuously disjoint) case $W_1 = W_2 = \\emptyset$ is admitted.</p>\n<p><em>Reading of “CADMG” in the LN's prose.</em>\n    The LN's literal text opens with “Let $G = (J, V, E, L)$ be a CADMG” and refers in the prose to “the CADMG obtained from $\\ldots$” for both sides of the displayed equality. Both sides of the equality are produced by applying $\\doit$ (def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a>) and $\\swig$ (def <a href=\"#def_3_12\" class=\"refrow\"><code>def:G_node-splitting_intervention</code></a>) to $G$. The constructor of def <a href=\"#def_3_12\" class=\"refrow\"><code>def:G_node-splitting_intervention</code></a> requires the CADMG hypothesis on its input – on $G$ for the RHS's inner $\\swig(W_2)$, and on $G_{\\doit(W_1)}$ for the LHS's outer $\\swig(W_2)$. Acyclicity of $G_{\\doit(W_1)}$ under the CADMG hypothesis on $G$ is established by <a href=\"#claim_3_3\" class=\"refrow\"><code>claim_3_3</code></a> (<em>AcyclicPreservedUnderDo</em>); acyclicity of $G_{\\swig(W_2)}$ under the same hypothesis is established by <a href=\"#claim_3_9\" class=\"refrow\"><code>claim_3_9</code></a> (<em>SwigAcyclic</em>). Hence both sides of the displayed equality are CADMGs. At the underlying CDMG-type level, the asserted equality below is first an equality of CDMGs (def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>) – componentwise on the four components $(J, V, E, L)$ – and the upgrade to an equality of CADMGs is automatic once both sides are known to be acyclic CDMGs (acyclicity is a propositional condition on a fixed CDMG, transported along the CDMG-equality).</p>\n<p><em>Well-typedness of the inner SWIG on the LHS.</em>\n    The LHS $\\lp G_{\\doit(W_1)} \\rp_{\\swig(W_2)}$ requires its outer $\\swig(W_2)$ to act on a CADMG whose output-node set contains $W_2$, i.e.\\ $W_2 \\subseteq V_{\\doit(W_1)}$ per def <a href=\"#def_3_12\" class=\"refrow\"><code>def:G_node-splitting_intervention</code></a>'s precondition (the CADMG-ness of $G_{\\doit(W_1)}$ itself comes from <a href=\"#claim_3_3\" class=\"refrow\"><code>claim_3_3</code></a> on top of $G$ being a CADMG). By item&nbsp;ii.\\ of def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a> we have $V_{\\doit(W_1)} = V \\sm W_1$, and the inclusion\n    \\[ W_2 \\;\\subseteq\\; V \\sm W_1 \\;=\\; V_{\\doit(W_1)} \\]\n    follows from $W_2 \\subseteq V$ together with $W_1 \\cap W_2 = \\emptyset$. Hence the LHS is defined per def <a href=\"#def_3_12\" class=\"refrow\"><code>def:G_node-splitting_intervention</code></a>. Well-typedness of the RHS $\\lp G_{\\swig(W_2)} \\rp_{\\doit(W_1)}$ is spelled out in the carrier-reading paragraph below the displayed equality.</p>\n<p><em>The conclusion.</em>\n    The CADMG obtained from first hard intervening on $W_1$ and then node-splitting (in the SWIG sense of def <a href=\"#def_3_12\" class=\"refrow\"><code>def:G_node-splitting_intervention</code></a>) on $W_2$ equals the CADMG obtained from first node-splitting (in the same sense) on $W_2$ and then hard intervening on $W_1$:\n    \\[ \\lp G_{\\doit(W_1)} \\rp_{\\swig(W_2)} \\;=\\; \\lp G_{\\swig(W_2)} \\rp_{\\doit(W_1)}. \\]\n    The equality is understood componentwise on the four components $(J, V, E, L)$ of def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>: equal input-node sets ($J_{\\text{LHS}} = J_{\\text{RHS}}$), equal output-node sets ($V_{\\text{LHS}} = V_{\\text{RHS}}$), equal directed-edge sets ($E_{\\text{LHS}} = E_{\\text{RHS}}$), and equal bidirected-edge sets ($L_{\\text{LHS}} = L_{\\text{RHS}}$); the conjunctive unpacking into these four field-by-field equalities is deferred to the proof.</p>\n<p><em>Carrier reading of the equality.</em>\n    By def <a href=\"#def_3_12\" class=\"refrow\"><code>def:G_node-splitting_intervention</code></a> the operation $\\swig(W_2)$ shifts the carrier from $J \\cup V$ to the tagged-disjoint-union carrier\n    \\[ J_{\\swig(W_2)} \\cup V_{\\swig(W_2)} \\;=\\; \\lp J \\dcup W_2^i \\rp \\;\\cup\\; \\lp (V \\sm W_2) \\dcup W_2^o \\rp, \\]\n    where $W_2^o := \\lC w^o \\st w \\in W_2 \\rC$ and $W_2^i := \\lC w^i \\st w \\in W_2 \\rC$ are the two type-level tagged copies of $W_2$ introduced in def <a href=\"#def_3_12\" class=\"refrow\"><code>def:G_node-splitting_intervention</code></a> (so $W_2^o \\cap W_2^i = \\emptyset$ and both $W_2^o$ and $W_2^i$ are disjoint from every element of $J \\cup V$). Crucially, the input-side tagged copy $W_2^i$ sits in $J_{\\swig(W_2)}$ and the output-side tagged copy $W_2^o$ sits in $V_{\\swig(W_2)}$ – this is the distinguishing feature of $\\swig$ versus the regular node-splitting $\\spl$ of def <a href=\"#def_3_11\" class=\"refrow\"><code>def:G_node-splitting</code></a>, whose output-node set carries <em>both</em> tagged copies. By def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a> the operation $\\doit$ preserves the carrier $J \\cup V$ of its input as a set (items&nbsp;i.\\ and&nbsp;ii.\\ give $J_{\\doit(W)} \\cup V_{\\doit(W)} = (J \\cup W) \\cup (V \\sm W) = J \\cup V$ whenever $W \\subseteq J \\cup V$). Thus both sides of the displayed equality live in a CDMG over the same $\\swig(W_2)$-carrier just displayed, and the asserted equality is a <em>literal</em> $=$ of CDMGs (def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>) – <em>not</em> an equality up to a carrier-relabelling map. On the right-hand side, $\\doit(W_1)$ is read as hard-intervening on the <em>unsplit copies</em> of the nodes of $W_1$ inside the $\\swig(W_2)$-carrier: every $w \\in W_1$ satisfies $w \\in J \\cup V$ (by $W_1 \\subseteq J \\cup V$) and $w \\notin W_2$ (by $W_1 \\cap W_2 = \\emptyset$), so $w \\in J \\cup (V \\sm W_2)$, which is the untagged piece of $J_{\\swig(W_2)} \\cup V_{\\swig(W_2)}$ under def <a href=\"#def_3_12\" class=\"refrow\"><code>def:G_node-splitting_intervention</code></a>'s notational shorthand “$v^o := v^i := v$ for $v \\in J \\cup (V \\sm W_2)$” (i.e.\\ $W_1$ injects into the $\\swig(W_2)$-carrier as the unsplit injection $w \\mapsto w$). The precondition $W_1 \\subseteq J_{\\swig(W_2)} \\cup V_{\\swig(W_2)}$ of def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a> is thereby met by these unsplit copies, so the RHS is also defined per def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a>, and the LN's $G_{\\swig(W_2)}$ carrier reading agrees with the unique LN-faithful reading of $\\doit(W_1)$ on it.</p></div>\n<div class=\"tex-proof\"><span class=\"proof-label\">Proof.</span><p>We verify the equality $\\lp G_{\\doit(W_1)} \\rp_{\\swig(W_2)} = \\lp G_{\\swig(W_2)} \\rp_{\\doit(W_1)}$ componentwise on the four fields $(J, V, E, L)$ of def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>, organised as in the LN under three section headings: “Node sets” (covering $J$ and $V$ jointly), “Directed edges” (the $E$ field), and “Bidirected edges” (the $L$ field).</p>\n<p><em>Well-typedness of both iteration orders.</em>\nFor the LHS $\\lp G_{\\doit(W_1)} \\rp_{\\swig(W_2)}$: the outer $\\swig(W_2)$ of def <a href=\"#def_3_12\" class=\"refrow\"><code>def:G_node-splitting_intervention</code></a> requires (a) its input to be a CADMG and (b) $W_2 \\subseteq V_{\\doit(W_1)}$. For (a), $G_{\\doit(W_1)}$ is itself a CADMG by <a href=\"#claim_3_3\" class=\"refrow\"><code>claim_3_3</code></a> (<em>AcyclicPreservedUnderDo</em>) applied to the standing CADMG hypothesis on $G$ – this is the SWIG-specific acyclicity step that the spl analog (<a href=\"#claim_3_8\" class=\"refrow\"><code>claim_3_8</code></a>) did not need, since the regular node-splitting $\\spl$ of def <a href=\"#def_3_11\" class=\"refrow\"><code>def:G_node-splitting</code></a> does not require a CADMG input. For (b), by def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a> item&nbsp;ii., $V_{\\doit(W_1)} = V \\sm W_1$, and the inclusion $W_2 \\subseteq V \\sm W_1$ follows from $W_2 \\subseteq V$ together with $W_1 \\cap W_2 = \\emptyset$. For the RHS $\\lp G_{\\swig(W_2)} \\rp_{\\doit(W_1)}$: the outer $\\doit(W_1)$ of def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a> requires only $W_1 \\subseteq J_{\\swig(W_2)} \\cup V_{\\swig(W_2)}$, which holds by the carrier-reading paragraph of the statement above: every $w \\in W_1$ satisfies $w \\in J \\cup V$ (from $W_1 \\subseteq J \\cup V$) and $w \\notin W_2$ (from $W_1 \\cap W_2 = \\emptyset$), so $w \\in J \\cup (V \\sm W_2)$, the untagged piece of $J_{\\swig(W_2)} \\cup V_{\\swig(W_2)}$ under def <a href=\"#def_3_12\" class=\"refrow\"><code>def:G_node-splitting_intervention</code></a>'s unsplit shorthand $v^o := v^i := v$.</p>\n<p><em>Notational reminder.</em>\nFor $w \\in W_2$ we write $w^o$, $w^i$ for the two tagged copies produced by $\\swig(W_2)$ per def <a href=\"#def_3_12\" class=\"refrow\"><code>def:G_node-splitting_intervention</code></a>; these live in the tagged-disjoint-union pieces $W_2^o$ and $W_2^i$ respectively, and are formally distinct from every element of $J \\cup V$ (and from each other), per def <a href=\"#def_3_12\" class=\"refrow\"><code>def:G_node-splitting_intervention</code></a>'s tagged-copies disjointness assertions $W_2^o \\cap V = W_2^i \\cap V = W_2^o \\cap J = W_2^i \\cap J = W_2^o \\cap W_2^i = \\emptyset$. For $v \\in J \\cup (V \\sm W_2)$ we use def <a href=\"#def_3_12\" class=\"refrow\"><code>def:G_node-splitting_intervention</code></a>'s unsplit shorthand $v^o := v^i := v$, which leaves $v$ in the untagged piece of $J_{\\swig(W_2)} \\cup V_{\\swig(W_2)}$. <em>Crucially for SWIG, the input copies $W_2^i$ sit in $J_{\\swig(W_2)}$ and the output copies $W_2^o$ sit in $V_{\\swig(W_2)}$</em> – the distinguishing feature of $\\swig$ versus the regular node-splitting $\\spl$ of def <a href=\"#def_3_11\" class=\"refrow\"><code>def:G_node-splitting</code></a>, whose output-node set carries <em>both</em> tagged copies.</p>\n<p>On the right-hand side, the outer $\\doit(W_1)$ is read on the lifted intervention set “$W_1$ inside the $\\swig(W_2)$-carrier”, which by the carrier-reading paragraph of the statement is the unsplit image $\\lC w \\st w \\in W_1 \\rC \\subseteq J \\cup (V \\sm W_2)$ (using $W_1 \\subseteq J \\cup V$ and $W_1 \\cap W_2 = \\emptyset$); throughout the proof we write $W_1$ for this lifted set, using the unsplit shorthand $w = w^o = w^i$ for $w \\in W_1$. The SWIG-specific carrier-arithmetic observation we use repeatedly below is\n\\[ W_1 \\cap W_2^o \\;=\\; \\emptyset \\qquad\\text{and}\\qquad W_1 \\cap W_2^i \\;=\\; \\emptyset, \\]\nboth of which follow from $W_1 \\subseteq J \\cup V$ together with the tagged-construction disjointness $W_2^o \\cap (J \\cup V) = W_2^i \\cap (J \\cup V) = \\emptyset$ of def <a href=\"#def_3_12\" class=\"refrow\"><code>def:G_node-splitting_intervention</code></a>.</p>\n<p>“Head” of a directed edge $(a, b) \\in E$ means its second component $b$; “endpoints” of a bidirected edge $v_1 \\huh v_2 \\in L$ means the unordered pair $\\{v_1, v_2\\}$ of its two vertices.</p>\n<p><em>Node sets.</em></p>\n<p><em>Input nodes ($J$).</em>\nWe show\n\\[ J_{\\lp G_{\\doit(W_1)} \\rp_{\\swig(W_2)}} \\;=\\; J_{\\lp G_{\\swig(W_2)} \\rp_{\\doit(W_1)}}. \\]\nBy def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a> item&nbsp;i.\\ applied to $G$ with intervention set $W_1$,\n\\[ J_{G_{\\doit(W_1)}} \\;=\\; J \\cup W_1. \\]\nBy def <a href=\"#def_3_12\" class=\"refrow\"><code>def:G_node-splitting_intervention</code></a> item&nbsp;i.\\ applied to $G_{\\doit(W_1)}$ with split set $W_2$ (well-typed by $W_2 \\subseteq V \\sm W_1 = V_{\\doit(W_1)}$),\n\\[ J_{\\lp G_{\\doit(W_1)} \\rp_{\\swig(W_2)}} \\;=\\; J_{G_{\\doit(W_1)}} \\dcup W_2^i \\;=\\; (J \\cup W_1) \\dcup W_2^i. \\]\nThe $\\dcup$ here is a literal disjoint union: $W_2^i$ is disjoint from $J \\cup W_1$ since $W_1 \\subseteq J \\cup V$ gives $(J \\cup W_1) \\subseteq J \\cup V$, and $W_2^i \\cap (J \\cup V) = \\emptyset$ by the tagged-construction disjointness from the notational reminder.</p>\n<p>For the RHS, by def <a href=\"#def_3_12\" class=\"refrow\"><code>def:G_node-splitting_intervention</code></a> item&nbsp;i.\\ applied to $G$ with split set $W_2$,\n\\[ J_{G_{\\swig(W_2)}} \\;=\\; J \\dcup W_2^i. \\]\nBy def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a> item&nbsp;i.\\ applied to $G_{\\swig(W_2)}$ with intervention set $W_1$ (lifted via the unsplit injection per the well-typedness paragraph),\n\\[ J_{\\lp G_{\\swig(W_2)} \\rp_{\\doit(W_1)}} \\;=\\; J_{G_{\\swig(W_2)}} \\cup W_1 \\;=\\; (J \\dcup W_2^i) \\cup W_1. \\]\nBy $W_1 \\cap W_2^i = \\emptyset$ (from the notational reminder), adjoining $W_1$ to $J \\dcup W_2^i$ folds $W_1$ into the $J$ piece while leaving the $W_2^i$ piece untouched:\n\\[ (J \\dcup W_2^i) \\cup W_1 \\;=\\; (J \\cup W_1) \\dcup W_2^i. \\]\nHence\n\\[ J_{\\lp G_{\\doit(W_1)} \\rp_{\\swig(W_2)}} \\;=\\; (J \\cup W_1) \\dcup W_2^i \\;=\\; J_{\\lp G_{\\swig(W_2)} \\rp_{\\doit(W_1)}}. \\]\n✓</p>\n<p><em>Output nodes ($V$).</em>\nWe show\n\\[ V_{\\lp G_{\\doit(W_1)} \\rp_{\\swig(W_2)}} \\;=\\; V_{\\lp G_{\\swig(W_2)} \\rp_{\\doit(W_1)}}. \\]\nBy def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a> item&nbsp;ii.\\ applied to $G$ with intervention set $W_1$ and then def <a href=\"#def_3_12\" class=\"refrow\"><code>def:G_node-splitting_intervention</code></a> item&nbsp;ii.\\ applied to $G_{\\doit(W_1)}$ with split set $W_2 \\subseteq V \\sm W_1 = V_{\\doit(W_1)}$,\n\\[\n    V_{\\lp G_{\\doit(W_1)} \\rp_{\\swig(W_2)}}\n    \\;=\\; \\lp V_{G_{\\doit(W_1)}} \\sm W_2 \\rp \\;\\dcup\\; W_2^o\n    \\;=\\; \\lp (V \\sm W_1) \\sm W_2 \\rp \\;\\dcup\\; W_2^o,\n\\]\nwhich simplifies (by $(V \\sm W_1) \\sm W_2 = V \\sm W_1 \\sm W_2$, the standard “two-step removal” identity for set-difference) to\n\\[\n    V_{\\lp G_{\\doit(W_1)} \\rp_{\\swig(W_2)}}\n    \\;=\\; (V \\sm W_1 \\sm W_2) \\;\\dcup\\; W_2^o.\n\\]</p>\n<p>For the RHS, by def <a href=\"#def_3_12\" class=\"refrow\"><code>def:G_node-splitting_intervention</code></a> item&nbsp;ii.\\ applied to $G$ with split set $W_2$ and then def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a> item&nbsp;ii.\\ applied to $G_{\\swig(W_2)}$ with intervention set $W_1$ (lifted to the SWIG carrier via the unsplit injection),\n\\[\n    V_{\\lp G_{\\swig(W_2)} \\rp_{\\doit(W_1)}}\n    \\;=\\; V_{G_{\\swig(W_2)}} \\sm W_1\n    \\;=\\; \\lp (V \\sm W_2) \\;\\dcup\\; W_2^o \\rp \\sm W_1.\n\\]\nWe simplify the latter by decomposing the set-difference across the two disjoint pieces of the disjoint union, using both the disjointness hypothesis $W_1 \\cap W_2 = \\emptyset$ (on the untagged piece) and the SWIG-specific tagged-construction disjointness (on the tagged piece):</p>\n<ul><li>$W_1 \\cap W_2^o = \\emptyset$: this is the SWIG-specific carrier-arithmetic observation from the notational reminder. Since the lifted $W_1$ sits in the untagged piece $J \\cup (V \\sm W_2) \\subseteq J \\cup V$, and $W_2^o \\cap (J \\cup V) = \\emptyset$ by def <a href=\"#def_3_12\" class=\"refrow\"><code>def:G_node-splitting_intervention</code></a>'s tagged-construction disjointness, the lifted $W_1$ does not meet $W_2^o$. Hence the set-difference acts as the identity on $W_2^o$.</li><li>$(V \\sm W_2) \\sm W_1 = V \\sm W_1 \\sm W_2$: indeed, for any $x$, the chain of equivalences $x \\in (V \\sm W_2) \\sm W_1$ iff $x \\in V$, $x \\notin W_2$, $x \\notin W_1$ iff $x \\in V$, $x \\notin W_1$, $x \\notin W_2$ iff $x \\in V \\sm W_1 \\sm W_2$ holds. (Here the disjointness $W_1 \\cap W_2 = \\emptyset$ is <em>not</em> needed for this equality; the two iterated removals commute by associativity of $\\sm$ alone.)</li></ul>\n<p>Combining the two piecewise calculations,\n\\[\n    \\lp (V \\sm W_2) \\;\\dcup\\; W_2^o \\rp \\sm W_1\n    \\;=\\; \\lp (V \\sm W_2) \\sm W_1 \\rp \\;\\dcup\\; W_2^o\n    \\;=\\; (V \\sm W_1 \\sm W_2) \\;\\dcup\\; W_2^o.\n\\]\nHence\n\\[\n    V_{\\lp G_{\\doit(W_1)} \\rp_{\\swig(W_2)}}\n    \\;=\\; (V \\sm W_1 \\sm W_2) \\;\\dcup\\; W_2^o\n    \\;=\\; V_{\\lp G_{\\swig(W_2)} \\rp_{\\doit(W_1)}}.\n\\]\n✓</p>\n<p><em>Directed edges.</em>\nWe show\n\\[ E_{\\lp G_{\\doit(W_1)} \\rp_{\\swig(W_2)}} \\;=\\; E_{\\lp G_{\\swig(W_2)} \\rp_{\\doit(W_1)}}. \\]\nBy def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a> item&nbsp;iii.\\ applied to $G$ with intervention set $W_1$,\n\\[\n    E_{G_{\\doit(W_1)}}\n    \\;=\\; E \\sm \\lC (v_1, v_2) \\in E \\st v_2 \\in W_1 \\rC\n    \\;=\\; \\lC (v_1, v_2) \\in E \\st v_2 \\notin W_1 \\rC.\n\\]\nBy def <a href=\"#def_3_12\" class=\"refrow\"><code>def:G_node-splitting_intervention</code></a> item&nbsp;iii.\\ applied to $G_{\\doit(W_1)}$ with split set $W_2$ (every surviving directed edge $(v_1, v_2) \\in E_{G_{\\doit(W_1)}}$ is lifted to $(v_1^i, v_2^o)$; note that the SWIG construction of def <a href=\"#def_3_12\" class=\"refrow\"><code>def:G_node-splitting_intervention</code></a> introduces <em>no</em> transfer edges, in contrast to the regular node-splitting $\\spl$ of def <a href=\"#def_3_11\" class=\"refrow\"><code>def:G_node-splitting</code></a>),\n\\[\n    E_{\\lp G_{\\doit(W_1)} \\rp_{\\swig(W_2)}}\n    \\;=\\; \\lC (v_1^i, v_2^o) \\st (v_1, v_2) \\in E_{G_{\\doit(W_1)}} \\rC\n    \\;=\\; \\lC (v_1^i, v_2^o) \\st (v_1, v_2) \\in E,\\; v_2 \\notin W_1 \\rC.\n\\]</p>\n<p>For the RHS, by def <a href=\"#def_3_12\" class=\"refrow\"><code>def:G_node-splitting_intervention</code></a> item&nbsp;iii.\\ applied to $G$ with split set $W_2$,\n\\[\n    E_{G_{\\swig(W_2)}}\n    \\;=\\; \\lC (v_1^i, v_2^o) \\st (v_1, v_2) \\in E \\rC.\n\\]\nBy def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a> item&nbsp;iii.\\ applied to $G_{\\swig(W_2)}$ with intervention set $W_1$ (lifted to the SWIG carrier via the unsplit injection),\n\\[\n    E_{\\lp G_{\\swig(W_2)} \\rp_{\\doit(W_1)}}\n    \\;=\\; E_{G_{\\swig(W_2)}} \\sm \\lC (a, b) \\in E_{G_{\\swig(W_2)}} \\st b \\in W_1 \\rC.\n\\]\nThe removal condition “$b \\in W_1$” inspects the head $b = v_2^o$ of each candidate generator $(v_1^i, v_2^o)$ arising from $(v_1, v_2) \\in E$. By def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>'s typing $E \\subseteq (J \\cup V) \\times V$ we have $v_2 \\in V$, so we case-split on whether $v_2 \\in W_2$:</p>\n<ul><li><em>If $v_2 \\in W_2$:</em> $v_2^o \\in W_2^o$ is a tagged copy, lying in the disjoint-union piece $W_2^o$. By $W_1 \\cap W_2^o = \\emptyset$ (from the notational reminder), $v_2^o \\notin W_1$ (read in the SWIG carrier via the unsplit lift). The edge <em>survives</em> the RHS removal. Cross-check with the LHS: by the disjointness hypothesis $W_1 \\cap W_2 = \\emptyset$, $v_2 \\in W_2$ also forces $v_2 \\notin W_1$, so the LHS-side condition “$v_2 \\notin W_1$” is automatically met – both sides keep this edge.</li><li><em>If $v_2 \\in V \\sm W_2$:</em> $v_2^o = v_2$ (unsplit shorthand, since $v_2 \\in V \\sm W_2 \\subseteq J \\cup (V \\sm W_2)$). The lifted $W_1$ in the SWIG carrier is the unsplit image $\\lC w \\st w \\in W_1 \\rC$, so $v_2^o \\in W_1$ iff $v_2 \\in W_1$ (the unsplit injection $J \\cup (V \\sm W_2) \\hookrightarrow J_{\\swig(W_2)} \\cup V_{\\swig(W_2)}$ is injective). The edge is removed by the RHS iff $v_2 \\in W_1$, equivalently kept iff $v_2 \\notin W_1$. This matches the LHS condition “$v_2 \\notin W_1$” verbatim.</li></ul>\n<p>Combining the two sub-cases, the generator $(v_1^i, v_2^o)$ from $(v_1, v_2) \\in E$ survives the RHS removal iff $v_2 \\notin W_1$, matching the LHS surviving condition exactly. (In the $v_2 \\in W_2$ branch this is supplied automatically by the disjointness $W_1 \\cap W_2 = \\emptyset$; in the $v_2 \\in V \\sm W_2$ branch it is read off the unsplit identification.) Therefore\n\\[\n    E_{\\lp G_{\\swig(W_2)} \\rp_{\\doit(W_1)}}\n    \\;=\\; \\lC (v_1^i, v_2^o) \\st (v_1, v_2) \\in E,\\; v_2 \\notin W_1 \\rC.\n\\]\nComparing with the LHS,\n\\[\n    E_{\\lp G_{\\doit(W_1)} \\rp_{\\swig(W_2)}}\n    \\;=\\; \\lC (v_1^i, v_2^o) \\st (v_1, v_2) \\in E,\\; v_2 \\notin W_1 \\rC\n    \\;=\\; E_{\\lp G_{\\swig(W_2)} \\rp_{\\doit(W_1)}}.\n\\]\n✓</p>\n<p><em>Bidirected edges.</em>\nWe show\n\\[ L_{\\lp G_{\\doit(W_1)} \\rp_{\\swig(W_2)}} \\;=\\; L_{\\lp G_{\\swig(W_2)} \\rp_{\\doit(W_1)}}. \\]</p>\n<p><em>Refactor-context note on the removal clause of $\\doit$ for $L$.</em>\nAt the LN level the bidirected-edge set $L$ consists of unordered pairs of distinct vertices in $V$, and def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a> item&nbsp;iv.\\ removes those pairs that touch $W_1$. The LN's literal one-sided gloss “$L \\sm \\lC v \\huh w \\mid w \\in W_1 \\rC$” selects an ordered-pair representative of each unordered pair and tests its second component, which presumes an ordered-pair encoding of $L$. The post-refactor encoding of def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a> (and downstream def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a>) carries $L$ as a set of <em>unordered</em> pairs, so there is no ordered “second component” to single out: the structurally well-defined reading of the LN's removal clause is the endpoint-universal form\n\\[\n    L_{\\doit(W)} \\;=\\; \\lC v_1 \\huh v_2 \\in L \\st v_1 \\notin W,\\; v_2 \\notin W \\rC,\n\\]\ni.e.\\ “$v_1 \\huh v_2$ survives iff every endpoint of the unordered pair lies outside $W$”. This is precisely the LN's intended “remove all edges into nodes from $W$” applied to bidirected edges (which are incident to <em>both</em> endpoints) and agrees on the survivor set with the LN's literal one-sided gloss after closing under the implicit swap of representatives. The pre-refactor encoding of def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a> had to spell this symmetrisation out as a registered deviation; under the post-refactor unordered-pair encoding the LN-faithful reading is the literal one and no symmetrisation step is required. We use the endpoint-universal form throughout the calculations below.</p>\n<p>By def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a> item&nbsp;iv.\\ (endpoint-universal form), applied to $G$ with intervention set $W_1$,\n\\[\n    L_{G_{\\doit(W_1)}}\n    \\;=\\; \\lC v_1 \\huh v_2 \\in L \\st v_1 \\notin W_1,\\; v_2 \\notin W_1 \\rC.\n\\]\nBy def <a href=\"#def_3_12\" class=\"refrow\"><code>def:G_node-splitting_intervention</code></a> item&nbsp;iv.\\ applied to $G_{\\doit(W_1)}$ with split set $W_2$ (every surviving bidirected edge $v_1 \\huh v_2 \\in L_{G_{\\doit(W_1)}}$ is lifted to $v_1^o \\huh v_2^o$, with <em>both</em> endpoints carrying the output-side $^o$ superscript – in particular no element of $W_2^i$ appears as an endpoint of any bidirected edge),\n\\[\n    L_{\\lp G_{\\doit(W_1)} \\rp_{\\swig(W_2)}}\n    \\;=\\; \\lC v_1^o \\huh v_2^o \\st v_1 \\huh v_2 \\in L_{G_{\\doit(W_1)}} \\rC\n    \\;=\\; \\lC v_1^o \\huh v_2^o \\st v_1 \\huh v_2 \\in L,\\; v_1 \\notin W_1,\\; v_2 \\notin W_1 \\rC.\n\\]</p>\n<p>For the RHS, by def <a href=\"#def_3_12\" class=\"refrow\"><code>def:G_node-splitting_intervention</code></a> item&nbsp;iv.\\ applied to $G$ with split set $W_2$,\n\\[\n    L_{G_{\\swig(W_2)}}\n    \\;=\\; \\lC v_1^o \\huh v_2^o \\st v_1 \\huh v_2 \\in L \\rC.\n\\]\nBy def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a> item&nbsp;iv.\\ (endpoint-universal form), applied to $G_{\\swig(W_2)}$ with intervention set $W_1$ (lifted to the SWIG carrier via the unsplit injection),\n\\[\n    L_{\\lp G_{\\swig(W_2)} \\rp_{\\doit(W_1)}}\n    \\;=\\; \\lC a \\huh b \\in L_{G_{\\swig(W_2)}} \\st a \\notin W_1,\\; b \\notin W_1 \\rC.\n\\]\nWe compute the surviving pairs by analysing each endpoint of the generator $v_1^o \\huh v_2^o$ arising from $v_1 \\huh v_2 \\in L$. Since $L$ consists of unordered pairs of distinct vertices in $V$, both $v_1, v_2 \\in V$. For each $k \\in \\{1, 2\\}$:</p>\n<ul><li><em>If $v_k \\in W_2$:</em> $v_k^o \\in W_2^o$ is a tagged copy. By $W_1 \\cap W_2^o = \\emptyset$ (from the notational reminder), $v_k^o \\notin W_1$ (in the SWIG carrier). Cross-check with the LHS: by the disjointness hypothesis $W_1 \\cap W_2 = \\emptyset$, $v_k \\in W_2$ also forces $v_k \\notin W_1$, so both the RHS-side condition “$v_k^o \\notin W_1$” and the LHS-side condition “$v_k \\notin W_1$” are satisfied automatically in this branch.</li><li><em>If $v_k \\in V \\sm W_2$:</em> $v_k^o = v_k$ (unsplit shorthand, since $v_k \\in V \\sm W_2 \\subseteq J \\cup (V \\sm W_2)$). The lifted $W_1$ in the SWIG carrier is the unsplit image $\\lC w \\st w \\in W_1 \\rC$, so $v_k^o \\in W_1$ iff $v_k \\in W_1$ (the unsplit injection is injective). Hence the RHS-side condition “$v_k^o \\notin W_1$” is equivalent to the LHS-side condition “$v_k \\notin W_1$”.</li></ul>\n<p>In both sub-cases the condition “$v_k^o \\notin W_1$” is equivalent to “$v_k \\notin W_1$”. Applying this to both endpoints $v_1$ and $v_2$ independently,\n\\[\n    L_{\\lp G_{\\swig(W_2)} \\rp_{\\doit(W_1)}}\n    \\;=\\; \\lC v_1^o \\huh v_2^o \\st v_1 \\huh v_2 \\in L,\\; v_1 \\notin W_1,\\; v_2 \\notin W_1 \\rC.\n\\]\nComparing with the LHS,\n\\[\n    L_{\\lp G_{\\doit(W_1)} \\rp_{\\swig(W_2)}}\n    \\;=\\; \\lC v_1^o \\huh v_2^o \\st v_1 \\huh v_2 \\in L,\\; v_1 \\notin W_1,\\; v_2 \\notin W_1 \\rC\n    \\;=\\; L_{\\lp G_{\\swig(W_2)} \\rp_{\\doit(W_1)}}.\n\\]\n✓</p>\n<p>Combining the four componentwise equalities ($J$, $V$, $E$, $L$) above, the CDMGs $\\lp G_{\\doit(W_1)} \\rp_{\\swig(W_2)}$ and $\\lp G_{\\swig(W_2)} \\rp_{\\doit(W_1)}$ agree field by field on each of $(J, V, E, L)$ and are therefore equal as CDMGs (def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>). The upgrade to an equality of CADMGs is automatic: acyclicity is a propositional condition on a fixed CDMG and transports along the CDMG-equality (acyclicity of the LHS follows from <a href=\"#claim_3_9\" class=\"refrow\"><code>claim_3_9</code></a> (<em>SwigAcyclic</em>) applied to $G_{\\doit(W_1)}$, which is itself a CADMG by <a href=\"#claim_3_3\" class=\"refrow\"><code>claim_3_3</code></a>; acyclicity of the RHS follows from <a href=\"#claim_3_3\" class=\"refrow\"><code>claim_3_3</code></a> applied to $G_{\\swig(W_2)}$, which is itself a CADMG by <a href=\"#claim_3_9\" class=\"refrow\"><code>claim_3_9</code></a>).</p></div>",
    "source_path": "leanification/Chapter3_GraphTheory/Section3_2/tex/claim_3_11_proof_DisjointHardInterventions.tex"
  },
  "lean_blocks": [
    {
      "kind": "helper",
      "code": "variable {Node : Type*} [DecidableEq Node]",
      "explanation": "This is a `variable` declaration: Lean auto-binds `Node : Type*` and the instance `[DecidableEq Node]` as implicit arguments to every subsequent declaration in the surrounding `CDMG` namespace, sparing us from rewriting the same parameters on every helper and on the main theorem. The `Type*` notation makes `Node` universe-polymorphic, so callers can instantiate it at any universe level. The `DecidableEq Node` assumption is load-bearing for this row in particular: the wrapped signature directly mentions `Disjoint W₁ W₂` on `Finset Node` and `W₁.image SplitNode.unsplit`, and traverses `G.J`, `G.V`, `G.E`, `G.L` through `G.IsCADMG`, `G.hardInterventionOn`, and `G.nodeSplittingHard` — each consumes `[DecidableEq Node]` through `Finset`-backed membership, image, `Finset.filter`, `Finset.union`, and `Finset.sdiff` operations. Stronger structure on `Node` (e.g.\\ `Fintype`, `LinearOrder`) is not required here and is deferred to downstream consumers that actually use it. Same convention as the rest of `Chapter3_GraphTheory`'s files.",
      "code_annotated": "-- Auto-binds `Node` + `DecidableEq Node` as implicits; decidability drives `Finset.image`/`filter`/`union`/`sdiff` and `Disjoint` below.\nvariable {Node : Type*} [DecidableEq Node]"
    },
    {
      "kind": "helper",
      "code": "private lemma subset_V_of_hardInterventionOn\n    {G : CDMG Node} {W₁ : Finset Node} (hW₁ : W₁ ⊆ G.J ∪ G.V)\n    {W₂ : Finset Node} (hW₂ : W₂ ⊆ G.V) (hDisj : Disjoint W₁ W₂) :\n    W₂ ⊆ (G.hardInterventionOn W₁ hW₁).V",
      "explanation": "This `private lemma` discharges one of the two well-typedness side conditions of the main theorem's signature: it proves that $W_2$ sits inside the output-node set of the intervened graph $G_{\\mathrm{doit}(W_1)}$. The conclusion `W₂ ⊆ (G.hardInterventionOn W₁ hW₁).V` is exactly the precondition demanded by the LHS's outer `nodeSplittingHard` — namely that the SWIG operation can split on $W_2$ inside the post-intervention graph. By `def_3_10` item ii., `(G.hardInterventionOn W₁ hW₁).V` definitionally reduces to `G.V \\ W₁`, so the goal is $W_2 \\subseteq V \\setminus W_1$; this follows from `hW₂ : W₂ ⊆ G.V` together with disjointness $W_1 \\cap W_2 = \\emptyset$. The hypothesis is bundled as Mathlib's lattice predicate `Disjoint W₁ W₂` (the canonical `Finset` form used by `claim_3_8` and `claim_3_10`), consumed in one step by `Finset.disjoint_right`. The lemma is wrapped with helper markers and travels with the rendered statement on the website, because the main signature directly references its name in the LHS's outer SWIG precondition slot — removing it would break type-checking of the wrapped theorem head.",
      "code_annotated": "-- Private helper: discharges the LHS's outer SWIG precondition `W₂ ⊆ (G_{doit(W₁)}).V`.\nprivate lemma subset_V_of_hardInterventionOn\n    -- Ambient CDMG and the subset `W₁ ⊆ J ∪ V` matching the LN's first intervention target.\n    {G : CDMG Node} {W₁ : Finset Node} (hW₁ : W₁ ⊆ G.J ∪ G.V)\n    -- Second subset `W₂ ⊆ V` and the disjointness side condition (Mathlib `Finset` form).\n    {W₂ : Finset Node} (hW₂ : W₂ ⊆ G.V) (hDisj : Disjoint W₁ W₂) :\n    -- Conclusion unfolds to `W₂ ⊆ G.V \\ W₁` by `def_3_10` item ii.\n    W₂ ⊆ (G.hardInterventionOn W₁ hW₁).V"
    },
    {
      "kind": "helper",
      "code": "private lemma image_unsplit_subset_carrier_of_nodeSplittingHard\n    {G : CDMG Node} {hG : G.IsCADMG}\n    {W₁ : Finset Node} (hW₁ : W₁ ⊆ G.J ∪ G.V)\n    {W₂ : Finset Node} (hW₂ : W₂ ⊆ G.V) (hDisj : Disjoint W₁ W₂) :\n    W₁.image SplitNode.unsplit ⊆\n      (G.nodeSplittingHard hG W₂ hW₂).J ∪\n        (G.nodeSplittingHard hG W₂ hW₂).V",
      "explanation": "This `private lemma` discharges the *other* well-typedness side condition of the main theorem's signature — the RHS's outer `hardInterventionOn` precondition. The conclusion `W₁.image SplitNode.unsplit ⊆ (G.nodeSplittingHard hG W₂ hW₂).J ∪ (G.nodeSplittingHard hG W₂ hW₂).V` says the unsplit-lifted $W_1$ lands inside the split-graph carrier produced by SWIG. The proof case-splits each $v \\in W_1$ on `hW₁`: if $v \\in G.J$, then `.unsplit v` lands in `G.J.image .unsplit ⊆ J_{\\mathrm{swig}(W_2)}` without invoking disjointness; if $v \\in G.V$, then `Disjoint W₁ W₂` gives $v \\notin W_2$, so `.unsplit v` lands in `(G.V \\setminus W_2).image .unsplit ⊆ V_{\\mathrm{swig}(W_2)}` via `Finset.disjoint_left`. The `{hG : G.IsCADMG}` binder is implicit so callers elide it when in scope; this is the only binder-shape divergence from `claim_3_8`'s spl-side analog, whose `nodeSplittingOn` carries no acyclicity precondition. Wrapped with helper markers because the main theorem head references this name directly in the RHS's outer `hardInterventionOn` slot — without it, the wrapped signature would not type-check.",
      "code_annotated": "-- Private helper: discharges the RHS's outer hard-intervention precondition `W₁.image .unsplit ⊆ J_{swig(W₂)} ∪ V_{swig(W₂)}`.\nprivate lemma image_unsplit_subset_carrier_of_nodeSplittingHard\n    -- Ambient CDMG `G` with `IsCADMG` witness `hG` (implicit: callers elide when in scope; required by `def_3_12`).\n    {G : CDMG Node} {hG : G.IsCADMG}\n    -- First subset `W₁ ⊆ J ∪ V` matching the LN's hard-intervention target.\n    {W₁ : Finset Node} (hW₁ : W₁ ⊆ G.J ∪ G.V)\n    -- Second subset `W₂ ⊆ V` (SWIG target) with disjointness in Mathlib `Finset` form.\n    {W₂ : Finset Node} (hW₂ : W₂ ⊆ G.V) (hDisj : Disjoint W₁ W₂) :\n    -- Conclusion: every `.unsplit`-lifted node of `W₁` sits in the SWIG carrier `J_{swig(W₂)} ∪ V_{swig(W₂)}`.\n    W₁.image SplitNode.unsplit ⊆\n      (G.nodeSplittingHard hG W₂ hW₂).J ∪\n        (G.nodeSplittingHard hG W₂ hW₂).V"
    },
    {
      "kind": "main",
      "code": "theorem disjointHardInterventionsAndNodeSplittingHardsCommute\n    (G : CDMG Node) (hG : G.IsCADMG)\n    (W₁ W₂ : Finset Node) (hW₁ : W₁ ⊆ G.J ∪ G.V) (hW₂ : W₂ ⊆ G.V)\n    (hDisj : Disjoint W₁ W₂) :\n    (G.hardInterventionOn W₁ hW₁).nodeSplittingHard\n        (hardInterventionOn_isCADMG_of_isCADMG hG hW₁) W₂\n        (subset_V_of_hardInterventionOn hW₁ hW₂ hDisj)\n      = (G.nodeSplittingHard hG W₂ hW₂).hardInterventionOn\n          (W₁.image SplitNode.unsplit)\n          (image_unsplit_subset_carrier_of_nodeSplittingHard\n            hW₁ hW₂ hDisj)",
      "explanation": "This is the main theorem of the row: the LN equality $(G_{\\mathrm{doit}(W_1)})_{\\mathrm{swig}(W_2)} = (G_{\\mathrm{swig}(W_2)})_{\\mathrm{doit}(W_1)}$ rendered as a literal `=` of `CDMG (SplitNode Node)` terms. The signature threads the CADMG hypothesis `hG` into both the LHS's outer `nodeSplittingHard` (through the wrapper `hardInterventionOn_isCADMG_of_isCADMG hG hW₁`, which extracts the acyclicity conjunct from `claim_3_3`) and the RHS's inner `nodeSplittingHard` (fed `hG` directly). The two helper lemmas — `subset_V_of_hardInterventionOn` and `image_unsplit_subset_carrier_of_nodeSplittingHard` — discharge the LHS's inner-SWIG and RHS's outer-`doit` carrier-membership preconditions. On the RHS, `W₁.image SplitNode.unsplit` lifts `W₁` to the split-graph carrier via the unsplit injection, faithful to the tex's “Carrier reading” paragraph: every $w \\in W_1$ lies in $J \\cup (V \\setminus W_2)$, the untagged piece of the SWIG carrier under `def_3_12`'s shorthand. Because `hardInterventionOn` preserves the node carrier and SWIG lifts it once, both sides have identical Lean type — no carrier-relabelling map is needed.",
      "code_annotated": "-- Main theorem: literal `=` of CDMGs over `SplitNode Node` expressing the LN's commutation.\ntheorem disjointHardInterventionsAndNodeSplittingHardsCommute\n    -- Ambient CADMG: graph `G` together with the acyclicity witness `hG` required by `def_3_12`.\n    (G : CDMG Node) (hG : G.IsCADMG)\n    -- Two `Finset` subsets of nodes with their respective carrier-subset hypotheses.\n    (W₁ W₂ : Finset Node) (hW₁ : W₁ ⊆ G.J ∪ G.V) (hW₂ : W₂ ⊆ G.V)\n    -- Disjointness side condition `W₁ ∩ W₂ = ∅` in Mathlib `Finset` lattice form.\n    (hDisj : Disjoint W₁ W₂) :\n    -- LHS: first hard-intervene on `W₁`, then SWIG on `W₂` (carrier becomes `SplitNode Node`).\n    (G.hardInterventionOn W₁ hW₁).nodeSplittingHard\n        -- `hardInterventionOn_isCADMG_of_isCADMG` projects acyclicity preservation (`claim_3_3`) to feed the outer SWIG.\n        (hardInterventionOn_isCADMG_of_isCADMG hG hW₁) W₂\n        -- `subset_V_of_hardInterventionOn` discharges the LHS inner-SWIG precondition `W₂ ⊆ G.V \\ W₁`.\n        (subset_V_of_hardInterventionOn hW₁ hW₂ hDisj)\n      -- Asserted equality: literal `=` of CDMGs (no `eqViaNodeMap` / `flattenSplit`).\n      = (G.nodeSplittingHard hG W₂ hW₂).hardInterventionOn\n          -- RHS: SWIG on `W₂` first; outer `doit` then targets `W₁` lifted via `.unsplit` into the SWIG carrier.\n          (W₁.image SplitNode.unsplit)\n          -- `image_unsplit_subset_carrier_of_nodeSplittingHard` discharges the RHS outer-`doit` carrier-membership precondition.\n          (image_unsplit_subset_carrier_of_nodeSplittingHard\n            hW₁ hW₂ hDisj)"
    }
  ],
  "lean_source_url": "https://github.com/schipeltaper/causality-leanified/blob/main/leanification/Chapter3_GraphTheory/Section3_2/DisjointHardInterventionsSwig.lean",
  "lean_file_path": "leanification/Chapter3_GraphTheory/Section3_2/DisjointHardInterventionsSwig.lean",
  "addition_to_the_LN": "",
  "design_choices": "**Literal `=` of `CDMG`s over `SplitNode Node`, not `eqViaNodeMap` / `flattenSplit`.** Both sides apply a single SWIG on $W_2$ and a single `hardInterventionOn` on $W_1$, and `hardInterventionOn` preserves the node carrier (`CDMG α → CDMG α`), so both sides land in the identical Lean type `CDMG (SplitNode Node)`. Contrast `claim_3_10`, where iterated `nodeSplittingHard` produces `CDMG (SplitNode (SplitNode Node))` with disagreeing constructor wrappings, forcing the `eqViaNodeMap` / `flattenSplit` workaround. Mirrors `claim_3_8`'s shape lifted to the SWIG side.\n\n**`(hG : G.IsCADMG)` on the signature.** Required by `def_3_12`'s `nodeSplittingHard`: the RHS's inner SWIG is fed `hG` directly; the LHS's outer SWIG receives it via the private wrapper `hardInterventionOn_isCADMG_of_isCADMG`, which projects the acyclicity conjunct from `claim_3_3`'s `acyclic_preserved_under_do`. `claim_3_8` has no such binder because `def_3_11`'s `nodeSplittingOn` has no acyclicity precondition.\n\n**Disjointness `Disjoint W₁ W₂` is load-bearing twice.** The LHS's inner SWIG needs $W_2 \\subseteq V \\setminus W_1$ (discharged by `subset_V_of_hardInterventionOn`); the RHS's outer hard intervention needs the unsplit-lifted $W_1$ inside $J_{\\mathrm{swig}(W_2)} \\cup V_{\\mathrm{swig}(W_2)}$ (discharged by `image_unsplit_subset_carrier_of_nodeSplittingHard`). Encoded as Mathlib's `Disjoint`, consumed in one step by `Finset.disjoint_left` / `_right`.\n\n**Lift $W_1$ via `Finset.image SplitNode.unsplit`.** Every $w \\in W_1$ lies in $J \\cup (V \\setminus W_2)$ — the untagged piece of the SWIG carrier under `def_3_12`'s shorthand $v^o := v^i := v$. The RHS's outer hard intervention is expressed in terms of the original `W₁` rather than a fresh `Finset (SplitNode Node)`. Same convention as `claim_3_8` and `claim_3_10`.\n\n**L-field shape under `cdmg_typed_edges`: `Sym2 Node` plus endpoint-universal filter.** `L` is typed as `Finset (Sym2 Node)`, with swap-symmetry built into `Sym2` (no `hL_symm` axiom). The SWIG lift is `Sym2.map (toCopy0 W₂)` — Mathlib's quotient-respecting lift on unordered pairs — and the `doit` removal clause is the endpoint-universal filter `fun s : Sym2 _ => ∀ v ∈ s, v ∉ W₁`. The `hard_intervention_l_symmetrized_removal` two-sided-filter deviation that an ordered-pair `L`-encoding would force is structurally absent under this typing. The L sub-goal closes via `Finset.filter_image` + `Finset.filter_congr` reducing to a per-element iff through `Sym2.mem_map` plus `toCopy0_notMem_iff` (the same iff used on each E-generator's head).\n\n**Local `private instance disjointHardInterventionsSwig_decidable_bAll`.** Elaborates the `Finset.filter` over the endpoint-universal predicate at the L-branch `change` step. Polymorphic copy of `HardInterventionOn.lean`'s `private` def-site instance, covering both `Sym2 Node` and `Sym2 (SplitNode Node)`; built via `Sym2.recOnSubsingleton` + `Sym2.ball`. Renamed off the spl-side sibling to stay uniquely resolvable.\n\n**`cdmgExt` destructures eight fields, not nine.** `CDMG` carries no `hL_symm` field (vacuous on `Sym2`), so destructurings of `cdmgExt` enumerate eight components. A nine-field destructuring pattern would not type-check against the structure as defined."
}
