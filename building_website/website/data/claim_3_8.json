{
  "ref": "claim_3_8",
  "kind": "claim",
  "section": "3.2",
  "title": "DisjointHardInterventions",
  "type": "lemma",
  "status": {
    "formalized": "yes",
    "proven": "proven",
    "solved": "yes"
  },
  "tex_block_html": "<p>Let $G=(J,V,E,L)$ be a CDMG and $W_1 \\ins J \\cup V$ and $W_2 \\ins V$ two disjoint subsets of nodes of $G$.\n   Then the CDMG obtained from first hard intervening on $W_1$ and then node-splitting $W_2$ is the same CDMG that arises from first node-splitting $W_2$ and then hard intervening on $W_1$.\n      \\[ \\lp G_{\\doit(W_1)} \\rp_{\\spl(W_2)} =  \\lp G_{\\spl(W_2)} \\rp_{\\doit(W_1)}.   \\]</p>",
  "tex_statement": {
    "raw": "% Claim statement file for row claim_3_8 -- \"DisjointHardInterventions\".\n% Auto-generated stub by scaffold/solve_chapter.py at row start. The\n% manager agent (or a worker it dispatches) fills the body below with the\n% claim's statement(s) from the lecture notes -- statement only, no proof\n% (proofs live in the sibling `claim_3_8_proof_*.tex` file).\n%\n% This file is a sibling of `main.tex` inside `<subsection>/tex/`. The\n% `subfiles` package lets it render both standalone and as part of\n% `main.tex`. Note: `main.tex` does NOT \\subfile this statement file -- the\n% sibling `_proof_` file restates the statement above the proof, so\n% including both in `main.tex` would be redundant. This file exists for\n% standalone reading / grep convenience.\n\n\\documentclass[main]{subfiles}\n\n\\begin{document}\n% ref: claim_3_8 (statement)\n% title: DisjointHardInterventions\n\\def\\rowref{claim\\_3\\_8}\n\\phantomsection\\label{claim_3_8}\n\n\\begin{Lem}[Disjoint hard interventions and node-splittings commute]\\label{disjoint-hard-interventions-node-splittings-commute}\n    Let $G = (J, V, E, L)$ be a CDMG (def \\ref{def-cdmg}); throughout we use the notation of def \\ref{not-cdmg} and recall the typing and axioms of def \\ref{def-cdmg}, namely $J \\cap V = \\emptyset$, $E \\subseteq (J \\cup V) \\times V$, $L \\subseteq V \\times V$, $L$ irreflexive ($(v_1, v_2) \\in L \\Rightarrow v_1 \\neq v_2$), and $L$ symmetric ($(v_1, v_2) \\in L \\Leftrightarrow (v_2, v_1) \\in L$). Let\n    \\[ W_1 \\subseteq J \\cup V \\quad\\text{and}\\quad W_2 \\subseteq V \\]\n    be two subsets of nodes of $G$, subject to the disjointness side condition\n    \\[ W_1 \\cap W_2 \\;=\\; \\emptyset. \\]\n    All three quantifiers ``for every CDMG $G$'', ``for every $W_1 \\subseteq J \\cup V$'', and ``for every $W_2 \\subseteq V$ with $W_1 \\cap W_2 = \\emptyset$'' are read \\emph{universally}; in particular the (vacuously disjoint) case $W_1 = W_2 = \\emptyset$ is admitted.\n\n    \\emph{Well-typedness of the inner splitting on the LHS.}\n    The LHS $\\lp G_{\\doit(W_1)} \\rp_{\\spl(W_2)}$ requires its outer $\\spl(W_2)$ to act on a CDMG whose output-node set contains $W_2$, i.e.\\ $W_2 \\subseteq V_{\\doit(W_1)}$ per def \\ref{def:G_node-splitting}'s precondition. By item ii.\\ of def \\ref{def:G_hard_intervention} we have $V_{\\doit(W_1)} = V \\sm W_1$, and the inclusion\n    \\[ W_2 \\;\\subseteq\\; V \\sm W_1 \\;=\\; V_{\\doit(W_1)} \\]\n    follows from $W_2 \\subseteq V$ together with $W_1 \\cap W_2 = \\emptyset$. Hence the LHS is defined per def \\ref{def:G_node-splitting}. Well-typedness of the RHS $\\lp G_{\\spl(W_2)} \\rp_{\\doit(W_1)}$ is spelled out in the carrier-reading paragraph below the displayed equality.\n\n    \\emph{The conclusion.}\n    The CDMG obtained from first hard intervening on $W_1$ and then node-splitting $W_2$ equals the CDMG obtained from first node-splitting $W_2$ and then hard intervening on $W_1$:\n    \\[ \\lp G_{\\doit(W_1)} \\rp_{\\spl(W_2)} \\;=\\; \\lp G_{\\spl(W_2)} \\rp_{\\doit(W_1)}. \\]\n    The equality is understood componentwise on the four components $(J, V, E, L)$ of def \\ref{def-cdmg}; the conjunctive unpacking into the four field-by-field equalities is deferred to the proof.\n\n    \\medskip\\emph{Carrier reading of the equality.}\n    By def \\ref{def:G_node-splitting} the operation $\\spl(W_2)$ shifts the carrier from $J \\cup V$ to the tagged-disjoint-union carrier\n    \\[ J_{\\spl(W_2)} \\cup V_{\\spl(W_2)} \\;=\\; J \\;\\cup\\; \\lp (V \\sm W_2) \\dcup W_2^0 \\dcup W_2^1 \\rp, \\]\n    while by def \\ref{def:G_hard_intervention} the operation $\\doit(W)$ preserves the carrier $J \\cup V$ of its input as a set (items i.\\ and ii.\\ give $J_{\\doit(W)} \\cup V_{\\doit(W)} = (J \\cup W) \\cup (V \\sm W) = J \\cup V$ whenever $W \\subseteq J \\cup V$). Thus both sides of the displayed equality live in a CDMG over the same $\\spl(W_2)$-carrier just displayed, and the asserted equality is a \\emph{literal} $=$ of CDMGs (def \\ref{def-cdmg}) — \\emph{not} an equality up to a carrier-relabelling map. On the right-hand side, $\\doit(W_1)$ is read as hard-intervening on the \\emph{unsplit copies} of the nodes of $W_1$ inside the split-graph carrier: every $w \\in W_1$ satisfies $w \\in J \\cup V$ (by $W_1 \\subseteq J \\cup V$) and $w \\notin W_2$ (by $W_1 \\cap W_2 = \\emptyset$), so $w \\in J \\cup (V \\sm W_2)$, which is the untagged piece of $J_{\\spl(W_2)} \\cup V_{\\spl(W_2)}$ under def \\ref{def:G_node-splitting}'s notational shorthand ``$v^0 := v^1 := v$ for $v \\in J \\cup (V \\sm W_2)$'' (i.e.\\ $W_1$ injects into the split-graph carrier as the unsplit injection $w \\mapsto w$). The precondition $W_1 \\subseteq J_{\\spl(W_2)} \\cup V_{\\spl(W_2)}$ of def \\ref{def:G_hard_intervention} is thereby met by the unsplit copies, so the RHS is also defined per def \\ref{def:G_hard_intervention}, and the LN's $G_{\\spl(W_2)}$ carrier reading agrees with the unique LN-faithful reading of $\\doit(W_1)$ on it.\n\\end{Lem}\n\n\\end{document}\n",
    "html": "<p>Let $G = (J, V, E, L)$ be a CDMG (def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>); throughout we use the notation of def <a href=\"#def_3_2\" class=\"refrow\"><code>not-cdmg</code></a> and recall the typing and axioms of def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>, namely $J \\cap V = \\emptyset$, $E \\subseteq (J \\cup V) \\times V$, $L \\subseteq V \\times V$, $L$ irreflexive ($(v_1, v_2) \\in L \\Rightarrow v_1 \\neq v_2$), and $L$ symmetric ($(v_1, v_2) \\in L \\Leftrightarrow (v_2, v_1) \\in L$). Let\n    \\[ W_1 \\subseteq J \\cup V \\quad\\text{and}\\quad W_2 \\subseteq V \\]\n    be two subsets of nodes of $G$, subject to the disjointness side condition\n    \\[ W_1 \\cap W_2 \\;=\\; \\emptyset. \\]\n    All three quantifiers “for every CDMG $G$”, “for every $W_1 \\subseteq J \\cup V$”, and “for every $W_2 \\subseteq V$ with $W_1 \\cap W_2 = \\emptyset$” are read <em>universally</em>; in particular the (vacuously disjoint) case $W_1 = W_2 = \\emptyset$ is admitted.</p>\n<p><em>Well-typedness of the inner splitting on the LHS.</em>\n    The LHS $\\lp G_{\\doit(W_1)} \\rp_{\\spl(W_2)}$ requires its outer $\\spl(W_2)$ to act on a CDMG whose output-node set contains $W_2$, i.e.\\ $W_2 \\subseteq V_{\\doit(W_1)}$ per def <a href=\"#def_3_11\" class=\"refrow\"><code>def:G_node-splitting</code></a>'s precondition. By item ii.\\ of def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a> we have $V_{\\doit(W_1)} = V \\sm W_1$, and the inclusion\n    \\[ W_2 \\;\\subseteq\\; V \\sm W_1 \\;=\\; V_{\\doit(W_1)} \\]\n    follows from $W_2 \\subseteq V$ together with $W_1 \\cap W_2 = \\emptyset$. Hence the LHS is defined per def <a href=\"#def_3_11\" class=\"refrow\"><code>def:G_node-splitting</code></a>. Well-typedness of the RHS $\\lp G_{\\spl(W_2)} \\rp_{\\doit(W_1)}$ is spelled out in the carrier-reading paragraph below the displayed equality.</p>\n<p><em>The conclusion.</em>\n    The CDMG obtained from first hard intervening on $W_1$ and then node-splitting $W_2$ equals the CDMG obtained from first node-splitting $W_2$ and then hard intervening on $W_1$:\n    \\[ \\lp G_{\\doit(W_1)} \\rp_{\\spl(W_2)} \\;=\\; \\lp G_{\\spl(W_2)} \\rp_{\\doit(W_1)}. \\]\n    The equality is understood componentwise on the four components $(J, V, E, L)$ of def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>; the conjunctive unpacking into the four field-by-field equalities is deferred to the proof.</p>\n<p><em>Carrier reading of the equality.</em>\n    By def <a href=\"#def_3_11\" class=\"refrow\"><code>def:G_node-splitting</code></a> the operation $\\spl(W_2)$ shifts the carrier from $J \\cup V$ to the tagged-disjoint-union carrier\n    \\[ J_{\\spl(W_2)} \\cup V_{\\spl(W_2)} \\;=\\; J \\;\\cup\\; \\lp (V \\sm W_2) \\dcup W_2^0 \\dcup W_2^1 \\rp, \\]\n    while by def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a> the operation $\\doit(W)$ preserves the carrier $J \\cup V$ of its input as a set (items i.\\ and ii.\\ give $J_{\\doit(W)} \\cup V_{\\doit(W)} = (J \\cup W) \\cup (V \\sm W) = J \\cup V$ whenever $W \\subseteq J \\cup V$). Thus both sides of the displayed equality live in a CDMG over the same $\\spl(W_2)$-carrier just displayed, and the asserted equality is a <em>literal</em> $=$ of CDMGs (def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>) — <em>not</em> an equality up to a carrier-relabelling map. On the right-hand side, $\\doit(W_1)$ is read as hard-intervening on the <em>unsplit copies</em> of the nodes of $W_1$ inside the split-graph carrier: every $w \\in W_1$ satisfies $w \\in J \\cup V$ (by $W_1 \\subseteq J \\cup V$) and $w \\notin W_2$ (by $W_1 \\cap W_2 = \\emptyset$), so $w \\in J \\cup (V \\sm W_2)$, which is the untagged piece of $J_{\\spl(W_2)} \\cup V_{\\spl(W_2)}$ under def <a href=\"#def_3_11\" class=\"refrow\"><code>def:G_node-splitting</code></a>'s notational shorthand “$v^0 := v^1 := v$ for $v \\in J \\cup (V \\sm W_2)$” (i.e.\\ $W_1$ injects into the split-graph carrier as the unsplit injection $w \\mapsto w$). The precondition $W_1 \\subseteq J_{\\spl(W_2)} \\cup V_{\\spl(W_2)}$ of def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a> is thereby met by the unsplit copies, so the RHS is also defined per def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a>, and the LN's $G_{\\spl(W_2)}$ carrier reading agrees with the unique LN-faithful reading of $\\doit(W_1)$ on it.</p>",
    "env": "Lem",
    "env_title": "Disjoint hard interventions and node-splittings commute",
    "source_path": "leanification/Chapter3_GraphTheory/Section3_2/tex/claim_3_8_statement_DisjointHardInterventions.tex"
  },
  "tex_proof": {
    "raw": "% Claim proof file for row claim_3_8 -- \"DisjointHardInterventions\".\n% Auto-generated stub by scaffold/solve_chapter.py at row start. A manager\n% agent dedicated to writing the TeX proof fills in the proof body below.\n%\n% This file is a sibling of `main.tex` inside `<subsection>/tex/`. The\n% `subfiles` package lets it render both standalone and as part of\n% `main.tex` via `\\subfile{...}`.\n%\n% The statement is restated above the proof so the file is self-contained\n% when rendered on its own. The proof must:\n%   - Stay close to the lecture notes' proof if one exists (always search\n%     the LN first for a `\\begin{proof}` block immediately following the\n%     claim; copy / lightly edit when present).\n%   - Otherwise use the LN paradigm and cite prior claims by their `ref`.\n%   - Be self-contained enough that a mathematician can follow it without\n%     re-reading the LN.\n%   - Have no `sorry`, `omitted`, or \"obvious\" shortcuts.\n\n\\documentclass[main]{subfiles}\n\n\\begin{document}\n% ref: claim_3_8 (proof, with statement restated for readability)\n% title: DisjointHardInterventions\n\\def\\rowref{claim\\_3\\_8}\n\\phantomsection\\label{claim_3_8}\n\n% --- statement (restated) ---------------------------------------------------\n% Statement body below is synced from the canonical statement file\n% `tex/claim_3_8_statement_DisjointHardInterventions.tex` (which has\n% already passed `verify_tex_statement_only` and\n% `verify_tex_statement_equivalence`). Do not edit it here; fix the\n% canonical file first and re-sync.\n%\n% The inner `\\label{disjoint-hard-interventions-node-splittings-commute}`\n% of the canonical Lem is kept so the body is byte-faithful to the\n% canonical statement. No duplicate-label clash arises with the outer\n% `\\phantomsection\\label{claim_3_8}` because the two labels resolve to\n% distinct strings; and `main.tex` only includes the proof file (not the\n% statement file) for claims, so the canonical file's labels never fire\n% in the same compile as this file.\n\n\\begin{Lem}[Disjoint hard interventions and node-splittings commute]\\label{disjoint-hard-interventions-node-splittings-commute}\n    Let $G = (J, V, E, L)$ be a CDMG (def \\ref{def-cdmg}); throughout we use the notation of def \\ref{not-cdmg} and recall the typing and axioms of def \\ref{def-cdmg}, namely $J \\cap V = \\emptyset$, $E \\subseteq (J \\cup V) \\times V$, $L \\subseteq V \\times V$, $L$ irreflexive ($(v_1, v_2) \\in L \\Rightarrow v_1 \\neq v_2$), and $L$ symmetric ($(v_1, v_2) \\in L \\Leftrightarrow (v_2, v_1) \\in L$). Let\n    \\[ W_1 \\subseteq J \\cup V \\quad\\text{and}\\quad W_2 \\subseteq V \\]\n    be two subsets of nodes of $G$, subject to the disjointness side condition\n    \\[ W_1 \\cap W_2 \\;=\\; \\emptyset. \\]\n    All three quantifiers ``for every CDMG $G$'', ``for every $W_1 \\subseteq J \\cup V$'', and ``for every $W_2 \\subseteq V$ with $W_1 \\cap W_2 = \\emptyset$'' are read \\emph{universally}; in particular the (vacuously disjoint) case $W_1 = W_2 = \\emptyset$ is admitted.\n\n    \\emph{Well-typedness of the inner splitting on the LHS.}\n    The LHS $\\lp G_{\\doit(W_1)} \\rp_{\\spl(W_2)}$ requires its outer $\\spl(W_2)$ to act on a CDMG whose output-node set contains $W_2$, i.e.\\ $W_2 \\subseteq V_{\\doit(W_1)}$ per def \\ref{def:G_node-splitting}'s precondition. By item ii.\\ of def \\ref{def:G_hard_intervention} we have $V_{\\doit(W_1)} = V \\sm W_1$, and the inclusion\n    \\[ W_2 \\;\\subseteq\\; V \\sm W_1 \\;=\\; V_{\\doit(W_1)} \\]\n    follows from $W_2 \\subseteq V$ together with $W_1 \\cap W_2 = \\emptyset$. Hence the LHS is defined per def \\ref{def:G_node-splitting}. Well-typedness of the RHS $\\lp G_{\\spl(W_2)} \\rp_{\\doit(W_1)}$ is spelled out in the carrier-reading paragraph below the displayed equality.\n\n    \\emph{The conclusion.}\n    The CDMG obtained from first hard intervening on $W_1$ and then node-splitting $W_2$ equals the CDMG obtained from first node-splitting $W_2$ and then hard intervening on $W_1$:\n    \\[ \\lp G_{\\doit(W_1)} \\rp_{\\spl(W_2)} \\;=\\; \\lp G_{\\spl(W_2)} \\rp_{\\doit(W_1)}. \\]\n    The equality is understood componentwise on the four components $(J, V, E, L)$ of def \\ref{def-cdmg}; the conjunctive unpacking into the four field-by-field equalities is deferred to the proof.\n\n    \\medskip\\emph{Carrier reading of the equality.}\n    By def \\ref{def:G_node-splitting} the operation $\\spl(W_2)$ shifts the carrier from $J \\cup V$ to the tagged-disjoint-union carrier\n    \\[ J_{\\spl(W_2)} \\cup V_{\\spl(W_2)} \\;=\\; J \\;\\cup\\; \\lp (V \\sm W_2) \\dcup W_2^0 \\dcup W_2^1 \\rp, \\]\n    while by def \\ref{def:G_hard_intervention} the operation $\\doit(W)$ preserves the carrier $J \\cup V$ of its input as a set (items i.\\ and ii.\\ give $J_{\\doit(W)} \\cup V_{\\doit(W)} = (J \\cup W) \\cup (V \\sm W) = J \\cup V$ whenever $W \\subseteq J \\cup V$). Thus both sides of the displayed equality live in a CDMG over the same $\\spl(W_2)$-carrier just displayed, and the asserted equality is a \\emph{literal} $=$ of CDMGs (def \\ref{def-cdmg}) --- \\emph{not} an equality up to a carrier-relabelling map. On the right-hand side, $\\doit(W_1)$ is read as hard-intervening on the \\emph{unsplit copies} of the nodes of $W_1$ inside the split-graph carrier: every $w \\in W_1$ satisfies $w \\in J \\cup V$ (by $W_1 \\subseteq J \\cup V$) and $w \\notin W_2$ (by $W_1 \\cap W_2 = \\emptyset$), so $w \\in J \\cup (V \\sm W_2)$, which is the untagged piece of $J_{\\spl(W_2)} \\cup V_{\\spl(W_2)}$ under def \\ref{def:G_node-splitting}'s notational shorthand ``$v^0 := v^1 := v$ for $v \\in J \\cup (V \\sm W_2)$'' (i.e.\\ $W_1$ injects into the split-graph carrier as the unsplit injection $w \\mapsto w$). The precondition $W_1 \\subseteq J_{\\spl(W_2)} \\cup V_{\\spl(W_2)}$ of def \\ref{def:G_hard_intervention} is thereby met by the unsplit copies, so the RHS is also defined per def \\ref{def:G_hard_intervention}, and the LN's $G_{\\spl(W_2)}$ carrier reading agrees with the unique LN-faithful reading of $\\doit(W_1)$ on it.\n\\end{Lem}\n\n% --- proof ------------------------------------------------------------------\n% Proof body adapted from the LN's \\Claude{...} block at\n% lecture-notes/lecture_notes/graphs.tex lines 504-534, with the following\n% refinements:\n% (i) the LN's terse parenthetical \"$W_1 \\cap W_2^0 = W_1 \\cap W_2^1 =\n%     \\emptyset$\" is replaced with an explicit decomposition of\n%     $((V \\sm W_2) \\dcup W_2^0 \\dcup W_2^1) \\sm W_1$ that splits the\n%     intersection across the three disjoint pieces and cites the\n%     disjointness $W_1 \\cap W_2 = \\emptyset$ at the $(V \\sm W_2)$-piece;\n% (ii) the LN's \"head $v_2^0 \\in W_1$ iff $v_2 \\in W_1$\" claim is unpacked\n%      into two sub-cases on whether $v_2 \\in W_2$ or $v_2 \\in V \\sm W_2$,\n%      with the disjointness $W_1 \\cap W_2 = \\emptyset$ cross-checked in\n%      the $v_2 \\in W_2$ branch;\n% (iii) the bidirected-edge section carries an explicit paragraph on the\n%       registered deviation `hard_intervention_l_symmetrized_removal'\n%       attached to def_3_10's encoding (two-sided filter on $L$ instead\n%       of the LN's literal one-sided filter), with the reconciliation\n%       under $L$'s symmetry axiom (def_3_1) spelled out.\n% The proof is organised under the LN's three section headings\n% (Node sets / Directed edges / Bidirected edges); the \"Node sets\" section\n% covers the $J$ and $V$ fields jointly, matching the LN's grouping and\n% the four-field componentwise check of the downstream Lean proof.\n\\begin{proof}\nWe verify the equality $\\lp G_{\\doit(W_1)} \\rp_{\\spl(W_2)} = \\lp G_{\\spl(W_2)} \\rp_{\\doit(W_1)}$ componentwise on the four fields $(J, V, E, L)$ of def \\ref{def-cdmg}, organised as in the LN under three section headings: ``Node sets'' (covering $J$ and $V$ jointly), ``Directed edges'' (the $E$ field), and ``Bidirected edges'' (the $L$ field). The well-typedness of both iteration orders has been established in the statement block above: the LHS by $W_2 \\subseteq V \\sm W_1 = V_{\\doit(W_1)}$ (using $W_2 \\subseteq V$ and the disjointness $W_1 \\cap W_2 = \\emptyset$), and the RHS by $W_1 \\subseteq J_{\\spl(W_2)} \\cup V_{\\spl(W_2)}$ via the unsplit injection of $W_1$ into the split-graph carrier (using $W_1 \\subseteq J \\cup V$ and $W_1 \\cap W_2 = \\emptyset$).\n\n\\smallskip\\noindent\\emph{Notational reminder.}\nFor $w \\in W_2$ we write $w^0$, $w^1$ for the two tagged copies produced by $\\spl(W_2)$ per def \\ref{def:G_node-splitting} item ii.; these live in the tagged-disjoint-union pieces $W_2^0$ and $W_2^1$ respectively, and are formally distinct from every element of $J \\cup V$ (and from each other), per def \\ref{def:G_node-splitting}'s tagged-copies disjointness assertions $W_2^0 \\cap V = W_2^1 \\cap V = W_2^0 \\cap J = W_2^1 \\cap J = W_2^0 \\cap W_2^1 = \\emptyset$. For $v \\in J \\cup (V \\sm W_2)$ we use def \\ref{def:G_node-splitting}'s unsplit shorthand $v^0 := v^1 := v$, which leaves $v$ in the untagged piece of $J_{\\spl(W_2)} \\cup V_{\\spl(W_2)}$. On the right-hand side, the outer $\\doit(W_1)$ is read on the lifted intervention set ``$W_1$ inside the split-graph carrier'', which by the carrier-reading paragraph of the statement is the unsplit image $\\lC w \\st w \\in W_1 \\rC \\subseteq J \\cup (V \\sm W_2)$ (using $W_1 \\subseteq J \\cup V$ and $W_1 \\cap W_2 = \\emptyset$); throughout the proof we write $W_1$ for this lifted set, using the unsplit shorthand $w = w^0 = w^1$ for $w \\in W_1$. ``Head'' of a directed edge $(a, b) \\in E$ means its second component $b$; ``endpoints'' of a bidirected edge $(a, b) \\in L$ means the pair $\\{a, b\\}$ of its first and second components.\n\n\\medskip\\noindent\\emph{Node sets.}\n\n\\smallskip\\noindent\\emph{Input nodes ($J$).}\nBy def \\ref{def:G_hard_intervention} item i.\\ applied to $G$ with intervention set $W_1$,\n\\[ J_{G_{\\doit(W_1)}} \\;=\\; J \\cup W_1. \\]\nBy def \\ref{def:G_node-splitting} item i.\\ applied to $G_{\\doit(W_1)}$ with split set $W_2$ (well-typed by $W_2 \\subseteq V \\sm W_1 = V_{\\doit(W_1)}$),\n\\[ J_{\\lp G_{\\doit(W_1)} \\rp_{\\spl(W_2)}} \\;=\\; J_{G_{\\doit(W_1)}} \\;=\\; J \\cup W_1. \\]\nFor the RHS, by def \\ref{def:G_node-splitting} item i.\\ applied to $G$ with split set $W_2$ and then def \\ref{def:G_hard_intervention} item i.\\ applied to $G_{\\spl(W_2)}$ with intervention set $W_1$ (lifted via the unsplit injection),\n\\[ J_{\\lp G_{\\spl(W_2)} \\rp_{\\doit(W_1)}} \\;=\\; J_{G_{\\spl(W_2)}} \\cup W_1 \\;=\\; J \\cup W_1. \\]\nHence\n\\[ J_{\\lp G_{\\doit(W_1)} \\rp_{\\spl(W_2)}} \\;=\\; J \\cup W_1 \\;=\\; J_{\\lp G_{\\spl(W_2)} \\rp_{\\doit(W_1)}}. \\]\n\n\\smallskip\\noindent\\emph{Output nodes ($V$).}\nBy def \\ref{def:G_hard_intervention} item ii.\\ applied to $G$ with intervention set $W_1$ and then def \\ref{def:G_node-splitting} item ii.\\ applied to $G_{\\doit(W_1)}$ with split set $W_2 \\subseteq V \\sm W_1 = V_{\\doit(W_1)}$,\n\\[\n    V_{\\lp G_{\\doit(W_1)} \\rp_{\\spl(W_2)}}\n    \\;=\\; \\lp V_{G_{\\doit(W_1)}} \\sm W_2 \\rp \\;\\dcup\\; W_2^0 \\;\\dcup\\; W_2^1\n    \\;=\\; \\lp (V \\sm W_1) \\sm W_2 \\rp \\;\\dcup\\; W_2^0 \\;\\dcup\\; W_2^1,\n\\]\nwhich simplifies (by $(V \\sm W_1) \\sm W_2 = V \\sm W_1 \\sm W_2$, the standard ``two-step removal'' identity for set-difference) to\n\\[\n    V_{\\lp G_{\\doit(W_1)} \\rp_{\\spl(W_2)}}\n    \\;=\\; (V \\sm W_1 \\sm W_2) \\;\\dcup\\; W_2^0 \\;\\dcup\\; W_2^1.\n\\]\n\nFor the RHS, by def \\ref{def:G_node-splitting} item ii.\\ applied to $G$ with split set $W_2$ and then def \\ref{def:G_hard_intervention} item ii.\\ applied to $G_{\\spl(W_2)}$ with intervention set $W_1$ (lifted to the split-graph carrier via the unsplit injection),\n\\[\n    V_{\\lp G_{\\spl(W_2)} \\rp_{\\doit(W_1)}}\n    \\;=\\; V_{G_{\\spl(W_2)}} \\sm W_1\n    \\;=\\; \\lp (V \\sm W_2) \\;\\dcup\\; W_2^0 \\;\\dcup\\; W_2^1 \\rp \\sm W_1.\n\\]\nWe simplify the latter by decomposing the set-difference across the three disjoint pieces of the disjoint union, using both the disjointness hypothesis $W_1 \\cap W_2 = \\emptyset$ (at the untagged piece) and def \\ref{def:G_node-splitting}'s tagged-construction disjointness (at the tagged pieces):\n\\begin{itemize}\n    \\item $W_1 \\cap W_2^0 = \\emptyset$ and $W_1 \\cap W_2^1 = \\emptyset$: by def \\ref{def:G_node-splitting} item ii.'s tagged-construction disjointness, $W_2^0$ and $W_2^1$ satisfy $W_2^0 \\cap (J \\cup V) = W_2^1 \\cap (J \\cup V) = \\emptyset$. Since $W_1 \\subseteq J \\cup V$ (and the lifted $W_1$ in the split-graph carrier sits in the untagged piece $J \\cup (V \\sm W_2)$, which is a subset of $J \\cup V$ under the unsplit injection), the lifted $W_1$ does not meet $W_2^0$ or $W_2^1$. Hence the set-difference acts as the identity on both tagged pieces.\n    \\item $(V \\sm W_2) \\sm W_1 = V \\sm W_1 \\sm W_2$: indeed, for any $x$, the chain of equivalences $x \\in (V \\sm W_2) \\sm W_1$ iff $x \\in V$, $x \\notin W_2$, $x \\notin W_1$ iff $x \\in V$, $x \\notin W_1$, $x \\notin W_2$ iff $x \\in V \\sm W_1 \\sm W_2$ holds. (Here the disjointness $W_1 \\cap W_2 = \\emptyset$ is \\emph{not} needed for this equality; the two iterated removals commute by associativity of $\\sm$ alone.)\n\\end{itemize}\nCombining the three piecewise calculations,\n\\[\n    \\lp (V \\sm W_2) \\;\\dcup\\; W_2^0 \\;\\dcup\\; W_2^1 \\rp \\sm W_1\n    \\;=\\; \\lp (V \\sm W_2) \\sm W_1 \\rp \\;\\dcup\\; W_2^0 \\;\\dcup\\; W_2^1\n    \\;=\\; (V \\sm W_1 \\sm W_2) \\;\\dcup\\; W_2^0 \\;\\dcup\\; W_2^1.\n\\]\nHence\n\\[\n    V_{\\lp G_{\\doit(W_1)} \\rp_{\\spl(W_2)}}\n    \\;=\\; (V \\sm W_1 \\sm W_2) \\;\\dcup\\; W_2^0 \\;\\dcup\\; W_2^1\n    \\;=\\; V_{\\lp G_{\\spl(W_2)} \\rp_{\\doit(W_1)}}.\n\\]\n\\checkmark\n\n\\medskip\\noindent\\emph{Directed edges.}\nBy def \\ref{def:G_hard_intervention} item iii.\\ applied to $G$ with intervention set $W_1$,\n\\[\n    E_{G_{\\doit(W_1)}}\n    \\;=\\; E \\sm \\lC (v_1, v_2) \\in E \\st v_2 \\in W_1 \\rC\n    \\;=\\; \\lC (v_1, v_2) \\in E \\st v_2 \\notin W_1 \\rC.\n\\]\nBy def \\ref{def:G_node-splitting} item iii.\\ applied to $G_{\\doit(W_1)}$ with split set $W_2$ (each surviving directed edge $(v_1, v_2) \\in E_{G_{\\doit(W_1)}}$ is lifted to $(v_1^1, v_2^0)$, and each $w \\in W_2$ contributes a transfer edge $(w^0, w^1)$),\n\\begin{align*}\n    E_{\\lp G_{\\doit(W_1)} \\rp_{\\spl(W_2)}}\n    &= \\lC (v_1^1, v_2^0) \\st (v_1, v_2) \\in E_{G_{\\doit(W_1)}} \\rC \\;\\cup\\; \\lC (w^0, w^1) \\st w \\in W_2 \\rC \\\\\n    &= \\lC (v_1^1, v_2^0) \\st (v_1, v_2) \\in E,\\; v_2 \\notin W_1 \\rC \\;\\cup\\; \\lC (w^0, w^1) \\st w \\in W_2 \\rC.\n\\end{align*}\n\nFor the RHS, by def \\ref{def:G_node-splitting} item iii.\\ applied to $G$ with split set $W_2$,\n\\[\n    E_{G_{\\spl(W_2)}}\n    \\;=\\; \\lC (v_1^1, v_2^0) \\st (v_1, v_2) \\in E \\rC \\;\\cup\\; \\lC (w^0, w^1) \\st w \\in W_2 \\rC.\n\\]\nBy def \\ref{def:G_hard_intervention} item iii.\\ applied to $G_{\\spl(W_2)}$ with intervention set $W_1$ (lifted to the split-graph carrier via the unsplit injection),\n\\[\n    E_{\\lp G_{\\spl(W_2)} \\rp_{\\doit(W_1)}}\n    \\;=\\; E_{G_{\\spl(W_2)}} \\sm \\lC (a, b) \\in E_{G_{\\spl(W_2)}} \\st b \\in W_1 \\rC.\n\\]\nThe removal condition ``$b \\in W_1$'' inspects the head $b$ of each candidate generator. We analyse it case by case on the two generator types of $E_{G_{\\spl(W_2)}}$.\n\n\\begin{itemize}\n    \\item \\emph{Lifted edges $(v_1^1, v_2^0)$ from $(v_1, v_2) \\in E$.}\n    The head is $v_2^0$. By def \\ref{def-cdmg}'s typing $E \\subseteq (J \\cup V) \\times V$ we have $v_2 \\in V$, so we case-split on whether $v_2 \\in W_2$:\n    \\begin{itemize}\n        \\item \\emph{If $v_2 \\in W_2$:} $v_2^0 \\in W_2^0$ is a tagged copy, lying in the disjoint-union piece $W_2^0$. By def \\ref{def:G_node-splitting} item ii.'s tagged-construction disjointness $W_2^0 \\cap (J \\cup V) = \\emptyset$, and since $W_1 \\subseteq J \\cup V$, we have $v_2^0 \\notin W_1$ (read in the split-graph carrier via the unsplit lift). The edge \\emph{survives} the RHS removal. Cross-check with the LHS: by the disjointness hypothesis $W_1 \\cap W_2 = \\emptyset$, $v_2 \\in W_2$ also forces $v_2 \\notin W_1$, so the LHS-side condition ``$v_2 \\notin W_1$'' is automatically met --- both sides keep this edge.\n        \\item \\emph{If $v_2 \\in V \\sm W_2$:} $v_2^0 = v_2$ (unsplit shorthand, since $v_2 \\in V \\sm W_2 \\subseteq J \\cup (V \\sm W_2)$). The lifted $W_1$ in the split-graph carrier is the unsplit image $\\lC w \\st w \\in W_1 \\rC$, so $v_2^0 \\in W_1$ iff $v_2 \\in W_1$ (the unsplit injection $J \\cup (V \\sm W_2) \\hookrightarrow J_{\\spl(W_2)} \\cup V_{\\spl(W_2)}$ is injective). The edge is removed by the RHS iff $v_2 \\in W_1$, equivalently kept iff $v_2 \\notin W_1$. This matches the LHS condition ``$v_2 \\notin W_1$'' verbatim.\n    \\end{itemize}\n    Combining the two sub-cases, the lifted edge $(v_1^1, v_2^0)$ from $(v_1, v_2) \\in E$ survives the RHS removal iff $v_2 \\notin W_1$, matching the LHS surviving condition exactly. (In the $v_2 \\in W_2$ branch this is supplied automatically by the disjointness $W_1 \\cap W_2 = \\emptyset$; in the $v_2 \\in V \\sm W_2$ branch it is read off the unsplit identification.)\n    \\item \\emph{Transfer edges $(w^0, w^1)$ for $w \\in W_2$.}\n    The head is $w^1 \\in W_2^1$, a tagged copy in the disjoint-union piece $W_2^1$. By def \\ref{def:G_node-splitting} item ii.'s tagged-construction disjointness $W_2^1 \\cap (J \\cup V) = \\emptyset$, and since $W_1 \\subseteq J \\cup V$, we have $w^1 \\notin W_1$ (in the split-graph carrier). Hence every transfer edge survives the RHS removal; the LHS contributes the same transfer edges $\\lC (w^0, w^1) \\st w \\in W_2 \\rC$ unchanged.\n\\end{itemize}\nTherefore\n\\[\n    E_{\\lp G_{\\spl(W_2)} \\rp_{\\doit(W_1)}}\n    \\;=\\; \\lC (v_1^1, v_2^0) \\st (v_1, v_2) \\in E,\\; v_2 \\notin W_1 \\rC \\;\\cup\\; \\lC (w^0, w^1) \\st w \\in W_2 \\rC.\n\\]\nComparing with the LHS,\n\\[\n    E_{\\lp G_{\\doit(W_1)} \\rp_{\\spl(W_2)}}\n    \\;=\\; \\lC (v_1^1, v_2^0) \\st (v_1, v_2) \\in E,\\; v_2 \\notin W_1 \\rC \\;\\cup\\; \\lC (w^0, w^1) \\st w \\in W_2 \\rC\n    \\;=\\; E_{\\lp G_{\\spl(W_2)} \\rp_{\\doit(W_1)}}.\n\\]\n\\checkmark\n\n\\medskip\\noindent\\emph{Bidirected edges.}\n\n\\smallskip\\noindent\\emph{Registered two-sided removal of $L$.}\nThe Lean encoding of def \\ref{def:G_hard_intervention} item iv.\\ filters $L_{\\doit(W)}$ \\emph{two-sidedly}: an ordered pair $(v_1, v_2) \\in L$ is removed whenever \\emph{either} endpoint lies in $W$, not only when the second component does. This is the registered deviation \\emph{hard\\_intervention\\_l\\_symmetrized\\_removal} attached to def\\_3\\_10's encoding, motivated in its remark on the symmetry of $L$: the LN's literal one-sided removal set $\\lC (v_1, v_2) \\in L \\st v_2 \\in W \\rC$ does not in general preserve the symmetry axiom of def \\ref{def-cdmg}, since for $w \\in W \\cap V$ and $v \\in V \\sm W$ both ordered pairs $(v, w)$ and $(w, v)$ lie in $L$ by symmetry but only $(v, w)$ is caught by the one-sided clause. The two-sided clause ``$v_1 \\in W \\vee v_2 \\in W$'' symmetrises the removal and agrees with the LN's natural-language gloss ``remove all edges into nodes from $W$'' (a bidirected edge $v_1 \\huh v_2$ is into \\emph{both} of its endpoints). Crucially, under the symmetry axiom of $L$ (def \\ref{def-cdmg}'s axiom $(v_1, v_2) \\in L \\Leftrightarrow (v_2, v_1) \\in L$), the LN's one-sided form and the registered two-sided form produce the \\emph{same} survivor set: a pair $(v_1, v_2) \\in L$ survives the one-sided filter iff $v_2 \\notin W$, and its symmetric partner $(v_2, v_1) \\in L$ (which is also in $L$ by symmetry) survives iff $v_1 \\notin W$; closing under symmetry, both readings land on\n\\[\n    \\lC (v_1, v_2) \\in L \\st v_1 \\notin W,\\; v_2 \\notin W \\rC.\n\\]\nWe adopt the two-sided form throughout the calculations below; the LN's literal one-sided gloss ``$L \\sm \\lC v \\huh w \\mid w \\in W_1 \\rC$'' of \\texttt{graphs.tex} lines 504--534 yields the same survivor set by the just-noted symmetric-closure argument.\n\nBy def \\ref{def:G_hard_intervention} item iv.\\ (two-sided), applied to $G$ with intervention set $W_1$,\n\\[\n    L_{G_{\\doit(W_1)}}\n    \\;=\\; \\lC (v_1, v_2) \\in L \\st v_1 \\notin W_1,\\; v_2 \\notin W_1 \\rC.\n\\]\nBy def \\ref{def:G_node-splitting} item iv.\\ applied to $G_{\\doit(W_1)}$ with split set $W_2$ (each surviving bidirected edge $(v_1, v_2) \\in L_{G_{\\doit(W_1)}}$ is lifted to $(v_1^0, v_2^0)$, with \\emph{both} endpoints carrying the $^0$ superscript),\n\\[\n    L_{\\lp G_{\\doit(W_1)} \\rp_{\\spl(W_2)}}\n    \\;=\\; \\lC (v_1^0, v_2^0) \\st (v_1, v_2) \\in L_{G_{\\doit(W_1)}} \\rC\n    \\;=\\; \\lC (v_1^0, v_2^0) \\st (v_1, v_2) \\in L,\\; v_1 \\notin W_1,\\; v_2 \\notin W_1 \\rC.\n\\]\n\nFor the RHS, by def \\ref{def:G_node-splitting} item iv.\\ applied to $G$ with split set $W_2$,\n\\[\n    L_{G_{\\spl(W_2)}}\n    \\;=\\; \\lC (v_1^0, v_2^0) \\st (v_1, v_2) \\in L \\rC.\n\\]\nBy def \\ref{def:G_hard_intervention} item iv.\\ (two-sided), applied to $G_{\\spl(W_2)}$ with intervention set $W_1$ (lifted to the split-graph carrier via the unsplit injection),\n\\[\n    L_{\\lp G_{\\spl(W_2)} \\rp_{\\doit(W_1)}}\n    \\;=\\; \\lC (a, b) \\in L_{G_{\\spl(W_2)}} \\st a \\notin W_1,\\; b \\notin W_1 \\rC.\n\\]\nWe compute the surviving pairs by analysing each endpoint of the generator $(v_1^0, v_2^0)$ arising from $(v_1, v_2) \\in L$. By def \\ref{def-cdmg}'s typing $L \\subseteq V \\times V$ we have $v_1, v_2 \\in V$. For each $k \\in \\{1, 2\\}$:\n\\begin{itemize}\n    \\item \\emph{If $v_k \\in W_2$:} $v_k^0 \\in W_2^0$ is a tagged copy. By def \\ref{def:G_node-splitting} item ii.'s tagged-construction disjointness $W_2^0 \\cap (J \\cup V) = \\emptyset$, and since $W_1 \\subseteq J \\cup V$, we have $v_k^0 \\notin W_1$ (in the split-graph carrier). Cross-check with the LHS: by the disjointness hypothesis $W_1 \\cap W_2 = \\emptyset$, $v_k \\in W_2$ also forces $v_k \\notin W_1$, so both the RHS-side condition ``$v_k^0 \\notin W_1$'' and the LHS-side condition ``$v_k \\notin W_1$'' are satisfied automatically in this branch.\n    \\item \\emph{If $v_k \\in V \\sm W_2$:} $v_k^0 = v_k$ (unsplit shorthand, since $v_k \\in V \\sm W_2 \\subseteq J \\cup (V \\sm W_2)$). The lifted $W_1$ in the split-graph carrier is the unsplit image $\\lC w \\st w \\in W_1 \\rC$, so $v_k^0 \\in W_1$ iff $v_k \\in W_1$ (the unsplit injection is injective). Hence the RHS-side condition ``$v_k^0 \\notin W_1$'' is equivalent to the LHS-side condition ``$v_k \\notin W_1$''.\n\\end{itemize}\nIn both sub-cases the condition ``$v_k^0 \\notin W_1$'' is equivalent to ``$v_k \\notin W_1$''. Applying this to both endpoints $v_1$ and $v_2$ independently,\n\\[\n    L_{\\lp G_{\\spl(W_2)} \\rp_{\\doit(W_1)}}\n    \\;=\\; \\lC (v_1^0, v_2^0) \\st (v_1, v_2) \\in L,\\; v_1 \\notin W_1,\\; v_2 \\notin W_1 \\rC.\n\\]\nComparing with the LHS,\n\\[\n    L_{\\lp G_{\\doit(W_1)} \\rp_{\\spl(W_2)}}\n    \\;=\\; \\lC (v_1^0, v_2^0) \\st (v_1, v_2) \\in L,\\; v_1 \\notin W_1,\\; v_2 \\notin W_1 \\rC\n    \\;=\\; L_{\\lp G_{\\spl(W_2)} \\rp_{\\doit(W_1)}}.\n\\]\n\\checkmark\n\n\\smallskip\nCombining the four componentwise equalities ($J$, $V$, $E$, $L$) above, the CDMGs $\\lp G_{\\doit(W_1)} \\rp_{\\spl(W_2)}$ and $\\lp G_{\\spl(W_2)} \\rp_{\\doit(W_1)}$ agree field by field on each of $(J, V, E, L)$ and are therefore equal as CDMGs (def \\ref{def-cdmg}).\n\\end{proof}\n\n\\end{document}\n",
    "html": "<div class=\"theorem-block\"><div class=\"theorem-label\">Lemma — <span class=\"theorem-title\">Disjoint hard interventions and node-splittings commute</span></div><p>Let $G = (J, V, E, L)$ be a CDMG (def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>); throughout we use the notation of def <a href=\"#def_3_2\" class=\"refrow\"><code>not-cdmg</code></a> and recall the typing and axioms of def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>, namely $J \\cap V = \\emptyset$, $E \\subseteq (J \\cup V) \\times V$, $L \\subseteq V \\times V$, $L$ irreflexive ($(v_1, v_2) \\in L \\Rightarrow v_1 \\neq v_2$), and $L$ symmetric ($(v_1, v_2) \\in L \\Leftrightarrow (v_2, v_1) \\in L$). Let\n    \\[ W_1 \\subseteq J \\cup V \\quad\\text{and}\\quad W_2 \\subseteq V \\]\n    be two subsets of nodes of $G$, subject to the disjointness side condition\n    \\[ W_1 \\cap W_2 \\;=\\; \\emptyset. \\]\n    All three quantifiers “for every CDMG $G$”, “for every $W_1 \\subseteq J \\cup V$”, and “for every $W_2 \\subseteq V$ with $W_1 \\cap W_2 = \\emptyset$” are read <em>universally</em>; in particular the (vacuously disjoint) case $W_1 = W_2 = \\emptyset$ is admitted.</p>\n<p><em>Well-typedness of the inner splitting on the LHS.</em>\n    The LHS $\\lp G_{\\doit(W_1)} \\rp_{\\spl(W_2)}$ requires its outer $\\spl(W_2)$ to act on a CDMG whose output-node set contains $W_2$, i.e.\\ $W_2 \\subseteq V_{\\doit(W_1)}$ per def <a href=\"#def_3_11\" class=\"refrow\"><code>def:G_node-splitting</code></a>'s precondition. By item ii.\\ of def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a> we have $V_{\\doit(W_1)} = V \\sm W_1$, and the inclusion\n    \\[ W_2 \\;\\subseteq\\; V \\sm W_1 \\;=\\; V_{\\doit(W_1)} \\]\n    follows from $W_2 \\subseteq V$ together with $W_1 \\cap W_2 = \\emptyset$. Hence the LHS is defined per def <a href=\"#def_3_11\" class=\"refrow\"><code>def:G_node-splitting</code></a>. Well-typedness of the RHS $\\lp G_{\\spl(W_2)} \\rp_{\\doit(W_1)}$ is spelled out in the carrier-reading paragraph below the displayed equality.</p>\n<p><em>The conclusion.</em>\n    The CDMG obtained from first hard intervening on $W_1$ and then node-splitting $W_2$ equals the CDMG obtained from first node-splitting $W_2$ and then hard intervening on $W_1$:\n    \\[ \\lp G_{\\doit(W_1)} \\rp_{\\spl(W_2)} \\;=\\; \\lp G_{\\spl(W_2)} \\rp_{\\doit(W_1)}. \\]\n    The equality is understood componentwise on the four components $(J, V, E, L)$ of def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>; the conjunctive unpacking into the four field-by-field equalities is deferred to the proof.</p>\n<p><em>Carrier reading of the equality.</em>\n    By def <a href=\"#def_3_11\" class=\"refrow\"><code>def:G_node-splitting</code></a> the operation $\\spl(W_2)$ shifts the carrier from $J \\cup V$ to the tagged-disjoint-union carrier\n    \\[ J_{\\spl(W_2)} \\cup V_{\\spl(W_2)} \\;=\\; J \\;\\cup\\; \\lp (V \\sm W_2) \\dcup W_2^0 \\dcup W_2^1 \\rp, \\]\n    while by def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a> the operation $\\doit(W)$ preserves the carrier $J \\cup V$ of its input as a set (items i.\\ and ii.\\ give $J_{\\doit(W)} \\cup V_{\\doit(W)} = (J \\cup W) \\cup (V \\sm W) = J \\cup V$ whenever $W \\subseteq J \\cup V$). Thus both sides of the displayed equality live in a CDMG over the same $\\spl(W_2)$-carrier just displayed, and the asserted equality is a <em>literal</em> $=$ of CDMGs (def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>) — <em>not</em> an equality up to a carrier-relabelling map. On the right-hand side, $\\doit(W_1)$ is read as hard-intervening on the <em>unsplit copies</em> of the nodes of $W_1$ inside the split-graph carrier: every $w \\in W_1$ satisfies $w \\in J \\cup V$ (by $W_1 \\subseteq J \\cup V$) and $w \\notin W_2$ (by $W_1 \\cap W_2 = \\emptyset$), so $w \\in J \\cup (V \\sm W_2)$, which is the untagged piece of $J_{\\spl(W_2)} \\cup V_{\\spl(W_2)}$ under def <a href=\"#def_3_11\" class=\"refrow\"><code>def:G_node-splitting</code></a>'s notational shorthand “$v^0 := v^1 := v$ for $v \\in J \\cup (V \\sm W_2)$” (i.e.\\ $W_1$ injects into the split-graph carrier as the unsplit injection $w \\mapsto w$). The precondition $W_1 \\subseteq J_{\\spl(W_2)} \\cup V_{\\spl(W_2)}$ of def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a> is thereby met by the unsplit copies, so the RHS is also defined per def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a>, and the LN's $G_{\\spl(W_2)}$ carrier reading agrees with the unique LN-faithful reading of $\\doit(W_1)$ on it.</p></div>\n<div class=\"tex-proof\"><span class=\"proof-label\">Proof.</span><p>We verify the equality $\\lp G_{\\doit(W_1)} \\rp_{\\spl(W_2)} = \\lp G_{\\spl(W_2)} \\rp_{\\doit(W_1)}$ componentwise on the four fields $(J, V, E, L)$ of def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>, organised as in the LN under three section headings: “Node sets” (covering $J$ and $V$ jointly), “Directed edges” (the $E$ field), and “Bidirected edges” (the $L$ field). The well-typedness of both iteration orders has been established in the statement block above: the LHS by $W_2 \\subseteq V \\sm W_1 = V_{\\doit(W_1)}$ (using $W_2 \\subseteq V$ and the disjointness $W_1 \\cap W_2 = \\emptyset$), and the RHS by $W_1 \\subseteq J_{\\spl(W_2)} \\cup V_{\\spl(W_2)}$ via the unsplit injection of $W_1$ into the split-graph carrier (using $W_1 \\subseteq J \\cup V$ and $W_1 \\cap W_2 = \\emptyset$).</p>\n<p><em>Notational reminder.</em>\nFor $w \\in W_2$ we write $w^0$, $w^1$ for the two tagged copies produced by $\\spl(W_2)$ per def <a href=\"#def_3_11\" class=\"refrow\"><code>def:G_node-splitting</code></a> item ii.; these live in the tagged-disjoint-union pieces $W_2^0$ and $W_2^1$ respectively, and are formally distinct from every element of $J \\cup V$ (and from each other), per def <a href=\"#def_3_11\" class=\"refrow\"><code>def:G_node-splitting</code></a>'s tagged-copies disjointness assertions $W_2^0 \\cap V = W_2^1 \\cap V = W_2^0 \\cap J = W_2^1 \\cap J = W_2^0 \\cap W_2^1 = \\emptyset$. For $v \\in J \\cup (V \\sm W_2)$ we use def <a href=\"#def_3_11\" class=\"refrow\"><code>def:G_node-splitting</code></a>'s unsplit shorthand $v^0 := v^1 := v$, which leaves $v$ in the untagged piece of $J_{\\spl(W_2)} \\cup V_{\\spl(W_2)}$. On the right-hand side, the outer $\\doit(W_1)$ is read on the lifted intervention set “$W_1$ inside the split-graph carrier”, which by the carrier-reading paragraph of the statement is the unsplit image $\\lC w \\st w \\in W_1 \\rC \\subseteq J \\cup (V \\sm W_2)$ (using $W_1 \\subseteq J \\cup V$ and $W_1 \\cap W_2 = \\emptyset$); throughout the proof we write $W_1$ for this lifted set, using the unsplit shorthand $w = w^0 = w^1$ for $w \\in W_1$. “Head” of a directed edge $(a, b) \\in E$ means its second component $b$; “endpoints” of a bidirected edge $(a, b) \\in L$ means the pair $\\{a, b\\}$ of its first and second components.</p>\n<p><em>Node sets.</em></p>\n<p><em>Input nodes ($J$).</em>\nBy def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a> item i.\\ applied to $G$ with intervention set $W_1$,\n\\[ J_{G_{\\doit(W_1)}} \\;=\\; J \\cup W_1. \\]\nBy def <a href=\"#def_3_11\" class=\"refrow\"><code>def:G_node-splitting</code></a> item i.\\ applied to $G_{\\doit(W_1)}$ with split set $W_2$ (well-typed by $W_2 \\subseteq V \\sm W_1 = V_{\\doit(W_1)}$),\n\\[ J_{\\lp G_{\\doit(W_1)} \\rp_{\\spl(W_2)}} \\;=\\; J_{G_{\\doit(W_1)}} \\;=\\; J \\cup W_1. \\]\nFor the RHS, by def <a href=\"#def_3_11\" class=\"refrow\"><code>def:G_node-splitting</code></a> item i.\\ applied to $G$ with split set $W_2$ and then def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a> item i.\\ applied to $G_{\\spl(W_2)}$ with intervention set $W_1$ (lifted via the unsplit injection),\n\\[ J_{\\lp G_{\\spl(W_2)} \\rp_{\\doit(W_1)}} \\;=\\; J_{G_{\\spl(W_2)}} \\cup W_1 \\;=\\; J \\cup W_1. \\]\nHence\n\\[ J_{\\lp G_{\\doit(W_1)} \\rp_{\\spl(W_2)}} \\;=\\; J \\cup W_1 \\;=\\; J_{\\lp G_{\\spl(W_2)} \\rp_{\\doit(W_1)}}. \\]</p>\n<p><em>Output nodes ($V$).</em>\nBy def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a> item ii.\\ applied to $G$ with intervention set $W_1$ and then def <a href=\"#def_3_11\" class=\"refrow\"><code>def:G_node-splitting</code></a> item ii.\\ applied to $G_{\\doit(W_1)}$ with split set $W_2 \\subseteq V \\sm W_1 = V_{\\doit(W_1)}$,\n\\[\n    V_{\\lp G_{\\doit(W_1)} \\rp_{\\spl(W_2)}}\n    \\;=\\; \\lp V_{G_{\\doit(W_1)}} \\sm W_2 \\rp \\;\\dcup\\; W_2^0 \\;\\dcup\\; W_2^1\n    \\;=\\; \\lp (V \\sm W_1) \\sm W_2 \\rp \\;\\dcup\\; W_2^0 \\;\\dcup\\; W_2^1,\n\\]\nwhich simplifies (by $(V \\sm W_1) \\sm W_2 = V \\sm W_1 \\sm W_2$, the standard “two-step removal” identity for set-difference) to\n\\[\n    V_{\\lp G_{\\doit(W_1)} \\rp_{\\spl(W_2)}}\n    \\;=\\; (V \\sm W_1 \\sm W_2) \\;\\dcup\\; W_2^0 \\;\\dcup\\; W_2^1.\n\\]</p>\n<p>For the RHS, by def <a href=\"#def_3_11\" class=\"refrow\"><code>def:G_node-splitting</code></a> item ii.\\ applied to $G$ with split set $W_2$ and then def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a> item ii.\\ applied to $G_{\\spl(W_2)}$ with intervention set $W_1$ (lifted to the split-graph carrier via the unsplit injection),\n\\[\n    V_{\\lp G_{\\spl(W_2)} \\rp_{\\doit(W_1)}}\n    \\;=\\; V_{G_{\\spl(W_2)}} \\sm W_1\n    \\;=\\; \\lp (V \\sm W_2) \\;\\dcup\\; W_2^0 \\;\\dcup\\; W_2^1 \\rp \\sm W_1.\n\\]\nWe simplify the latter by decomposing the set-difference across the three disjoint pieces of the disjoint union, using both the disjointness hypothesis $W_1 \\cap W_2 = \\emptyset$ (at the untagged piece) and def <a href=\"#def_3_11\" class=\"refrow\"><code>def:G_node-splitting</code></a>'s tagged-construction disjointness (at the tagged pieces):</p>\n<ul><li>$W_1 \\cap W_2^0 = \\emptyset$ and $W_1 \\cap W_2^1 = \\emptyset$: by def <a href=\"#def_3_11\" class=\"refrow\"><code>def:G_node-splitting</code></a> item ii.'s tagged-construction disjointness, $W_2^0$ and $W_2^1$ satisfy $W_2^0 \\cap (J \\cup V) = W_2^1 \\cap (J \\cup V) = \\emptyset$. Since $W_1 \\subseteq J \\cup V$ (and the lifted $W_1$ in the split-graph carrier sits in the untagged piece $J \\cup (V \\sm W_2)$, which is a subset of $J \\cup V$ under the unsplit injection), the lifted $W_1$ does not meet $W_2^0$ or $W_2^1$. Hence the set-difference acts as the identity on both tagged pieces.</li><li>$(V \\sm W_2) \\sm W_1 = V \\sm W_1 \\sm W_2$: indeed, for any $x$, the chain of equivalences $x \\in (V \\sm W_2) \\sm W_1$ iff $x \\in V$, $x \\notin W_2$, $x \\notin W_1$ iff $x \\in V$, $x \\notin W_1$, $x \\notin W_2$ iff $x \\in V \\sm W_1 \\sm W_2$ holds. (Here the disjointness $W_1 \\cap W_2 = \\emptyset$ is <em>not</em> needed for this equality; the two iterated removals commute by associativity of $\\sm$ alone.)</li></ul>\n<p>Combining the three piecewise calculations,\n\\[\n    \\lp (V \\sm W_2) \\;\\dcup\\; W_2^0 \\;\\dcup\\; W_2^1 \\rp \\sm W_1\n    \\;=\\; \\lp (V \\sm W_2) \\sm W_1 \\rp \\;\\dcup\\; W_2^0 \\;\\dcup\\; W_2^1\n    \\;=\\; (V \\sm W_1 \\sm W_2) \\;\\dcup\\; W_2^0 \\;\\dcup\\; W_2^1.\n\\]\nHence\n\\[\n    V_{\\lp G_{\\doit(W_1)} \\rp_{\\spl(W_2)}}\n    \\;=\\; (V \\sm W_1 \\sm W_2) \\;\\dcup\\; W_2^0 \\;\\dcup\\; W_2^1\n    \\;=\\; V_{\\lp G_{\\spl(W_2)} \\rp_{\\doit(W_1)}}.\n\\]\n✓</p>\n<p><em>Directed edges.</em>\nBy def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a> item iii.\\ applied to $G$ with intervention set $W_1$,\n\\[\n    E_{G_{\\doit(W_1)}}\n    \\;=\\; E \\sm \\lC (v_1, v_2) \\in E \\st v_2 \\in W_1 \\rC\n    \\;=\\; \\lC (v_1, v_2) \\in E \\st v_2 \\notin W_1 \\rC.\n\\]\nBy def <a href=\"#def_3_11\" class=\"refrow\"><code>def:G_node-splitting</code></a> item iii.\\ applied to $G_{\\doit(W_1)}$ with split set $W_2$ (each surviving directed edge $(v_1, v_2) \\in E_{G_{\\doit(W_1)}}$ is lifted to $(v_1^1, v_2^0)$, and each $w \\in W_2$ contributes a transfer edge $(w^0, w^1)$),\n\\[\\begin{aligned}\n    E_{\\lp G_{\\doit(W_1)} \\rp_{\\spl(W_2)}}\n    &= \\lC (v_1^1, v_2^0) \\st (v_1, v_2) \\in E_{G_{\\doit(W_1)}} \\rC \\;\\cup\\; \\lC (w^0, w^1) \\st w \\in W_2 \\rC \\\\\n    &= \\lC (v_1^1, v_2^0) \\st (v_1, v_2) \\in E,\\; v_2 \\notin W_1 \\rC \\;\\cup\\; \\lC (w^0, w^1) \\st w \\in W_2 \\rC.\n\\end{aligned}\\]</p>\n<p>For the RHS, by def <a href=\"#def_3_11\" class=\"refrow\"><code>def:G_node-splitting</code></a> item iii.\\ applied to $G$ with split set $W_2$,\n\\[\n    E_{G_{\\spl(W_2)}}\n    \\;=\\; \\lC (v_1^1, v_2^0) \\st (v_1, v_2) \\in E \\rC \\;\\cup\\; \\lC (w^0, w^1) \\st w \\in W_2 \\rC.\n\\]\nBy def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a> item iii.\\ applied to $G_{\\spl(W_2)}$ with intervention set $W_1$ (lifted to the split-graph carrier via the unsplit injection),\n\\[\n    E_{\\lp G_{\\spl(W_2)} \\rp_{\\doit(W_1)}}\n    \\;=\\; E_{G_{\\spl(W_2)}} \\sm \\lC (a, b) \\in E_{G_{\\spl(W_2)}} \\st b \\in W_1 \\rC.\n\\]\nThe removal condition “$b \\in W_1$” inspects the head $b$ of each candidate generator. We analyse it case by case on the two generator types of $E_{G_{\\spl(W_2)}}$.</p>\n<ul><li><em>Lifted edges $(v_1^1, v_2^0)$ from $(v_1, v_2) \\in E$.</em>\n    The head is $v_2^0$. By def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>'s typing $E \\subseteq (J \\cup V) \\times V$ we have $v_2 \\in V$, so we case-split on whether $v_2 \\in W_2$:\n    \\begin{itemize}</li><li><em>If $v_2 \\in W_2$:</em> $v_2^0 \\in W_2^0$ is a tagged copy, lying in the disjoint-union piece $W_2^0$. By def <a href=\"#def_3_11\" class=\"refrow\"><code>def:G_node-splitting</code></a> item ii.'s tagged-construction disjointness $W_2^0 \\cap (J \\cup V) = \\emptyset$, and since $W_1 \\subseteq J \\cup V$, we have $v_2^0 \\notin W_1$ (read in the split-graph carrier via the unsplit lift). The edge <em>survives</em> the RHS removal. Cross-check with the LHS: by the disjointness hypothesis $W_1 \\cap W_2 = \\emptyset$, $v_2 \\in W_2$ also forces $v_2 \\notin W_1$, so the LHS-side condition “$v_2 \\notin W_1$” is automatically met — both sides keep this edge.</li><li><em>If $v_2 \\in V \\sm W_2$:</em> $v_2^0 = v_2$ (unsplit shorthand, since $v_2 \\in V \\sm W_2 \\subseteq J \\cup (V \\sm W_2)$). The lifted $W_1$ in the split-graph carrier is the unsplit image $\\lC w \\st w \\in W_1 \\rC$, so $v_2^0 \\in W_1$ iff $v_2 \\in W_1$ (the unsplit injection $J \\cup (V \\sm W_2) \\hookrightarrow J_{\\spl(W_2)} \\cup V_{\\spl(W_2)}$ is injective). The edge is removed by the RHS iff $v_2 \\in W_1$, equivalently kept iff $v_2 \\notin W_1$. This matches the LHS condition “$v_2 \\notin W_1$” verbatim.\n    \\end{itemize}\n    Combining the two sub-cases, the lifted edge $(v_1^1, v_2^0)$ from $(v_1, v_2) \\in E$ survives the RHS removal iff $v_2 \\notin W_1$, matching the LHS surviving condition exactly. (In the $v_2 \\in W_2$ branch this is supplied automatically by the disjointness $W_1 \\cap W_2 = \\emptyset$; in the $v_2 \\in V \\sm W_2$ branch it is read off the unsplit identification.)</li><li><em>Transfer edges $(w^0, w^1)$ for $w \\in W_2$.</em>\n    The head is $w^1 \\in W_2^1$, a tagged copy in the disjoint-union piece $W_2^1$. By def <a href=\"#def_3_11\" class=\"refrow\"><code>def:G_node-splitting</code></a> item ii.'s tagged-construction disjointness $W_2^1 \\cap (J \\cup V) = \\emptyset$, and since $W_1 \\subseteq J \\cup V$, we have $w^1 \\notin W_1$ (in the split-graph carrier). Hence every transfer edge survives the RHS removal; the LHS contributes the same transfer edges $\\lC (w^0, w^1) \\st w \\in W_2 \\rC$ unchanged.</li></ul>\n<p>Therefore\n\\[\n    E_{\\lp G_{\\spl(W_2)} \\rp_{\\doit(W_1)}}\n    \\;=\\; \\lC (v_1^1, v_2^0) \\st (v_1, v_2) \\in E,\\; v_2 \\notin W_1 \\rC \\;\\cup\\; \\lC (w^0, w^1) \\st w \\in W_2 \\rC.\n\\]\nComparing with the LHS,\n\\[\n    E_{\\lp G_{\\doit(W_1)} \\rp_{\\spl(W_2)}}\n    \\;=\\; \\lC (v_1^1, v_2^0) \\st (v_1, v_2) \\in E,\\; v_2 \\notin W_1 \\rC \\;\\cup\\; \\lC (w^0, w^1) \\st w \\in W_2 \\rC\n    \\;=\\; E_{\\lp G_{\\spl(W_2)} \\rp_{\\doit(W_1)}}.\n\\]\n✓</p>\n<p><em>Bidirected edges.</em></p>\n<p><em>Registered two-sided removal of $L$.</em>\nThe Lean encoding of def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a> item iv.\\ filters $L_{\\doit(W)}$ <em>two-sidedly</em>: an ordered pair $(v_1, v_2) \\in L$ is removed whenever <em>either</em> endpoint lies in $W$, not only when the second component does. This is the registered deviation <em>hard\\_intervention\\_l\\_symmetrized\\_removal</em> attached to def\\_3\\_10's encoding, motivated in its remark on the symmetry of $L$: the LN's literal one-sided removal set $\\lC (v_1, v_2) \\in L \\st v_2 \\in W \\rC$ does not in general preserve the symmetry axiom of def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>, since for $w \\in W \\cap V$ and $v \\in V \\sm W$ both ordered pairs $(v, w)$ and $(w, v)$ lie in $L$ by symmetry but only $(v, w)$ is caught by the one-sided clause. The two-sided clause “$v_1 \\in W \\vee v_2 \\in W$” symmetrises the removal and agrees with the LN's natural-language gloss “remove all edges into nodes from $W$” (a bidirected edge $v_1 \\huh v_2$ is into <em>both</em> of its endpoints). Crucially, under the symmetry axiom of $L$ (def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>'s axiom $(v_1, v_2) \\in L \\Leftrightarrow (v_2, v_1) \\in L$), the LN's one-sided form and the registered two-sided form produce the <em>same</em> survivor set: a pair $(v_1, v_2) \\in L$ survives the one-sided filter iff $v_2 \\notin W$, and its symmetric partner $(v_2, v_1) \\in L$ (which is also in $L$ by symmetry) survives iff $v_1 \\notin W$; closing under symmetry, both readings land on\n\\[\n    \\lC (v_1, v_2) \\in L \\st v_1 \\notin W,\\; v_2 \\notin W \\rC.\n\\]\nWe adopt the two-sided form throughout the calculations below; the LN's literal one-sided gloss “$L \\sm \\lC v \\huh w \\mid w \\in W_1 \\rC$” of <code>graphs.tex</code> lines 504–534 yields the same survivor set by the just-noted symmetric-closure argument.</p>\n<p>By def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a> item iv.\\ (two-sided), applied to $G$ with intervention set $W_1$,\n\\[\n    L_{G_{\\doit(W_1)}}\n    \\;=\\; \\lC (v_1, v_2) \\in L \\st v_1 \\notin W_1,\\; v_2 \\notin W_1 \\rC.\n\\]\nBy def <a href=\"#def_3_11\" class=\"refrow\"><code>def:G_node-splitting</code></a> item iv.\\ applied to $G_{\\doit(W_1)}$ with split set $W_2$ (each surviving bidirected edge $(v_1, v_2) \\in L_{G_{\\doit(W_1)}}$ is lifted to $(v_1^0, v_2^0)$, with <em>both</em> endpoints carrying the $^0$ superscript),\n\\[\n    L_{\\lp G_{\\doit(W_1)} \\rp_{\\spl(W_2)}}\n    \\;=\\; \\lC (v_1^0, v_2^0) \\st (v_1, v_2) \\in L_{G_{\\doit(W_1)}} \\rC\n    \\;=\\; \\lC (v_1^0, v_2^0) \\st (v_1, v_2) \\in L,\\; v_1 \\notin W_1,\\; v_2 \\notin W_1 \\rC.\n\\]</p>\n<p>For the RHS, by def <a href=\"#def_3_11\" class=\"refrow\"><code>def:G_node-splitting</code></a> item iv.\\ applied to $G$ with split set $W_2$,\n\\[\n    L_{G_{\\spl(W_2)}}\n    \\;=\\; \\lC (v_1^0, v_2^0) \\st (v_1, v_2) \\in L \\rC.\n\\]\nBy def <a href=\"#def_3_10\" class=\"refrow\"><code>def:G_hard_intervention</code></a> item iv.\\ (two-sided), applied to $G_{\\spl(W_2)}$ with intervention set $W_1$ (lifted to the split-graph carrier via the unsplit injection),\n\\[\n    L_{\\lp G_{\\spl(W_2)} \\rp_{\\doit(W_1)}}\n    \\;=\\; \\lC (a, b) \\in L_{G_{\\spl(W_2)}} \\st a \\notin W_1,\\; b \\notin W_1 \\rC.\n\\]\nWe compute the surviving pairs by analysing each endpoint of the generator $(v_1^0, v_2^0)$ arising from $(v_1, v_2) \\in L$. By def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>'s typing $L \\subseteq V \\times V$ we have $v_1, v_2 \\in V$. For each $k \\in \\{1, 2\\}$:</p>\n<ul><li><em>If $v_k \\in W_2$:</em> $v_k^0 \\in W_2^0$ is a tagged copy. By def <a href=\"#def_3_11\" class=\"refrow\"><code>def:G_node-splitting</code></a> item ii.'s tagged-construction disjointness $W_2^0 \\cap (J \\cup V) = \\emptyset$, and since $W_1 \\subseteq J \\cup V$, we have $v_k^0 \\notin W_1$ (in the split-graph carrier). Cross-check with the LHS: by the disjointness hypothesis $W_1 \\cap W_2 = \\emptyset$, $v_k \\in W_2$ also forces $v_k \\notin W_1$, so both the RHS-side condition “$v_k^0 \\notin W_1$” and the LHS-side condition “$v_k \\notin W_1$” are satisfied automatically in this branch.</li><li><em>If $v_k \\in V \\sm W_2$:</em> $v_k^0 = v_k$ (unsplit shorthand, since $v_k \\in V \\sm W_2 \\subseteq J \\cup (V \\sm W_2)$). The lifted $W_1$ in the split-graph carrier is the unsplit image $\\lC w \\st w \\in W_1 \\rC$, so $v_k^0 \\in W_1$ iff $v_k \\in W_1$ (the unsplit injection is injective). Hence the RHS-side condition “$v_k^0 \\notin W_1$” is equivalent to the LHS-side condition “$v_k \\notin W_1$”.</li></ul>\n<p>In both sub-cases the condition “$v_k^0 \\notin W_1$” is equivalent to “$v_k \\notin W_1$”. Applying this to both endpoints $v_1$ and $v_2$ independently,\n\\[\n    L_{\\lp G_{\\spl(W_2)} \\rp_{\\doit(W_1)}}\n    \\;=\\; \\lC (v_1^0, v_2^0) \\st (v_1, v_2) \\in L,\\; v_1 \\notin W_1,\\; v_2 \\notin W_1 \\rC.\n\\]\nComparing with the LHS,\n\\[\n    L_{\\lp G_{\\doit(W_1)} \\rp_{\\spl(W_2)}}\n    \\;=\\; \\lC (v_1^0, v_2^0) \\st (v_1, v_2) \\in L,\\; v_1 \\notin W_1,\\; v_2 \\notin W_1 \\rC\n    \\;=\\; L_{\\lp G_{\\spl(W_2)} \\rp_{\\doit(W_1)}}.\n\\]\n✓</p>\n<p>Combining the four componentwise equalities ($J$, $V$, $E$, $L$) above, the CDMGs $\\lp G_{\\doit(W_1)} \\rp_{\\spl(W_2)}$ and $\\lp G_{\\spl(W_2)} \\rp_{\\doit(W_1)}$ agree field by field on each of $(J, V, E, L)$ and are therefore equal as CDMGs (def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>).</p></div>",
    "source_path": "leanification/Chapter3_GraphTheory/Section3_2/tex/claim_3_8_proof_DisjointHardInterventions.tex"
  },
  "lean_blocks": [
    {
      "kind": "helper",
      "code": "variable {Node : Type*} [DecidableEq Node]",
      "explanation": "This is a `variable` declaration: Lean auto-binds `Node : Type*` and the instance `[DecidableEq Node]` as implicit arguments to every subsequent declaration in the surrounding `CDMG` namespace, sparing us from rewriting the same parameters on the two helpers and the main theorem. The `Type*` notation makes `Node` universe-polymorphic, so callers can instantiate it at any universe level. The `DecidableEq Node` assumption is load-bearing for this row: the proof manipulates `Finset.filter`, `Finset.union`, `Finset.sdiff`, and `Finset.image` over both `Node` and `SplitNode Node`, all of which need decidable equality to compute the resulting `Finset`. The split-graph carrier `SplitNode Node` inherits `[DecidableEq (SplitNode Node)]` automatically via the `deriving DecidableEq` clause on `SplitNode`, so the single ambient assumption propagates through both iteration orders of $\\doit$ and $\\spl$. Stronger structure on `Node` (e.g.\\ `Fintype`, `LinearOrder`) is not required here. Same convention as the rest of `Chapter3_GraphTheory`'s files.",
      "code_annotated": "-- Auto-binds `Node` + `DecidableEq Node` as implicits; decidability drives `Finset.filter`/`union`/`sdiff`/`image` on both `Node` and `SplitNode Node`.\nvariable {Node : Type*} [DecidableEq Node]"
    },
    {
      "kind": "helper",
      "code": "private lemma subset_V_of_hardInterventionOn\n    {G : CDMG Node} {W₁ : Finset Node} (hW₁ : W₁ ⊆ G.J ∪ G.V)\n    {W₂ : Finset Node} (hW₂ : W₂ ⊆ G.V) (hDisj : Disjoint W₁ W₂) :\n    W₂ ⊆ (G.hardInterventionOn W₁ hW₁).V",
      "explanation": "This block is the first of two load-bearing statement helpers for the mixed commutativity claim (one `hardInterventionOn` + one `nodeSplittingOn`, on disjoint node sets). The `private lemma` records that for $W_1, W_2$ with $W_2 \\subseteq V$ disjoint from $W_1$, the set $W_2$ still lies inside the output-node set of $G_{\\doit(W_1)}$ — concretely $W_2 \\subseteq (G.do(W_1)).V$. The fact is needed inside the main theorem's signature to type the call `(G.hardInterventionOn W₁ ...).nodeSplittingOn W₂ ...`, which demands $W_2 \\subseteq V$ on the intervened carrier. Disjointness of $W_1$ and $W_2$ is what guarantees $W_2$ doesn't get sucked into $W_1$ during the intervention's set-difference $V \\setminus W_1$. The marker pair wraps the signature only (signature-only helper-wrap convention); the `:= by ...` tactic proof body sits below the end marker and routes elementhood through `Finset.mem_sdiff` to land each $w \\in W_2$ in $G.V$ minus $W_1$.",
      "code_annotated": "-- ref: claim_3_8 (helper) — statement-supporting subset lemma for the mixed commutativity claim.\n-- For `W₁, W₂` with `W₂ ⊆ V` disjoint from `W₁`, `W₂` survives `hardInterventionOn W₁` on the V-side.\nprivate lemma subset_V_of_hardInterventionOn\n    {G : CDMG Node} {W₁ : Finset Node} (hW₁ : W₁ ⊆ G.J ∪ G.V)\n    {W₂ : Finset Node} (hW₂ : W₂ ⊆ G.V) (hDisj : Disjoint W₁ W₂) :\n    W₂ ⊆ (G.hardInterventionOn W₁ hW₁).V"
    },
    {
      "kind": "helper",
      "code": "private lemma image_unsplit_subset_carrier_of_nodeSplittingOn\n    {G : CDMG Node} {W₁ : Finset Node} (hW₁ : W₁ ⊆ G.J ∪ G.V)\n    {W₂ : Finset Node} (hW₂ : W₂ ⊆ G.V) (hDisj : Disjoint W₁ W₂) :\n    W₁.image SplitNode.unsplit ⊆\n      (G.nodeSplittingOn W₂ hW₂).J ∪ (G.nodeSplittingOn W₂ hW₂).V",
      "explanation": "This block is the second of two load-bearing statement helpers for the mixed commutativity claim. The `private lemma` is the mirror of the first: where the previous helper handles the `hardInterventionOn W₁` then `nodeSplittingOn W₂` order, this one handles the reverse — `nodeSplittingOn W₂` first, then `hardInterventionOn W₁`. It records that for disjoint $W_1 \\subseteq J \\cup V$ and $W_2 \\subseteq V$, the `unsplit`-image of $W_1$ sits inside the carrier of $G_{\\spl(W_2)}$. The marker pair wraps the signature only (signature-only helper-wrap convention); the `:= by ...` tactic proof body sits below the end marker. The proof unpacks `Finset.mem_image` to get a pre-image $w \\in W_1$, then routes through `Finset.subset_union_left` / `Finset.subset_union_right` against $(V \\setminus W_2)$.image\\,unsplit and the original $J$.image\\,unsplit components of $G_{\\spl(W_2)}$'s carrier — leveraging disjointness $W_1 \\cap W_2 = \\emptyset$ to keep $W_1$ entirely inside $V \\setminus W_2$ (when in $V$) or inside $J$.",
      "code_annotated": "-- ref: claim_3_8 (helper) — mirror-direction statement-supporting subset lemma.\n-- For disjoint `W₁ ⊆ J ∪ V`, `W₂ ⊆ V`: the `unsplit`-image of `W₁` lives in `G.spl(W₂)`'s carrier.\nprivate lemma image_unsplit_subset_carrier_of_nodeSplittingOn\n    {G : CDMG Node} {W₁ : Finset Node} (hW₁ : W₁ ⊆ G.J ∪ G.V)\n    {W₂ : Finset Node} (hW₂ : W₂ ⊆ G.V) (hDisj : Disjoint W₁ W₂) :\n    W₁.image SplitNode.unsplit ⊆\n      (G.nodeSplittingOn W₂ hW₂).J ∪ (G.nodeSplittingOn W₂ hW₂).V"
    },
    {
      "kind": "main",
      "code": "theorem disjointHardInterventionsAndNodeSplittingsCommute (G : CDMG Node)\n    (W₁ W₂ : Finset Node) (hW₁ : W₁ ⊆ G.J ∪ G.V) (hW₂ : W₂ ⊆ G.V)\n    (hDisj : Disjoint W₁ W₂) :\n    (G.hardInterventionOn W₁ hW₁).nodeSplittingOn W₂\n        (subset_V_of_hardInterventionOn hW₁ hW₂ hDisj)\n      = (G.nodeSplittingOn W₂ hW₂).hardInterventionOn\n          (W₁.image SplitNode.unsplit)\n          (image_unsplit_subset_carrier_of_nodeSplittingOn hW₁ hW₂ hDisj)",
      "explanation": "This block is the main claim: the iterated CDMG operations $\\doit(W_1)$ and $\\spl(W_2)$ commute when their target sets are disjoint. The theorem takes a CDMG `G : CDMG Node`, two `Finset Node` arguments `W₁ W₂`, the LN typings `hW₁ : W₁ ⊆ G.J ∪ G.V` and `hW₂ : W₂ ⊆ G.V`, and the disjointness hypothesis `hDisj : Disjoint W₁ W₂` (the Mathlib idiom for $W_1 \\cap W_2 = \\emptyset$). The signature is the row's headline deliverable; the two private helpers above (`subset_V_of_hardInterventionOn` and `image_unsplit_subset_carrier_of_nodeSplittingOn`) feed the inner-`hW` arguments of the LHS's outer `nodeSplittingOn` and the RHS's outer `hardInterventionOn` respectively. Because `hardInterventionOn` preserves the node carrier per `def_3_10` and `nodeSplittingOn` lifts it to `SplitNode Node` per `def_3_11`, both sides land in `CDMG (SplitNode Node)`, so the equality is a literal `=` of CDMGs — no carrier relabelling. The `W₁.image SplitNode.unsplit` on the RHS encodes the LN's reading of $\\doit(W_1)$ on the split-graph carrier via the unsplit injection $w \\mapsto w$. Mirrors the literal-`=` pattern of `claim_3_4`.",
      "code_annotated": "-- Main claim: `doit(W₁)`-then-`spl(W₂)` equals `spl(W₂)`-then-`doit(W₁)` under disjointness.\ntheorem disjointHardInterventionsAndNodeSplittingsCommute (G : CDMG Node)\n    -- `W₁`, `W₂` are the LN's intervention/split sets; `Finset` is the chapter-wide encoding.\n    (W₁ W₂ : Finset Node) (hW₁ : W₁ ⊆ G.J ∪ G.V) (hW₂ : W₂ ⊆ G.V)\n    -- `Disjoint W₁ W₂` is Mathlib's `Finset` form of `W₁ ∩ W₂ = ∅`.\n    (hDisj : Disjoint W₁ W₂) :\n    -- LHS: first hard-intervene on `W₁`, then node-split on `W₂` (well-typed by the LHS helper).\n    (G.hardInterventionOn W₁ hW₁).nodeSplittingOn W₂\n        (subset_V_of_hardInterventionOn hW₁ hW₂ hDisj)\n      -- RHS: first node-split on `W₂`, then hard-intervene on the unsplit lift of `W₁`.\n      = (G.nodeSplittingOn W₂ hW₂).hardInterventionOn\n          -- `W₁.image SplitNode.unsplit` injects `W₁` into the split-graph carrier.\n          (W₁.image SplitNode.unsplit)\n          -- Well-typedness of the RHS's outer intervention is discharged by the RHS helper.\n          (image_unsplit_subset_carrier_of_nodeSplittingOn hW₁ hW₂ hDisj)"
    }
  ],
  "lean_source_url": "https://github.com/schipeltaper/causality-leanified/blob/main/leanification/Chapter3_GraphTheory/Section3_2/DisjointHardInterventions.lean",
  "lean_file_path": "leanification/Chapter3_GraphTheory/Section3_2/DisjointHardInterventions.lean",
  "addition_to_the_LN": "",
  "design_choices": "**Literal `=` of CDMGs, not `eqViaNodeMap` / `flattenSplit`.** This is the dominant simplification relative to `claim_3_7` (`TwoDisjointNode`). Both sides take a *single* node-splitting on the same `W₂`, and `hardInterventionOn` preserves the node carrier per def 3.10, so both sides land in `CDMG (SplitNode Node)`: no carrier mismatch arises and the asserted equality is a literal `=` between two terms of identical Lean type. Contrast `claim_3_7`, where iterating `nodeSplittingOn` twice produces `CDMG (SplitNode (SplitNode Node))` with disagreeing constructor wrappings, forcing a canonical-flatten relabelling. Here the LN's 'the same CDMG' reading is delivered by Lean's structural `=`; no carrier-relabelling map, no quotient types. Mirrors the literal-`=` pattern of `claim_3_4` (`HardInterventionsCommute`), lifted to the `SplitNode Node` carrier.\n\n**Disjointness `Disjoint W₁ W₂` is genuinely load-bearing.** Two distinct uses appear inside the signature: the LHS's inner splitting needs $W_2 \\subseteq V \\setminus W_1$ (discharged by `subset_V_of_hardInterventionOn`), and the RHS's outer intervention needs the unsplit-lifted $W_1$ to sit inside the split-graph carrier (discharged by `image_unsplit_subset_carrier_of_nodeSplittingOn`). Without disjointness, a node $w \\in W_1 \\cap W_2$ would simultaneously be hard-intervened (becomes an input on one side) and split (gets new copies on the other), committing both sides to incompatible carrier placements; the equality would no longer hold set-theoretically. Encoded as Mathlib's `Disjoint` rather than a raw `Finset.inter` equality, so the helper proofs are one step from `Finset.disjoint_left.mp` / `_right.mp`.\n\n**Lift `W₁` to the split carrier via `Finset.image SplitNode.unsplit`.** Every $w \\in W_1$ lies in $J \\cup (V \\setminus W_2)$ by `hW₁` and disjointness — the untagged piece of the split-graph carrier under def 3.11's shorthand $v^0 := v^1 := v$. Lifting via `.image .unsplit` lets the RHS's outer hard intervention be expressed in terms of the *original* `W₁`, rather than forcing the consumer to construct a fresh `Finset (SplitNode Node)` from scratch. The same lift convention is used in `claim_3_7`; reusing it here keeps the two formalisations parallel.\n\n**Single theorem, not a conjunction.** The LN statement is a single equality $\\mathrm{LHS} = \\mathrm{RHS}$, not a triple equality of the kind that `claim_3_4` and `claim_3_7` decomposed into `(a) ∧ (b)`. There is no joint-intervention form analogous to $G_{\\mathrm{do}(W_1 \\cup W_2)}$ available here, because `doit` and `spl` are structurally distinct operations (different carriers, different field equations) and do not admit a single combined invocation.\n\n**Two-sided filter on $L$ (registered deviation).** The encoding of $\\mathrm{doit}(W)$ in def 3.10 filters $L_{\\mathrm{doit}(W)}$ two-sidedly — removing pairs whenever *either* endpoint lies in $W$ — instead of the LN's literal one-sided clause. The one-sided form does not preserve $L$'s symmetry axiom, since for $w \\in W$ and $v \\notin W$ the symmetric partners $(v, w)$ and $(w, v)$ would otherwise be treated asymmetrically. Under the symmetry axiom both readings collapse to the same survivor set, so the bidirected-edge component is unaffected by the choice."
}
