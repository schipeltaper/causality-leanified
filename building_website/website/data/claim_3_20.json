{
  "ref": "claim_3_20",
  "kind": "claim",
  "section": "3.3",
  "title": "AcyclicNonCollidersBlockable",
  "type": "remark",
  "status": {
    "formalized": "yes",
    "proven": "proven",
    "solved": "yes"
  },
  "tex_block_html": "<p>If $G$ is acyclic then all non-colliders are blockable.</p>",
  "tex_statement": {
    "raw": "% Claim statement file for row claim_3_20 -- \"AcyclicNonCollidersBlockable\".\n% Auto-generated stub by scaffold/solve_chapter.py at row start. The\n% manager agent (or a worker it dispatches) fills the body below with the\n% claim's statement(s) from the lecture notes -- statement only, no proof\n% (proofs live in the sibling `claim_3_20_proof_*.tex` file).\n%\n% This file is a sibling of `main.tex` inside `<subsection>/tex/`. The\n% `subfiles` package lets it render both standalone and as part of\n% `main.tex`. Note: `main.tex` does NOT \\subfile this statement file -- the\n% sibling `_proof_` file restates the statement above the proof, so\n% including both in `main.tex` would be redundant. This file exists for\n% standalone reading / grep convenience.\n\n\\documentclass[main]{subfiles}\n\n\\begin{document}\n% ref: claim_3_20 (statement)\n% title: AcyclicNonCollidersBlockable\n\\def\\rowref{claim\\_3\\_20}\n\\phantomsection\\label{claim_3_20}\n\n\\begin{Rem}[AcyclicNonCollidersBlockable]\n    Let $G = (J, V, E, L)$ be a CDMG (def \\ref{def-cdmg}) and suppose $G$ is acyclic in the sense of def \\ref{def-acylic}. Then, for every integer $n \\ge 0$, every walk\n    \\[\n        \\pi = (v_0, a_0, v_1, a_1, \\dots, v_{n-1}, a_{n-1}, v_n)\n    \\]\n    in $G$ in the sense of def \\ref{def:walks}, item~i.\\ (so $v_0, \\dots, v_n \\in J \\cup V$, $a_0, \\dots, a_{n-1} \\in E \\cup L$, and the walk constraint\n    \\[\n        \\bigl(a_k = (v_k, v_{k+1}) \\;\\land\\; a_k \\in E \\cup L\\bigr)\n        \\;\\lor\\;\n        \\bigl(a_k = (v_{k+1}, v_k) \\;\\land\\; a_k \\in E\\bigr)\n    \\]\n    holds at every $k \\in \\{0, 1, \\dots, n - 1\\}$), and every position $k \\in \\{0, 1, \\dots, n\\}$, the following implication holds:\n    \\[\n        \\bigl(\\text{$k$ is a non-collider on $\\pi$ per def \\ref{def:collider_noncollider}, item~i.}\\bigr)\n        \\;\\implies\\;\n        \\bigl(\\text{$k$ is a blockable non-collider on $\\pi$ per def \\ref{def:unblockable_noncollider}}\\bigr).\n    \\]\n\\end{Rem}\n\n\\end{document}\n",
    "html": "<p>Let $G = (J, V, E, L)$ be a CDMG (def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>) and suppose $G$ is acyclic in the sense of def <a href=\"#def_3_6\" class=\"refrow\"><code>def-acylic</code></a>. Then, for every integer $n \\ge 0$, every walk\n    \\[\n        \\pi = (v_0, a_0, v_1, a_1, \\dots, v_{n-1}, a_{n-1}, v_n)\n    \\]\n    in $G$ in the sense of def <a href=\"#def_3_4\" class=\"refrow\"><code>def:walks</code></a>, item&nbsp;i.\\ (so $v_0, \\dots, v_n \\in J \\cup V$, $a_0, \\dots, a_{n-1} \\in E \\cup L$, and the walk constraint\n    \\[\n        \\bigl(a_k = (v_k, v_{k+1}) \\;\\land\\; a_k \\in E \\cup L\\bigr)\n        \\;\\lor\\;\n        \\bigl(a_k = (v_{k+1}, v_k) \\;\\land\\; a_k \\in E\\bigr)\n    \\]\n    holds at every $k \\in \\{0, 1, \\dots, n - 1\\}$), and every position $k \\in \\{0, 1, \\dots, n\\}$, the following implication holds:\n    \\[\n        \\bigl(\\text{$k$ is a non-collider on $\\pi$ per def \\ref{def:collider_noncollider}, item~i.}\\bigr)\n        \\;\\implies\\;\n        \\bigl(\\text{$k$ is a blockable non-collider on $\\pi$ per def \\ref{def:unblockable_noncollider}}\\bigr).\n    \\]</p>",
    "env": "Rem",
    "env_title": "AcyclicNonCollidersBlockable",
    "source_path": "leanification/Chapter3_GraphTheory/Section3_3/tex/claim_3_20_statement_AcyclicNonCollidersBlockable.tex"
  },
  "tex_proof": {
    "raw": "% Refactor twin of claim_3_20 -- \"AcyclicNonCollidersBlockable\".\n%\n% Refactor: `collider_side_aware`.  Roots of this refactor are\n% `def_3_15` (`CollidersAndNon.lean`); this row (`claim_3_20`) is a\n% DEPENDENT, pulled in because `def_3_15`'s Lean-side per-step\n% arrowhead-contribution predicate changed underneath it.  The\n% refactor replaces the node-equality reading\n% `WalkStep.IsInto : … → Node → Prop` with the side-aware\n% constructor-tag pair `refactor_HeadAtTarget` /\n% `refactor_HeadAtSource` (zero-arg, reading the WalkStep's\n% constructor tag plus type indices; an opposite-channel\n% `L`-disjunct re-fires at writing-mirror non-self-loop pairs so the\n% encoding still matches the LN's literal stored-pair\n% OR-of-channels test pointwise on every non-self-loop walk-step).\n% The manager-accepted deviation\n% `collider_side_aware_at_self_loops` (see\n% `leanification/deviations.json`) records the precise corner where\n% the side-aware reading strictly refines the literal stored-pair\n% reading: at a directed self-loop step `(v, v) \\in E`, the\n% side-aware reading commits to \"arrowhead at the walk-traversal\n% target end only, tail at the source end\", preserved verbatim by\n% the `hL_irrefl` field of `def_3_1` ruling out\n% `s(v, v) \\in L` and hence the L-disjunct re-firing at the\n% self-loop.\n%\n% The LN-level mathematics of this claim -- the implication\n% \"non-collider on \\pi implies blockable non-collider on \\pi\", under\n% acyclicity of G -- is **unchanged** by `collider_side_aware`.  The\n% refactor is purely a Lean-side encoding choice for the per-step\n% predicate \"edge into v_k\"; the LN-prose proof below operates on\n% the LN's literal stored-pair / walk-constraint reading directly,\n% which is the surface the side-aware reading still agrees with on\n% every walk that traverses no directed self-loop step.  At a\n% directed self-loop step the deviation is invisible to this\n% particular proof, because the proof never extracts an arrowhead\n% from a self-loop step: it only argues that the non-collider\n% hypothesis forces at least one walk-incident slot at v_k to host\n% an outgoing directed edge `(v_k, w) \\in E`, and that under\n% acyclicity, `w \\notin Sc^G(v_k)` (a contradiction with\n% `def_3_5`'s `\\Anc \\cap \\Desc` reading otherwise yielding a\n% non-trivial directed walk from v_k to itself).  Neither leg of\n% the argument inspects the head/tail attribution at a self-loop\n% step; the proof body is therefore byte-for-byte identical to the\n% canonical original\n% (`claim_3_20_proof_AcyclicNonCollidersBlockable.tex`, itself a\n% twin from the previous `cdmg_typed_edges` refactor whose\n% mathematical content already establishes the LN claim).\n%\n% Twin of `claim_3_20_proof_AcyclicNonCollidersBlockable.tex`; the\n% original proof stays in place untouched until the Phase 7 cleanup\n% of `collider_side_aware` swaps this twin over the original.\n% Statement block is synced verbatim from the canonical statement\n% file `claim_3_20_statement_AcyclicNonCollidersBlockable.tex` --\n% the claim is unchanged by the refactor.\n\n\\documentclass[main]{subfiles}\n\n\\begin{document}\n% ref: claim_3_20 (refactor twin proof, with statement restated for readability)\n% title: AcyclicNonCollidersBlockable\n\\def\\rowref{claim\\_3\\_20}\n\\phantomsection\\label{claim_3_20}\n\n% --- statement (restated, synced verbatim from\n% --- claim_3_20_statement_AcyclicNonCollidersBlockable.tex) ----------------\n\\begin{Rem}[AcyclicNonCollidersBlockable]\n    Let $G = (J, V, E, L)$ be a CDMG (def \\ref{def-cdmg}) and suppose $G$ is acyclic in the sense of def \\ref{def-acylic}. Then, for every integer $n \\ge 0$, every walk\n    \\[\n        \\pi = (v_0, a_0, v_1, a_1, \\dots, v_{n-1}, a_{n-1}, v_n)\n    \\]\n    in $G$ in the sense of def \\ref{def:walks}, item~i.\\ (so $v_0, \\dots, v_n \\in J \\cup V$, $a_0, \\dots, a_{n-1} \\in E \\cup L$, and the walk constraint\n    \\[\n        \\bigl(a_k = (v_k, v_{k+1}) \\;\\land\\; a_k \\in E \\cup L\\bigr)\n        \\;\\lor\\;\n        \\bigl(a_k = (v_{k+1}, v_k) \\;\\land\\; a_k \\in E\\bigr)\n    \\]\n    holds at every $k \\in \\{0, 1, \\dots, n - 1\\}$), and every position $k \\in \\{0, 1, \\dots, n\\}$, the following implication holds:\n    \\[\n        \\bigl(\\text{$k$ is a non-collider on $\\pi$ per def \\ref{def:collider_noncollider}, item~i.}\\bigr)\n        \\;\\implies\\;\n        \\bigl(\\text{$k$ is a blockable non-collider on $\\pi$ per def \\ref{def:unblockable_noncollider}}\\bigr).\n    \\]\n\\end{Rem}\n\n% --- proof ------------------------------------------------------------------\n\\begin{proof}\n% Constructive proof targeting the refactored \"blockable\" disjunction\n% characterisation of def \\ref{def:unblockable_noncollider}'s\n% \"Blockable non-collider on \\pi\" paragraph: end-position, OR outgoing\n% walk-edge at slot k - 1 to a non-Sc node, OR outgoing walk-edge at\n% slot k to a non-Sc node.  Step C.1 reuses Step 2.1 of\n% claim_3_20_proof_AcyclicNonCollidersBlockable.tex (deriving an\n% outgoing walk-edge of v_k from the non-collider count); Step C.3\n% reuses Steps 2.3-2.5 of that original proof (the cycle\n% construction), now in service of v_{k \\pm 1} \\notin \\Sc^G(v_k)\n% rather than False.\nFix $n \\ge 0$, a walk $\\pi = (v_0, a_0, v_1, a_1, \\dots, v_{n-1}, a_{n-1}, v_n)$ in $G$ (def \\ref{def:walks}, item~i.), and a position $k \\in \\{0, 1, \\dots, n\\}$. Suppose $k$ is a non-collider on $\\pi$ (def \\ref{def:collider_noncollider}, item~i.). We must show $k$ is a blockable non-collider on $\\pi$ in the sense of def \\ref{def:unblockable_noncollider}, namely:\n\\begin{itemize}\n    \\item[(B1)] $k$ is a non-collider on $\\pi$ in the sense of def \\ref{def:collider_noncollider}, item~i.; AND\n    \\item[(B2)] the blockable disjunction of def \\ref{def:unblockable_noncollider} holds at $k$ on $\\pi$:\n        \\[\n            k \\in \\{0, n\\}\n            \\;\\;\\lor\\;\\;\n            \\bigl(a_{k-1} = (v_k, v_{k-1}) \\in E \\;\\land\\; v_{k-1} \\notin \\Sc^G(v_k)\\bigr)\n            \\;\\;\\lor\\;\\;\n            \\bigl(a_k = (v_k, v_{k+1}) \\in E \\;\\land\\; v_{k+1} \\notin \\Sc^G(v_k)\\bigr).\n        \\]\n\\end{itemize}\nClause (B1) is the hypothesis. It remains to establish clause (B2). We split on the position $k$ along $\\pi$.\n\n\\medskip\\noindent\\textbf{Case A -- left end-position ($k = 0$).}\nThe first disjunct of (B2) (namely $k \\in \\{0, n\\}$) holds directly with the $k = 0$ branch. Hence (B2).\n\n\\medskip\\noindent\\textbf{Case B -- right end-position ($k = n$).}\nThe first disjunct of (B2) holds directly with the $k = n$ branch. Hence (B2).\n\n\\medskip\\noindent\\textbf{Case C -- interior position ($1 \\le k \\le n - 1$).}\nNeither end-position disjunct of (B2) fires (since $k \\notin \\{0, n\\}$), so we must establish one of the two existential disjuncts. We do so constructively: we extract from the non-collider hypothesis an outgoing walk-edge of $v_k$ at slot $i^* \\in \\{k - 1, k\\}$, derive that its other endpoint along $\\pi$ lies outside $\\Sc^G(v_k)$ by an acyclicity argument, and plug both into the matching disjunct.\n\n\\smallskip\\noindent\\emph{Step C.1 -- the non-collider hypothesis yields an outgoing walk-edge of $v_k$.}\n(Reused with light editing from Step~2.1 of the original proof\n\\texttt{claim\\_3\\_20\\_proof\\_AcyclicNonCollidersBlockable.tex}.) Since $k$ is a non-collider on $\\pi$, def \\ref{def:collider_noncollider}, item~i.\\ gives $\\mathrm{ah}_\\pi(k) \\le 1$, where\n\\[\n    \\mathrm{ah}_\\pi(k) = \\Bigl|\\Bigl\\{\\, i \\in \\{k - 1, k\\} \\,\\Bigm|\\, 0 \\le i \\le n - 1 \\,\\land\\, a_i \\text{ is an edge into } v_k \\text{ in } G \\,\\Bigr\\}\\Bigr|.\n\\]\nSince $1 \\le k \\le n - 1$, both indices $i \\in \\{k - 1, k\\}$ are admissible (they satisfy $0 \\le i \\le n - 1$), so the index set defining $\\mathrm{ah}_\\pi(k)$ is $\\{k - 1, k\\}$ and $|\\{k - 1, k\\}| = 2$. Hence $\\mathrm{ah}_\\pi(k) \\le 1$ means at least one of the two walk-incident edges $a_{k - 1}, a_k$ is \\emph{not} an edge into $v_k$ in the sense of def \\ref{def-edge-relations}, item~ii.\n\nWe expand what ``not an edge into $v_k$'' forces on each walk-incident edge using the walk constraint of def \\ref{def:walks}, item~i.\\ (recalled in the statement above). For each admissible index $i \\in \\{k - 1, k\\}$, the walk constraint at $i$ states\n\\[\n    \\bigl(a_i = (v_i, v_{i+1}) \\;\\land\\; a_i \\in E \\cup L\\bigr)\n        \\;\\lor\\;\n    \\bigl(a_i = (v_{i+1}, v_i) \\;\\land\\; a_i \\in E\\bigr),\n\\]\nso $a_i$ is one of three configurations:\n\\begin{itemize}\n    \\item[(C1)] $a_i = (v_i, v_{i+1}) \\in E$ (a directed edge from $v_i$ to $v_{i+1}$);\n    \\item[(C2)] $a_i = (v_i, v_{i+1}) \\in L$ (a bidirected edge with the ordered-pair writing $(v_i, v_{i+1})$);\n    \\item[(C3)] $a_i = (v_{i+1}, v_i) \\in E$ (a directed edge from $v_{i+1}$ to $v_i$).\n\\end{itemize}\nApplying the ``edge into $v_k$'' predicate of def \\ref{def-edge-relations}, item~ii.\\ ($a_i \\in E$ with second coordinate $v_k$, or $a_i \\in L$ with either coordinate $v_k$) to each configuration at slot $i \\in \\{k - 1, k\\}$:\n\\begin{itemize}\n    \\item For \\emph{slot $i = k - 1$} ($v_i = v_{k - 1}, v_{i+1} = v_k$):\n    \\begin{itemize}\n        \\item (C1) $a_{k-1} = (v_{k-1}, v_k) \\in E$: into $v_k$ (the $E$-clause fires, $e_2 = v_k$).\n        \\item (C2) $a_{k-1} = (v_{k-1}, v_k) \\in L$: into $v_k$ (the $L$-clause fires, $e_2 = v_k$).\n        \\item (C3) $a_{k-1} = (v_k, v_{k-1}) \\in E$: \\emph{not} into $v_k$ (the $E$-clause requires $e_2 = v_k$, but $e_2 = v_{k-1}$; and the $L$-clause is excluded since $a_{k-1} \\in E$). This is the only configuration at slot $k - 1$ for which $a_{k - 1}$ fails the ``edge into $v_k$'' predicate.\n    \\end{itemize}\n    \\item For \\emph{slot $i = k$} ($v_i = v_k, v_{i+1} = v_{k+1}$):\n    \\begin{itemize}\n        \\item (C1) $a_k = (v_k, v_{k+1}) \\in E$: \\emph{not} into $v_k$ (the $E$-clause requires $e_2 = v_k$, but $e_2 = v_{k+1}$; and the $L$-clause is excluded since $a_k \\in E$). This is the only configuration at slot $k$ for which $a_k$ fails the ``edge into $v_k$'' predicate.\n        \\item (C2) $a_k = (v_k, v_{k+1}) \\in L$: into $v_k$ (the $L$-clause fires, $e_1 = v_k$).\n        \\item (C3) $a_k = (v_{k+1}, v_k) \\in E$: into $v_k$ (the $E$-clause fires, $e_2 = v_k$).\n    \\end{itemize}\n\\end{itemize}\nIn summary, an admissible slot $i \\in \\{k - 1, k\\}$ has $a_i$ \\emph{not} an edge into $v_k$ \\emph{iff} either\n\\[\n    i = k - 1 \\;\\text{and}\\; a_{k - 1} = (v_k, v_{k - 1}) \\in E,\n    \\qquad\\text{or}\\qquad\n    i = k \\;\\text{and}\\; a_k = (v_k, v_{k + 1}) \\in E.\n\\]\nBy the previous paragraph, the non-collider hypothesis forces at least one such slot to occur. We capture this in a single statement: there exists a slot $i^* \\in \\{k - 1, k\\}$ at which $a_{i^*}$ is an \\emph{outgoing walk-edge of $v_k$ at position $k$ on $\\pi$} in the sense of def \\ref{def:unblockable_noncollider} (the predicate ``$a_i \\in E \\land e_1 = v_k$''), with the two sub-cases:\n\\begin{itemize}\n    \\item[(O1)] $i^* = k - 1$ with $a_{k - 1} = (v_k, v_{k - 1}) \\in E$ -- the other endpoint of $a_{k - 1}$ along $\\pi$ is $v_{k - 1}$;\n    \\item[(O2)] $i^* = k$ with $a_k = (v_k, v_{k + 1}) \\in E$ -- the other endpoint of $a_k$ along $\\pi$ is $v_{k + 1}$.\n\\end{itemize}\n(Both sub-cases may simultaneously hold -- corresponding to the ``fork'' pattern -- but the argument below requires only that at least one slot $i^*$ exists; we may pick either when both hold, since the resulting witness goes into the matching disjunct of (B2).)\n\nFix such a slot $i^* \\in \\{k - 1, k\\}$ for the rest of Case~C, and write\n\\[\n    w \\;:=\\; v_{k - 1} \\text{ if } i^* = k - 1, \\qquad w \\;:=\\; v_{k + 1} \\text{ if } i^* = k.\n\\]\nBy the case description, $w \\in J \\cup V$ (it is one of the walk's vertices) and the single directed edge\n\\[\n    (v_k, w) \\;\\in\\; E\n\\]\nis on the graph $G$ (it is the walk-edge $a_{i^*}$ written with $v_k$ as tail).\n\n\\smallskip\\noindent\\emph{Step C.2 -- the other endpoint $w$ lies outside $\\Sc^G(v_k)$.}\nWe claim\n\\[\n    w \\;\\notin\\; \\Sc^G(v_k).\n\\]\nSuppose, toward contradiction, that $w \\in \\Sc^G(v_k)$. We derive a non-trivial directed walk from $v_k$ to itself in $G$, contradicting def \\ref{def-acylic}.\n\n\\smallskip\\noindent\\emph{Step C.2.a -- $w \\in \\Sc^G(v_k)$ supplies a directed walk from $w$ to $v_k$.}\n(Reused from Step~2.3 of the original proof.) By def \\ref{def_3_5}, item~vii.,\n\\[\n    \\Sc^G(v_k) \\;=\\; \\Anc^G(v_k) \\,\\cap\\, \\Desc^G(v_k),\n\\]\nso $w \\in \\Sc^G(v_k)$ entails $w \\in \\Anc^G(v_k)$. By def \\ref{def_3_5}, item~iv., $w \\in \\Anc^G(v_k)$ means there exists a directed walk $\\rho$ from $w$ to $v_k$ in $G$ in the sense of def \\ref{def:walks}, item~ii.\\ -- a tuple\n\\[\n    \\rho \\;=\\; (w = u_0, b_0, u_1, b_1, \\dots, u_{m-1}, b_{m-1}, u_m = v_k)\n\\]\nfor some $m \\ge 0$, with each $b_j = (u_j, u_{j+1}) \\in E$ pointing forward along $\\rho$ from $u_j$ to $u_{j+1}$.\n\n\\smallskip\\noindent\\emph{Step C.2.b -- concatenating the single edge $(v_k, w) \\in E$ with $\\rho$ yields a non-trivial directed walk from $v_k$ to itself.}\n(Reused from Step~2.4 of the original proof.) We assemble the directed walk\n\\[\n    \\widetilde{\\rho} \\;:=\\; (v_k, \\;(v_k, w), \\;w = u_0, \\;b_0, \\;u_1, \\;b_1, \\;\\dots, \\;u_{m-1}, \\;b_{m-1}, \\;u_m = v_k).\n\\]\nThat is, prepend the node $v_k$ and the edge $(v_k, w) \\in E$ to $\\rho$. We verify that $\\widetilde{\\rho}$ is a directed walk from $v_k$ to itself in the sense of def \\ref{def:walks}, item~ii.\n\\begin{itemize}\n    \\item \\emph{Node and edge typing.} The nodes of $\\widetilde{\\rho}$ are $v_k, w = u_0, u_1, \\dots, u_m = v_k$, all in $J \\cup V$ ($v_k$ from the walk $\\pi$, the $u_j$ from $\\rho$). The edges are $(v_k, w)$ followed by $b_0, b_1, \\dots, b_{m-1}$, all in $E$ ($(v_k, w) \\in E$ from Step~C.1, the $b_j \\in E$ from $\\rho$).\n    \\item \\emph{Length.} The length of $\\widetilde{\\rho}$, counted by the number of edges, is $1 + m$, where $m \\ge 0$. Hence $1 + m \\ge 1$, so $\\widetilde{\\rho}$ is a non-trivial walk (its length is at least~$1$, i.e.\\ it has at least one edge).\n    \\item \\emph{Endpoints.} The first node of $\\widetilde{\\rho}$ is $v_k$ (by construction), and the last node is $u_m = v_k$ (the terminus of $\\rho$). So $\\widetilde{\\rho}$ starts and ends at $v_k$.\n    \\item \\emph{Directed-walk shape.} Per def \\ref{def:walks}, item~ii., a directed walk has all its edges of the form ``forward'' $(u_j, u_{j+1}) \\in E$ at each successive slot, with arrowheads consistently pointing forward. The first edge of $\\widetilde{\\rho}$ is $(v_k, w) \\in E$, fitting the slot writing $(\\text{first node}, \\text{second node}) = (v_k, w)$ in $E$; the subsequent edges $b_0, b_1, \\dots, b_{m-1}$ are the directed-walk edges of $\\rho$, hence fit the same forward-slot writing relative to $\\rho$'s nodes; and the join between $(v_k, w)$ and $\\rho$ at the node $w = u_0$ is consistent (the head of $(v_k, w)$ equals the start of $\\rho$). All arrowheads point forward (from $v_k$ to $w$, then from $u_0$ through to $u_m$); no arrowhead points backward. Hence $\\widetilde{\\rho}$ has the directed-walk shape of def \\ref{def:walks}, item~ii.\n\\end{itemize}\nTherefore $\\widetilde{\\rho}$ is a directed walk from $v_k$ to $v_k$ in $G$ of length at least~$1$ -- equivalently, a \\emph{non-trivial} directed walk from $v_k$ to itself in $G$.\n\n\\smallskip\\noindent\\emph{Step C.2.c -- contradiction with acyclicity.}\nBy def \\ref{def-acylic}, $G$ acyclic asserts that no non-trivial directed walk from any node to itself exists in $G$. But Step~C.2.b has constructed exactly such a walk $\\widetilde{\\rho}$ at the node $v_k \\in J \\cup V$. Contradiction. Hence the assumption $w \\in \\Sc^G(v_k)$ is false, so $w \\notin \\Sc^G(v_k)$, as claimed.\n\n\\smallskip\\noindent\\emph{Step C.3 -- assemble the disjunction witness.}\nWe now combine the directed-edge fact $(v_k, w) \\in E$ from Step~C.1 with $w \\notin \\Sc^G(v_k)$ from Step~C.2, splitting on the two sub-cases of $i^*$ from Step~C.1:\n\\begin{itemize}\n    \\item \\emph{Sub-case~(O1)} ($i^* = k - 1$, $w = v_{k - 1}$): we have $a_{k - 1} = (v_k, v_{k - 1}) \\in E$ and $v_{k - 1} \\notin \\Sc^G(v_k)$. This is exactly the second disjunct of (B2). Hence (B2).\n    \\item \\emph{Sub-case~(O2)} ($i^* = k$, $w = v_{k + 1}$): we have $a_k = (v_k, v_{k + 1}) \\in E$ and $v_{k + 1} \\notin \\Sc^G(v_k)$. This is exactly the third disjunct of (B2). Hence (B2).\n\\end{itemize}\nIn both sub-cases of Case~C, (B2) is established.\n\n\\medskip\nAcross the three cases (left end-position, right end-position, interior), the hypothesis ``$k$ is a non-collider on $\\pi$'' yields (B2) in each case. Combining (B1) (from the hypothesis) with (B2), $k$ is a blockable non-collider on $\\pi$ in the sense of def \\ref{def:unblockable_noncollider}.\n\\end{proof}\n\n\\end{document}\n",
    "html": "<div class=\"theorem-block\"><div class=\"theorem-label\">Remark — <span class=\"theorem-title\">AcyclicNonCollidersBlockable</span></div><p>Let $G = (J, V, E, L)$ be a CDMG (def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>) and suppose $G$ is acyclic in the sense of def <a href=\"#def_3_6\" class=\"refrow\"><code>def-acylic</code></a>. Then, for every integer $n \\ge 0$, every walk\n    \\[\n        \\pi = (v_0, a_0, v_1, a_1, \\dots, v_{n-1}, a_{n-1}, v_n)\n    \\]\n    in $G$ in the sense of def <a href=\"#def_3_4\" class=\"refrow\"><code>def:walks</code></a>, item&nbsp;i.\\ (so $v_0, \\dots, v_n \\in J \\cup V$, $a_0, \\dots, a_{n-1} \\in E \\cup L$, and the walk constraint\n    \\[\n        \\bigl(a_k = (v_k, v_{k+1}) \\;\\land\\; a_k \\in E \\cup L\\bigr)\n        \\;\\lor\\;\n        \\bigl(a_k = (v_{k+1}, v_k) \\;\\land\\; a_k \\in E\\bigr)\n    \\]\n    holds at every $k \\in \\{0, 1, \\dots, n - 1\\}$), and every position $k \\in \\{0, 1, \\dots, n\\}$, the following implication holds:\n    \\[\n        \\bigl(\\text{$k$ is a non-collider on $\\pi$ per def \\ref{def:collider_noncollider}, item~i.}\\bigr)\n        \\;\\implies\\;\n        \\bigl(\\text{$k$ is a blockable non-collider on $\\pi$ per def \\ref{def:unblockable_noncollider}}\\bigr).\n    \\]</p></div>\n<div class=\"tex-proof\"><span class=\"proof-label\">Proof.</span><p>Fix $n \\ge 0$, a walk $\\pi = (v_0, a_0, v_1, a_1, \\dots, v_{n-1}, a_{n-1}, v_n)$ in $G$ (def <a href=\"#def_3_4\" class=\"refrow\"><code>def:walks</code></a>, item&nbsp;i.), and a position $k \\in \\{0, 1, \\dots, n\\}$. Suppose $k$ is a non-collider on $\\pi$ (def <a href=\"#def_3_15\" class=\"refrow\"><code>def:collider_noncollider</code></a>, item&nbsp;i.). We must show $k$ is a blockable non-collider on $\\pi$ in the sense of def <a href=\"#def_3_16\" class=\"refrow\"><code>def:unblockable_noncollider</code></a>, namely:</p>\n<ul><li>[(B1)] $k$ is a non-collider on $\\pi$ in the sense of def <a href=\"#def_3_15\" class=\"refrow\"><code>def:collider_noncollider</code></a>, item&nbsp;i.; AND</li><li>[(B2)] the blockable disjunction of def <a href=\"#def_3_16\" class=\"refrow\"><code>def:unblockable_noncollider</code></a> holds at $k$ on $\\pi$:\n        \\[\n            k \\in \\{0, n\\}\n            \\;\\;\\lor\\;\\;\n            \\bigl(a_{k-1} = (v_k, v_{k-1}) \\in E \\;\\land\\; v_{k-1} \\notin \\Sc^G(v_k)\\bigr)\n            \\;\\;\\lor\\;\\;\n            \\bigl(a_k = (v_k, v_{k+1}) \\in E \\;\\land\\; v_{k+1} \\notin \\Sc^G(v_k)\\bigr).\n        \\]</li></ul>\n<p>Clause (B1) is the hypothesis. It remains to establish clause (B2). We split on the position $k$ along $\\pi$.</p>\n<p><strong>Case A – left end-position ($k = 0$).</strong>\nThe first disjunct of (B2) (namely $k \\in \\{0, n\\}$) holds directly with the $k = 0$ branch. Hence (B2).</p>\n<p><strong>Case B – right end-position ($k = n$).</strong>\nThe first disjunct of (B2) holds directly with the $k = n$ branch. Hence (B2).</p>\n<p><strong>Case C – interior position ($1 \\le k \\le n - 1$).</strong>\nNeither end-position disjunct of (B2) fires (since $k \\notin \\{0, n\\}$), so we must establish one of the two existential disjuncts. We do so constructively: we extract from the non-collider hypothesis an outgoing walk-edge of $v_k$ at slot $i^* \\in \\{k - 1, k\\}$, derive that its other endpoint along $\\pi$ lies outside $\\Sc^G(v_k)$ by an acyclicity argument, and plug both into the matching disjunct.</p>\n<p><em>Step C.1 – the non-collider hypothesis yields an outgoing walk-edge of $v_k$.</em>\n(Reused with light editing from Step&nbsp;2.1 of the original proof\n<code>claim_3_20_proof_AcyclicNonCollidersBlockable.tex</code>.) Since $k$ is a non-collider on $\\pi$, def <a href=\"#def_3_15\" class=\"refrow\"><code>def:collider_noncollider</code></a>, item&nbsp;i.\\ gives $\\mathrm{ah}_\\pi(k) \\le 1$, where\n\\[\n    \\mathrm{ah}_\\pi(k) = \\Bigl|\\Bigl\\{\\, i \\in \\{k - 1, k\\} \\,\\Bigm|\\, 0 \\le i \\le n - 1 \\,\\land\\, a_i \\text{ is an edge into } v_k \\text{ in } G \\,\\Bigr\\}\\Bigr|.\n\\]\nSince $1 \\le k \\le n - 1$, both indices $i \\in \\{k - 1, k\\}$ are admissible (they satisfy $0 \\le i \\le n - 1$), so the index set defining $\\mathrm{ah}_\\pi(k)$ is $\\{k - 1, k\\}$ and $|\\{k - 1, k\\}| = 2$. Hence $\\mathrm{ah}_\\pi(k) \\le 1$ means at least one of the two walk-incident edges $a_{k - 1}, a_k$ is <em>not</em> an edge into $v_k$ in the sense of def <a href=\"#def_3_3\" class=\"refrow\"><code>def-edge-relations</code></a>, item&nbsp;ii.</p>\n<p>We expand what “not an edge into $v_k$” forces on each walk-incident edge using the walk constraint of def <a href=\"#def_3_4\" class=\"refrow\"><code>def:walks</code></a>, item&nbsp;i.\\ (recalled in the statement above). For each admissible index $i \\in \\{k - 1, k\\}$, the walk constraint at $i$ states\n\\[\n    \\bigl(a_i = (v_i, v_{i+1}) \\;\\land\\; a_i \\in E \\cup L\\bigr)\n        \\;\\lor\\;\n    \\bigl(a_i = (v_{i+1}, v_i) \\;\\land\\; a_i \\in E\\bigr),\n\\]\nso $a_i$ is one of three configurations:</p>\n<ul><li>[(C1)] $a_i = (v_i, v_{i+1}) \\in E$ (a directed edge from $v_i$ to $v_{i+1}$);</li><li>[(C2)] $a_i = (v_i, v_{i+1}) \\in L$ (a bidirected edge with the ordered-pair writing $(v_i, v_{i+1})$);</li><li>[(C3)] $a_i = (v_{i+1}, v_i) \\in E$ (a directed edge from $v_{i+1}$ to $v_i$).</li></ul>\n<p>Applying the “edge into $v_k$” predicate of def <a href=\"#def_3_3\" class=\"refrow\"><code>def-edge-relations</code></a>, item&nbsp;ii.\\ ($a_i \\in E$ with second coordinate $v_k$, or $a_i \\in L$ with either coordinate $v_k$) to each configuration at slot $i \\in \\{k - 1, k\\}$:</p>\n<ul><li>For <em>slot $i = k - 1$</em> ($v_i = v_{k - 1}, v_{i+1} = v_k$):\n    \\begin{itemize}</li><li>(C1) $a_{k-1} = (v_{k-1}, v_k) \\in E$: into $v_k$ (the $E$-clause fires, $e_2 = v_k$).</li><li>(C2) $a_{k-1} = (v_{k-1}, v_k) \\in L$: into $v_k$ (the $L$-clause fires, $e_2 = v_k$).</li><li>(C3) $a_{k-1} = (v_k, v_{k-1}) \\in E$: <em>not</em> into $v_k$ (the $E$-clause requires $e_2 = v_k$, but $e_2 = v_{k-1}$; and the $L$-clause is excluded since $a_{k-1} \\in E$). This is the only configuration at slot $k - 1$ for which $a_{k - 1}$ fails the “edge into $v_k$” predicate.\n    \\end{itemize}</li><li>For <em>slot $i = k$</em> ($v_i = v_k, v_{i+1} = v_{k+1}$):\n    \\begin{itemize}</li><li>(C1) $a_k = (v_k, v_{k+1}) \\in E$: <em>not</em> into $v_k$ (the $E$-clause requires $e_2 = v_k$, but $e_2 = v_{k+1}$; and the $L$-clause is excluded since $a_k \\in E$). This is the only configuration at slot $k$ for which $a_k$ fails the “edge into $v_k$” predicate.</li><li>(C2) $a_k = (v_k, v_{k+1}) \\in L$: into $v_k$ (the $L$-clause fires, $e_1 = v_k$).</li><li>(C3) $a_k = (v_{k+1}, v_k) \\in E$: into $v_k$ (the $E$-clause fires, $e_2 = v_k$).\n    \\end{itemize}</li></ul>\n<p>In summary, an admissible slot $i \\in \\{k - 1, k\\}$ has $a_i$ <em>not</em> an edge into $v_k$ <em>iff</em> either\n\\[\n    i = k - 1 \\;\\text{and}\\; a_{k - 1} = (v_k, v_{k - 1}) \\in E,\n    \\qquad\\text{or}\\qquad\n    i = k \\;\\text{and}\\; a_k = (v_k, v_{k + 1}) \\in E.\n\\]\nBy the previous paragraph, the non-collider hypothesis forces at least one such slot to occur. We capture this in a single statement: there exists a slot $i^* \\in \\{k - 1, k\\}$ at which $a_{i^*}$ is an <em>outgoing walk-edge of $v_k$ at position $k$ on $\\pi$</em> in the sense of def <a href=\"#def_3_16\" class=\"refrow\"><code>def:unblockable_noncollider</code></a> (the predicate “$a_i \\in E \\land e_1 = v_k$”), with the two sub-cases:</p>\n<ul><li>[(O1)] $i^* = k - 1$ with $a_{k - 1} = (v_k, v_{k - 1}) \\in E$ – the other endpoint of $a_{k - 1}$ along $\\pi$ is $v_{k - 1}$;</li><li>[(O2)] $i^* = k$ with $a_k = (v_k, v_{k + 1}) \\in E$ – the other endpoint of $a_k$ along $\\pi$ is $v_{k + 1}$.</li></ul>\n<p>(Both sub-cases may simultaneously hold – corresponding to the “fork” pattern – but the argument below requires only that at least one slot $i^*$ exists; we may pick either when both hold, since the resulting witness goes into the matching disjunct of (B2).)</p>\n<p>Fix such a slot $i^* \\in \\{k - 1, k\\}$ for the rest of Case&nbsp;C, and write\n\\[\n    w \\;:=\\; v_{k - 1} \\text{ if } i^* = k - 1, \\qquad w \\;:=\\; v_{k + 1} \\text{ if } i^* = k.\n\\]\nBy the case description, $w \\in J \\cup V$ (it is one of the walk's vertices) and the single directed edge\n\\[\n    (v_k, w) \\;\\in\\; E\n\\]\nis on the graph $G$ (it is the walk-edge $a_{i^*}$ written with $v_k$ as tail).</p>\n<p><em>Step C.2 – the other endpoint $w$ lies outside $\\Sc^G(v_k)$.</em>\nWe claim\n\\[\n    w \\;\\notin\\; \\Sc^G(v_k).\n\\]\nSuppose, toward contradiction, that $w \\in \\Sc^G(v_k)$. We derive a non-trivial directed walk from $v_k$ to itself in $G$, contradicting def <a href=\"#def_3_6\" class=\"refrow\"><code>def-acylic</code></a>.</p>\n<p><em>Step C.2.a – $w \\in \\Sc^G(v_k)$ supplies a directed walk from $w$ to $v_k$.</em>\n(Reused from Step&nbsp;2.3 of the original proof.) By def <a href=\"#def_3_5\" class=\"refrow\"><code>def_3_5</code></a>, item&nbsp;vii.,\n\\[\n    \\Sc^G(v_k) \\;=\\; \\Anc^G(v_k) \\,\\cap\\, \\Desc^G(v_k),\n\\]\nso $w \\in \\Sc^G(v_k)$ entails $w \\in \\Anc^G(v_k)$. By def <a href=\"#def_3_5\" class=\"refrow\"><code>def_3_5</code></a>, item&nbsp;iv., $w \\in \\Anc^G(v_k)$ means there exists a directed walk $\\rho$ from $w$ to $v_k$ in $G$ in the sense of def <a href=\"#def_3_4\" class=\"refrow\"><code>def:walks</code></a>, item&nbsp;ii.\\ – a tuple\n\\[\n    \\rho \\;=\\; (w = u_0, b_0, u_1, b_1, \\dots, u_{m-1}, b_{m-1}, u_m = v_k)\n\\]\nfor some $m \\ge 0$, with each $b_j = (u_j, u_{j+1}) \\in E$ pointing forward along $\\rho$ from $u_j$ to $u_{j+1}$.</p>\n<p><em>Step C.2.b – concatenating the single edge $(v_k, w) \\in E$ with $\\rho$ yields a non-trivial directed walk from $v_k$ to itself.</em>\n(Reused from Step&nbsp;2.4 of the original proof.) We assemble the directed walk\n\\[\n    \\widetilde{\\rho} \\;:=\\; (v_k, \\;(v_k, w), \\;w = u_0, \\;b_0, \\;u_1, \\;b_1, \\;\\dots, \\;u_{m-1}, \\;b_{m-1}, \\;u_m = v_k).\n\\]\nThat is, prepend the node $v_k$ and the edge $(v_k, w) \\in E$ to $\\rho$. We verify that $\\widetilde{\\rho}$ is a directed walk from $v_k$ to itself in the sense of def <a href=\"#def_3_4\" class=\"refrow\"><code>def:walks</code></a>, item&nbsp;ii.</p>\n<ul><li><em>Node and edge typing.</em> The nodes of $\\widetilde{\\rho}$ are $v_k, w = u_0, u_1, \\dots, u_m = v_k$, all in $J \\cup V$ ($v_k$ from the walk $\\pi$, the $u_j$ from $\\rho$). The edges are $(v_k, w)$ followed by $b_0, b_1, \\dots, b_{m-1}$, all in $E$ ($(v_k, w) \\in E$ from Step&nbsp;C.1, the $b_j \\in E$ from $\\rho$).</li><li><em>Length.</em> The length of $\\widetilde{\\rho}$, counted by the number of edges, is $1 + m$, where $m \\ge 0$. Hence $1 + m \\ge 1$, so $\\widetilde{\\rho}$ is a non-trivial walk (its length is at least&nbsp;$1$, i.e.\\ it has at least one edge).</li><li><em>Endpoints.</em> The first node of $\\widetilde{\\rho}$ is $v_k$ (by construction), and the last node is $u_m = v_k$ (the terminus of $\\rho$). So $\\widetilde{\\rho}$ starts and ends at $v_k$.</li><li><em>Directed-walk shape.</em> Per def <a href=\"#def_3_4\" class=\"refrow\"><code>def:walks</code></a>, item&nbsp;ii., a directed walk has all its edges of the form “forward” $(u_j, u_{j+1}) \\in E$ at each successive slot, with arrowheads consistently pointing forward. The first edge of $\\widetilde{\\rho}$ is $(v_k, w) \\in E$, fitting the slot writing $(\\text{first node}, \\text{second node}) = (v_k, w)$ in $E$; the subsequent edges $b_0, b_1, \\dots, b_{m-1}$ are the directed-walk edges of $\\rho$, hence fit the same forward-slot writing relative to $\\rho$'s nodes; and the join between $(v_k, w)$ and $\\rho$ at the node $w = u_0$ is consistent (the head of $(v_k, w)$ equals the start of $\\rho$). All arrowheads point forward (from $v_k$ to $w$, then from $u_0$ through to $u_m$); no arrowhead points backward. Hence $\\widetilde{\\rho}$ has the directed-walk shape of def <a href=\"#def_3_4\" class=\"refrow\"><code>def:walks</code></a>, item&nbsp;ii.</li></ul>\n<p>Therefore $\\widetilde{\\rho}$ is a directed walk from $v_k$ to $v_k$ in $G$ of length at least&nbsp;$1$ – equivalently, a <em>non-trivial</em> directed walk from $v_k$ to itself in $G$.</p>\n<p><em>Step C.2.c – contradiction with acyclicity.</em>\nBy def <a href=\"#def_3_6\" class=\"refrow\"><code>def-acylic</code></a>, $G$ acyclic asserts that no non-trivial directed walk from any node to itself exists in $G$. But Step&nbsp;C.2.b has constructed exactly such a walk $\\widetilde{\\rho}$ at the node $v_k \\in J \\cup V$. Contradiction. Hence the assumption $w \\in \\Sc^G(v_k)$ is false, so $w \\notin \\Sc^G(v_k)$, as claimed.</p>\n<p><em>Step C.3 – assemble the disjunction witness.</em>\nWe now combine the directed-edge fact $(v_k, w) \\in E$ from Step&nbsp;C.1 with $w \\notin \\Sc^G(v_k)$ from Step&nbsp;C.2, splitting on the two sub-cases of $i^*$ from Step&nbsp;C.1:</p>\n<ul><li><em>Sub-case&nbsp;(O1)</em> ($i^* = k - 1$, $w = v_{k - 1}$): we have $a_{k - 1} = (v_k, v_{k - 1}) \\in E$ and $v_{k - 1} \\notin \\Sc^G(v_k)$. This is exactly the second disjunct of (B2). Hence (B2).</li><li><em>Sub-case&nbsp;(O2)</em> ($i^* = k$, $w = v_{k + 1}$): we have $a_k = (v_k, v_{k + 1}) \\in E$ and $v_{k + 1} \\notin \\Sc^G(v_k)$. This is exactly the third disjunct of (B2). Hence (B2).</li></ul>\n<p>In both sub-cases of Case&nbsp;C, (B2) is established.</p>\n<p>Across the three cases (left end-position, right end-position, interior), the hypothesis “$k$ is a non-collider on $\\pi$” yields (B2) in each case. Combining (B1) (from the hypothesis) with (B2), $k$ is a blockable non-collider on $\\pi$ in the sense of def <a href=\"#def_3_16\" class=\"refrow\"><code>def:unblockable_noncollider</code></a>.</p></div>",
    "source_path": "leanification/Chapter3_GraphTheory/Section3_3/tex/claim_3_20_proof_AcyclicNonCollidersBlockable.tex"
  },
  "lean_blocks": [
    {
      "kind": "helper",
      "code": "variable {Node : Type*} [DecidableEq Node]",
      "explanation": "This is a `variable` declaration: Lean auto-binds `Node : Type*` and the instance `[DecidableEq Node]` as implicit arguments to every subsequent declaration in the surrounding `CDMG` namespace, sparing us from rewriting the same parameters on `acyclic_non_colliders_blockable`. The `Type*` notation makes `Node` universe-polymorphic, so callers can instantiate it at any universe level. The `DecidableEq Node` assumption is load-bearing because the per-position predicates `IsNonCollider` (from `def_3_15`) and `IsBlockableNonCollider` (from `def_3_16`) both require it — their internal `Finset.filter` over the walk's incident edges needs decidable equality on `Node` to compute the arrowhead count $\\mathrm{ah}_\\pi(k)$. Stronger structure on `Node` (e.g.\\ `Fintype`, `LinearOrder`) is not required here and is deferred to downstream consumers that actually use it. Same convention as the rest of `Chapter3_GraphTheory`'s files.",
      "code_annotated": "-- Auto-binds `Node` + `DecidableEq Node` as implicits; decidability is required by `IsNonCollider`/`IsBlockableNonCollider`'s internal `Finset` machinery.\nvariable {Node : Type*} [DecidableEq Node]"
    },
    {
      "kind": "main",
      "code": "theorem acyclic_non_colliders_blockable\n    (G : CDMG Node) (hG : G.IsAcyclic)\n    {u v : Node} (π : Walk G u v) (k : ℕ) :\n    π.IsNonCollider k → π.IsBlockableNonCollider k",
      "explanation": "This block is the statement head of the LN remark: in an acyclic CDMG, every non-collider position on every walk is blockable. The binder list reads `(G : CDMG Node) (hG : G.IsAcyclic) {u v : Node} (π : Walk G u v) (k : ℕ)`, mirroring the LN's layered \"let $G$ be a CDMG, suppose $G$ is acyclic, then for every walk $\\pi$ … every position $k$\". CDMG-hood is structural (typed `G : CDMG Node`), while acyclicity is the propositional `def_3_6` predicate `G.IsAcyclic`. The endpoints `{u v}` are implicit because `Walk G u v` already carries them. The conclusion `π.IsNonCollider k → π.IsBlockableNonCollider k` reuses the per-position predicates of `def_3_15` and `def_3_16` verbatim; by `def_3_16`, `IsBlockableNonCollider` unfolds to `IsNonCollider ∧ ¬ IsUnblockableNonCollider`, so the conclusion definitionally re-asserts the antecedent in its first conjunct. No `k ≤ π.length` side condition appears: `IsNonCollider`'s own in-range conjunct makes the antecedent `False` at out-of-range positions, rendering the implication vacuous and the LN's $k \\in \\{0, \\dots, n\\}$ scope implicit. The proof body, omitted from this surface block, executes the TeX proof's Step 2.1–2.5 cascade.",
      "code_annotated": "-- ref: claim_3_20 — statement head; the proof body lives below the wrapped surface.\ntheorem acyclic_non_colliders_blockable\n    -- `G` typed as a CDMG (structural CDMG-hood); `hG` carries `def_3_6`'s acyclicity proposition.\n    (G : CDMG Node) (hG : G.IsAcyclic)\n    -- Endpoints `{u v}` implicit — recoverable from `Walk G u v`'s type; `π` and `k` explicit per `def_3_16`'s binder shape.\n    {u v : Node} (π : Walk G u v) (k : ℕ) :\n    -- LN-faithful implication: `def_3_15`'s `IsNonCollider` ⟹ `def_3_16`'s `IsBlockableNonCollider`; `IsNonCollider`'s in-range conjunct makes out-of-range `k` vacuous.\n    π.IsNonCollider k → π.IsBlockableNonCollider k"
    }
  ],
  "lean_source_url": "https://github.com/schipeltaper/causality-leanified/blob/main/leanification/Chapter3_GraphTheory/Section3_3/AcyclicNonCollidersBlockable.lean",
  "lean_file_path": "leanification/Chapter3_GraphTheory/Section3_3/AcyclicNonCollidersBlockable.lean",
  "addition_to_the_LN": "[acyclic_does_not_imply_directed_in_text] In this remark $G$ is a CDMG, as fixed by the surrounding context; the hypothesis \"$G$ is acyclic\" is to be read as acyclicity in the CDMG sense, so the edge orientations needed to define (non-)colliders are already part of the ambient structure.",
  "design_choices": "**`Node : Type*` with `[DecidableEq Node]` auto-bound via `variable`.** Matches the chapter convention used by every `CDMG`-opening file; decidability is inherited from `def_3_15` and `def_3_16`'s per-position predicates.\n\n**Three-dash `--- helper` markers on the `variable` line, two-dash `-- statement` markers on the theorem head.** The implicit `Node` + `DecidableEq Node` is statement-typing material, not formalised LN content; the three-dash flavour is the chapter convention for that distinction. The two-dash markers wrap only the `theorem acyclic_non_colliders_blockable …` declaration head.\n\n**`outgoing_E_not_in_Sc` factored out as a private helper.** Given `(x, y) ∈ G.E` and `G.IsAcyclic`, the target `y` cannot lie in `G.Sc x`: `y ∈ G.Anc x` supplies a directed walk `ρ : y → x`, and `Walk.cons y (.forwardE hxy) ρ` yields a closed directed walk `x → x` of length ≥ 1, contradicting `hG`. Lifting this argument to its own lemma removes duplication across the two slot branches of `blocking_interior_helper` and exposes the load-bearing acyclicity-to-non-`Sc` translation at the call site.\n\n**`blocking_interior_helper` by induction on `π` with cons-cons head pattern.** `nil` and `cons …nil` base cases dispatch by `Walk.length` arithmetic. The substantive `k = 1` case reads `s₀` and `s₁` off the head pair; the inductive `k = m + 2` step delegates to the IH on the tail walk via the recursion equations for `IsCollider`, `HasBlockingLeftSlot`, and `HasBlockingRightSlot`. The outer matcher requires `s₀` destructed before routing to the wildcard-cons cons-pattern, so each IH lift case-splits trivially on `s₀`'s three constructors.\n\n**Per-step \"arrowhead at `v_k`\" via the side-aware tag pair `s₀.HeadAtTarget` / `s₁.HeadAtSource` from `def_3_15`.** At position 1 the hypothesis `¬ π.IsCollider 1` unfolds to `¬ (s₀.HeadAtTarget ∧ s₁.HeadAtSource)`; `not_and_or` splits the conjunction and each disjunct case-splits on the `WalkStep` constructor. Natural-side head branches (`.forwardE` for target, `.backwardE` for source, `.bidir` for both) reduce the predicate to `True`, dischargeable by `absurd trivial`. Writing-mirror branches carry a constructor parameter `h` that is the outgoing `G.E` witness fed straight to `outgoing_E_not_in_Sc hG h`. The opposite-channel L-disjunct is never inspected: the side-aware predicates read constructor identity, not `G.L`.\n\n**Top-level three-case structure for `acyclic_non_colliders_blockable`.** Case A (`k = 0`) hits the first `IsBlockableNonCollider` disjunct via `Or.inl`; Case B (`k = π.length`) the second via `Or.inr (Or.inl _)`; Case C (interior) delegates to `blocking_interior_helper` and routes the returned blocking slot into the third / fourth disjuncts. The `IsNonCollider` conjunct is supplied by `refine ⟨h_nc, ?_⟩`; interior bounds follow from `k ≠ 0`, `k ≠ π.length`, and `IsNonCollider`'s built-in `k ≤ π.length` conjunct, so the signature carries no separate range hypothesis."
}
