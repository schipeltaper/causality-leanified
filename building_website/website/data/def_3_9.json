{
  "ref": "def_3_9",
  "kind": "def",
  "section": "3.1",
  "title": "Predecessors",
  "type": "definition",
  "status": {
    "formalized": "yes",
    "proven": "n/a",
    "solved": "yes"
  },
  "tex_block_html": "<p>Let $G=(J,V,E,L)$ be a CDMG and $<$ a total order of $J \\cup V$.\n   The set of <em>predecessors</em> of $v$ in $G$ are:\n   \\[\\Pred^G_<(v) : = \\lC w\\in G\\,|\\, w < v \\rC.\\]\n   We also put:\n   \\[\\Pred^G_\\le(v) : = \\lC w\\in G\\,|\\, w < v \\rC \\cup \\{v\\}.\\]</p>",
  "tex_statement": {
    "raw": "% Definition file for row def_3_9 -- \"Predecessors\".\n% Auto-generated stub by scaffold/solve_chapter.py at row start. The\n% manager agent (or a worker dispatched by it) fills the body below with the\n% Lean-faithful definition statement(s). Stays close to the lecture notes.\n%\n% This file is a sibling of `main.tex` inside `<subsection>/tex/`. The\n% `subfiles` package lets it compile both standalone (resolving its\n% preamble via `main.tex`) and as part of `main.tex` via `\\subfile{...}`.\n\n\\documentclass[main]{subfiles}\n\n\\begin{document}\n% ref: def_3_9\n% title: Predecessors\n\\def\\rowref{def\\_3\\_9}\n\\phantomsection\\label{def_3_9}\n\n\\begin{Def}[Predecessors]\n    \\label{def-predecessors}\n    Let $G = (J, V, E, L)$ be a CDMG (def \\ref{def-cdmg}); throughout we use the notation of def \\ref{not-cdmg}. Let $<$ be a total order of $J \\cup V$, read as a \\emph{strict} total order in the sense of def \\ref{def-topological-order} (irreflexive, transitive, and trichotomous on $J \\cup V$); the symbol $<$ is read strictly throughout. The element $v$ is left unconstrained by the LN -- in particular, the LN does \\emph{not} impose $v \\in J \\cup V$ -- and we follow that literal stance below; note that the comparison $w < v$ is only directly given by $<$ when $v \\in J \\cup V$, since $<$ is supplied only as a total order of $J \\cup V$. Adopting the convention of def \\ref{not-cdmg}, item~1, that ``$w \\in G$'' abbreviates ``$w \\in J \\cup V$'', we write the set-builder bodies below explicitly with $w \\in J \\cup V$.\n\n    The set of \\emph{predecessors} of $v$ in $G$ is\n    \\[\n        \\Pred^G_<(v) := \\lC w \\in J \\cup V \\,|\\, w < v \\rC.\n    \\]\n    We also put\n    \\[\n        \\Pred^G_\\le(v) := \\lC w \\in J \\cup V \\,|\\, w < v \\rC \\cup \\{v\\}.\n    \\]\n\n    \\emph{Notational quirk: the ``$\\le$'' subscript vs.\\ the strict body.} The subscript ``$\\le$'' on $\\Pred^G_\\le$ marks the non-strict variant, but the set-builder body the LN writes is \\emph{strict} (the membership condition is $w < v$, not $w \\le v$), with the singleton $\\{v\\}$ adjoined separately. We implement this literal LN body $\\Pred^G_<(v) \\cup \\{v\\}$, \\emph{not} the alternative $\\lC w \\in J \\cup V \\,|\\, w \\le v \\rC$; the two forms coincide whenever $v \\in J \\cup V$ (irreflexivity of $<$ keeps $v$ out of the strict body, while the reflexive-closure form $w \\le v$ would pick it up via $v \\le v$), and they diverge only in the corner case noted next.\n\n    \\emph{Corner case $v \\notin J \\cup V$.} Under the literal LN body, $w < v$ for $w \\in J \\cup V$ is not provided by the total order $<$ (whose domain is $J \\cup V$), so the natural reading is $\\Pred^G_<(v) = \\emptyset$; the non-strict body's adjoined singleton then degenerately gives $\\Pred^G_\\le(v) = \\{v\\}$, so $v$ is its own non-strict predecessor even when $v$ lies outside the underlying node set of $G$. We follow the LN's lack of hypothesis on $v$ and do \\emph{not} impose $v \\in J \\cup V$ here; downstream consumers that pattern-match on the shape of an element of $\\Pred^G_<(v)$ or $\\Pred^G_\\le(v)$ may assume $v \\in J \\cup V$ at the point of use as a separate hypothesis.\n\\end{Def}\n\n\\end{document}\n",
    "html": "<p>Let $G = (J, V, E, L)$ be a CDMG (def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>); throughout we use the notation of def <a href=\"#def_3_2\" class=\"refrow\"><code>not-cdmg</code></a>. Let $<$ be a total order of $J \\cup V$, read as a <em>strict</em> total order in the sense of def <a href=\"#def_3_8\" class=\"refrow\"><code>def-topological-order</code></a> (irreflexive, transitive, and trichotomous on $J \\cup V$); the symbol $<$ is read strictly throughout. The element $v$ is left unconstrained by the LN – in particular, the LN does <em>not</em> impose $v \\in J \\cup V$ – and we follow that literal stance below; note that the comparison $w < v$ is only directly given by $<$ when $v \\in J \\cup V$, since $<$ is supplied only as a total order of $J \\cup V$. Adopting the convention of def <a href=\"#def_3_2\" class=\"refrow\"><code>not-cdmg</code></a>, item&nbsp;1, that “$w \\in G$” abbreviates “$w \\in J \\cup V$”, we write the set-builder bodies below explicitly with $w \\in J \\cup V$.</p>\n<p>The set of <em>predecessors</em> of $v$ in $G$ is\n    \\[\n        \\Pred^G_<(v) := \\lC w \\in J \\cup V \\,|\\, w < v \\rC.\n    \\]\n    We also put\n    \\[\n        \\Pred^G_\\le(v) := \\lC w \\in J \\cup V \\,|\\, w < v \\rC \\cup \\{v\\}.\n    \\]</p>\n<p><em>Notational quirk: the “$\\le$” subscript vs.\\ the strict body.</em> The subscript “$\\le$” on $\\Pred^G_\\le$ marks the non-strict variant, but the set-builder body the LN writes is <em>strict</em> (the membership condition is $w < v$, not $w \\le v$), with the singleton $\\{v\\}$ adjoined separately. We implement this literal LN body $\\Pred^G_<(v) \\cup \\{v\\}$, <em>not</em> the alternative $\\lC w \\in J \\cup V \\,|\\, w \\le v \\rC$; the two forms coincide whenever $v \\in J \\cup V$ (irreflexivity of $<$ keeps $v$ out of the strict body, while the reflexive-closure form $w \\le v$ would pick it up via $v \\le v$), and they diverge only in the corner case noted next.</p>\n<p><em>Corner case $v \\notin J \\cup V$.</em> Under the literal LN body, $w < v$ for $w \\in J \\cup V$ is not provided by the total order $<$ (whose domain is $J \\cup V$), so the natural reading is $\\Pred^G_<(v) = \\emptyset$; the non-strict body's adjoined singleton then degenerately gives $\\Pred^G_\\le(v) = \\{v\\}$, so $v$ is its own non-strict predecessor even when $v$ lies outside the underlying node set of $G$. We follow the LN's lack of hypothesis on $v$ and do <em>not</em> impose $v \\in J \\cup V$ here; downstream consumers that pattern-match on the shape of an element of $\\Pred^G_<(v)$ or $\\Pred^G_\\le(v)$ may assume $v \\in J \\cup V$ at the point of use as a separate hypothesis.</p>",
    "env": "Def",
    "env_title": "Predecessors",
    "source_path": "leanification/Chapter3_GraphTheory/Section3_1/tex/def_3_9_Predecessors.tex"
  },
  "tex_proof": null,
  "lean_blocks": [
    {
      "kind": "helper",
      "code": "variable {Node : Type*} [DecidableEq Node]",
      "explanation": "This is a `variable` declaration: Lean auto-binds `Node : Type*` and the instance `[DecidableEq Node]` as implicit arguments to every subsequent declaration in the surrounding `CDMG` namespace, sparing us from rewriting the same parameters on `Pred` and `PredLE`. The `Type*` notation makes `Node` universe-polymorphic, so callers can instantiate it at any universe level. The `DecidableEq Node` assumption is load-bearing for this row in particular: the set-builder body `{w | w ∈ G ∧ lt w v}` invokes `w ∈ G`, which via the `Membership Node (CDMG Node)` instance from `def_3_2` reduces to `Finset` membership on `G.J ∪ G.V` — and `Finset` operations require decidable equality on the element type. Stronger structure on `Node` (e.g.\\ `Fintype`, `LinearOrder`) is not needed here and is left to downstream use sites that actually consume it.",
      "code_annotated": "-- Auto-binds `Node` + `DecidableEq Node` as implicits; decidability drives `w ∈ G` via Finset membership.\nvariable {Node : Type*} [DecidableEq Node]"
    },
    {
      "kind": "main",
      "code": "def Pred (G : CDMG Node) (lt : Node → Node → Prop) (v : Node) : Set Node :=\n  {w | w ∈ G ∧ lt w v}",
      "explanation": "This block defines the strict-predecessor operator `Pred`, taking three arguments — a CDMG `G`, an external order relation `lt : Node → Node → Prop`, and a node `v` — and returning a `Set Node`. The body is a set-builder collecting every node `w` that both lies in the graph (`w ∈ G`, which via `def_3_2`'s `Membership` instance unfolds to `w ∈ G.J ∪ G.V`) and is ordered before `v` under `lt`. A crucial design point: `lt` is taken as raw data rather than pinned to a canonical `[LT Node]` typeclass, so downstream consumers (d-/σ-separation, do-calculus) can plug in whichever order they want — typically a topological order produced by `claim_3_2`. The return type is `Set Node` rather than `Finset Node`: this avoids demanding `Decidable (lt w v)` at the definitional level, with finiteness recoverable later from `G.J ∪ G.V` being a `Finset`. Namespacing under `CDMG` parallels the `Pa`/`Ch` family-set conventions of `def_3_5`, so call sites read `G.Pred lt v`.",
      "code_annotated": "-- Strict-predecessor operator on `G` under order `lt`, evaluated at node `v`; returns a `Set Node`.\ndef Pred (G : CDMG Node) (lt : Node → Node → Prop) (v : Node) : Set Node :=\n  -- Set-builder: nodes `w` lying in `G` (i.e.\\ `G.J ∪ G.V`) and strictly before `v` under `lt`.\n  {w | w ∈ G ∧ lt w v}"
    },
    {
      "kind": "main",
      "code": "def PredLE (G : CDMG Node) (lt : Node → Node → Prop) (v : Node) : Set Node :=\n  G.Pred lt v ∪ {v}",
      "explanation": "This block defines the non-strict variant `PredLE`, obtained from the strict `Pred` by adjoining `v` itself. The `LE` suffix flags the non-strict version, paralleling Mathlib's `<`/`≤` naming distinction. The body `G.Pred lt v ∪ {v}` uses dot-notation through the `CDMG` namespace — `G.Pred lt v` desugars to `CDMG.Pred G lt v` — together with Mathlib's `Set.union` operator `∪` and the singleton `{v}` (which Lean elaborates to `Set.singleton v` because the left-hand side of `∪` is a `Set Node`). An equivalent spelling would be `insert v (G.Pred lt v)`, but `∪ {v}` reads most directly as the LN's \"predecessors plus the node itself\". A subtle consequence of taking the literal LN spelling: `v ∈ G.PredLE lt v` holds unconditionally, even when `v ∉ G`, since the adjoined singleton carries no membership guard. Downstream \"ancestors-plus-self\" lemmas can now invoke `PredLE` without case-splitting on `w = v ∨ lt w v`.",
      "code_annotated": "-- Non-strict-predecessor variant (`LE` suffix): strict `Pred` together with `v` itself.\ndef PredLE (G : CDMG Node) (lt : Node → Node → Prop) (v : Node) : Set Node :=\n  -- Take the strict predecessor set and union in the singleton `{v}` via `Set.union`.\n  G.Pred lt v ∪ {v}"
    }
  ],
  "lean_source_url": "https://github.com/schipeltaper/causality-leanified/blob/main/leanification/Chapter3_GraphTheory/Section3_1/Predecessors.lean",
  "lean_file_path": "leanification/Chapter3_GraphTheory/Section3_1/Predecessors.lean",
  "addition_to_the_LN": "",
  "design_choices": "**Typing context `Node : Type*` with `[DecidableEq Node]`, inherited verbatim from `def_3_1`.** Both fixtures are load-bearing for the set-builder body `{w | w ∈ G ∧ lt w v}`. The conjunct `w ∈ G` is not custom notation: it routes through the `Membership Node (CDMG Node)` instance from `def_3_2` (`CDMGNotation.lean`), which unfolds `w ∈ G` to `w ∈ G.J ∪ G.V` — a `Finset` membership check that reduces to decidable equality on `Node`. Dropping `[DecidableEq Node]` would break the `Finset.mem` reduction and the body would fail to elaborate. Stronger instances (`Fintype`, `LinearOrder`) are deliberately *not* picked up here and are deferred to consumers who need them. The `variable` line is gated with the three-dash `--- start helper` marker (rather than the two-dash `-- start statement` reserved for formalised LN content), matching the convention across `CDMG.lean`, `TopologicalOrder.lean`, and friends — it binds the implicit parameters the two `def`s below rely on but is not itself part of the LN definition.\n\n**The strict order is passed as an explicit `lt : Node → Node → Prop` parameter, not via a `[LT Node]` typeclass, a bundled structure field, or an `∃ lt, …` existential.** The LN reads \"let $<$ be a total order of $J \\cup V$\" and then writes $\\operatorname{Pred}^G_<(v)$ — the subscript $<$ is a *parameter* the predecessor set is computed against, not a structural feature of nodes. Mirroring the parameter convention of `def_3_8`'s `IsTopologicalOrder`, `Pred G lt v` reads on the nose as the LN's \"predecessors of $v$ in $G$ under $<$\". A `[LT Node]` instance would glue a single canonical strict order to the *type* `Node`, but a single `Node` type underlies every CDMG and the theory admits many topological orders per graph. Threading the witness from `claim_3_2` (existence of *a* topological order on an acyclic CDMG) would conflate this row's predecessor-set computation with the downstream existence statement, and would prevent consumers from feeding in non-topological `lt` for ad hoc constructions. `Pred` and `PredLE` are *predicate-style operators* that accept any strict relation; the d-separation and σ-separation machinery in later chapters typically supplies an `lt` carrying a `G.IsTopologicalOrder lt` hypothesis, but the definitional shape does not require it.\n\n**Two top-level defs — strict `Pred` and non-strict `PredLE` — rather than a single operator parameterised on `<`/`≤` or `PredLE` as a thin notation wrapper.** The LN explicitly introduces *both* $\\operatorname{Pred}^G_<$ and $\\operatorname{Pred}^G_\\le$, and both names recur under that distinction in later chapters: CBN factorisation and do-calculus reach for the strict form, while iSCM recursion (\"values determined up to and including $v$\") reaches for the non-strict one. Folding the choice into `def Pred (G) (lt) (strict : Bool) (v)` would force every call site to thread a `Bool` and peel off a `match` before reasoning about set membership. A pure `notation` abbreviation would leave `PredLE` un-`@[simp]`-able as its own head symbol. Both downsides are avoided by exposing two named `def`s; the second is a one-line wrapper `G.Pred lt v ∪ {v}`, so the duplication cost is negligible.\n\n**Output type `Set Node`, not `Finset Node` or the subtype `↥(G.J ∪ G.V)`.** The chapter's family-set primitives (`Pa`, `Ch`, `Sib`, `Anc`, `Desc`, `Sc`, `Dist` from `def_3_5`) all live in `Set Node`, and downstream uses freely intersect predecessor sets with family sets (e.g. $\\operatorname{Pred}^G_<(v) \\cap \\operatorname{Pa}^G(v)$ in do-calculus, $\\operatorname{Pred}^G_<(v) \\cap V$ in CBN factorisation). A `Finset Node` carrier would thread a `Decidable (lt w v)` instance through every call site — the LN never picks a decidable representative for $<$ — and would force `Finset.coe` round-trips against the ambient family sets. The subtype carrier would force `Subtype.val` coercions throughout and would make `PredLE`'s adjoined `{v}` ill-typed in the corner case $v \\notin J \\cup V$. The LN-promised finiteness is still recoverable downstream — the underlying carrier `G.J ∪ G.V` *is* a `Finset` with decidable membership — but is not exposed at the API surface here; a `Finset`-flavoured parallel lemma can be derived later if needed.\n\n**The `w ∈ G` conjunct lives inside the set-builder body, not as a downstream intersection.** The LN's literal $\\{w \\in J \\cup V \\mid w < v\\}$ bounds the first conjunct on $w \\in J \\cup V$ rather than letting $w$ range over arbitrary `Node`. Two payoffs: (i) under `def_3_2`'s `Membership` instance, `w ∈ G` unfolds transparently to `w ∈ G.J ∪ G.V`, preserving the literal LN grep correspondence with no convention to smuggle in; (ii) destructuring `h : w ∈ Pred G lt v` as `⟨hw_mem, hw_lt⟩` immediately delivers `hw_mem : w ∈ G` without a separate intersection step, matching the precedent set by `Pa`, `Ch`, `Sib` in `FamilyRelationships.lean`. If `lt` happens to be well-typed (e.g. the strict part of a topological order on `G`, whose domain is `J ∪ V`), then `lt w v` already only fires for `w ∈ G` and the conjunct is redundant — but the definition does *not* assume well-typedness, and the explicit `w ∈ G` makes the API robust to arbitrary `lt`.\n\n**`PredLE`'s body is the literal LN spelling `G.Pred lt v ∪ {v}`, not the reflexive-closure form `{w | w ∈ G ∧ (lt w v ∨ w = v)}` or `{w | w ≤ v}`.** The LN subscript reads $\\le$ but the body it actually writes is *strict* — $\\{w \\in J \\cup V \\mid w < v\\} \\cup \\{v\\}$, with the singleton adjoined unconditionally. The two forms agree whenever $v \\in J \\cup V$ (irreflexivity of $<$ keeps $v$ out of the strict body, while $w \\le v$ would pick it up via $v \\le v$) and diverge only in the corner case $v \\notin J \\cup V$: under the literal LN body the adjoined `{v}` carries no `v ∈ G` guard, so `v ∈ PredLE G lt v` *unconditionally*. The body is spelled with the named `G.Pred` rather than unfolded — partly because it reads on the nose as \"the strict predecessor set plus $v$\", and partly because downstream proofs holding `h : w ∈ G.Pred lt v` can apply `Or.inl h` to obtain `w ∈ G.PredLE lt v` with no intermediate `Set.mem_setOf` unfolding. The union is Mathlib's `Set.union` / `∪` notation; the singleton `{v}` resolves to `Set.instSingleton` because the left operand of `∪` is `Set Node`, so no ascription is needed. `∪ {v}` is preferred over `insert v (G.Pred lt v)` — both are valid Mathlib idioms, but the union form reads most directly as the LN's \"predecessors plus the node itself\", and the standard destructors `Set.mem_union` and `Set.mem_singleton_iff` handle membership cleanly. Naming-wise, `Pred` and `PredLE` are namespaced under `CDMG` (enabling `G.Pred lt v`), use Mathlib `PascalCase` for set-valued operators paralleling `Pa` / `PaSet` from `def_3_5`, and the `LE` suffix signals the non-strict variant while keeping the strict form's name short."
}
