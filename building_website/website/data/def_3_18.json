{
  "ref": "def_3_18",
  "kind": "def",
  "section": "3.3",
  "title": "ISigmaSeparation",
  "type": "definition",
  "status": {
    "formalized": "yes",
    "proven": "n/a",
    "solved": "yes"
  },
  "tex_block_html": "<p>Let $G=(J,V,E,L)$ be a CDMG and $A,B,C \\ins J \\cup V$ (not necessarily disjoint) subset of nodes.\n    We then say that:</p>\n<ol class=\"enum\"><li><em>$A$ is $i\\sigma$-separated from $B$ given $C$ in $G$</em>, in symbols:\n    \\[ A \\isPerp_G B \\given C, \\]\n    if every walk from a node in $A$ to a node in $J \\cup B$ (sic!) is $\\sigma$-blocked by $C$.</li><li>If that property does not hold we will write:\n            \\[ A \\nisPerp_G B \\given C. \\]</li><li>We also define the special case:\n                \\[ A \\isPerp_G B \\qquad :\\iff \\qquad A \\isPerp_G B \\given \\emptyset.\\]</li><li>For the special case $J = \\emptyset$—that is, if $G$ has no input nodes—$i\\sigma$-separation is also called <em>$\\sigma$-separation</em>, and we write:\n          \\[A \\sPerp_G B \\given C := A \\isPerp_G B \\given C, \\qquad A \\nsPerp_G B \\given C := A \\nisPerp_G B \\given C.\\]\n<p>Note that $\\sigma$-separation is symmetric: $A \\sPerp_G B \\given C \\iff B \\sPerp_G A \\given C$, since when $J = \\emptyset$ the set of walks between $A$ and $B$ is the same regardless of direction, and the $\\sigma$-blocking conditions are invariant under walk reversal.</li></ol></p>",
  "tex_statement": {
    "raw": "% Definition file for row def_3_18 -- \"ISigmaSeparation\".\n% Auto-generated stub by scaffold/solve_chapter.py at row start. The\n% manager agent (or a worker dispatched by it) fills the body below with the\n% Lean-faithful definition statement(s). Stays close to the lecture notes.\n%\n% This file is a sibling of `main.tex` inside `<subsection>/tex/`. The\n% `subfiles` package lets it compile both standalone (resolving its\n% preamble via `main.tex`) and as part of `main.tex` via `\\subfile{...}`.\n\n\\documentclass[main]{subfiles}\n\n\\begin{document}\n% ref: def_3_18\n% title: ISigmaSeparation\n\\def\\rowref{def\\_3\\_18}\n\\phantomsection\\label{def_3_18}\n\n\\begin{Def}[$i\\sigma$-separation]\\label{def:sigma_separation}\n    Let $G = (J, V, E, L)$ be a CDMG (def \\ref{def-cdmg}); throughout we use the notation of def \\ref{not-cdmg}, the walk definition of def \\ref{def:walks}, item~i., and the $\\sigma$-blocking predicate ``$\\pi$ is $C$-$\\sigma$-blocked'' (equivalently ``$\\sigma$-blocked given $C$'') of def \\ref{def:sigma_blocking}. Let $A, B, C \\ins J \\cup V$ be subsets of nodes; $A$, $B$, $C$ are \\emph{not} assumed to be pairwise disjoint, nor disjoint from the input-node set $J$, nor non-empty.\n\n    We then say that:\n    \\begin{enumerate}\n        \\item \\emph{$A$ is $i\\sigma$-separated from $B$ given $C$ in $G$,} in symbols\n            \\[\n                A \\isPerp_G B \\given C,\n            \\]\n            iff: for every integer $n \\ge 0$ and every walk\n            \\[\n                \\pi = (v_0, a_0, v_1, a_1, \\dots, v_{n-1}, a_{n-1}, v_n)\n            \\]\n            in $G$ (def \\ref{def:walks}, item~i.) with $v_0 \\in A$ and $v_n \\in J \\cup B$\\footnote{The choice to include $J$ here in this place is non-standard in the literature. However, if we include $J$ in this definition here the implied (asymmetric) separoid rules for $id$-/$i\\sigma$-separation will be of the same form as those for Markov kernels regarding conditional independence. This is the reason we include $J$ here.\\label{fn:why-J}}, the walk $\\pi$ is $C$-$\\sigma$-blocked in the sense of def \\ref{def:sigma_blocking}.\n\n            \\emph{Range of the walk quantifier.} The universal quantifier ranges over \\emph{all} walks of every admissible length $n \\ge 0$, including the trivial (length-zero) walk: whenever there exists $a \\in A \\cap (J \\cup B)$, the tuple $\\pi = (a)$ is, by def \\ref{def:walks}, item~i.\\ (case $n = 0$), a walk from $a$ to $a$ in $G$ with $v_0 = v_n = a$, hence $v_0 \\in A$ and $v_n \\in J \\cup B$, and is therefore in the scope of the quantifier. Whether such a trivial walk is $C$-$\\sigma$-blocked is determined by applying def \\ref{def:sigma_blocking} at $n = 0$ (the blocking status reduces to: is the single end-position $k = 0$ a blockable non-collider with $v_0 \\in C$, in the sense of def \\ref{def:unblockable_noncollider}? --- which is the formalizer's downstream derivation, not part of the literal definition stated here). In particular, the condition is \\emph{not} vacuously satisfied when $A \\cap (J \\cup B)$ is non-empty, and downstream consumers should not assume otherwise.\n\n            \\emph{Asymmetric inclusion of $J$ on the right.} The right-hand endpoint constraint $v_n \\in J \\cup B$ (rather than $v_n \\in B$) is the deliberate, non-standard choice flagged in footnote~\\ref{fn:why-J} of the LN: with $J$ included on the right, the implied (asymmetric) separoid rules for $i\\sigma$-separation match the corresponding rules for conditional independence under Markov kernels. As a direct consequence, the marginal case $B = \\emptyset$ is \\emph{not} vacuous: $A \\isPerp_G \\emptyset \\given C$ asserts that every walk in $G$ from a node in $A$ to a node in $J$ is $C$-$\\sigma$-blocked, which is a genuine condition whenever $J \\neq \\emptyset$. Only when $J = \\emptyset$ does $A \\isPerp_G \\emptyset \\given C$ recover the usual ``separation from the empty set is vacuous'' reading.\n\n        \\item \\emph{If the property in item~1 does not hold} --- equivalently, if there exist an integer $n \\ge 0$ and a walk\n            \\[\n                \\pi = (v_0, a_0, v_1, \\dots, a_{n-1}, v_n)\n            \\]\n            in $G$ with $v_0 \\in A$, $v_n \\in J \\cup B$, and $\\pi$ is \\emph{not} $C$-$\\sigma$-blocked (equivalently, $\\pi$ is $C$-$\\sigma$-open in the sense of def \\ref{def:sigma_blocking}) --- we write\n            \\[\n                A \\nisPerp_G B \\given C.\n            \\]\n\n        \\item The unconditional ($C = \\emptyset$) special case is defined as the abbreviation\n            \\[\n                A \\isPerp_G B \\quad :\\iff \\quad A \\isPerp_G B \\given \\emptyset.\n            \\]\n            Unfolded via item~1: $A \\isPerp_G B$ iff for every $n \\ge 0$ and every walk $\\pi$ in $G$ from a node $v_0 \\in A$ to a node $v_n \\in J \\cup B$, $\\pi$ is $\\emptyset$-$\\sigma$-blocked in the sense of def \\ref{def:sigma_blocking}.\n\n        \\item For the special case $J = \\emptyset$ --- that is, $G$ has no input nodes --- $i\\sigma$-separation is also called \\emph{$\\sigma$-separation}, and we write\n            \\[\n                A \\sPerp_G B \\given C \\;\\; := \\;\\; A \\isPerp_G B \\given C,\n                \\qquad\n                A \\nsPerp_G B \\given C \\;\\; := \\;\\; A \\nisPerp_G B \\given C.\n            \\]\n            Under $J = \\emptyset$ the right-hand endpoint constraint $v_n \\in J \\cup B$ reduces to $v_n \\in B$, so $A \\sPerp_G B \\given C$ unfolds to: for every $n \\ge 0$ and every walk $\\pi$ in $G$ from a node $v_0 \\in A$ to a node $v_n \\in B$, $\\pi$ is $C$-$\\sigma$-blocked in the sense of def \\ref{def:sigma_blocking}.\n    \\end{enumerate}\n\n    \\emph{Treatment of the trailing LN remark.}\n    The LN source block (\\texttt{graphs.tex}, \\texttt{\\textbackslash label\\{def:sigma\\_separation\\}}) closes item~4 with an embedded \\texttt{claimmark} stating that $\\sigma$-separation is symmetric ($A \\sPerp_G B \\given C \\iff B \\sPerp_G A \\given C$ when $J = \\emptyset$), justified informally by the observation that walks ``between $A$ and $B$'' are the same regardless of direction and that the $\\sigma$-blocking conditions are invariant under walk reversal. Per the convention adopted in the sibling row \\texttt{def\\_3\\_17} (whose embedded \\texttt{claimmark} for \\texttt{claim\\_3\\_21} is treated identically), that informal pre-statement is \\emph{intentionally excluded} from the formalization of def \\ref{def:sigma_separation}: it is not part of the definition proper, and its substantive content is the dedicated separate claim row \\texttt{claim\\_3\\_22} (\\emph{$\\sigma$-separation is symmetric}), whose statement and proof live in \\texttt{claim\\_3\\_22\\_statement\\_SigmaSeparationSymmetric.tex} and \\texttt{claim\\_3\\_22\\_proof\\_SigmaSeparationSymmetric.tex} respectively. The row's authoritative \\texttt{addition\\_to\\_the\\_LN} clause \\texttt{[sigma\\_symmetry\\_claim\\_invokes\\_unstated\\_reversal\\_invariance]} records (as background, not as a Lean obligation within this definition) the precise sense in which the informal justification is to be read: in a CDMG, a walk per def \\ref{def:walks}, item~i.\\ is a sequence of incident edges \\emph{without regard to traversal orientation}, so reading the tuple in reverse yields another walk with start- and end-nodes swapped; this walk-reversal map is an involution on the set of walks in $G$. Symmetry of $\\sigma$-separation (when $J = \\emptyset$) is to be understood as resting on this involution together with the fact that the $\\sigma$-blocking conditions of def \\ref{def:sigma_blocking} on internal positions are stated invariantly under walk reversal --- but this reasoning is the content of the proof of \\texttt{claim\\_3\\_22}, not of the present definition. No Lean obligation is to be derived from the embedded \\texttt{claimmark} within this definition.\n\\end{Def}\n\n\\end{document}\n",
    "html": "<p>Let $G = (J, V, E, L)$ be a CDMG (def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>); throughout we use the notation of def <a href=\"#def_3_2\" class=\"refrow\"><code>not-cdmg</code></a>, the walk definition of def <a href=\"#def_3_4\" class=\"refrow\"><code>def:walks</code></a>, item&nbsp;i., and the $\\sigma$-blocking predicate “$\\pi$ is $C$-$\\sigma$-blocked” (equivalently “$\\sigma$-blocked given $C$”) of def <a href=\"#def_3_17\" class=\"refrow\"><code>def:sigma_blocking</code></a>. Let $A, B, C \\ins J \\cup V$ be subsets of nodes; $A$, $B$, $C$ are <em>not</em> assumed to be pairwise disjoint, nor disjoint from the input-node set $J$, nor non-empty.</p>\n<p>We then say that:</p>\n<ol class=\"enum\"><li><em>$A$ is $i\\sigma$-separated from $B$ given $C$ in $G$,</em> in symbols\n            \\[\n                A \\isPerp_G B \\given C,\n            \\]\n            iff: for every integer $n \\ge 0$ and every walk\n            \\[\n                \\pi = (v_0, a_0, v_1, a_1, \\dots, v_{n-1}, a_{n-1}, v_n)\n            \\]\n            in $G$ (def <a href=\"#def_3_4\" class=\"refrow\"><code>def:walks</code></a>, item&nbsp;i.) with $v_0 \\in A$ and $v_n \\in J \\cup B$, the walk $\\pi$ is $C$-$\\sigma$-blocked in the sense of def <a href=\"#def_3_17\" class=\"refrow\"><code>def:sigma_blocking</code></a>.\n<p><em>Range of the walk quantifier.</em> The universal quantifier ranges over <em>all</em> walks of every admissible length $n \\ge 0$, including the trivial (length-zero) walk: whenever there exists $a \\in A \\cap (J \\cup B)$, the tuple $\\pi = (a)$ is, by def <a href=\"#def_3_4\" class=\"refrow\"><code>def:walks</code></a>, item&nbsp;i.\\ (case $n = 0$), a walk from $a$ to $a$ in $G$ with $v_0 = v_n = a$, hence $v_0 \\in A$ and $v_n \\in J \\cup B$, and is therefore in the scope of the quantifier. Whether such a trivial walk is $C$-$\\sigma$-blocked is determined by applying def <a href=\"#def_3_17\" class=\"refrow\"><code>def:sigma_blocking</code></a> at $n = 0$ (the blocking status reduces to: is the single end-position $k = 0$ a blockable non-collider with $v_0 \\in C$, in the sense of def <a href=\"#def_3_16\" class=\"refrow\"><code>def:unblockable_noncollider</code></a>? — which is the formalizer's downstream derivation, not part of the literal definition stated here). In particular, the condition is <em>not</em> vacuously satisfied when $A \\cap (J \\cup B)$ is non-empty, and downstream consumers should not assume otherwise.</p>\n<p><em>Asymmetric inclusion of $J$ on the right.</em> The right-hand endpoint constraint $v_n \\in J \\cup B$ (rather than $v_n \\in B$) is the deliberate, non-standard choice flagged in footnote&nbsp;<a href=\"#def_3_18\" class=\"refrow\"><code>fn:why-J</code></a> of the LN: with $J$ included on the right, the implied (asymmetric) separoid rules for $i\\sigma$-separation match the corresponding rules for conditional independence under Markov kernels. As a direct consequence, the marginal case $B = \\emptyset$ is <em>not</em> vacuous: $A \\isPerp_G \\emptyset \\given C$ asserts that every walk in $G$ from a node in $A$ to a node in $J$ is $C$-$\\sigma$-blocked, which is a genuine condition whenever $J \\neq \\emptyset$. Only when $J = \\emptyset$ does $A \\isPerp_G \\emptyset \\given C$ recover the usual “separation from the empty set is vacuous” reading.</li><li><em>If the property in item&nbsp;1 does not hold</em> — equivalently, if there exist an integer $n \\ge 0$ and a walk\n            \\[\n                \\pi = (v_0, a_0, v_1, \\dots, a_{n-1}, v_n)\n            \\]\n            in $G$ with $v_0 \\in A$, $v_n \\in J \\cup B$, and $\\pi$ is <em>not</em> $C$-$\\sigma$-blocked (equivalently, $\\pi$ is $C$-$\\sigma$-open in the sense of def <a href=\"#def_3_17\" class=\"refrow\"><code>def:sigma_blocking</code></a>) — we write\n            \\[\n                A \\nisPerp_G B \\given C.\n            \\]</li><li>The unconditional ($C = \\emptyset$) special case is defined as the abbreviation\n            \\[\n                A \\isPerp_G B \\quad :\\iff \\quad A \\isPerp_G B \\given \\emptyset.\n            \\]\n            Unfolded via item&nbsp;1: $A \\isPerp_G B$ iff for every $n \\ge 0$ and every walk $\\pi$ in $G$ from a node $v_0 \\in A$ to a node $v_n \\in J \\cup B$, $\\pi$ is $\\emptyset$-$\\sigma$-blocked in the sense of def <a href=\"#def_3_17\" class=\"refrow\"><code>def:sigma_blocking</code></a>.</li><li>For the special case $J = \\emptyset$ — that is, $G$ has no input nodes — $i\\sigma$-separation is also called <em>$\\sigma$-separation</em>, and we write\n            \\[\n                A \\sPerp_G B \\given C \\;\\; := \\;\\; A \\isPerp_G B \\given C,\n                \\qquad\n                A \\nsPerp_G B \\given C \\;\\; := \\;\\; A \\nisPerp_G B \\given C.\n            \\]\n            Under $J = \\emptyset$ the right-hand endpoint constraint $v_n \\in J \\cup B$ reduces to $v_n \\in B$, so $A \\sPerp_G B \\given C$ unfolds to: for every $n \\ge 0$ and every walk $\\pi$ in $G$ from a node $v_0 \\in A$ to a node $v_n \\in B$, $\\pi$ is $C$-$\\sigma$-blocked in the sense of def <a href=\"#def_3_17\" class=\"refrow\"><code>def:sigma_blocking</code></a>.</li></ol></p>\n<p><em>Treatment of the trailing LN remark.</em>\n    The LN source block (<code>graphs.tex</code>, \\texttt{\\label\\{def:sigma\\_separation\\}}) closes item&nbsp;4 with an embedded <code>claimmark</code> stating that $\\sigma$-separation is symmetric ($A \\sPerp_G B \\given C \\iff B \\sPerp_G A \\given C$ when $J = \\emptyset$), justified informally by the observation that walks “between $A$ and $B$” are the same regardless of direction and that the $\\sigma$-blocking conditions are invariant under walk reversal. Per the convention adopted in the sibling row <code>def_3_17</code> (whose embedded <code>claimmark</code> for <code>claim_3_21</code> is treated identically), that informal pre-statement is <em>intentionally excluded</em> from the formalization of def <a href=\"#def_3_18\" class=\"refrow\"><code>def:sigma_separation</code></a>: it is not part of the definition proper, and its substantive content is the dedicated separate claim row <code>claim_3_22</code> (<em>$\\sigma$-separation is symmetric</em>), whose statement and proof live in <code>claim_3_22_statement_SigmaSeparationSymmetric.tex</code> and <code>claim_3_22_proof_SigmaSeparationSymmetric.tex</code> respectively. The row's authoritative <code>addition_to_the_LN</code> clause <code>[sigma_symmetry_claim_invokes_unstated_reversal_invariance]</code> records (as background, not as a Lean obligation within this definition) the precise sense in which the informal justification is to be read: in a CDMG, a walk per def <a href=\"#def_3_4\" class=\"refrow\"><code>def:walks</code></a>, item&nbsp;i.\\ is a sequence of incident edges <em>without regard to traversal orientation</em>, so reading the tuple in reverse yields another walk with start- and end-nodes swapped; this walk-reversal map is an involution on the set of walks in $G$. Symmetry of $\\sigma$-separation (when $J = \\emptyset$) is to be understood as resting on this involution together with the fact that the $\\sigma$-blocking conditions of def <a href=\"#def_3_17\" class=\"refrow\"><code>def:sigma_blocking</code></a> on internal positions are stated invariantly under walk reversal — but this reasoning is the content of the proof of <code>claim_3_22</code>, not of the present definition. No Lean obligation is to be derived from the embedded <code>claimmark</code> within this definition.</p>",
    "env": "Def",
    "env_title": "$i\\sigma$-separation",
    "source_path": "leanification/Chapter3_GraphTheory/Section3_3/tex/def_3_18_ISigmaSeparation.tex"
  },
  "tex_proof": null,
  "lean_blocks": [
    {
      "kind": "helper",
      "code": "variable {Node : Type*} [DecidableEq Node]",
      "explanation": "This is a `variable` declaration: Lean auto-binds `Node : Type*` and the instance `[DecidableEq Node]` as implicit arguments to every subsequent declaration in the surrounding `CDMG` namespace, sparing us from rewriting the same parameters on `IsISigmaSeparated` and its four notational siblings. The `Type*` notation makes `Node` universe-polymorphic, so callers can instantiate it at any universe level. The `DecidableEq Node` assumption is load-bearing because the ambient `CDMG Node` structure stores `J, V : Finset Node` and `E, L : Finset (Node × Node)`; decidable equality is what lets those `Finset`s exist and what enables the `(G.J : Set Node)` coercion appearing in the main predicate. Stronger structure on `Node` (e.g. `Fintype`, `LinearOrder`) is not required here and is deferred to downstream consumers that actually use it. Same convention as the rest of `Chapter3_GraphTheory`'s files.",
      "code_annotated": "-- Auto-binds `Node` + `DecidableEq Node` as implicits; decidability backs `CDMG`'s `Finset` fields and the `G.J` coercion below.\nvariable {Node : Type*} [DecidableEq Node]"
    },
    {
      "kind": "helper",
      "code": "def IsNotISigmaSeparated (G : CDMG Node) (A B C : Set Node) : Prop :=\n  ¬ G.IsISigmaSeparated A B C",
      "explanation": "This block formalises item 2 of the LN: the named negation $A \\nisPerp_G B \\given C$, the failure of $i\\sigma$-separation. Rather than re-quantifying as a positive existential (\"there exists a walk from $A$ into $J \\cup B$ that is not $C$-$\\sigma$-blocked\"), it is defined as the literal Lean negation `¬ G.IsISigmaSeparated A B C`. The two predicates are then definitionally equal: `unfold IsNotISigmaSeparated` collapses one to the other by `rfl`, and any downstream interchange site avoids invoking a classical bridging lemma. The LN's \"equivalently, $\\exists$ walk \\dots not $C$-$\\sigma$-blocked\" reformulation is the classical De Morgan dual of the negated universal, derivable as a one-line lemma when needed. The named alias keeps the LN's `\\nisPerp` notation grep-aligned with the Lean source: every LN reference to $A \\nisPerp_G B \\given C$ has a literal `G.IsNotISigmaSeparated A B C` counterpart.",
      "code_annotated": "-- `IsNotISigmaSeparated` is the LN's `\\nisPerp` — named alias for the negation of `IsISigmaSeparated`.\ndef IsNotISigmaSeparated (G : CDMG Node) (A B C : Set Node) : Prop :=\n  -- Definitionally equal to `¬ IsISigmaSeparated G A B C`; `rfl`-interchangeable at every site.\n  ¬ G.IsISigmaSeparated A B C"
    },
    {
      "kind": "helper",
      "code": "abbrev IsISigmaSeparatedEmpty (G : CDMG Node) (A B : Set Node) : Prop :=\n  G.IsISigmaSeparated A B ∅",
      "explanation": "This block formalises item 3 of the LN: the unconditional shorthand $A \\isPerp_G B :\\iff A \\isPerp_G B \\given \\emptyset$. There is no fresh mathematical content — $A \\isPerp_G B$ is *defined to mean* $A \\isPerp_G B \\given \\emptyset$ — so it is encoded as an `abbrev`, the Lean keyword for a fully transparent alias. Lean reduces `G.IsISigmaSeparatedEmpty A B` to `G.IsISigmaSeparated A B ∅` at every elaboration site without an `unfold` step, so any lemma targeting the underlying predicate fires automatically on the shorthand. The empty set on the right is Lean's `Set Node`-valued `∅` (the standard `EmptyCollection` instance), matching the conditioning slot's `Set Node` carrier. The LN introduces no separate symbol for the negation of this case, so no `IsNotISigmaSeparatedEmpty` is provided; downstream consumers spell it as `¬ G.IsISigmaSeparatedEmpty A B` or `G.IsNotISigmaSeparated A B ∅`, interchangeable via the `abbrev`'s transparency.",
      "code_annotated": "-- LN item 3: unconditional shorthand `A ⫫ⁱˢ_G B := A ⫫ⁱˢ_G B | ∅`. `abbrev` keeps it transparent at every use site.\nabbrev IsISigmaSeparatedEmpty (G : CDMG Node) (A B : Set Node) : Prop :=\n  -- Body: instantiate the conditioning slot with `Set Node`-valued `∅` (Lean's `EmptyCollection`).\n  G.IsISigmaSeparated A B ∅"
    },
    {
      "kind": "helper",
      "code": "abbrev IsSigmaSeparated (G : CDMG Node) (A B C : Set Node) : Prop :=\n  G.IsISigmaSeparated A B C",
      "explanation": "This block formalises the first half of item 4 of the LN: under $J = \\emptyset$, $i\\sigma$-separation is renamed to $\\sigma$-separation (the LN's $A \\sPerp_G B \\given C$). The underlying predicate is unchanged — the same mathematical object acquires a new name when the input-node set is empty. Mirroring the LN, this is encoded as an `abbrev`, transparent to elaboration, so `G.IsSigmaSeparated A B C` reduces to `G.IsISigmaSeparated A B C` automatically. Crucially, no `(hJ : G.J = ∅)` hypothesis is attached: the LN treats $J = \\emptyset$ as a *call-site* fact about the consumer's CDMG (handed in as an explicit hypothesis by e.g. `claim_3_22`), not a logical condition baked into the predicate. Under that $J = \\emptyset$ assumption, the right-endpoint constraint `v ∈ (G.J : Set Node) ∪ B` of the main predicate reduces to `v ∈ B`, recovering the literature-standard $\\sigma$-separation. The alias is the predicate this section's downstream consumers (most prominently `claim_3_22` on $\\sigma$-symmetry) speak in.",
      "code_annotated": "-- LN item 4: under `G.J = ∅` we rename `IsISigmaSeparated` to `IsSigmaSeparated`. Predicate unchanged; precondition lives at the call site.\nabbrev IsSigmaSeparated (G : CDMG Node) (A B C : Set Node) : Prop :=\n  -- Body is literally `IsISigmaSeparated`; `abbrev` makes the rename `rfl`-transparent.\n  G.IsISigmaSeparated A B C"
    },
    {
      "kind": "helper",
      "code": "abbrev IsNotSigmaSeparated (G : CDMG Node) (A B C : Set Node) : Prop :=\n  G.IsNotISigmaSeparated A B C",
      "explanation": "This block formalises the negation half of item 4 of the LN: the $J = \\emptyset$ notation alias for $A \\nsPerp_G B \\given C$. The LN renames `\\nisPerp` to `\\nsPerp` under $J = \\emptyset$ in the same breath that it renames `\\isPerp` to `\\sPerp`; the pair is introduced and used as a unit — claim statements and proof case-splits in `claim_3_22` (and chapter 4+) routinely write $A \\sPerp_G B \\given C$ and $A \\nsPerp_G B \\given C$ side by side. The encoding is the matching `abbrev`, transparently unfolding to `G.IsNotISigmaSeparated A B C`. A proof that establishes `G.IsNotSigmaSeparated A B C` immediately discharges a goal `G.IsNotISigmaSeparated A B C` and vice versa, without a bridging lemma. As with `IsSigmaSeparated`, no $J = \\emptyset$ hypothesis is bound — the renaming is *under* the consumer's assumption, not *conditional on* a Lean-tracked proof of it.",
      "code_annotated": "-- LN item 4 (negation half): `J = ∅` rename of `IsNotISigmaSeparated`. Paired with `IsSigmaSeparated` at every call site.\nabbrev IsNotSigmaSeparated (G : CDMG Node) (A B C : Set Node) : Prop :=\n  -- Body delegates to `IsNotISigmaSeparated`, itself definitionally `¬ IsISigmaSeparated`.\n  G.IsNotISigmaSeparated A B C"
    },
    {
      "kind": "main",
      "code": "def IsISigmaSeparated (G : CDMG Node) (A B C : Set Node) : Prop :=\n  ∀ {u v : Node} (π : Walk G u v),\n      u ∈ A → v ∈ (G.J : Set Node) ∪ B → π.IsSigmaBlockedGiven C",
      "explanation": "This block formalises item 1 of the LN: the central predicate $A \\isPerp_G B \\given C$, $i\\sigma$-separation of $A$ from $B$ given $C$ in $G$. The body is a single universal over walks: for every walk $\\pi : \\mathtt{Walk}\\,G\\,u\\,v$ in $G$ (per `def_3_4` item i.) with $u \\in A$ and $v \\in J \\cup B$, $\\pi$ is $C$-$\\sigma$-blocked in the sense of `def_3_17`. The endpoint variables `{u v : Node}` are implicit so Lean infers them from $\\pi$'s type; the walk itself is the explicit subject — the same shape as `def_3_16`'s `IsBlockableNonCollider`. The right-endpoint constraint `v ∈ (G.J : Set Node) ∪ B` coerces `G.J : Finset Node` up to `Set Node` so the set-union with `B : Set Node` is well-typed; `G.J ∪ B` directly would be ill-typed because no union instance bridges the two carriers. Dot-notation `π.IsSigmaBlockedGiven C` invokes the `def_3_17` blocking predicate on the walk. Length-zero walks `Walk.nil` are in scope by construction. The asymmetric inclusion of $J$ on the right is the LN's deliberate non-standard choice (footnote `fn:why-J`), aligning the implied separoid rules with Markov-kernel conditional independence; the marginal case $B = \\emptyset$ is therefore *not* vacuous when $J \\neq \\emptyset$.",
      "code_annotated": "-- ref: def_3_18 item 1 — the central `IsISigmaSeparated` predicate. LN's `A ⫫ⁱˢ_G B | C`.\ndef IsISigmaSeparated (G : CDMG Node) (A B C : Set Node) : Prop :=\n  -- Walk-first universal: endpoints `{u v}` implicit (inferred from `π`), walk `π` explicit subject.\n  ∀ {u v : Node} (π : Walk G u v),\n      -- Endpoint constraints: source `u ∈ A`; target `v ∈ J ∪ B` with `G.J` coerced from `Finset` to `Set Node`.\n      u ∈ A → v ∈ (G.J : Set Node) ∪ B → π.IsSigmaBlockedGiven C"
    }
  ],
  "lean_source_url": "https://github.com/schipeltaper/causality-leanified/blob/main/leanification/Chapter3_GraphTheory/Section3_3/ISigmaSeparation.lean",
  "lean_file_path": "leanification/Chapter3_GraphTheory/Section3_3/ISigmaSeparation.lean",
  "addition_to_the_LN": "[sigma_symmetry_claim_invokes_unstated_reversal_invariance] In a CDMG, a walk is a sequence of incident edges without regard to edge orientation; consequently, every walk from $A$ to $B$ is, read in reverse, a walk from $B$ to $A$, and this reversal is an involution on the set of walks. The symmetry of $\\sigma$-separation (when $J = \\emptyset$) is to be understood as resting on this walk-reversal involution together with the fact that the $\\sigma$-blocking conditions on internal nodes are stated in a manner invariant under reversal.",
  "design_choices": "**`Set Node` (not `Finset Node`) for `A`, `B`, `C`.** The LN's \"$A, B, C \\subseteq J \\cup V$, not necessarily disjoint, nor disjoint from $J$, nor non-empty\" admits $\\emptyset$ and imposes no finiteness obligation. `Set Node` carries the boolean algebra (`∪`, `∩`, `∅`) needed to spell `J ∪ B` and `C = ∅` directly, interoperates with `G.AncSet` (used inside `IsSigmaBlockedGiven`) without coercion, and matches `def_3_17`'s `C : Set Node`. `Finset Node` was rejected as forcing finiteness on consumers that only need set algebra; holders of `s : Finset Node` can pass `(↑s : Set Node)`. No `A, B, C ⊆ J ∪ V` hypothesis is enforced at the def site either — out-of-graph nodes contribute vacuously.\n\n**Asymmetric inclusion of `J` on the right endpoint.** Per LN footnote `fn:why-J`, putting $J$ on the right (`v ∈ J ∪ B`) rather than just `B` is what makes the implied (asymmetric) separoid rules for $i\\sigma$-separation match those for conditional independence under Markov kernels — load-bearing for chapter 4+. Direct consequence: the marginal case $B = \\emptyset$ is *not* vacuous when $J \\neq \\emptyset$; `G.IsISigmaSeparated A ∅ C` asserts every walk from $A$ into $J$ is $C$-$\\sigma$-blocked. Symmetrising or dropping `J` to recover the literature convention would break this alignment.\n\n**Endpoint coercion via `(G.J : Set Node) ∪ B`.** `G.J : Finset Node` and `B : Set Node` are different carriers; the `Finset`-level union is ill-typed, so coercing `G.J` to `Set Node` and unioning at the set level is the only well-typed move. The set-union form lifts every `Set`-algebra lemma (`Set.mem_union`, `Set.union_empty`) directly to `IsISigmaSeparated`. The boolean spelling `v ∈ G.J ∨ v ∈ B` is definitionally equivalent but loses the \"$J \\cup B$ as a set\" reading.\n\n**Negation as a definitionally-equal `def`, not a parallel existential.** `IsNotISigmaSeparated G A B C` unfolds to `¬ G.IsISigmaSeparated A B C` by `rfl`, so proofs switching between the two need no bridging lemma. The tex's \"equivalently, $\\exists$ walk $\\pi$ … not $C$-$\\sigma$-blocked\" reformulation is the classical De Morgan dual of the negated universal — a one-line lemma when needed, not a parallel definition. A positive-existential encoding would break the definitional link and force a classical bridge at every interconversion. The named alias keeps the LN's `\\nisPerp` grep-aligned with Lean source.\n\n**`abbrev` for the three notational shorthands; no `J = ∅` hypothesis baked in.** LN items 3 and 4 are pure notation: $A \\isPerp_G B$ *means* $A \\isPerp_G B \\given \\emptyset$, and $A \\sPerp_G B \\given C$ is the renaming under $J = \\emptyset$. `abbrev` is fully transparent — `IsISigmaSeparatedEmpty`, `IsSigmaSeparated`, `IsNotSigmaSeparated` reduce to their underlying predicates at every elaboration site without `unfold`, so lemmas targeting the underlying form fire on the shorthand automatically. A `def` would create an opaque alias and force `unfold` everywhere. Crucially, no `(hJ : G.J = ∅)` hypothesis is attached to `IsSigmaSeparated` / `IsNotSigmaSeparated`: the LN treats $J = \\emptyset$ as a *call-site* fact about the consumer's CDMG (handed in by e.g. `claim_3_22`), not a logical condition on the predicate.\n\n**Walk-first universal; length-zero walks in scope.** The walk is the primary subject of `∀ {u v} (π : Walk G u v), …`; endpoint memberships are properties of its endpoints, mirroring `def_3_16`'s `IsBlockableNonCollider` binder shape. The quantifier ranges over both `Walk.nil` ($n = 0$) and `Walk.cons` ($n \\ge 1$): when $a \\in A \\cap (J \\cup B)$, the trivial walk `Walk.nil a _` is admitted, and `def_3_17`'s `IsSigmaBlockedGiven` is `False` on it (its existential over collider / blockable-non-collider positions is empty at length zero). Practical downstream surprise: `G.IsISigmaSeparated A A C` requires every $a \\in A$'s trivial self-walk to not be $C$-$\\sigma$-blocked, which fails for any non-empty $A$."
}
