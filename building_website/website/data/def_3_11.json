{
  "ref": "def_3_11",
  "kind": "def",
  "section": "3.2",
  "title": "NodeSplittingOn",
  "type": "definition",
  "status": {
    "formalized": "yes",
    "proven": "n/a",
    "solved": "yes"
  },
  "tex_block_html": "<p>Let $G=(J,V,E,L)$ be a CDMG and $W \\ins V$ a subset of the output nodes.\n    The <em>node-split graph</em> w.r.t.\\ $W$ of $G$ is the CDMG: \n    \\[ G_{\\spl(W)} :=\\lp J_{\\spl(W)}, V_{\\spl(W)}, E_{\\spl(W)},L_{\\spl(W)} \\rp,\\]</p>\n<p>constructed as follows.\n    We first make two disjont copies of the nodes in $W$:\n    \\[ W^0:=\\lC w^0\\st w \\in W \\rC, \\qquad W^1:=\\lC w^1 \\st w \\in W \\rC.  \\]\n    Note that we consider $w^0 \\neq w^1$ for $w \\in W$.\n    Additionally (for convenience), for $v \\in J \\cup V \\sm W$ we put:\n  \\[ v^0:=v^1:=v.  \\]\n  We then define:</p>\n<ol class=\"roman-paren\"><li>$J_{\\spl(W)} := J$,</li><li>$V_{\\spl(W)} := (V \\sm W) \\dcup W^0 \\dcup W^1$,</li><li>$E_{\\spl(W)} := \\lC v^1_1 \\tuh v_2^0 \\st v_1 \\tuh v_2 \\in E \\rC \\cup \\lC w^0 \\tuh w^1 \\st w \\in W \\rC$,</li><li>$L_{\\spl(W)} :=\\lC v_1^0 \\huh v_2^0 \\st v_1 \\huh v_2 \\in L \\rC$.</li></ol>\n<p>So all incoming edges onto nodes in $W$ become incoming edges into the corresponding nodes in $W^0$,\n  all outgoing edges out of nodes in $W$ become outgoing edges out of the corresponding nodes in $W^1$,\n  and edges $w^0 \\tuh w^1$ are added for all nodes in $W$.</p>",
  "tex_statement": {
    "raw": "% Definition file for row def_3_11 -- \"NodeSplittingOn\".\n% Auto-generated stub by scaffold/solve_chapter.py at row start. The\n% manager agent (or a worker dispatched by it) fills the body below with the\n% Lean-faithful definition statement(s). Stays close to the lecture notes.\n%\n% This file is a sibling of `main.tex` inside `<subsection>/tex/`. The\n% `subfiles` package lets it compile both standalone (resolving its\n% preamble via `main.tex`) and as part of `main.tex` via `\\subfile{...}`.\n\n\\documentclass[main]{subfiles}\n\n\\begin{document}\n% ref: def_3_11\n% title: NodeSplittingOn\n\\def\\rowref{def\\_3\\_11}\n\\phantomsection\\label{def_3_11}\n\n\\begin{Def}[Node-splitting on CDMGs]\n  \\label{def:G_node-splitting}\n    Let $G = (J, V, E, L)$ be a CDMG (def \\ref{def-cdmg}); throughout we use the notation of def \\ref{not-cdmg} and recall the typing and axioms of def \\ref{def-cdmg}, namely $J \\cap V = \\emptyset$, $E \\subseteq (J \\cup V) \\times V$, $L \\subseteq V \\times V$, $L$ irreflexive ($(v_1, v_2) \\in L \\Rightarrow v_1 \\neq v_2$), and $L$ symmetric ($(v_1, v_2) \\in L \\Leftrightarrow (v_2, v_1) \\in L$). Let $W \\ins V$ be a subset of the output nodes; in particular $W \\cap J = \\emptyset$, since $W \\ins V$ and $J \\cap V = \\emptyset$, so the splitting acts only on output-side nodes.\n\n    The \\emph{node-split graph} w.r.t.\\ $W$ of $G$ is the CDMG\n    \\[ G_{\\spl(W)} := \\lp J_{\\spl(W)}, V_{\\spl(W)}, E_{\\spl(W)}, L_{\\spl(W)} \\rp, \\]\n    constructed as follows.\n\n    \\emph{Tagged copies of $W$.}\n    We introduce two \\emph{tagged} copies of the nodes in $W$,\n    \\[ W^0 := \\lC w^0 \\st w \\in W \\rC, \\qquad W^1 := \\lC w^1 \\st w \\in W \\rC, \\]\n    where $w \\mapsto w^0$ and $w \\mapsto w^1$ are injective and produce elements that are of a formally distinct kind from every element of $J \\cup V$ and from each other: in a formal encoding, $W^0$ and $W^1$ are realised as tagged copies (e.g.\\ via a $\\Sigma$-type or tagged sum over $W$) so that the disjointness assertions\n    \\[ W^0 \\cap V \\;=\\; W^1 \\cap V \\;=\\; W^0 \\cap J \\;=\\; W^1 \\cap J \\;=\\; W^0 \\cap W^1 \\;=\\; \\emptyset \\]\n    hold \\emph{automatically by typing} rather than being asserted as a side condition; in particular $w^0 \\neq w^1$ for every $w \\in W$.\n\n    \\emph{Notational shorthand for unsplit nodes.}\n    For $v \\in J \\cup (V \\sm W)$ we put\n    \\[ v^0 \\;:=\\; v^1 \\;:=\\; v, \\]\n    purely as notational shorthand to be used \\emph{inside} the set-builder definitions of $E_{\\spl(W)}$ and $L_{\\spl(W)}$ below. This convention does \\emph{not} reassign such $v$ into either tagged-copy carrier $W^0$ or $W^1$: untagged nodes $v \\in J \\cup (V \\sm W)$ remain of their original kind in the ambient carrier, and the writings $v^0$, $v^1$ are simply two labels for $v$ itself. (Note that $J \\cup (V \\sm W)$ is itself a disjoint union, since $J \\cap V = \\emptyset$ and $V \\sm W \\ins V$.)\n\n    With these conventions in place, the four components of $G_{\\spl(W)}$ are defined as follows.\n    \\begin{enumerate}[label=\\roman*.)]\n        \\item \\emph{Input nodes:}\n            \\[ J_{\\spl(W)} := J. \\]\n        \\item \\emph{Output nodes:}\n            \\[ V_{\\spl(W)} := (V \\sm W) \\dcup W^0 \\dcup W^1, \\]\n            understood as a type-level disjoint union per the tagged construction above: each $w^0$ and each $w^1$ is automatically distinct from every element of $V \\sm W$ (and from every element of $J$), and $w^0 \\neq w^1$ for every $w \\in W$.\n        \\item \\emph{Directed edges:}\n            \\[\n                E_{\\spl(W)} \\;:=\\; \\lC (v_1^1, v_2^0) \\st (v_1, v_2) \\in E \\rC \\;\\cup\\; \\lC (w^0, w^1) \\st w \\in W \\rC.\n            \\]\n            Here the first set-builder ranges over $(v_1, v_2) \\in E$, so $v_1 \\in J \\cup V$ and $v_2 \\in V$ by the typing $E \\ins (J \\cup V) \\times V$, and the shorthand $v_1^1, v_2^0$ resolves as follows:\n            \\begin{itemize}\n                \\item if $v_1 \\in W$, then $v_1^1$ denotes the tagged copy of $v_1$ in $W^1$; otherwise $v_1 \\in J \\cup (V \\sm W)$ and $v_1^1 = v_1$ denotes $v_1$ itself (untagged);\n                \\item if $v_2 \\in W$, then $v_2^0$ denotes the tagged copy of $v_2$ in $W^0$; otherwise $v_2 \\in V \\sm W$ and $v_2^0 = v_2$ denotes $v_2$ itself (untagged).\n            \\end{itemize}\n            The second set-builder ranges over $w \\in W$ and contributes the \\emph{transfer} edges $(w^0, w^1)$, with $w^0 \\in W^0$ and $w^1 \\in W^1$ both genuinely tagged. Equivalently, rendered through def \\ref{not-cdmg}, item~2: every directed edge $v_1 \\tuh v_2 \\in E$ of $G$ is lifted to a directed edge $v_1^1 \\tuh v_2^0 \\in E_{\\spl(W)}$, and additionally a transfer edge $w^0 \\tuh w^1$ is included in $E_{\\spl(W)}$ for every $w \\in W$.\n        \\item \\emph{Bidirected edges:}\n            \\[\n                L_{\\spl(W)} \\;:=\\; \\lC (v_1^0, v_2^0) \\st (v_1, v_2) \\in L \\rC.\n            \\]\n            Here the set-builder ranges over $(v_1, v_2) \\in L$, so $v_1, v_2 \\in V$ by the typing $L \\ins V \\times V$, and the shorthand $v_i^0$ (for $i \\in \\{1, 2\\}$) resolves as: if $v_i \\in W$ then $v_i^0$ denotes the tagged copy of $v_i$ in $W^0$; otherwise $v_i \\in V \\sm W$ and $v_i^0 = v_i$ denotes $v_i$ itself. Equivalently, rendered through def \\ref{not-cdmg}: every bidirected edge $v_1 \\huh v_2 \\in L$ of $G$ is lifted to a bidirected edge $v_1^0 \\huh v_2^0 \\in L_{\\spl(W)}$. Note that \\emph{both} endpoints carry the same superscript $^0$; no element of $W^1$ appears as an endpoint of any bidirected edge of $G_{\\spl(W)}$.\n    \\end{enumerate}\n    The CDMG axioms (def \\ref{def-cdmg}) hold for $G_{\\spl(W)}$: $J_{\\spl(W)} \\cap V_{\\spl(W)} = \\emptyset$ (since $J \\cap V = \\emptyset$, $J \\cap W^0 = J \\cap W^1 = \\emptyset$ by the tagged construction); the typing $E_{\\spl(W)} \\ins (J_{\\spl(W)} \\cup V_{\\spl(W)}) \\times V_{\\spl(W)}$ holds by case analysis on the two set-builder clauses (every lifted source $v_1^1$ lies in $J_{\\spl(W)} \\cup V_{\\spl(W)}$ and every lifted target $v_2^0$ lies in $V_{\\spl(W)}$; every transfer-edge source $w^0$ and target $w^1$ lies in $V_{\\spl(W)}$); the typing $L_{\\spl(W)} \\ins V_{\\spl(W)} \\times V_{\\spl(W)}$ holds analogously; symmetry of $L_{\\spl(W)}$ follows from symmetry of $L$ (the set-builder commutes with swapping the two coordinates); and irreflexivity of $L_{\\spl(W)}$ follows from irreflexivity of $L$ together with injectivity of $v \\mapsto v^0$ on $V$.\n\n    Informally: every incoming directed edge onto a node $w \\in W$ becomes an incoming directed edge into $w^0$; every outgoing directed edge out of a node $w \\in W$ becomes an outgoing directed edge out of $w^1$; a transfer edge $w^0 \\tuh w^1$ is added for every $w \\in W$; and every bidirected edge whose endpoint was in $W$ is reattached at the $W^0$-copy of that endpoint.\n\n    \\emph{Asymmetry between directed and bidirected edges at $W^0$ vs.\\ $W^1$.}\n    The construction at item iv.\\ is intentionally one-sided: every lifted bidirected edge uses the $^0$ superscript on \\emph{both} endpoints, so no element of $W^1$ appears as an endpoint of any bidirected edge of $L_{\\spl(W)}$. Concretely, for $w_1, w_2 \\in W$, a bidirected edge $w_1 \\huh w_2 \\in L$ of $G$ lifts only to $w_1^0 \\huh w_2^0 \\in L_{\\spl(W)}$ -- never to $w_1^1 \\huh w_2^1$, never to a mixed pair $w_1^0 \\huh w_2^1$ or $w_1^1 \\huh w_2^0$. The informal end-paragraph above mentions only directed edges (incoming / outgoing / transfer) and gives no hint of this asymmetry; we record it here explicitly because it is the LN's literal reading, and downstream rows that reason about the bidirected / latent structure of $G_{\\spl(W)}$ (sibling relations, m-separation, latent projection, and the SWIG construction of def \\ref{def:G_node-splitting_intervention}) build on this one-sided convention and cannot equivalently swap $W^0$ for $W^1$ on the bidirected side.\n\n    \\emph{Self-loops on $W$ produce $2$-cycles, and $G_{\\spl(W)}$ is still a CDMG.}\n    The CDMG type of def \\ref{def-cdmg} does \\emph{not} require $E$ to be self-loop-free: the directed-self-loop case $(v, v) \\in E$ for $v \\in J \\cup V$ is admitted, and acyclicity is the separate downstream notion of def \\ref{def-acylic}. Consequently, for $w \\in W$ with $(w, w) \\in E$, the first set-builder in item iii.\\ produces the lifted edge $(w^1, w^0) \\in E_{\\spl(W)}$; together with the transfer edge $(w^0, w^1) \\in E_{\\spl(W)}$ from the second set-builder, this realises a length-$2$ directed cycle $w^0 \\tuh w^1 \\tuh w^0$ in $G_{\\spl(W)}$. This does not invalidate the CDMG axioms of def \\ref{def-cdmg} (the typing-and-symmetry case analysis above goes through unchanged), so $G_{\\spl(W)}$ remains a well-defined CDMG even when $G$ carries self-loops on $W$. Any downstream claim that node-splitting preserves acyclicity (cf.\\ the lemma on split topological orders downstream of this row) must therefore add a precondition that $G$ has no directed self-loops on $W$ -- or, more strongly, that $G$ is acyclic in the sense of def \\ref{def-acylic} -- since it cannot be derived from the splitting construction alone.\n\\end{Def}\n\n\\end{document}\n",
    "html": "<p>Let $G = (J, V, E, L)$ be a CDMG (def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>); throughout we use the notation of def <a href=\"#def_3_2\" class=\"refrow\"><code>not-cdmg</code></a> and recall the typing and axioms of def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>, namely $J \\cap V = \\emptyset$, $E \\subseteq (J \\cup V) \\times V$, $L \\subseteq V \\times V$, $L$ irreflexive ($(v_1, v_2) \\in L \\Rightarrow v_1 \\neq v_2$), and $L$ symmetric ($(v_1, v_2) \\in L \\Leftrightarrow (v_2, v_1) \\in L$). Let $W \\ins V$ be a subset of the output nodes; in particular $W \\cap J = \\emptyset$, since $W \\ins V$ and $J \\cap V = \\emptyset$, so the splitting acts only on output-side nodes.</p>\n<p>The <em>node-split graph</em> w.r.t.\\ $W$ of $G$ is the CDMG\n    \\[ G_{\\spl(W)} := \\lp J_{\\spl(W)}, V_{\\spl(W)}, E_{\\spl(W)}, L_{\\spl(W)} \\rp, \\]\n    constructed as follows.</p>\n<p><em>Tagged copies of $W$.</em>\n    We introduce two <em>tagged</em> copies of the nodes in $W$,\n    \\[ W^0 := \\lC w^0 \\st w \\in W \\rC, \\qquad W^1 := \\lC w^1 \\st w \\in W \\rC, \\]\n    where $w \\mapsto w^0$ and $w \\mapsto w^1$ are injective and produce elements that are of a formally distinct kind from every element of $J \\cup V$ and from each other: in a formal encoding, $W^0$ and $W^1$ are realised as tagged copies (e.g.\\ via a $\\Sigma$-type or tagged sum over $W$) so that the disjointness assertions\n    \\[ W^0 \\cap V \\;=\\; W^1 \\cap V \\;=\\; W^0 \\cap J \\;=\\; W^1 \\cap J \\;=\\; W^0 \\cap W^1 \\;=\\; \\emptyset \\]\n    hold <em>automatically by typing</em> rather than being asserted as a side condition; in particular $w^0 \\neq w^1$ for every $w \\in W$.</p>\n<p><em>Notational shorthand for unsplit nodes.</em>\n    For $v \\in J \\cup (V \\sm W)$ we put\n    \\[ v^0 \\;:=\\; v^1 \\;:=\\; v, \\]\n    purely as notational shorthand to be used <em>inside</em> the set-builder definitions of $E_{\\spl(W)}$ and $L_{\\spl(W)}$ below. This convention does <em>not</em> reassign such $v$ into either tagged-copy carrier $W^0$ or $W^1$: untagged nodes $v \\in J \\cup (V \\sm W)$ remain of their original kind in the ambient carrier, and the writings $v^0$, $v^1$ are simply two labels for $v$ itself. (Note that $J \\cup (V \\sm W)$ is itself a disjoint union, since $J \\cap V = \\emptyset$ and $V \\sm W \\ins V$.)</p>\n<p>With these conventions in place, the four components of $G_{\\spl(W)}$ are defined as follows.</p>\n<ol class=\"roman-paren\"><li><em>Input nodes:</em>\n            \\[ J_{\\spl(W)} := J. \\]</li><li><em>Output nodes:</em>\n            \\[ V_{\\spl(W)} := (V \\sm W) \\dcup W^0 \\dcup W^1, \\]\n            understood as a type-level disjoint union per the tagged construction above: each $w^0$ and each $w^1$ is automatically distinct from every element of $V \\sm W$ (and from every element of $J$), and $w^0 \\neq w^1$ for every $w \\in W$.</li><li><em>Directed edges:</em>\n            \\[\n                E_{\\spl(W)} \\;:=\\; \\lC (v_1^1, v_2^0) \\st (v_1, v_2) \\in E \\rC \\;\\cup\\; \\lC (w^0, w^1) \\st w \\in W \\rC.\n            \\]\n            Here the first set-builder ranges over $(v_1, v_2) \\in E$, so $v_1 \\in J \\cup V$ and $v_2 \\in V$ by the typing $E \\ins (J \\cup V) \\times V$, and the shorthand $v_1^1, v_2^0$ resolves as follows:\n<ul><li>if $v_1 \\in W$, then $v_1^1$ denotes the tagged copy of $v_1$ in $W^1$; otherwise $v_1 \\in J \\cup (V \\sm W)$ and $v_1^1 = v_1$ denotes $v_1$ itself (untagged);</li><li>if $v_2 \\in W$, then $v_2^0$ denotes the tagged copy of $v_2$ in $W^0$; otherwise $v_2 \\in V \\sm W$ and $v_2^0 = v_2$ denotes $v_2$ itself (untagged).</li></ul>\n<p>The second set-builder ranges over $w \\in W$ and contributes the <em>transfer</em> edges $(w^0, w^1)$, with $w^0 \\in W^0$ and $w^1 \\in W^1$ both genuinely tagged. Equivalently, rendered through def <a href=\"#def_3_2\" class=\"refrow\"><code>not-cdmg</code></a>, item&nbsp;2: every directed edge $v_1 \\tuh v_2 \\in E$ of $G$ is lifted to a directed edge $v_1^1 \\tuh v_2^0 \\in E_{\\spl(W)}$, and additionally a transfer edge $w^0 \\tuh w^1$ is included in $E_{\\spl(W)}$ for every $w \\in W$.</li><li><em>Bidirected edges:</em>\n            \\[\n                L_{\\spl(W)} \\;:=\\; \\lC (v_1^0, v_2^0) \\st (v_1, v_2) \\in L \\rC.\n            \\]\n            Here the set-builder ranges over $(v_1, v_2) \\in L$, so $v_1, v_2 \\in V$ by the typing $L \\ins V \\times V$, and the shorthand $v_i^0$ (for $i \\in \\{1, 2\\}$) resolves as: if $v_i \\in W$ then $v_i^0$ denotes the tagged copy of $v_i$ in $W^0$; otherwise $v_i \\in V \\sm W$ and $v_i^0 = v_i$ denotes $v_i$ itself. Equivalently, rendered through def <a href=\"#def_3_2\" class=\"refrow\"><code>not-cdmg</code></a>: every bidirected edge $v_1 \\huh v_2 \\in L$ of $G$ is lifted to a bidirected edge $v_1^0 \\huh v_2^0 \\in L_{\\spl(W)}$. Note that <em>both</em> endpoints carry the same superscript $^0$; no element of $W^1$ appears as an endpoint of any bidirected edge of $G_{\\spl(W)}$.</li></ol></p>\n<p>The CDMG axioms (def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>) hold for $G_{\\spl(W)}$: $J_{\\spl(W)} \\cap V_{\\spl(W)} = \\emptyset$ (since $J \\cap V = \\emptyset$, $J \\cap W^0 = J \\cap W^1 = \\emptyset$ by the tagged construction); the typing $E_{\\spl(W)} \\ins (J_{\\spl(W)} \\cup V_{\\spl(W)}) \\times V_{\\spl(W)}$ holds by case analysis on the two set-builder clauses (every lifted source $v_1^1$ lies in $J_{\\spl(W)} \\cup V_{\\spl(W)}$ and every lifted target $v_2^0$ lies in $V_{\\spl(W)}$; every transfer-edge source $w^0$ and target $w^1$ lies in $V_{\\spl(W)}$); the typing $L_{\\spl(W)} \\ins V_{\\spl(W)} \\times V_{\\spl(W)}$ holds analogously; symmetry of $L_{\\spl(W)}$ follows from symmetry of $L$ (the set-builder commutes with swapping the two coordinates); and irreflexivity of $L_{\\spl(W)}$ follows from irreflexivity of $L$ together with injectivity of $v \\mapsto v^0$ on $V$.</p>\n<p>Informally: every incoming directed edge onto a node $w \\in W$ becomes an incoming directed edge into $w^0$; every outgoing directed edge out of a node $w \\in W$ becomes an outgoing directed edge out of $w^1$; a transfer edge $w^0 \\tuh w^1$ is added for every $w \\in W$; and every bidirected edge whose endpoint was in $W$ is reattached at the $W^0$-copy of that endpoint.</p>\n<p><em>Asymmetry between directed and bidirected edges at $W^0$ vs.\\ $W^1$.</em>\n    The construction at item iv.\\ is intentionally one-sided: every lifted bidirected edge uses the $^0$ superscript on <em>both</em> endpoints, so no element of $W^1$ appears as an endpoint of any bidirected edge of $L_{\\spl(W)}$. Concretely, for $w_1, w_2 \\in W$, a bidirected edge $w_1 \\huh w_2 \\in L$ of $G$ lifts only to $w_1^0 \\huh w_2^0 \\in L_{\\spl(W)}$ – never to $w_1^1 \\huh w_2^1$, never to a mixed pair $w_1^0 \\huh w_2^1$ or $w_1^1 \\huh w_2^0$. The informal end-paragraph above mentions only directed edges (incoming / outgoing / transfer) and gives no hint of this asymmetry; we record it here explicitly because it is the LN's literal reading, and downstream rows that reason about the bidirected / latent structure of $G_{\\spl(W)}$ (sibling relations, m-separation, latent projection, and the SWIG construction of def <a href=\"#def_3_12\" class=\"refrow\"><code>def:G_node-splitting_intervention</code></a>) build on this one-sided convention and cannot equivalently swap $W^0$ for $W^1$ on the bidirected side.</p>\n<p><em>Self-loops on $W$ produce $2$-cycles, and $G_{\\spl(W)}$ is still a CDMG.</em>\n    The CDMG type of def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a> does <em>not</em> require $E$ to be self-loop-free: the directed-self-loop case $(v, v) \\in E$ for $v \\in J \\cup V$ is admitted, and acyclicity is the separate downstream notion of def <a href=\"#def_3_6\" class=\"refrow\"><code>def-acylic</code></a>. Consequently, for $w \\in W$ with $(w, w) \\in E$, the first set-builder in item iii.\\ produces the lifted edge $(w^1, w^0) \\in E_{\\spl(W)}$; together with the transfer edge $(w^0, w^1) \\in E_{\\spl(W)}$ from the second set-builder, this realises a length-$2$ directed cycle $w^0 \\tuh w^1 \\tuh w^0$ in $G_{\\spl(W)}$. This does not invalidate the CDMG axioms of def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a> (the typing-and-symmetry case analysis above goes through unchanged), so $G_{\\spl(W)}$ remains a well-defined CDMG even when $G$ carries self-loops on $W$. Any downstream claim that node-splitting preserves acyclicity (cf.\\ the lemma on split topological orders downstream of this row) must therefore add a precondition that $G$ has no directed self-loops on $W$ – or, more strongly, that $G$ is acyclic in the sense of def <a href=\"#def_3_6\" class=\"refrow\"><code>def-acylic</code></a> – since it cannot be derived from the splitting construction alone.</p>",
    "env": "Def",
    "env_title": "Node-splitting on CDMGs",
    "source_path": "leanification/Chapter3_GraphTheory/Section3_2/tex/def_3_11_NodeSplittingOn.tex"
  },
  "tex_proof": null,
  "lean_blocks": [
    {
      "kind": "helper",
      "code": "variable {Node : Type*} [DecidableEq Node]",
      "explanation": "This is a `variable` declaration: Lean auto-binds `Node : Type*` and the instance `[DecidableEq Node]` as implicit arguments to every subsequent declaration in the surrounding `CDMG` namespace, sparing us from rewriting the same parameters on `toCopy0`, `toCopy1`, and `nodeSplittingOn`. The `Type*` notation makes `Node` universe-polymorphic, so callers can instantiate it at any universe level. The `DecidableEq Node` assumption is load-bearing for the node-splitting construction in particular: the body uses `Finset.image` (to inject `G.J`, `G.V \\ W`, and `W` through the `SplitNode` constructors), `Finset.filter`-style `if v ∈ W then …` branching inside `toCopy0` / `toCopy1`, and `Finset.disjoint_left` rewrites to discharge `hJV_disj` — all of which need decidable equality on the element type so the resulting `Finset` can be computed and membership in `W` is a decidable proposition. Stronger structure on `Node` (e.g.\\ `Fintype`, `LinearOrder`) is not required here and is deferred to downstream consumers that actually use it. Same convention as the rest of `Chapter3_GraphTheory`'s files.",
      "code_annotated": "-- Auto-binds `Node` + `DecidableEq Node` as implicits; decidability drives `Finset.image`, the `v ∈ W` branch in `toCopy0`/`toCopy1`, and `Finset.disjoint_*` below.\nvariable {Node : Type*} [DecidableEq Node]"
    },
    {
      "kind": "helper",
      "code": "inductive SplitNode (Node : Type*) where\n  | unsplit (v : Node) : SplitNode Node\n  | copy0 (w : Node) : SplitNode Node\n  | copy1 (w : Node) : SplitNode Node\n  deriving DecidableEq",
      "explanation": "This block introduces the carrier type of the split graph: an `inductive` with three named constructors that mirror the LN's three kinds of node one-for-one. `unsplit v` encodes an untagged node (any $v \\in J \\cup (V \\setminus W)$ in the LN), while `copy0 w` and `copy1 w` encode the tagged copies $w^0 \\in W^0$ and $w^1 \\in W^1$. Because the three constructors are structurally distinct, the LN's disjointness assertions $W^0 \\cap V = W^1 \\cap V = W^0 \\cap J = W^1 \\cap J = W^0 \\cap W^1 = \\emptyset$ hold automatically at the type level — they reduce to constructor mismatches via `cases` rather than being asserted as side conditions on the underlying `Finset`s. The `deriving DecidableEq` clause asks Lean to synthesise the decidability instance, which the upstream `CDMG (SplitNode Node)` carrier requires.",
      "code_annotated": "-- Carrier of the split graph; three named constructors mirror LN's `v`, `w^0`, `w^1`.\ninductive SplitNode (Node : Type*) where\n  -- `unsplit v` injects an untagged node `v ∈ J ∪ (V \\ W)` into the new carrier.\n  | unsplit (v : Node) : SplitNode Node\n  -- `copy0 w` is the tagged copy `w^0 ∈ W^0` (natural / observational side).\n  | copy0 (w : Node) : SplitNode Node\n  -- `copy1 w` is the tagged copy `w^1 ∈ W^1` (intervened / `do`-side).\n  | copy1 (w : Node) : SplitNode Node\n  -- Synthesises `DecidableEq (SplitNode Node)` from `[DecidableEq Node]`; required by `CDMG`.\n  deriving DecidableEq"
    },
    {
      "kind": "helper",
      "code": "def toCopy0 (W : Finset Node) (v : Node) : SplitNode Node :=\n  if v ∈ W then SplitNode.copy0 v else SplitNode.unsplit v",
      "explanation": "This helper realises the LN's notational shorthand $v^0$: branch on `v ∈ W` (decidable from `[DecidableEq Node]` since `W : Finset Node`) and return either the tagged copy `SplitNode.copy0 v` on the `W`-branch or the `.unsplit v` injection on the complement. The function is total on all of `Node`, including inputs `v ∈ G.J` or `v ∈ G.V \\ W`, where it returns `.unsplit v` — exactly the LN's \"$v^0 := v$ for $v \\in J \\cup (V \\setminus W)$\" convention. It is used inside `nodeSplittingOn`'s `L` clause to lift both endpoints of every bidirected edge, and on the target side of the `E` clause. Encoding the shorthand as a function (rather than as two inline branches inside every set-builder) mirrors the LN's framing of $v^0$ as meta-notation and keeps the edge clauses terse.",
      "code_annotated": "-- LN shorthand `v^0`: tagged on the `W`-branch, plain `.unsplit v` on the complement.\ndef toCopy0 (W : Finset Node) (v : Node) : SplitNode Node :=\n  -- `v ∈ W` is decidable via `DecidableEq Node` on `Finset Node`; total on all of `Node`.\n  if v ∈ W then SplitNode.copy0 v else SplitNode.unsplit v"
    },
    {
      "kind": "helper",
      "code": "def toCopy1 (W : Finset Node) (v : Node) : SplitNode Node :=\n  if v ∈ W then SplitNode.copy1 v else SplitNode.unsplit v",
      "explanation": "This helper realises the LN's notational shorthand $v^1$: same shape as `toCopy0` above, but returning `SplitNode.copy1 v` on the `W`-branch instead of `SplitNode.copy0 v`. Outside `W`, both helpers agree, returning the `.unsplit v` injection — exactly the LN's collapse $v^0 := v^1 := v$ for $v \\in J \\cup (V \\setminus W)$. `toCopy1` is used only on the source side of the lifted directed edges inside `nodeSplittingOn`'s `E` clause; the target side and both endpoints of every bidirected edge route through `toCopy0` instead. This asymmetric usage encodes the LN's directional convention that incoming edges land on the $^0$-copy of $W$ while outgoing edges originate from the $^1$-copy.",
      "code_annotated": "-- LN shorthand `v^1`: identical to `toCopy0` except the `W`-branch picks `.copy1 v`.\ndef toCopy1 (W : Finset Node) (v : Node) : SplitNode Node :=\n  -- Used only on the source side of lifted directed edges in `nodeSplittingOn`'s `E` clause.\n  if v ∈ W then SplitNode.copy1 v else SplitNode.unsplit v"
    },
    {
      "kind": "main",
      "code": "def nodeSplittingOn (G : CDMG Node) (W : Finset Node) (hW : W ⊆ G.V) :\n    CDMG (SplitNode Node) where\n  J := G.J.image SplitNode.unsplit\n  V := (G.V \\ W).image SplitNode.unsplit ∪ W.image SplitNode.copy0\n        ∪ W.image SplitNode.copy1\n  hJV_disj := by\n    -- `hW` is part of the signature for LN-faithfulness (\"Let\n    -- `W ⊆ V`\"), but is not consumed in this proof — the\n    -- type-level distinction of the three `SplitNode` constructors\n    -- discharges the constructor-mismatch branches, and `G.hJV_disj`\n    -- discharges the `unsplit`-vs-`unsplit` branch on its own.\n    let _ := hW\n    rw [Finset.disjoint_left]\n    rintro x hxJ hxV\n    obtain ⟨j, hjJ, rfl⟩ := Finset.mem_image.mp hxJ\n    rcases Finset.mem_union.mp hxV with hxV12 | hxC1\n    · rcases Finset.mem_union.mp hxV12 with hxVuns | hxC0\n      · -- `x = .unsplit j` is in `(G.V \\ W).image .unsplit`: the\n        -- preimage `v` agrees with `j` by constructor injectivity,\n        -- so `j ∈ G.V \\ W ⊆ G.V`, contradicting `j ∈ G.J`.\n        obtain ⟨v, hvVW, hveq⟩ := Finset.mem_image.mp hxVuns\n        cases hveq\n        exact Finset.disjoint_left.mp G.hJV_disj hjJ\n          (Finset.mem_sdiff.mp hvVW).1\n      · -- `x = .unsplit j` is in `W.image .copy0`: the constructors\n        -- `.unsplit` and `.copy0` differ — impossible.\n        obtain ⟨_, _, hweq⟩ := Finset.mem_image.mp hxC0\n        cases hweq\n    · -- `x = .unsplit j` is in `W.image .copy1`: same constructor\n      -- mismatch as above.\n      obtain ⟨_, _, hweq⟩ := Finset.mem_image.mp hxC1\n      cases hweq\n  E := G.E.image (fun e => (toCopy1 W e.1, toCopy0 W e.2))\n        ∪ W.image (fun w => (SplitNode.copy0 w, SplitNode.copy1 w))\n  hE_subset := by\n    intro e he\n    rcases Finset.mem_union.mp he with hImg | hTrans\n    · -- Lifted edge: `e = (toCopy1 W e'.1, toCopy0 W e'.2)` for\n      --   `(v_1, v_2) := e' ∈ G.E`, with `v_1 ∈ G.J ∪ G.V` and\n      --   `v_2 ∈ G.V` by `G.hE_subset`.\n      obtain ⟨e', he'E, rfl⟩ := Finset.mem_image.mp hImg\n      obtain ⟨he'1, he'2⟩ := G.hE_subset he'E\n      refine ⟨?_, ?_⟩\n      · -- `(toCopy1 W e'.1) ∈ J' ∪ V'`.\n        by_cases hW1 : e'.1 ∈ W\n        · -- `toCopy1 W e'.1 = .copy1 e'.1`, which lives in `V'`\n          --   (the `W.image .copy1` piece).\n          simp only [toCopy1, hW1, if_true]\n          refine Finset.mem_union_right _ ?_\n          refine Finset.mem_union_right _ ?_\n          exact Finset.mem_image.mpr ⟨e'.1, hW1, rfl⟩\n        · -- `toCopy1 W e'.1 = .unsplit e'.1`; branch on whether\n          --   `e'.1 ∈ G.J` (lives in `J'`) or `e'.1 ∈ G.V` (lives\n          --   in the `.unsplit (G.V \\ W)` piece of `V'`).\n          simp only [toCopy1, hW1, if_false]\n          rcases Finset.mem_union.mp he'1 with hJ | hV\n          · exact Finset.mem_union_left _ (Finset.mem_image.mpr ⟨e'.1, hJ, rfl⟩)\n          · refine Finset.mem_union_right _ ?_\n            refine Finset.mem_union_left _ ?_\n            refine Finset.mem_union_left _ ?_\n            exact Finset.mem_image.mpr\n              ⟨e'.1, Finset.mem_sdiff.mpr ⟨hV, hW1⟩, rfl⟩\n      · -- `(toCopy0 W e'.2) ∈ V'`.\n        by_cases hW2 : e'.2 ∈ W\n        · simp only [toCopy0, hW2, if_true]\n          refine Finset.mem_union_left _ ?_\n          refine Finset.mem_union_right _ ?_\n          exact Finset.mem_image.mpr ⟨e'.2, hW2, rfl⟩\n        · simp only [toCopy0, hW2, if_false]\n          refine Finset.mem_union_left _ ?_\n          refine Finset.mem_union_left _ ?_\n          exact Finset.mem_image.mpr\n            ⟨e'.2, Finset.mem_sdiff.mpr ⟨he'2, hW2⟩, rfl⟩\n    · -- Transfer edge: `e = (.copy0 w, .copy1 w)` for `w ∈ W`.\n      --   Both endpoints lie in `V'`: `.copy0 w ∈ W.image .copy0`,\n      --   `.copy1 w ∈ W.image .copy1`.\n      obtain ⟨w, hwW, rfl⟩ := Finset.mem_image.mp hTrans\n      refine ⟨?_, ?_⟩\n      · refine Finset.mem_union_right _ ?_\n        refine Finset.mem_union_left _ ?_\n        refine Finset.mem_union_right _ ?_\n        exact Finset.mem_image.mpr ⟨w, hwW, rfl⟩\n      · refine Finset.mem_union_right _ ?_\n        exact Finset.mem_image.mpr ⟨w, hwW, rfl⟩\n  L := G.L.image (fun e => (toCopy0 W e.1, toCopy0 W e.2))\n  hL_subset := by\n    intro e he\n    obtain ⟨e', he'L, rfl⟩ := Finset.mem_image.mp he\n    obtain ⟨he'1, he'2⟩ := G.hL_subset he'L\n    -- Reusable per-endpoint case analysis: for any `v ∈ G.V`,\n    --   `toCopy0 W v ∈ V'` because it is either `.copy0 v` (when\n    --   `v ∈ W`) or `.unsplit v` (when `v ∈ G.V \\ W`).\n    refine ⟨?_, ?_⟩\n    · by_cases hW1 : e'.1 ∈ W\n      · simp only [toCopy0, hW1, if_true]\n        refine Finset.mem_union_left _ ?_\n        refine Finset.mem_union_right _ ?_\n        exact Finset.mem_image.mpr ⟨e'.1, hW1, rfl⟩\n      · simp only [toCopy0, hW1, if_false]\n        refine Finset.mem_union_left _ ?_\n        refine Finset.mem_union_left _ ?_\n        exact Finset.mem_image.mpr\n          ⟨e'.1, Finset.mem_sdiff.mpr ⟨he'1, hW1⟩, rfl⟩\n    · by_cases hW2 : e'.2 ∈ W\n      · simp only [toCopy0, hW2, if_true]\n        refine Finset.mem_union_left _ ?_\n        refine Finset.mem_union_right _ ?_\n        exact Finset.mem_image.mpr ⟨e'.2, hW2, rfl⟩\n      · simp only [toCopy0, hW2, if_false]\n        refine Finset.mem_union_left _ ?_\n        refine Finset.mem_union_left _ ?_\n        exact Finset.mem_image.mpr\n          ⟨e'.2, Finset.mem_sdiff.mpr ⟨he'2, hW2⟩, rfl⟩\n  hL_irrefl := by\n    -- For `(v_1, v_2) ∈ L'`, the underlying `(v_1', v_2') ∈ G.L`\n    --   satisfies `v_1' ≠ v_2'` by `G.hL_irrefl`; injectivity of\n    --   `toCopy0 W` on `Node` lifts this to `toCopy0 W v_1' ≠\n    --   toCopy0 W v_2'`.  Injectivity of `toCopy0 W`: distinct\n    --   constructors `.copy0` vs `.unsplit` on the `v ∈ W` /\n    --   `v ∉ W` split, and constructor injectivity within each\n    --   branch.\n    intro v1 v2 h\n    obtain ⟨e', he'L, heq⟩ := Finset.mem_image.mp h\n    have hne : e'.1 ≠ e'.2 := G.hL_irrefl he'L\n    intro hv12\n    apply hne\n    have h1 : toCopy0 W e'.1 = v1 := by\n      have := congrArg Prod.fst heq; simpa using this\n    have h2 : toCopy0 W e'.2 = v2 := by\n      have := congrArg Prod.snd heq; simpa using this\n    -- From `toCopy0 W e'.1 = toCopy0 W e'.2`, deduce `e'.1 = e'.2`\n    --   by injectivity of `toCopy0 W`.\n    have hSplitEq : toCopy0 W e'.1 = toCopy0 W e'.2 := by\n      rw [h1, h2, hv12]\n    exact toCopy0_inj hSplitEq\n  hL_symm := by\n    -- `G.hL_symm` swaps the underlying ordered pair; the image\n    --   under `(toCopy0 W ·.1, toCopy0 W ·.2)` commutes with the\n    --   swap, so the symmetry transports literally.\n    intro v1 v2 h\n    obtain ⟨e', he'L, heq⟩ := Finset.mem_image.mp h\n    have h1 : toCopy0 W e'.1 = v1 := by\n      have := congrArg Prod.fst heq; simpa using this\n    have h2 : toCopy0 W e'.2 = v2 := by\n      have := congrArg Prod.snd heq; simpa using this\n    have hsym : (e'.2, e'.1) ∈ G.L := G.hL_symm he'L\n    refine Finset.mem_image.mpr ⟨(e'.2, e'.1), hsym, ?_⟩\n    simp [h1, h2]",
      "explanation": "This block constructs the node-split CDMG $G_{\\spl(W)}$ as a fresh `CDMG (SplitNode Node)` record, populated field by field via Lean 4's `where`-syntax. The defining feature versus `def_3_10` is the *carrier change*: the result lives over `SplitNode Node`, not `Node`, so every data field assembles its `Finset` by `Finset.image` through `SplitNode.unsplit` / `SplitNode.copy0` / `SplitNode.copy1` (or via the helpers `toCopy0` / `toCopy1`). `J` injects `G.J` through `.unsplit`. `V` is the literal three-piece union `(G.V \\ W).image .unsplit ∪ W.image .copy0 ∪ W.image .copy1`, spelling out $(V \\setminus W) \\dcup W^0 \\dcup W^1$. `E` is a two-clause union: lifted edges `G.E.image (fun e => (toCopy1 W e.1, toCopy0 W e.2))` carrying every $(v_1, v_2) \\in E$ to $(v_1^1, v_2^0)$, plus the transfer edges $\\{(w^0, w^1) \\mid w \\in W\\}$. `L` applies `toCopy0` to both endpoints — the LN's load-bearing one-sided convention. The five proof obligations rest on three Mathlib idioms: `Finset.mem_image` for the elementhood reading of every `Finset.image`, `Finset.mem_sdiff` for the `G.V \\ W` piece, and `Finset.disjoint_left` to discharge `hJV_disj` by case analysis. Most branches close by `cases hweq` on a constructor-mismatch equality; the unsplit-vs-unsplit branch routes through `G.hJV_disj`, and `hL_irrefl` lifts `G.hL_irrefl` via a private `toCopy0_inj` injectivity lemma. The signature carries `hW : W ⊆ G.V` for LN-faithfulness; `let _ := hW` records the hypothesis without consuming it.",
      "code_annotated": "-- ref: def_3_11 (main) — build `G_{spl(W)}` over the tagged carrier `SplitNode Node` field by field via `where`-syntax.\ndef nodeSplittingOn (G : CDMG Node) (W : Finset Node) (hW : W ⊆ G.V) :\n    CDMG (SplitNode Node) where\n  -- LN item i: inputs unchanged, injected through `.unsplit`.\n  J := G.J.image SplitNode.unsplit\n  -- LN item ii: literal three-piece union `(V \\ W) ⊍ W^0 ⊍ W^1`.\n  V := (G.V \\ W).image SplitNode.unsplit ∪ W.image SplitNode.copy0\n        ∪ W.image SplitNode.copy1\n  hJV_disj := by\n    -- `hW` is part of the signature for LN-faithfulness (\"Let\n    -- `W ⊆ V`\"), but is not consumed in this proof — the\n    -- type-level distinction of the three `SplitNode` constructors\n    -- discharges the constructor-mismatch branches, and `G.hJV_disj`\n    -- discharges the `unsplit`-vs-`unsplit` branch on its own.\n    let _ := hW\n    -- Rewrite `Disjoint` into the pointwise `∀ x, x ∈ J' → x ∉ V'` form.\n    rw [Finset.disjoint_left]\n    -- Every `x ∈ J'` is `.unsplit j` for some `j ∈ G.J`; `rfl` substitutes inline.\n    rintro x hxJ hxV\n    obtain ⟨j, hjJ, rfl⟩ := Finset.mem_image.mp hxJ\n    -- Case-split `x ∈ V'` across the outer-then-inner unions.\n    rcases Finset.mem_union.mp hxV with hxV12 | hxC1\n    · rcases Finset.mem_union.mp hxV12 with hxVuns | hxC0\n      · -- `x = .unsplit j` is in `(G.V \\ W).image .unsplit`: the\n        -- preimage `v` agrees with `j` by constructor injectivity,\n        -- so `j ∈ G.V \\ W ⊆ G.V`, contradicting `j ∈ G.J`.\n        obtain ⟨v, hvVW, hveq⟩ := Finset.mem_image.mp hxVuns\n        cases hveq\n        exact Finset.disjoint_left.mp G.hJV_disj hjJ\n          (Finset.mem_sdiff.mp hvVW).1\n      · -- `x = .unsplit j` is in `W.image .copy0`: the constructors\n        -- `.unsplit` and `.copy0` differ — impossible.\n        obtain ⟨_, _, hweq⟩ := Finset.mem_image.mp hxC0\n        cases hweq\n    · -- `x = .unsplit j` is in `W.image .copy1`: same constructor\n      -- mismatch as above.\n      obtain ⟨_, _, hweq⟩ := Finset.mem_image.mp hxC1\n      cases hweq\n  -- LN item iii: lifted edges `(v_1^1, v_2^0)` union transfer edges `(w^0, w^1)`.\n  E := G.E.image (fun e => (toCopy1 W e.1, toCopy0 W e.2))\n        ∪ W.image (fun w => (SplitNode.copy0 w, SplitNode.copy1 w))\n  hE_subset := by\n    -- Split membership in `E'` between the lifted-edge clause and the transfer-edge clause.\n    intro e he\n    rcases Finset.mem_union.mp he with hImg | hTrans\n    · -- Lifted edge: `e = (toCopy1 W e'.1, toCopy0 W e'.2)` for\n      --   `(v_1, v_2) := e' ∈ G.E`, with `v_1 ∈ G.J ∪ G.V` and\n      --   `v_2 ∈ G.V` by `G.hE_subset`.\n      obtain ⟨e', he'E, rfl⟩ := Finset.mem_image.mp hImg\n      obtain ⟨he'1, he'2⟩ := G.hE_subset he'E\n      refine ⟨?_, ?_⟩\n      · -- `(toCopy1 W e'.1) ∈ J' ∪ V'`.\n        by_cases hW1 : e'.1 ∈ W\n        · -- `toCopy1 W e'.1 = .copy1 e'.1`, which lives in `V'`\n          --   (the `W.image .copy1` piece).\n          simp only [toCopy1, hW1, if_true]\n          refine Finset.mem_union_right _ ?_\n          refine Finset.mem_union_right _ ?_\n          exact Finset.mem_image.mpr ⟨e'.1, hW1, rfl⟩\n        · -- `toCopy1 W e'.1 = .unsplit e'.1`; branch on whether\n          --   `e'.1 ∈ G.J` (lives in `J'`) or `e'.1 ∈ G.V` (lives\n          --   in the `.unsplit (G.V \\ W)` piece of `V'`).\n          simp only [toCopy1, hW1, if_false]\n          rcases Finset.mem_union.mp he'1 with hJ | hV\n          · exact Finset.mem_union_left _ (Finset.mem_image.mpr ⟨e'.1, hJ, rfl⟩)\n          · refine Finset.mem_union_right _ ?_\n            refine Finset.mem_union_left _ ?_\n            refine Finset.mem_union_left _ ?_\n            exact Finset.mem_image.mpr\n              ⟨e'.1, Finset.mem_sdiff.mpr ⟨hV, hW1⟩, rfl⟩\n      · -- `(toCopy0 W e'.2) ∈ V'`.\n        by_cases hW2 : e'.2 ∈ W\n        · simp only [toCopy0, hW2, if_true]\n          refine Finset.mem_union_left _ ?_\n          refine Finset.mem_union_right _ ?_\n          exact Finset.mem_image.mpr ⟨e'.2, hW2, rfl⟩\n        · simp only [toCopy0, hW2, if_false]\n          refine Finset.mem_union_left _ ?_\n          refine Finset.mem_union_left _ ?_\n          exact Finset.mem_image.mpr\n            ⟨e'.2, Finset.mem_sdiff.mpr ⟨he'2, hW2⟩, rfl⟩\n    · -- Transfer edge: `e = (.copy0 w, .copy1 w)` for `w ∈ W`.\n      --   Both endpoints lie in `V'`: `.copy0 w ∈ W.image .copy0`,\n      --   `.copy1 w ∈ W.image .copy1`.\n      obtain ⟨w, hwW, rfl⟩ := Finset.mem_image.mp hTrans\n      refine ⟨?_, ?_⟩\n      · refine Finset.mem_union_right _ ?_\n        refine Finset.mem_union_left _ ?_\n        refine Finset.mem_union_right _ ?_\n        exact Finset.mem_image.mpr ⟨w, hwW, rfl⟩\n      · refine Finset.mem_union_right _ ?_\n        exact Finset.mem_image.mpr ⟨w, hwW, rfl⟩\n  -- LN item iv: bidirected edges lifted with `^0` on BOTH endpoints (load-bearing one-sided convention).\n  L := G.L.image (fun e => (toCopy0 W e.1, toCopy0 W e.2))\n  hL_subset := by\n    -- Unpack `e ∈ L'` via `Finset.mem_image`; `rfl` substitutes the lifted pair inline.\n    intro e he\n    obtain ⟨e', he'L, rfl⟩ := Finset.mem_image.mp he\n    -- `G.hL_subset` gives `e'.1, e'.2 ∈ G.V`.\n    obtain ⟨he'1, he'2⟩ := G.hL_subset he'L\n    -- Reusable per-endpoint case analysis: for any `v ∈ G.V`,\n    --   `toCopy0 W v ∈ V'` because it is either `.copy0 v` (when\n    --   `v ∈ W`) or `.unsplit v` (when `v ∈ G.V \\ W`).\n    refine ⟨?_, ?_⟩\n    · by_cases hW1 : e'.1 ∈ W\n      · simp only [toCopy0, hW1, if_true]\n        refine Finset.mem_union_left _ ?_\n        refine Finset.mem_union_right _ ?_\n        exact Finset.mem_image.mpr ⟨e'.1, hW1, rfl⟩\n      · simp only [toCopy0, hW1, if_false]\n        refine Finset.mem_union_left _ ?_\n        refine Finset.mem_union_left _ ?_\n        exact Finset.mem_image.mpr\n          ⟨e'.1, Finset.mem_sdiff.mpr ⟨he'1, hW1⟩, rfl⟩\n    · by_cases hW2 : e'.2 ∈ W\n      · simp only [toCopy0, hW2, if_true]\n        refine Finset.mem_union_left _ ?_\n        refine Finset.mem_union_right _ ?_\n        exact Finset.mem_image.mpr ⟨e'.2, hW2, rfl⟩\n      · simp only [toCopy0, hW2, if_false]\n        refine Finset.mem_union_left _ ?_\n        refine Finset.mem_union_left _ ?_\n        exact Finset.mem_image.mpr\n          ⟨e'.2, Finset.mem_sdiff.mpr ⟨he'2, hW2⟩, rfl⟩\n  hL_irrefl := by\n    -- For `(v_1, v_2) ∈ L'`, the underlying `(v_1', v_2') ∈ G.L`\n    --   satisfies `v_1' ≠ v_2'` by `G.hL_irrefl`; injectivity of\n    --   `toCopy0 W` on `Node` lifts this to `toCopy0 W v_1' ≠\n    --   toCopy0 W v_2'`.  Injectivity of `toCopy0 W`: distinct\n    --   constructors `.copy0` vs `.unsplit` on the `v ∈ W` /\n    --   `v ∉ W` split, and constructor injectivity within each\n    --   branch.\n    intro v1 v2 h\n    -- Recover the preimage pair `e' ∈ G.L` and the equation `(toCopy0 W e'.1, toCopy0 W e'.2) = (v1, v2)`.\n    obtain ⟨e', he'L, heq⟩ := Finset.mem_image.mp h\n    have hne : e'.1 ≠ e'.2 := G.hL_irrefl he'L\n    intro hv12\n    apply hne\n    -- Project the pair equation `heq` onto each coordinate.\n    have h1 : toCopy0 W e'.1 = v1 := by\n      have := congrArg Prod.fst heq; simpa using this\n    have h2 : toCopy0 W e'.2 = v2 := by\n      have := congrArg Prod.snd heq; simpa using this\n    -- From `toCopy0 W e'.1 = toCopy0 W e'.2`, deduce `e'.1 = e'.2`\n    --   by injectivity of `toCopy0 W`.\n    have hSplitEq : toCopy0 W e'.1 = toCopy0 W e'.2 := by\n      rw [h1, h2, hv12]\n    -- `toCopy0_inj` is the private case-split injectivity lemma above this `def`.\n    exact toCopy0_inj hSplitEq\n  hL_symm := by\n    -- `G.hL_symm` swaps the underlying ordered pair; the image\n    --   under `(toCopy0 W ·.1, toCopy0 W ·.2)` commutes with the\n    --   swap, so the symmetry transports literally.\n    intro v1 v2 h\n    obtain ⟨e', he'L, heq⟩ := Finset.mem_image.mp h\n    have h1 : toCopy0 W e'.1 = v1 := by\n      have := congrArg Prod.fst heq; simpa using this\n    have h2 : toCopy0 W e'.2 = v2 := by\n      have := congrArg Prod.snd heq; simpa using this\n    -- Apply `G.hL_symm` to the underlying pair, then re-image the swapped pair.\n    have hsym : (e'.2, e'.1) ∈ G.L := G.hL_symm he'L\n    refine Finset.mem_image.mpr ⟨(e'.2, e'.1), hsym, ?_⟩\n    simp [h1, h2]"
    }
  ],
  "lean_source_url": "https://github.com/schipeltaper/causality-leanified/blob/main/leanification/Chapter3_GraphTheory/Section3_2/NodeSplittingOn.lean",
  "lean_file_path": "leanification/Chapter3_GraphTheory/Section3_2/NodeSplittingOn.lean",
  "addition_to_the_LN": "[disjointness_of_new_copies_only_partially_stipulated] The disjointness assertions $W^0 \\cap V = W^1 \\cap V = W^0 \\cap J = W^1 \\cap J = W^0 \\cap W^1 = \\emptyset$ are to be realized at the type level: in a formal encoding, $W^0$ and $W^1$ are constructed as tagged copies (e.g. via a $\\Sigma$-type or tagged sum) so that their elements are of a distinct type from elements of $V$ and $J$, making disjointness hold automatically rather than as a side condition. The convention $v^0 := v^1 := v$ for $v \\in J \\cup (V \\sm W)$ is purely notational shorthand used inside the definitions of $E_{\\spl(W)}$ and $L_{\\spl(W)}$, and does not reassign these elements to the tagged copy types.",
  "design_choices": "**The carrier of the result is `SplitNode Node`, not `Node`.** Unlike hard intervention (the sibling definition that keeps the original node universe), node-splitting genuinely *creates* new nodes $w^0$ and $w^1$, and the LN's disjointness assertions $W^0 \\cap V = W^1 \\cap V = W^0 \\cap J = W^1 \\cap J = W^0 \\cap W^1 = \\emptyset$ must hold automatically. Encoding the new carrier as an `inductive` type with three distinct constructors makes these disjointness facts true *at the type level* — they reduce to constructor mismatches via `cases` rather than being asserted as side conditions on the underlying `Finset`s.\n\n**Three named constructors, not `Sum Node (Node × Bool)`.** A sum-based encoding would express the same data, but every pattern match would have to navigate nested `Sum.inl` / `Sum.inr` destructuring. The named constructors `.unsplit`, `.copy0`, `.copy1` correspond to the LN symbols $v$, $w^0$, $w^1$ one-for-one and read cleanly at every use site, without a translation table between Lean syntax and LN notation.\n\n**Notational shorthand as functions, not as a coercion.** The LN's “$v^0 := v^1 := v$ for $v \\in J \\cup (V \\setminus W)$” is meta-notation used inside the set-builders — it does *not* re-assign $v$ to a different carrier. The Lean rendering as two functions `toCopy0 W` and `toCopy1 W`, each branching on `v ∈ W`, captures exactly this reading: the original `v : Node` stays in `Node`, and the function is just the per-set-builder lift into the tagged carrier. A `Coe Node (SplitNode Node)` instance was rejected because there are *two* such lifts (parameterised by `W`), neither canonical, and a global coercion would fire across the chapter unexpectedly.\n\n**`Finset.image` for every set-builder, not `Finset.filter` or recursion.** Node-splitting *creates* elements in the tagged carrier rather than selecting a subset of existing ones, so `Finset.filter` is the wrong primitive. `Finset.image` aligns directly with the LN's set-builder ranging over $E$, $L$, or $W$, and its membership lemma `Finset.mem_image` is the workhorse of every proof obligation below.\n\n**Bidirected edges are lifted with $^0$ on both endpoints, never $^1$.** The asymmetric convention $L_{\\spl(W)} := \\{(v_1^0, v_2^0) \\mid (v_1, v_2) \\in L\\}$ is load-bearing for the SWIG (Single-World Intervention Graph) construction composed in the next definition. Under SWIG semantics, $W^0$ is the *natural* / observational side of $W$ (inheriting latent confounding from $G$) and $W^1$ is the *intervened* side (causally isolated). Bidirected edges encode latent confounding, which lives entirely on the natural side; the intervened $W^1$-copies have no latent structure by design. Swapping $^0$ for $^1$ on either endpoint would silently change the chapter's semantics.\n\n**Self-loops on $W$ produce 2-cycles, and the result is still a CDMG.** If $(w, w) \\in G.E$ for $w \\in W$, the lifted edge $(w^1, w^0)$ together with the transfer edge $(w^0, w^1)$ realises a length-2 directed cycle $w^0 \\to w^1 \\to w^0$ in $E_{\\spl(W)}$. The CDMG definition does *not* require $E$ to be self-loop-free or acyclic, so this is admissible — but any downstream claim that node-splitting preserves acyclicity must add a self-loop-free precondition on $G$."
}
