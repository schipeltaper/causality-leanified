{
  "ref": "def_3_13",
  "kind": "def",
  "section": "3.2",
  "title": "ExtendingCDMGsWith",
  "type": "definition",
  "status": {
    "formalized": "yes",
    "proven": "n/a",
    "solved": "yes"
  },
  "tex_block_html": "<p>Let $G=(J,V,E,L)$ be a CDMG and $W \\ins J \\cup V$ a subset of nodes.\n  The <em>extended CDMG</em> of $G$ w.r.t.\\ nodes $W \\ins J \\cup V$  and corresponding <em>intervention nodes</em></p>\n<p>$I_W = \\{I_w \\,|\\, w \\in W\\}$ with $I_j:=j$ for $j \\in J \\cap W$,\n  is the CDMG: \n  \\[ G_{\\doit(I_W)}:=(J_{\\doit(I_W)},V_{\\doit(I_W)},E_{\\doit(I_W)},L_{\\doit(I_W)}),\\]</p>\n<p>where:</p>\n<ol class=\"roman-paren\"><li>$J_{\\doit(I_W)}:= J\\, \\dot{\\cup} \\, \\lC I_w\\,|\\, w \\in W \\sm J \\rC$,</li><li>$V_{\\doit(I_W)}:= V$,</li><li>$E_{\\doit(I_W)}:= E\\, \\dot{\\cup} \\, \\lC I_w \\tuh w \\,|\\, w \\in W\\sm J \\rC$,</li><li>$L_{\\doit(I_W)}:= L$,</li></ol>\n<p>where we just add nodes $I_w$ for $w \\in W \\sm J$ and edges $I_w \\tuh w$ for $ w \\in W \\sm J$.</p>",
  "tex_statement": {
    "raw": "% Definition file for row def_3_13 -- \"ExtendingCDMGsWith\".\n% Auto-generated stub by scaffold/solve_chapter.py at row start. The\n% manager agent (or a worker dispatched by it) fills the body below with the\n% Lean-faithful definition statement(s). Stays close to the lecture notes.\n%\n% This file is a sibling of `main.tex` inside `<subsection>/tex/`. The\n% `subfiles` package lets it compile both standalone (resolving its\n% preamble via `main.tex`) and as part of `main.tex` via `\\subfile{...}`.\n\n\\documentclass[main]{subfiles}\n\n\\begin{document}\n% ref: def_3_13\n% title: ExtendingCDMGsWith\n\\def\\rowref{def\\_3\\_13}\n\\phantomsection\\label{def_3_13}\n\n\\begin{Def}[Extending CDMGs with intervention nodes]\\label{def:cdmg_intervention_nodes}\n    Let $G = (J, V, E, L)$ be a CDMG (def \\ref{def-cdmg}); throughout we use the notation of def \\ref{not-cdmg} and recall the typing and axioms of def \\ref{def-cdmg}, namely $J \\cap V = \\emptyset$, $E \\ins (J \\cup V) \\times V$, $L \\ins V \\times V$, $L$ irreflexive ($(v_1, v_2) \\in L \\Rightarrow v_1 \\neq v_2$), and $L$ symmetric ($(v_1, v_2) \\in L \\Leftrightarrow (v_2, v_1) \\in L$). Let $W \\ins J \\cup V$ be a subset of nodes; we do \\emph{not} require $W \\cap J = \\emptyset$, i.e.\\ $W$ is permitted to overlap the input-node set $J$.\n\n    The \\emph{extended CDMG} of $G$ w.r.t.\\ nodes $W$ and corresponding \\emph{intervention nodes} $I_W := \\lC I_w \\st w \\in W \\rC$ is the CDMG\n    \\[ G_{\\doit(I_W)} \\;:=\\; \\lp J_{\\doit(I_W)},\\, V_{\\doit(I_W)},\\, E_{\\doit(I_W)},\\, L_{\\doit(I_W)} \\rp, \\]\n    constructed as follows.\n\n    \\emph{Fresh intervention nodes.}\n    For each $w \\in W \\sm J$ we introduce a single \\emph{fresh} intervention symbol $I_w$: a brand-new element of a formally distinct kind from every element of $J \\cup V$ and from every other $I_{w'}$ with $w' \\in W \\sm J$, $w' \\neq w$. In a formal encoding, the family\n    \\[ \\lC I_w \\st w \\in W \\sm J \\rC \\]\n    is realised as a \\emph{tagged copy} of $W \\sm J$ (e.g.\\ via a $\\Sigma$-type or tagged sum); concretely, the downstream Lean rendering uses an \\texttt{inductive} tagged-sum carrier with one constructor that lifts every original node of $J \\cup V$ and a separate constructor that produces the new symbol $I_w$ for each $w \\in W \\sm J$. Under this convention, the disjointness assertions\n    \\[ \\lC I_w \\st w \\in W \\sm J \\rC \\cap (J \\cup V) \\;=\\; \\emptyset, \\qquad \\text{and} \\qquad I_{w_1} \\neq I_{w_2} \\text{ for distinct } w_1, w_2 \\in W \\sm J, \\]\n    hold \\emph{automatically by typing} rather than being asserted as a side condition; in particular each fresh symbol $I_w$ is type-disjoint from $J$, from $V$, and from every other fresh $I_{w'}$.\n\n    \\emph{Notational shorthand $I_j := j$ for $j \\in J \\cap W$.}\n    For $j \\in J \\cap W$ we put $I_j := j$ purely as a notational shorthand: the symbol $I_j$ is a label for the pre-existing input node $j \\in J$ itself, and \\emph{no new node is introduced on the $J \\cap W$ branch}. In particular, the pre-existing $j \\in J$ retains in $G_{\\doit(I_W)}$ all edges that $G$ already assigned to it, with no fresh symbol added next to it and no extra edge added pointing at it. This convention is needed solely so that $I_W = \\lC I_w \\st w \\in W \\rC$ is well-defined as a set indexed by the entirety of $W$: without it, the symbol $I_w$ would have no referent on the $J \\cap W$ branch. With the convention, $I_W$ decomposes as\n    \\[ I_W \\;=\\; (J \\cap W) \\;\\cup\\; \\lC I_w \\st w \\in W \\sm J \\rC, \\]\n    where the first component contains the (notational) $I_j = j$ for $j \\in J \\cap W$ and the second component contains the genuinely fresh symbols $I_w$ for $w \\in W \\sm J$, the latter type-disjoint from the former by the freshness clause.\n\n    With these conventions in place, the four components of $G_{\\doit(I_W)}$ are defined as follows.\n    \\begin{enumerate}[label=\\roman*.)]\n        \\item \\emph{Input nodes:}\n            \\[ J_{\\doit(I_W)} \\;:=\\; J \\;\\cup\\; \\lC I_w \\st w \\in W \\sm J \\rC. \\]\n            The LN's $\\dot\\cup$ between $J$ and $\\lC I_w \\st w \\in W \\sm J \\rC$ is automatically disjoint: by the freshness clause, every $I_w$ with $w \\in W \\sm J$ is type-disjoint from every element of $J$, so $J \\cap \\lC I_w \\st w \\in W \\sm J \\rC = \\emptyset$ holds without a separate side condition.\n        \\item \\emph{Output nodes:}\n            \\[ V_{\\doit(I_W)} \\;:=\\; V. \\]\n        \\item \\emph{Directed edges:}\n            \\[ E_{\\doit(I_W)} \\;:=\\; E \\;\\cup\\; \\lC (I_w,\\, w) \\st w \\in W \\sm J \\rC. \\]\n            The LN's $\\dot\\cup$ between $E$ and $\\lC (I_w, w) \\st w \\in W \\sm J \\rC$ is automatically disjoint: every pair $(I_w, w)$ in the second set-builder has its source $I_w$ in $\\lC I_{w'} \\st w' \\in W \\sm J \\rC$, which by the freshness clause is type-disjoint from $J \\cup V$; but every edge in $E$ has its source in $J \\cup V$ (by $E \\ins (J \\cup V) \\times V$), so no edge of $E$ can coincide with any $(I_w, w)$. Equivalently, rendered through def \\ref{not-cdmg}, item~2: every directed edge $v_1 \\tuh v_2$ already in $E$ is retained, and additionally a single new directed edge $I_w \\tuh w$ is included for every $w \\in W \\sm J$ (and \\emph{none} for $w \\in J \\cap W$).\n        \\item \\emph{Bidirected edges:}\n            \\[ L_{\\doit(I_W)} \\;:=\\; L. \\]\n    \\end{enumerate}\n\n    \\emph{CDMG axioms for $G_{\\doit(I_W)}$.}\n    The CDMG axioms (def \\ref{def-cdmg}) hold for $G_{\\doit(I_W)}$, with the new intervention nodes landing in $J_{\\doit(I_W)}$ and the bidirected side and output side untouched:\n    \\begin{itemize}\n        \\item \\emph{Input--output disjointness, $J_{\\doit(I_W)} \\cap V_{\\doit(I_W)} = \\emptyset$:} $J \\cap V = \\emptyset$ by hypothesis on $G$, and $\\lC I_w \\st w \\in W \\sm J \\rC \\cap V = \\emptyset$ by the freshness clause (each $I_w$ is type-disjoint from $V$); since $V_{\\doit(I_W)} = V$ and $J_{\\doit(I_W)} = J \\cup \\lC I_w \\st w \\in W \\sm J \\rC$, distribution of $\\cap$ over $\\cup$ gives $J_{\\doit(I_W)} \\cap V_{\\doit(I_W)} = (J \\cap V) \\cup (\\lC I_w \\st w \\in W \\sm J \\rC \\cap V) = \\emptyset$.\n        \\item \\emph{Directed-edge typing, $E_{\\doit(I_W)} \\ins (J_{\\doit(I_W)} \\cup V_{\\doit(I_W)}) \\times V_{\\doit(I_W)}$:} for every $(v_1, v_2) \\in E$ we have $v_1 \\in J \\cup V \\ins J_{\\doit(I_W)} \\cup V_{\\doit(I_W)}$ and $v_2 \\in V = V_{\\doit(I_W)}$, by the hypothesis $E \\ins (J \\cup V) \\times V$ on $G$; for every new edge $(I_w, w)$ with $w \\in W \\sm J$, the source $I_w$ lies in $\\lC I_{w'} \\st w' \\in W \\sm J \\rC \\ins J_{\\doit(I_W)}$, and the target $w$ lies in $V_{\\doit(I_W)} = V$ (the latter because $w \\in W \\sm J$ together with $W \\ins J \\cup V$ forces $w \\in V$).\n        \\item \\emph{Bidirected-edge typing, $L_{\\doit(I_W)} \\ins V_{\\doit(I_W)} \\times V_{\\doit(I_W)}$:} since $L_{\\doit(I_W)} = L$ and $V_{\\doit(I_W)} = V$, this is exactly the hypothesis $L \\ins V \\times V$ on $G$, transported unchanged.\n        \\item \\emph{Irreflexivity of $L_{\\doit(I_W)}$:} since $L_{\\doit(I_W)} = L$, this is exactly irreflexivity of $L$, transported unchanged.\n        \\item \\emph{Symmetry of $L_{\\doit(I_W)}$:} since $L_{\\doit(I_W)} = L$, this is exactly symmetry of $L$, transported unchanged.\n    \\end{itemize}\n    In particular, none of the three $L$-axioms requires re-checking: $L$ is unchanged and $V$ is unchanged, so the three bidirected-side axioms transport directly from $G$. Only the directed-edge typing and the input--output disjointness involve genuinely new content, and both reduce to the freshness clause above.\n\n    Informally: for every node $w \\in W \\sm J$ we add a single fresh intervention node $I_w$ to the input side, together with a single new directed edge $I_w \\tuh w$ pointing at $w$ (which by $W \\ins J \\cup V$ and $w \\notin J$ must lie in $V$); for every node $w \\in J \\cap W$ we add nothing, since the identification $I_w := w$ is purely notational and the pre-existing input node $w$ plays the role of its own intervention node. The output-node set $V$ and the bidirected-edge set $L$ are not touched. Each fresh intervention node $I_w$ (for $w \\in W \\sm J$) has exactly one outgoing directed edge in $G_{\\doit(I_W)}$, namely $I_w \\tuh w$, and is the target of no edge of $G_{\\doit(I_W)}$: it is the target of no pre-existing edge in $E$ (since $I_w \\notin J \\cup V$, while every edge in $E$ has its target in $V$ and so cannot point at $I_w$), and it is the target of no new edge in $\\lC (I_{w'}, w') \\st w' \\in W \\sm J \\rC$ (since each such edge has its target $w' \\in V$, type-disjoint from $I_w$).\n\\end{Def}\n\n\\end{document}\n",
    "html": "<p>Let $G = (J, V, E, L)$ be a CDMG (def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>); throughout we use the notation of def <a href=\"#def_3_2\" class=\"refrow\"><code>not-cdmg</code></a> and recall the typing and axioms of def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>, namely $J \\cap V = \\emptyset$, $E \\ins (J \\cup V) \\times V$, $L \\ins V \\times V$, $L$ irreflexive ($(v_1, v_2) \\in L \\Rightarrow v_1 \\neq v_2$), and $L$ symmetric ($(v_1, v_2) \\in L \\Leftrightarrow (v_2, v_1) \\in L$). Let $W \\ins J \\cup V$ be a subset of nodes; we do <em>not</em> require $W \\cap J = \\emptyset$, i.e.\\ $W$ is permitted to overlap the input-node set $J$.</p>\n<p>The <em>extended CDMG</em> of $G$ w.r.t.\\ nodes $W$ and corresponding <em>intervention nodes</em> $I_W := \\lC I_w \\st w \\in W \\rC$ is the CDMG\n    \\[ G_{\\doit(I_W)} \\;:=\\; \\lp J_{\\doit(I_W)},\\, V_{\\doit(I_W)},\\, E_{\\doit(I_W)},\\, L_{\\doit(I_W)} \\rp, \\]\n    constructed as follows.</p>\n<p><em>Fresh intervention nodes.</em>\n    For each $w \\in W \\sm J$ we introduce a single <em>fresh</em> intervention symbol $I_w$: a brand-new element of a formally distinct kind from every element of $J \\cup V$ and from every other $I_{w'}$ with $w' \\in W \\sm J$, $w' \\neq w$. In a formal encoding, the family\n    \\[ \\lC I_w \\st w \\in W \\sm J \\rC \\]\n    is realised as a <em>tagged copy</em> of $W \\sm J$ (e.g.\\ via a $\\Sigma$-type or tagged sum); concretely, the downstream Lean rendering uses an <code>inductive</code> tagged-sum carrier with one constructor that lifts every original node of $J \\cup V$ and a separate constructor that produces the new symbol $I_w$ for each $w \\in W \\sm J$. Under this convention, the disjointness assertions\n    \\[ \\lC I_w \\st w \\in W \\sm J \\rC \\cap (J \\cup V) \\;=\\; \\emptyset, \\qquad \\text{and} \\qquad I_{w_1} \\neq I_{w_2} \\text{ for distinct } w_1, w_2 \\in W \\sm J, \\]\n    hold <em>automatically by typing</em> rather than being asserted as a side condition; in particular each fresh symbol $I_w$ is type-disjoint from $J$, from $V$, and from every other fresh $I_{w'}$.</p>\n<p><em>Notational shorthand $I_j := j$ for $j \\in J \\cap W$.</em>\n    For $j \\in J \\cap W$ we put $I_j := j$ purely as a notational shorthand: the symbol $I_j$ is a label for the pre-existing input node $j \\in J$ itself, and <em>no new node is introduced on the $J \\cap W$ branch</em>. In particular, the pre-existing $j \\in J$ retains in $G_{\\doit(I_W)}$ all edges that $G$ already assigned to it, with no fresh symbol added next to it and no extra edge added pointing at it. This convention is needed solely so that $I_W = \\lC I_w \\st w \\in W \\rC$ is well-defined as a set indexed by the entirety of $W$: without it, the symbol $I_w$ would have no referent on the $J \\cap W$ branch. With the convention, $I_W$ decomposes as\n    \\[ I_W \\;=\\; (J \\cap W) \\;\\cup\\; \\lC I_w \\st w \\in W \\sm J \\rC, \\]\n    where the first component contains the (notational) $I_j = j$ for $j \\in J \\cap W$ and the second component contains the genuinely fresh symbols $I_w$ for $w \\in W \\sm J$, the latter type-disjoint from the former by the freshness clause.</p>\n<p>With these conventions in place, the four components of $G_{\\doit(I_W)}$ are defined as follows.</p>\n<ol class=\"roman-paren\"><li><em>Input nodes:</em>\n            \\[ J_{\\doit(I_W)} \\;:=\\; J \\;\\cup\\; \\lC I_w \\st w \\in W \\sm J \\rC. \\]\n            The LN's $\\dot\\cup$ between $J$ and $\\lC I_w \\st w \\in W \\sm J \\rC$ is automatically disjoint: by the freshness clause, every $I_w$ with $w \\in W \\sm J$ is type-disjoint from every element of $J$, so $J \\cap \\lC I_w \\st w \\in W \\sm J \\rC = \\emptyset$ holds without a separate side condition.</li><li><em>Output nodes:</em>\n            \\[ V_{\\doit(I_W)} \\;:=\\; V. \\]</li><li><em>Directed edges:</em>\n            \\[ E_{\\doit(I_W)} \\;:=\\; E \\;\\cup\\; \\lC (I_w,\\, w) \\st w \\in W \\sm J \\rC. \\]\n            The LN's $\\dot\\cup$ between $E$ and $\\lC (I_w, w) \\st w \\in W \\sm J \\rC$ is automatically disjoint: every pair $(I_w, w)$ in the second set-builder has its source $I_w$ in $\\lC I_{w'} \\st w' \\in W \\sm J \\rC$, which by the freshness clause is type-disjoint from $J \\cup V$; but every edge in $E$ has its source in $J \\cup V$ (by $E \\ins (J \\cup V) \\times V$), so no edge of $E$ can coincide with any $(I_w, w)$. Equivalently, rendered through def <a href=\"#def_3_2\" class=\"refrow\"><code>not-cdmg</code></a>, item&nbsp;2: every directed edge $v_1 \\tuh v_2$ already in $E$ is retained, and additionally a single new directed edge $I_w \\tuh w$ is included for every $w \\in W \\sm J$ (and <em>none</em> for $w \\in J \\cap W$).</li><li><em>Bidirected edges:</em>\n            \\[ L_{\\doit(I_W)} \\;:=\\; L. \\]</li></ol>\n<p><em>CDMG axioms for $G_{\\doit(I_W)}$.</em>\n    The CDMG axioms (def <a href=\"#def_3_1\" class=\"refrow\"><code>def-cdmg</code></a>) hold for $G_{\\doit(I_W)}$, with the new intervention nodes landing in $J_{\\doit(I_W)}$ and the bidirected side and output side untouched:</p>\n<ul><li><em>Input–output disjointness, $J_{\\doit(I_W)} \\cap V_{\\doit(I_W)} = \\emptyset$:</em> $J \\cap V = \\emptyset$ by hypothesis on $G$, and $\\lC I_w \\st w \\in W \\sm J \\rC \\cap V = \\emptyset$ by the freshness clause (each $I_w$ is type-disjoint from $V$); since $V_{\\doit(I_W)} = V$ and $J_{\\doit(I_W)} = J \\cup \\lC I_w \\st w \\in W \\sm J \\rC$, distribution of $\\cap$ over $\\cup$ gives $J_{\\doit(I_W)} \\cap V_{\\doit(I_W)} = (J \\cap V) \\cup (\\lC I_w \\st w \\in W \\sm J \\rC \\cap V) = \\emptyset$.</li><li><em>Directed-edge typing, $E_{\\doit(I_W)} \\ins (J_{\\doit(I_W)} \\cup V_{\\doit(I_W)}) \\times V_{\\doit(I_W)}$:</em> for every $(v_1, v_2) \\in E$ we have $v_1 \\in J \\cup V \\ins J_{\\doit(I_W)} \\cup V_{\\doit(I_W)}$ and $v_2 \\in V = V_{\\doit(I_W)}$, by the hypothesis $E \\ins (J \\cup V) \\times V$ on $G$; for every new edge $(I_w, w)$ with $w \\in W \\sm J$, the source $I_w$ lies in $\\lC I_{w'} \\st w' \\in W \\sm J \\rC \\ins J_{\\doit(I_W)}$, and the target $w$ lies in $V_{\\doit(I_W)} = V$ (the latter because $w \\in W \\sm J$ together with $W \\ins J \\cup V$ forces $w \\in V$).</li><li><em>Bidirected-edge typing, $L_{\\doit(I_W)} \\ins V_{\\doit(I_W)} \\times V_{\\doit(I_W)}$:</em> since $L_{\\doit(I_W)} = L$ and $V_{\\doit(I_W)} = V$, this is exactly the hypothesis $L \\ins V \\times V$ on $G$, transported unchanged.</li><li><em>Irreflexivity of $L_{\\doit(I_W)}$:</em> since $L_{\\doit(I_W)} = L$, this is exactly irreflexivity of $L$, transported unchanged.</li><li><em>Symmetry of $L_{\\doit(I_W)}$:</em> since $L_{\\doit(I_W)} = L$, this is exactly symmetry of $L$, transported unchanged.</li></ul>\n<p>In particular, none of the three $L$-axioms requires re-checking: $L$ is unchanged and $V$ is unchanged, so the three bidirected-side axioms transport directly from $G$. Only the directed-edge typing and the input–output disjointness involve genuinely new content, and both reduce to the freshness clause above.</p>\n<p>Informally: for every node $w \\in W \\sm J$ we add a single fresh intervention node $I_w$ to the input side, together with a single new directed edge $I_w \\tuh w$ pointing at $w$ (which by $W \\ins J \\cup V$ and $w \\notin J$ must lie in $V$); for every node $w \\in J \\cap W$ we add nothing, since the identification $I_w := w$ is purely notational and the pre-existing input node $w$ plays the role of its own intervention node. The output-node set $V$ and the bidirected-edge set $L$ are not touched. Each fresh intervention node $I_w$ (for $w \\in W \\sm J$) has exactly one outgoing directed edge in $G_{\\doit(I_W)}$, namely $I_w \\tuh w$, and is the target of no edge of $G_{\\doit(I_W)}$: it is the target of no pre-existing edge in $E$ (since $I_w \\notin J \\cup V$, while every edge in $E$ has its target in $V$ and so cannot point at $I_w$), and it is the target of no new edge in $\\lC (I_{w'}, w') \\st w' \\in W \\sm J \\rC$ (since each such edge has its target $w' \\in V$, type-disjoint from $I_w$).</p>",
    "env": "Def",
    "env_title": "Extending CDMGs with intervention nodes",
    "source_path": "leanification/Chapter3_GraphTheory/Section3_2/tex/def_3_13_ExtendingCDMGsWith.tex"
  },
  "tex_proof": null,
  "lean_blocks": [
    {
      "kind": "helper",
      "code": "variable {Node : Type*} [DecidableEq Node]",
      "explanation": "This is a `variable` declaration: Lean auto-binds `Node : Type*` and the instance `[DecidableEq Node]` as implicit arguments to every subsequent declaration in the surrounding `CDMG` namespace, sparing us from rewriting the same parameters on `extendingCDMGsWith`. The `Type*` notation makes `Node` universe-polymorphic, so callers can instantiate it at any universe level. The `DecidableEq Node` assumption is load-bearing for the extension construction in particular: the body uses `Finset.image` (to lift `G.J`, `G.V`, `G.E`, `G.L` through the tagged-sum constructors and to build the fresh-symbol images on `W \\ G.J`), `Finset.union` (to assemble the new `J` and `E` fields from two image pieces), and `Finset.sdiff` (for `W \\ G.J`) — all of which need decidable equality on the element type to compute the resulting `Finset`. The lifted instance `DecidableEq (IntExtNode Node)` that `CDMG (IntExtNode Node)` requires is synthesised automatically by the next block's `deriving DecidableEq` clause from this `DecidableEq Node`. Stronger structure on `Node` (e.g.\\ `Fintype`, `LinearOrder`) is not required here and is deferred to downstream consumers that actually use it. Same convention as the rest of `Chapter3_GraphTheory`'s files.",
      "code_annotated": "-- Auto-binds `Node` + `DecidableEq Node` as implicits; decidability drives `Finset.image`/`union`/`sdiff` below.\nvariable {Node : Type*} [DecidableEq Node]"
    },
    {
      "kind": "helper",
      "code": "inductive IntExtNode (Node : Type*) where\n  | unsplit (v : Node) : IntExtNode Node\n  | intCopy (w : Node) : IntExtNode Node\n  deriving DecidableEq",
      "explanation": "This block introduces `IntExtNode Node`, the carrier type of the extended CDMG $G_{\\doit(I_W)}$. The LN demands that each fresh intervention symbol $I_w$ (for $w \\in W \\setminus J$) be type-disjoint from every element of $J \\cup V$ and from every other $I_{w'}$. Rather than carry these as `Finset.Disjoint` side conditions, the construction realises them at the type level: `IntExtNode` is a fresh `inductive` with two named constructors. The `unsplit v` constructor lifts an arbitrary original node $v \\in J \\cup V$ into the extended carrier, and `intCopy w` produces the fresh intervention symbol $I_w$. Constructor disjointness (`unsplit v ≠ intCopy w`) and constructor injectivity (`intCopy w₁ = intCopy w₂ → w₁ = w₂`) hold for free, so the LN's freshness claims become typing facts. The constructor name `unsplit` is borrowed across section 3.2 so cross-row pattern matches read identically; `intCopy` self-documents as `intervention copy of w`. The `deriving DecidableEq` clause asks Lean to synthesise the `DecidableEq (IntExtNode Node)` instance from the ambient `DecidableEq Node`, satisfying what `CDMG (IntExtNode Node)` requires. This helper sits between the `variable` block and the main `def`; the main `def`'s field assignments invoke both constructors via dot-notation.",
      "code_annotated": "-- Tagged-sum carrier for the extended CDMG; constructor disjointness encodes LN freshness at the type level.\ninductive IntExtNode (Node : Type*) where\n  -- Lifts every original `v ∈ J ∪ V` into the extended carrier; name shared across section 3.2.\n  | unsplit (v : Node) : IntExtNode Node\n  -- Produces the fresh intervention symbol `I_w` for each `w ∈ W \\ J`; type-disjoint from `unsplit`.\n  | intCopy (w : Node) : IntExtNode Node\n  -- Lifts `DecidableEq Node` to `DecidableEq (IntExtNode Node)`, which the result `CDMG` requires.\n  deriving DecidableEq"
    },
    {
      "kind": "main",
      "code": "def extendingCDMGsWith (G : CDMG Node) (W : Finset Node)\n    (hW : W ⊆ G.J ∪ G.V) : CDMG (IntExtNode Node)",
      "explanation": "",
      "code_annotated": ""
    }
  ],
  "lean_source_url": "https://github.com/schipeltaper/causality-leanified/blob/main/leanification/Chapter3_GraphTheory/Section3_2/ExtendingCDMGsWith.lean",
  "lean_file_path": "leanification/Chapter3_GraphTheory/Section3_2/ExtendingCDMGsWith.lean",
  "addition_to_the_LN": "[I_W_mixes_fresh_nodes_and_existing_context_nodes] Intervention nodes are by default fresh nodes disjoint from $J \\cup V$, not present in $G$ prior to the intervention. For $w \\in W \\sm J$, $I_w$ is such a new symbol, added to the junction set with a single outgoing edge $I_w \\tuh w$. For $w \\in J \\cap W$, the identification $I_w := w$ introduces no new node and serves only as a notational convenience so that $I_W$ is well-defined as a set; here the pre-existing context node $w \\in J$ retains whatever edges $G$ already assigned to it.",
  "design_choices": "**Type-level disjointness via a fresh two-constructor `inductive`.** The LN asserts that the new symbols $\\{I_w \\mid w \\in W \\setminus J\\}$ are pairwise distinct and type-disjoint from $J \\cup V$. A naive encoding could carry these as side conditions: a `Finset.Disjoint` obligation between the image set and `G.J ∪ G.V`, plus an injectivity hypothesis on the indexing. Instead, the carrier of the result is the `inductive IntExtNode Node` with two named constructors, `unsplit` and `intCopy`. Constructor disjointness (`unsplit v ≠ intCopy w` for all `v, w`) and constructor injectivity (`intCopy w₁ = intCopy w₂` implies `w₁ = w₂`) hold for free, so the LN's freshness claims become typing facts rather than proof obligations. The trade-off is that the carrier inflates — `IntExtNode Node` has twice as many type-level inhabitants as `Node` itself — and any downstream consumer that counts nodes must restrict to the inhabited slice `G.J.image .unsplit ∪ G.V.image .unsplit ∪ (W \\ G.J).image .intCopy` rather than range over the full type.\n\n**A fresh `IntExtNode` rather than reusing the neighbour row's three-constructor `SplitNode`.** A neighbouring definition (node-splitting) introduces a tagged sum `SplitNode` with three constructors `unsplit`, `copy0`, `copy1` for two-sided node copies. Reusing `SplitNode` here would leave one constructor permanently unused — the present extension introduces only *one* fresh symbol per $w \\in W \\setminus J$, not two. The minimal two-constructor carrier keeps the type's intent self-documenting and saves the `copy1` constructor for the SWIG-side semantics it was designed for. The `unsplit` name is borrowed across the section so cross-row pattern matches read identically.\n\n**The notational shorthand $I_j := j$ for $j \\in J \\cap W$ is captured implicitly by the carrier shape.** Every `intCopy`-ranging clause in the definition iterates over $W \\setminus J$, never over $W$. No `IntExtNode.intCopy j` is ever created for $j \\in J \\cap W$; the pre-existing `IntExtNode.unsplit j` (already in `G.J.image .unsplit`) is the sole inhabitant of the $I_W$-slot on the $J \\cap W$ branch and plays the role of its own intervention node. This realises the LN's purely-notational identification at the carrier level, with no runtime conditional and no fresh symbol on the $J \\cap W$ branch.\n\n**Proof obligations factored out as `private lemma`s above the `def`.** Each of the five CDMG axioms (`hJV_disj`, `hE_subset`, `hL_subset`, `hL_irrefl`, `hL_symm`) is discharged by a separately-named helper sitting above the definition. The body of `extendingCDMGsWith` is then pure data — four lines assigning the four LN clauses, plus references to the five named helpers — so a reader can compare the structure literal line-for-line against LN items i.–iv. without wading through tactic blocks. Only `hE_subset` genuinely consumes the precondition `hW`: each new edge `(.intCopy w, .unsplit w)` has target $w$, and we need $w \\in G.V$ to land it in the lifted output set `G.V.image .unsplit`. The other four obligations close by constructor disjointness/injectivity of `IntExtNode` plus `G`'s own CDMG axioms."
}
