{
  "ref": "claim_3_19",
  "kind": "claim",
  "section": "3.2",
  "title": "MarginalizingOutThe",
  "type": "lemma",
  "status": {
    "formalized": "yes",
    "proven": "yes",
    "solved": "yes"
  },
  "tex_statement": {
    "raw": "% Claim statement file for row claim_3_19 -- \"MarginalizingOutThe\".\n% Auto-generated stub by scaffold/solving_current_row.py at row start. The\n% manager agent (or a worker it dispatches) fills the body below with the\n% claim's statement(s) from the lecture notes -- statement only, no proof\n% (proofs live in the sibling claim_*_proof_*.tex file).\n%\n% Compiles standalone via the `subfiles` package.\n\n\\documentclass[main]{subfiles}\n\n\\begin{document}\n% ref: claim_3_19 (statement)\n% title: MarginalizingOutThe\n\\def\\rowref{claim\\_3\\_19}\n\\phantomsection\\label{claim_3_19}\n\n\\begin{Lem}[Marginalizing out the output part of splitted nodes equals hard intervention]\n     Let $G=(J,V,E,L)$ be a CDMG and $W \\ins V$ be a subset of output nodes from $G$.\n     Then the CDMG that arises by first splitting the nodes on $W$\n      and then marginalizing out the nodes from $W^o$\n     can be identified with the CDMG that arises by hard intervention on $W$:\n     \\[ G_{\\doit(W)} \\cong \\lp G_{\\swig(W)}\\rp^{\\sm W^o}, \\qquad w \\mapsto w^i. \\]\n\\end{Lem}\n\n\\end{document}\n",
    "html": "<p>Let $G=(J,V,E,L)$ be a CDMG and $W \\ins V$ be a subset of output nodes from $G$.\n     Then the CDMG that arises by first splitting the nodes on $W$\n      and then marginalizing out the nodes from $W^o$\n     can be identified with the CDMG that arises by hard intervention on $W$:\n     \\[ G_{\\doit(W)} \\cong \\lp G_{\\swig(W)}\\rp^{\\sm W^o}, \\qquad w \\mapsto w^i. \\]</p>",
    "env": "Lem",
    "env_title": "Marginalizing out the output part of splitted nodes equals hard intervention",
    "source_path": "leanification/Chapter3_GraphTheory/Section3_2/tex/claim_3_19_statement_MarginalizingOutThe.tex"
  },
  "tex_proof": {
    "raw": "% Claim proof file for row claim_3_19 -- \"MarginalizingOutThe\".\n%\n% Proof lifted verbatim from lecture-notes/lecture_notes/graphs.tex lines\n% 1177--1210 (the LN's `\\Claude{...}` wrapper stripped). The LN proof\n% already follows the three-section Node-sets / Directed-edges /\n% Bidirected-edges skeleton called for in the worker prompt; no\n% adaptation was needed.\n%\n% Compiles standalone via the `subfiles` package.\n\n\\documentclass[main]{subfiles}\n\n\\begin{document}\n% ref: claim_3_19 (proof, with statement restated for readability)\n% title: MarginalizingOutThe\n\\def\\rowref{claim\\_3\\_19}\n\\phantomsection\\label{claim_3_19}\n%\n% Cross-references: see claim_<ref>_statement_<title>.tex in this folder\n% for the corresponding statement.\n\n% --- statement (restated) ---------------------------------------------------\n\\begin{Lem}[Marginalizing out the output part of splitted nodes equals hard intervention]\n     Let $G=(J,V,E,L)$ be a CDMG and $W \\ins V$ be a subset of output nodes from $G$.\n     Then the CDMG that arises by first splitting the nodes on $W$\n      and then marginalizing out the nodes from $W^o$\n     can be identified with the CDMG that arises by hard intervention on $W$:\n     \\[ G_{\\doit(W)} \\cong \\lp G_{\\swig(W)}\\rp^{\\sm W^o}, \\qquad w \\mapsto w^i. \\]\n\\end{Lem}\n\n% --- proof ------------------------------------------------------------------\n\\begin{proof}\nWe establish the isomorphism $G_{\\doit(W)} \\cong \\lp G_{\\swig(W)}\\rp^{\\sm W^o}$ via the map $\\phi$ that is the identity on $J \\cup (V \\sm W)$ and sends $w^i \\mapsto w$ for each $w \\in W$.\n\nRecall:\n\\begin{itemize}\n    \\item $G_{\\doit(W)}$ has input nodes $J \\cup W$, output nodes $V \\sm W$, directed edges $E \\sm \\{v \\tuh w \\mid w \\in W\\}$, and bidirected edges $L \\sm \\{v \\huh w \\mid w \\in W\\}$.\n    \\item $G_{\\swig(W)}$ has input nodes $J \\dcup W^i$, output nodes $(V \\sm W) \\dcup W^o$, directed edges $\\{v_1^i \\tuh v_2^o \\mid v_1 \\tuh v_2 \\in E\\}$, and bidirected edges $\\{v_1^o \\huh v_2^o \\mid v_1 \\huh v_2 \\in L\\}$ (where $v^i = v^o = v$ for $v \\notin W$).\n\\end{itemize}\n\n\\medskip\\noindent\\emph{Node sets.}\nAfter marginalizing out $W^o$:\ninput nodes $J \\dcup W^i$ (unchanged) and output nodes $(V \\sm W) \\dcup W^o \\sm W^o = V \\sm W$.\nUnder $\\phi$ ($w^i \\mapsto w$) this matches $G_{\\doit(W)}$: input nodes $J \\cup W$, output nodes $V \\sm W$.\n\n\\medskip\\noindent\\emph{Directed edges.}\nIn $G_{\\swig(W)}$, nodes $w^o \\in W^o$ have no outgoing directed edges (all outgoing edges of $w$ go to $w^i$, not $w^o$).\nTherefore no directed walk can pass through a node in $W^o$ as an intermediate node.\nHence the marginalization step does not create any new directed edges: $\\ul{v} \\tuh \\ol{v} \\in (E_{\\swig(W)})^{\\sm W^o}$ iff $\\ul{v} \\tuh \\ol{v} \\in E_{\\swig(W)}$ with $\\ul{v},\\ol{v} \\notin W^o$.\n\nSuch an edge corresponds to some $v_1 \\tuh v_2 \\in E$ with $\\ul{v} = v_1^i$ and $\\ol{v} = v_2^o$.\nSince $\\ol{v} \\notin W^o$, we have $v_2 \\notin W$, so $v_2^o = v_2$.\nUnder $\\phi$: $\\phi(\\ul{v}) \\tuh \\phi(\\ol{v}) = v_1 \\tuh v_2 \\in E$ with $v_2 \\notin W$,\ni.e.\\ $v_1 \\tuh v_2 \\in E_{\\doit(W)}$.\nConversely, every edge in $E_{\\doit(W)}$ lifts to an edge in $E_{\\swig(W)}$ with both endpoints outside $W^o$.\n\n\\medskip\\noindent\\emph{Bidirected edges.}\nLet $\\ul{v}, \\ol{v} \\in V \\sm W$, $\\ul{v} \\neq \\ol{v}$.\nSince nodes in $W^o$ have no outgoing directed edges, the only bifurcations in $G_{\\swig(W)}$ with intermediate nodes in $W^o$ reduce to a single bidirected edge (the directed arms of the bifurcation cannot extend through $W^o$).\nSo $\\ul{v} \\huh \\ol{v} \\in (L_{\\swig(W)})^{\\sm W^o}$ iff $\\ul{v} \\huh \\ol{v} \\in L_{\\swig(W)}$, i.e.\\ iff $\\ul{v} \\huh \\ol{v} \\in L$ (since $\\ul{v}^o = \\ul{v}$ and $\\ol{v}^o = \\ol{v}$ for $\\ul{v}, \\ol{v} \\notin W$).\nUnder $\\phi$ this matches $\\ul{v} \\huh \\ol{v} \\in L_{\\doit(W)} = L \\sm \\{v \\huh w \\mid w \\in W\\}$, which equals $\\ul{v} \\huh \\ol{v} \\in L$ since $\\ul{v}, \\ol{v} \\notin W$.\n\n\\medskip\nSince $\\phi$ provides a bijection on nodes that preserves input/output status and all edges, the two CDMGs are isomorphic.\n\\end{proof}\n\n\\end{document}\n",
    "html": "<div class=\"theorem-block\"><div class=\"theorem-label\">Lemma — <span class=\"theorem-title\">Marginalizing out the output part of splitted nodes equals hard intervention</span></div><p>Let $G=(J,V,E,L)$ be a CDMG and $W \\ins V$ be a subset of output nodes from $G$.\n     Then the CDMG that arises by first splitting the nodes on $W$\n      and then marginalizing out the nodes from $W^o$\n     can be identified with the CDMG that arises by hard intervention on $W$:\n     \\[ G_{\\doit(W)} \\cong \\lp G_{\\swig(W)}\\rp^{\\sm W^o}, \\qquad w \\mapsto w^i. \\]</p></div>\n<div class=\"tex-proof\"><span class=\"proof-label\">Proof.</span><p>We establish the isomorphism $G_{\\doit(W)} \\cong \\lp G_{\\swig(W)}\\rp^{\\sm W^o}$ via the map $\\phi$ that is the identity on $J \\cup (V \\sm W)$ and sends $w^i \\mapsto w$ for each $w \\in W$.</p>\n<p>Recall:</p>\n<ul><li>$G_{\\doit(W)}$ has input nodes $J \\cup W$, output nodes $V \\sm W$, directed edges $E \\sm \\{v \\tuh w \\mid w \\in W\\}$, and bidirected edges $L \\sm \\{v \\huh w \\mid w \\in W\\}$.</li><li>$G_{\\swig(W)}$ has input nodes $J \\dcup W^i$, output nodes $(V \\sm W) \\dcup W^o$, directed edges $\\{v_1^i \\tuh v_2^o \\mid v_1 \\tuh v_2 \\in E\\}$, and bidirected edges $\\{v_1^o \\huh v_2^o \\mid v_1 \\huh v_2 \\in L\\}$ (where $v^i = v^o = v$ for $v \\notin W$).</li></ul>\n<p>\\medskip\\noindent<em>Node sets.</em>\nAfter marginalizing out $W^o$:\ninput nodes $J \\dcup W^i$ (unchanged) and output nodes $(V \\sm W) \\dcup W^o \\sm W^o = V \\sm W$.\nUnder $\\phi$ ($w^i \\mapsto w$) this matches $G_{\\doit(W)}$: input nodes $J \\cup W$, output nodes $V \\sm W$.</p>\n<p>\\medskip\\noindent<em>Directed edges.</em>\nIn $G_{\\swig(W)}$, nodes $w^o \\in W^o$ have no outgoing directed edges (all outgoing edges of $w$ go to $w^i$, not $w^o$).\nTherefore no directed walk can pass through a node in $W^o$ as an intermediate node.\nHence the marginalization step does not create any new directed edges: $\\ul{v} \\tuh \\ol{v} \\in (E_{\\swig(W)})^{\\sm W^o}$ iff $\\ul{v} \\tuh \\ol{v} \\in E_{\\swig(W)}$ with $\\ul{v},\\ol{v} \\notin W^o$.</p>\n<p>Such an edge corresponds to some $v_1 \\tuh v_2 \\in E$ with $\\ul{v} = v_1^i$ and $\\ol{v} = v_2^o$.\nSince $\\ol{v} \\notin W^o$, we have $v_2 \\notin W$, so $v_2^o = v_2$.\nUnder $\\phi$: $\\phi(\\ul{v}) \\tuh \\phi(\\ol{v}) = v_1 \\tuh v_2 \\in E$ with $v_2 \\notin W$,\ni.e.\\ $v_1 \\tuh v_2 \\in E_{\\doit(W)}$.\nConversely, every edge in $E_{\\doit(W)}$ lifts to an edge in $E_{\\swig(W)}$ with both endpoints outside $W^o$.</p>\n<p>\\medskip\\noindent<em>Bidirected edges.</em>\nLet $\\ul{v}, \\ol{v} \\in V \\sm W$, $\\ul{v} \\neq \\ol{v}$.\nSince nodes in $W^o$ have no outgoing directed edges, the only bifurcations in $G_{\\swig(W)}$ with intermediate nodes in $W^o$ reduce to a single bidirected edge (the directed arms of the bifurcation cannot extend through $W^o$).\nSo $\\ul{v} \\huh \\ol{v} \\in (L_{\\swig(W)})^{\\sm W^o}$ iff $\\ul{v} \\huh \\ol{v} \\in L_{\\swig(W)}$, i.e.\\ iff $\\ul{v} \\huh \\ol{v} \\in L$ (since $\\ul{v}^o = \\ul{v}$ and $\\ol{v}^o = \\ol{v}$ for $\\ul{v}, \\ol{v} \\notin W$).\nUnder $\\phi$ this matches $\\ul{v} \\huh \\ol{v} \\in L_{\\doit(W)} = L \\sm \\{v \\huh w \\mid w \\in W\\}$, which equals $\\ul{v} \\huh \\ol{v} \\in L$ since $\\ul{v}, \\ol{v} \\notin W$.</p>\n<p>\\medskip\nSince $\\phi$ provides a bijection on nodes that preserves input/output status and all edges, the two CDMGs are isomorphic.</p></div>",
    "source_path": "leanification/Chapter3_GraphTheory/Section3_2/tex/claim_3_19_proof_MarginalizingOutThe.tex"
  },
  "lean": [
    {
      "name": "CDMGNodeIso",
      "kind": "structure",
      "code": "-- Helper required to state the row: the cross-carrier graph isomorphism encoding the LN's `\\cong`.\n/-- A graph isomorphism between two CDMGs over potentially distinct\ncarrier types, encoded as a carrier *function* (not a carrier\nbijection) together with the injectivity-on-nodes condition that\nmakes it a bijection on the actual node sets.\n\nConcretely, a `CDMGNodeIso G H` packages:\n\n* a function `toFun : α → β`,\n* a proof `injOn_nodes` that `toFun` is injective on\n  `G.J ∪ G.V` (so, together with the four image-equality fields\n  below, it bijects `G`'s nodes onto `H.J ∪ H.V`),\n* four image equalities recording that `toFun` sends `G`'s four\n  data fields `J / V / E / L` exactly to those of `H`. -/\nstructure CDMGNodeIso {α β : Type u} (G : CDMG α) (H : CDMG β) where\n  /-- The underlying function on carrier types. -/\n  toFun : α → β\n  /-- The function is injective on `G`'s nodes. -/\n  injOn_nodes : Set.InjOn toFun (G.J ∪ G.V)\n  /-- The function sends `G.J` to `H.J`. -/\n  J_eq : H.J = toFun '' G.J\n  /-- The function sends `G.V` to `H.V`. -/\n  V_eq : H.V = toFun '' G.V\n  /-- The function sends `G.E` to `H.E` (applied componentwise to\n  endpoints). -/\n  E_eq : H.E = (Prod.map toFun toFun) '' G.E\n  /-- The function sends `G.L` to `H.L` (applied componentwise to\n  endpoints). -/\n  L_eq : H.L = (Prod.map toFun toFun) '' G.L"
    },
    {
      "name": "hardInterventionOn_nodeIso_swig_marginalize_outputs",
      "kind": "def",
      "code": "/-- claim_3_19 -- `MarginalizingOutThe`. The hard intervention\n`G.hardInterventionOn W` (LHS, over carrier `α`) is\ngraph-isomorphic to the marginalization of the SWIG over the\n`W^o = Sum.inl '' W` copies (RHS, over carrier `α ⊕ ↑W`). The\nidentification is `split1 W`, which sends `w ∈ W` to\n`Sum.inr ⟨w, hw⟩` (the LN's `w ↦ w^i`) and is the identity on\n`J ∪ (V ∖ W)` (under the canonical `inl`-embedding, which is the\nLN's `v ↦ v^o := v` for `v ∉ W`). -/\nnoncomputable def hardInterventionOn_nodeIso_swig_marginalize_outputs\n    {α : Type u} {G : CDMG α} {W : Set α} (hW : W ⊆ G.V) :\n    CDMGNodeIso (G.hardInterventionOn W)\n                ((G.swig W hW).marginalize (Sum.inl '' W)) where\n  toFun := split1 W\n  injOn_nodes := fun _ _ _ _ hxy => split1_injective W hxy\n  J_eq := by ...\n  V_eq := by ...\n  E_eq := by ...\n  L_eq := by ..."
    }
  ],
  "lean_file_path": "leanification/Chapter3_GraphTheory/Section3_2/MarginalizingOutSplitOutput.lean",
  "lean_explanation": "The LN states the graph isomorphism $G_{\\doit(W)} \\cong (G_{\\swig(W)})^{\\setminus W^o}$ with witness $w \\mapsto w^i$, for any CADMG $G = (J, V, E, L)$ and any subset of output nodes $W \\subseteq V$. In Lean: `α : Type u` names the ambient vertex type (left implicit in the LN), `G : CDMG α` is the CADMG, `W : Set α` is the LN's $W$, and `hW : W ⊆ G.V` discharges the LN's $W \\subseteq V$ precondition. The hypothesis is load-bearing only on the SWIG side — both `hardInterventionOn` and `marginalize` accept any `W : Set α` regardless of $V$-containment, so it appears in the signature only because `G.swig W hW` demands it.\n\nTwo Mathlib idioms do real work. `Set.InjOn f S` is set-restricted injectivity (`f` is injective on `S`), exactly what a node-level bijection needs: injectivity is required only on the actual nodes `G.J ∪ G.V`, not on the whole carrier `α`. The notation `f '' S` is `Set.image f S` (the set `{f(x) | x ∈ S}`). `Prod.map f f`, the canonical componentwise lift of a function to pairs, is how a carrier map acts on the edge sets `E` and `L`, both subsets of `α × α`. The RHS carrier `α ⊕ ↑W` (Lean's `Sum`, with `↑W` the subtype of elements of `W`) follows the convention pinned down in `NodeSplittingOn.lean`: `Sum.inl v` is the observation copy ($v^o$ in the LN) and `Sum.inr ⟨w, hw⟩` is the fresh intervention-input copy ($w^i$). So the LN's $W^o$ is precisely `Sum.inl '' W : Set (α ⊕ ↑W)`.\n\nThe statement bundles two declarations. `CDMGNodeIso G H` is a `structure` with one data field (`toFun : α → β`) and five proof fields (a `Set.InjOn` plus four image-equalities for `J / V / E / L`); this captures the LN's $\\cong$ between two CDMGs that may live over distinct carriers. `hardInterventionOn_nodeIso_swig_marginalize_outputs` then constructs a `CDMGNodeIso` between `G.hardInterventionOn W` (the LN's $G_{\\doit(W)}$) and `(G.swig W hW).marginalize (Sum.inl '' W)` (the LN's $(G_{\\swig(W)})^{\\setminus W^o}$). The witness carrier map is `split1 W : α → α ⊕ ↑W` (from `NodeSplittingOn.lean`): `v ∈ W` maps to `Sum.inr ⟨v, hv⟩` (the LN's $w \\mapsto w^i$), and `v ∉ W` maps to `Sum.inl v` (the identity on $J \\cup (V \\setminus W)$ under the $v^o = v$ convention).",
  "design_choices": "**The cross-carrier iso is encoded as a function `α → β` plus `Set.InjOn`, not a full carrier bijection `α ≃ β`.** The two CDMGs of this row live over carriers of *different cardinality* — LHS over `α`, RHS over `α ⊕ ↑W`. For any finite `α` and non-empty `W`, no `α ≃ α ⊕ ↑W` exists (the cardinalities differ by $|W|$), so an `Equiv`-shaped statement is literally unprovable in those cases. This is the central obstacle that rules out reusing the same-cardinality `CDMGEquiv` API of claim_3_7 / claim_3_10 (whose carrier bijections rebracket the *same* underlying carrier). The LN's $\\cong$ is a *node*-level bijection, not a carrier-level one — it bijects $J \\cup V$ on the LHS with $J' \\cup V'$ on the RHS and says nothing about the unused parts of the carrier. `Set.InjOn toFun (G.J ∪ G.V)` plus the four image-equalities for `J / V / E / L` captures exactly that, and no more.\n\n**`CDMGNodeIso` is kept local to this file, not promoted to a shared module.** Single-consumer rationale: lifting now would force a placement decision — Section 3.1 alongside `CDMG.lean` (where the four-data-field shape is defined) or Section 3.2 alongside the same-cardinality `CDMGEquiv` — and possibly a unification with `CDMGEquiv`, both better deferred until a *second* cross-carrier-iso row reveals the natural shared API. The same wait-for-second-consumer policy already governs `CDMGEquiv` itself, and the cost of lifting later (re-import, possibly rename) is small compared to locking in the wrong shape now. No `refl` / `symm` / `trans` groupoid laws are written for the same reason: no chaining is yet on the table.\n\n**The carrier map witness is `split1 W`, not a bespoke function.** `split1 W : α → α ⊕ ↑W` (from `NodeSplittingOn.lean`) is exactly the LN's identification by construction: `v ∉ W` maps to `Sum.inl v` (the observation copy, identifying with the original vertex under the $v^o = v$ convention) and `v ∈ W` maps to `Sum.inr ⟨v, hv⟩` (the LN's $w \\mapsto w^i$). The LN names only `w ↦ w^i` because the identity on the rest is \"implicit by absence of any other rule\"; in Lean the implicit rule is `Sum.inl`, which is precisely `split1`'s else-branch. Re-using `split1` removes a degree of freedom (any other equivalent encoding would be definitionally different and force `simp`-rewrites to convert) and aligns this row with the chapter-3 split-graph identifications used by claim_3_6 / claim_3_7 / claim_3_8 / claim_3_12 / def_3_12.\n\n**$W^o$ is rendered as `Sum.inl '' W`, not `Sum.inr '' W` or the raw `W`.** Under the `Sum.inl = w^o` / `Sum.inr = w^i` convention from `NodeSplittingOn.lean`, the LN's $W^o$ — the $o$-copies of $W$ inside the SWIG's carrier $\\alpha \\oplus \\uparrow W$ — is literally `Sum.inl '' W : Set (α ⊕ ↑W)`. Marginalizing `Sum.inr '' W` (the $w^i$ copies) instead would be a *different* and *false* statement: deleting the fresh intervention inputs would collapse the SWIG back toward the observational graph $G$, not toward $G_{\\doit(W)}$. Marginalizing the raw `W : Set α` is a type error — `marginalize` expects a `Set (α ⊕ ↑W)`, not a `Set α`. So `Sum.inl '' W` is the unique correct choice.\n\n**The conclusion uses $\\cong$ (a `CDMGNodeIso`), not literal `=`.** The two CDMGs live over different carrier types — `CDMG α` versus `CDMG (α ⊕ ↑W)` — so Lean's `=` between them is not even type-correct; an equality statement here would be a category error. The LN's $\\cong$ is exactly the graph isomorphism captured by `CDMGNodeIso`. Contrast claim_3_4 / claim_3_8 / claim_3_13, where both sides of the chained equation live over the *same* carrier and the LN writes literal `=` (matched in Lean by an ordinary equality `theorem`).\n\n**`hW : W ⊆ G.V` appears at the signature level, but only because of the SWIG side.** `G.swig W hW` and `G.nodeSplittingOn W hW` carry `hW` as a precondition — load-bearing because without it the fresh split edge `(Sum.inl w, Sum.inr ⟨w, _⟩)` would violate `V_subset` for `w ∈ W ∖ G.V`. By contrast, `hardInterventionOn` and `marginalize` both *dropped* their LN preconditions because their constructions are well-defined for any `W`. So the LHS `G.hardInterventionOn W` does not need `hW`; only the RHS does, which is why the hypothesis appears in the signature.\n\n**The declaration is a `noncomputable def`, not a `theorem`.** The conclusion type `CDMGNodeIso ..` is a `structure` (data, not a `Prop`), so `def` is the correct keyword. The `noncomputable` modifier propagates from the `split1` / `nodeSplittingOn` chain (both `noncomputable` because of the classical case-split on set membership). This matches the declaration style of `nodeSplittingOn_nodeSplittingOn_equiv` and `nodeSplittingOn_comm_equiv` in `TwoDisjointNodeSplittingsCommute.lean`, where a `CDMGEquiv`-valued conclusion is similarly a `noncomputable def`."
}
